# tasks — warranty

작성: 2026-08-19 · 권위: `requirements.md` · 설계: `design.md` + `design/*.md`

> 모든 태스크는 REQ와 설계 절을 가리킨다. `[x]`는 **테스트가 있고 변이로 red를 확인한 것**만.
> `[auto]`는 **상태 박스와 다른 축**이다 — 무인 루프(overnight)가 소비해도 되는 것,
> 즉 `make check`로 **오프라인·결정론적으로** 판정 가능한 것만 붙는다.
> ⛔ 실물 클라우드가 필요한 T0-3·T2-*·T7-*는 원리상 붙을 수 없다(태그 없음 = 무인 금지).
> ⚠️ **완료 항목의 서사는 여기가 아니다** — `docs/PROGRESS_LOG.md`와 `docs/archive/`가 소유한다.
> 여기 남는 것은 *무엇을 했는가*와 **다음 사람이 몰라서 다칠 것** 한 줄뿐이다.

---

## 일정

```
   08-19 ──── 08-21 ──── 08-24 ──────── 08-27 ──── 08-31
     T0/T1      T2      ★ 중단 기준      선택 판단   제출
   spec·도메인  배포     "Cloud Run에     (테넌트    09-01 09:00 KST
                        도는 게 없으면    신원·화해)
                        버린다"
```

⛔ **T2가 중단 기준의 판정 대상이다.** 나머지는 전부 그 뒤에 온다.

---

## T0 — 기반 *(대부분 완료)*

- [x] **T0-1** 레포·게이트·설정 계층 · `Implements: REQ-801, REQ-802` · `Design: 09§1, 08§5`
- [x] **T0-2** ★ **G6 추적성 가드** + 변이 하네스 · `Design: 09§3.1, 09§4`
- [ ] **T0-3** 전용 GCP 프로젝트 + 크레딧 결제 계정 연결 · `Implements: REQ-805` · `Design: 10§1`
      ⚠️ **막혀 있다**: 활성 계정이 `yeongsigchoe7@gmail.com`이고 Cloud Billing API 미활성이라
      크레딧이 어느 결제 계정에 붙었는지 **읽을 수 없다**. 콘솔 확인 필요.
- [ ] **T0-4** *(선택)* BQ 결제 내보내기 + 화해/차이 · `Implements: REQ-506, REQ-509` · `Design: 05§4`
      ⚠️ 하루 지연. **크리티컬 패스 아님** — REQ-506·509는 선택이다.
- [x] [auto] **T0-5** ★ **spec 참조 정합성 가드**(G6④) — `Spec:` 도크스트링이 가리키는 설계
      경로와 인용 REQ가 **실재하는지** 집행한다. 줄임 표기는 안 푼다(저장소 루트 전체 경로만).
      **M-23·M-24·M-25 red 확인.** ⚠️ 손으로 센 5곳이 아니라 6곳이었다 — 손 목록은 가드가 아니다.
- [x] [auto] **T0-6** 스테일 문자열 정리 — 셋으로 적었는데 **넷**이었다(`mutate.sh` 사용법·
      인라인 REQ 주석 · `spec_trace.py` 리포트 헤더 · `mutations.md` 절차 인용).
      ⚠️ **넷 다 기계가 안 읽는 자리**라 G6④도 `scan_mutation_refs`도 못 잡는다 —
      **다음 이름 변경 때 재발한다.**

- [x] [auto] **T0-7** 문서 예산 회수 — `PROGRESS_LOG` 281→**73**(월별 아카이브
      `docs/archive/PROGRESS_LOG-2026-08.md`로 분리) · `OVERVIEW` 245→**240** · 이 파일 211→**190**.
      상대 링크 14개 전부 해석됨 · `make check` **125 passed**(변동 없음).
      ⚠️ **예산 초과는 증상이었다**: `OVERVIEW` §10이 세어 적은 `120 passed`·`VERIFIED 18`·
      `M-01~M-32`·`커밋 6개`가 **넷 다 틀려 있었다** — PRINCIPLES #10("게이트 통과 수는
      `tasks.md` 한 곳에만")을 정면으로 어긴 자리다. 세는 대신 **가리키게** 바꿨다.
      ⛔ **아무 가드도 예산·중복 계수를 안 잡는다** — 다시 넘치면 또 사람이 눈으로 봐야 한다.
- [x] [auto] **T0-8** ★ **변이 기록 신선도 가드** — 맨 마지막「전체 스윕」절만 현재를 주장하는
      것으로 보고, 선언한 기준선이 **지금 스위트 크기**와 다르면 red다. 변이 집합도 하네스의
      `case`·`all`·사용법 문자열과 맞댄다. + `make mutate`(게이트 아님). **M-47·M-48·M-49 red 확인.**
      ⚠️ **신선도는 참임의 대리지 참임이 아니다** — red일 때 고칠 것은 숫자가 아니라 스윕이다.
      ⛔ **하중 분포는 여전히 아무도 안 잡는다**: 기록의 `M-19 16건`은 기준선 120일 때 값이고
      지금은 `17 failed + 10 errors`다. 그건 사람이 옮겨 적는 숫자로 남아 있다.
- [x] [auto] **T0-9** `type: ignore` **62→1** — 픽스처가 `object`를 반환하던 것을 실제 타입으로
      바꾸니 그 아래 억제가 통째로 사라졌다. 남은 하나는 *일부러* 타입이 금지한 짓을 하는
      자리(`Mapping` 대입)라 사유를 달았다. mypy `files`에 **`tests` 추가**(22→33 파일).
      ⛔ **62개 중 61개는 집행되지 않는 주석이었다** — 테스트가 mypy 밖이라 strict의
      `warn_unused_ignores`가 안 돌았고, 5개는 이미 죽어 있었다. **억제를 세기 전에 억제를
      읽는 사람을 먼저 만들어야 한다.**
      ⚠️ **억제는 자기가 이름 붙인 것만 덮지 않았다**: `attr-defined` 뒤의 진짜 오류는
      `union-attr`(`decision`은 `Decision | None`)이었고, `test_config`의 스파이에는
      `or {}`라는 죽은 표현이 있었다. 둘 다 지우자마자 나왔다.
      ⛔ **이 하중을 지키는 하네스 변이는 아직 없다** — red 확인은 손으로 했다(군더더기 억제
      하나 → `unused-ignore` red). `mutate.sh`에 넣으려면 T0-8 신선도 가드 때문에 전체 스윕을
      다시 돌려야 한다. ⚠️ 재정의 인자(`**over: Any`)는 여전히 검사 밖이다.
- [x] [auto] **T0-10** 값-대-구문 착각 전수 점검 (M-44 계열) — **세 자리**였고 셋 다 초록으로
      통과하고 있었다. ① `sleep`·`window_s` 인자가 `warranty.tunables`에서 **온 이름인지**를
      `ast`로(검사 ④ — 리터럴만 막으면 `delay_s = 45`로 우회된다) ② `DESIGN_COLUMNS` →
      design 05§5 ```jsonc **파싱** ③ `DESIGN_STEPS` → design 11§2 번호 줄 **파싱**.
      + `test_req_206_...used_once`를 이름 정정(그 단언은 값만 묻는다).
      **M-50·M-51·M-52 red 확인.** 새 가드가 M-44를 1→2, M-45를 3→4로 늘렸다.
      ⚠️ **테스트 이름은 약속이고, 약속과 단언은 따로 썩는다** — 셋 다 이름은 *"설계가 이름
      붙인 것"*을 지킨다고 말하는데 실제로는 **값**이나 **손 사본**과 맞대고 있었다.
      ⛔ M-52는 이름의 **정확한 일치**를 안 묻는다: design 11§2는 ③④를 `remediate #1/#2`로,
      코드는 `remediate`/`manual`로 부른다. 맞추려면 설계나 코드를 고쳐야 해서 범위 밖으로 뒀다 —
      집행하는 것은 개수·순서·겹침없음과 *"n번째 이름이 n번째 줄에 나오는가"*뿐이다.

## T1 — 도메인 *(일부 완료 · 전부 오프라인)*

- [x] **T1-1** 원장 행 · 비용 사실 · 귀속 · `Implements: REQ-503, REQ-504, REQ-505` · `Design: 05§1–3`
- [x] **T1-2** 저장소가 불변식 집행(범용 update 없음) · `Implements: REQ-505` · **G2**
- [x] **T1-3** 1회=1행 · 거부/실패 기록 · `Implements: REQ-501, REQ-507` · **G7**
- [x] **T1-4** **운영 계약 자료형** + 넷 필수 검증 · `Implements: REQ-102` · `Design: 01§2`
- [x] **T1-5** ★ `improved`를 **유도**로 (저장 금지) · `Implements: REQ-502` · `Design: 05§1.1` · **G8**
- [x] **T1-6** 3축 판정 행렬 **다섯 칸을 값으로** · `Implements: REQ-402` · `Design: 04§1` · **G9**

## T2 — ★ 배포 선행 (08-20~21) — **중단 기준**

- [~] **T2-1** ADK **실물 설치** + 최소 에이전트 로컬 응답 · `Implements: REQ-601` · `Design: 06§2`
      ✅ **라이브러리와 인터페이스는 실재한다**(`google-adk 2.7.1` introspect,
      증거 `evidence/adk-api-probe-2026-08-19.log`): `tools`가 평범한 `Callable`을 받고,
      ⚠️ `Runner`는 `session_service`가 **필수**다(`min-instances=0`이라 유휴 후 첫 요청은
      항상 새 세션 → 대화 연속성을 가정하지 않는다).
      ⛔ **실제 모델 호출은 아직 안 했다** — 프로젝트·인증 없음.
      **"임포트가 된다"와 "호출이 된다"는 다르다.**
- [ ] **T2-2** 컨테이너 → Artifact Registry → **Cloud Run 배포** · `Implements: REQ-602` · `Design: 10§2`
- [ ] **T2-3** `demo-target` 서비스 배포 (리비전 2개) · `Design: 10§2`
- [ ] **T2-4** 실물 왕복 증거 · `Implements: REQ-901` · `Design: 10§7`

⚠️ **수용 기준은 "테스트 통과"가 아니라 "실제 라이브러리로 실제 응답"이다.**
⛔ **08-24까지 T2-2가 안 되면 접는다.** 포기 비용 0.

## T3 — Day-1: 계약 방출 (08-21~22)

- [ ] **T3-1** Cloud Run 서비스 프로비저닝 · `Implements: REQ-101` · `Design: 01§1`
- [x] [auto] **T3-2** ★ 계약을 **생성 응답에서 유도** · `Implements: REQ-103` · `Design: 01§3`
      `usecases/provision.py` — `ProvisionResponse` 하나에서 계약을 유도한다. 데모도 이 경로를
      탄다(손으로 조립하던 `OperationalContract(...)`를 지웠다). **M-53~M-56 red 확인.**
      ★ **값이 아니라 출처를 묻는다**: 유도되는 자리(design 01§3 표를 **파싱**해서 읽는다)는
      `derive_contract`의 **인자로 들어올 수 없고**(`inspect.signature`), 프로덕션 코드에서
      계약을 조립하는 자리는 `provision.py` 하나뿐이다(`ast`). M-54는 **값을 하나도 안 바꾼다** —
      구문으로 안 물으면 그 회귀는 전부 초록이다.
      ⚠️ **스텁 초록 위험을 그대로 안고 있다**(#3): 실물 Cloud Run 응답이 `ProvisionResponse`
      모양으로 오는지는 **확인 못 했다**(T3-1). 데모 `caveats`가 그것을 출력에 적는다.
      ⛔ **설계와 어긋나는 자리를 하나 남겼다**: design 01§3은 가역성을 *"리소스 타입에서 유도"*로
      적지만, 타입만 보면 **돌아갈 리비전이 없는 첫 배포**가 REQ-102에 걸려 계약이 아예 성립하지
      않는다. 그래서 코드는 타입을 **필요조건**으로 쓰고 목적지 유무를 함께 본다 — 맞추려면
      설계를 고쳐야 해서 범위 밖으로 뒀다.
- [x] **T3-3** 계약 없는 리소스 → `MANUAL` · `Implements: REQ-104` · `Design: 01§4`
- [ ] **T3-4** 리소스 삭제 시 계약 `retired` · `Implements: REQ-105` · `Design: 01§5`

## T4 — Day-2: 조치 · 검증 · 롤백 (08-22~25) — **논지의 전부**

> ✅ **루프는 전부 fake 위에서 배선되고 검증됐다**(`usecases/remediate.py`).
> ⚠️ **남은 것은 어댑터뿐이다** — 실물 Cloud Monitoring·Cloud Run·Vertex.
> **스텁 위 통과는 REQ-601·602를 만족시키지 않는다.**

- [x] **T4-1** 기준선 측정 (Cloud Monitoring) · `Implements: REQ-201` · `Design: 02§2`
- [x] **T4-2** ★ 재측정 — **기준선과 같은 함수** · `Implements: REQ-202` · `Design: 02§2`
- [x] **T4-3** 계약 기준으로 판정 + 빈 창 → `unverifiable` · `Implements: REQ-203, REQ-205` · `Design: 02§3`
- [x] **T4-4** ★ **애매할 때 모델이 판단하고 근거를 남긴다** · `Implements: REQ-204` · `Design: 02§3.1`
- [x] **T4-5** 롤백 계획을 조치 **전에** 고정 · `Implements: REQ-301` · `Design: 03§2`
- [x] **T4-6** ★ 트래픽 전환 + **배분 재확인** · `Implements: REQ-302, REQ-303` · `Design: 03§3`
- [x] **T4-7** ★ **롤백 후 재측정** · `Implements: REQ-304` · `Design: 03§4`
- [x] **T4-8** 롤백 불가 → 에스컬레이션 · `Implements: REQ-305` · `Design: 03§5`
- [x] **T4-9** 게이트가 실행을 **막는다** · `Implements: REQ-403` · `Design: 04§2` · **G1**
- [x] **T4-10** 모든 항목에 `decision` · `Implements: REQ-401` · **G4**
- [x] [auto] **T4-11a** 승인 집행 — `awaiting_approval` 동안 **실행기를 부르지 않는다**,
      승인 시 게이트를 **재평가**한다 · `Implements: REQ-404` · `Design: 04§3`
      `Remediator.approve` + 원장의 `Approval`(원래 `decision`을 안 덮는다). AUTO와 **같은
      경로**(`_execute_and_verify`)를 탄다. **M-26·M-27 red 확인.**
- [x] [auto] **T4-11b** 예산 예약/정산 — AUTO·승인 시 예상 비용을 **예약**하고 실행 후 정산한다 ·
      `Implements: REQ-405` · `Design: 04§4`
      `commit`을 `reserve`→`settle`로 갈랐다. 예약은 `_execute_and_verify` **한 자리**에서만
      잡히고 정산은 `finally`다 — 안 풀린 예약은 여유를 **조용히 잠근다**. **M-28·M-29 red 확인.**
      ⚠️ **예약이 M-18·M-27을 가렸다** — 바깥 방어층이 안쪽 층의 변이를 흡수한다.
      ⇒ **새 방어층을 넣을 때마다 전체 스윕을 돌 것.**
- [ ] **T4-12** 파괴적 조치 강제 승인 · `Implements: REQ-406` · `Design: 04§5`
- [x] **T4-13** 재측정 상수 한 곳 · `Implements: REQ-206, REQ-804` · `Design: 02§4`

## T5 — 출력 (08-25~26)

- [ ] **T5-1** 응답에 판정·검증 근거·트래픽 배분 · `Implements: REQ-604` · `Design: 08§3.1`
- [x] [auto] **T5-2** ★ **회복률 리포트** · `Implements: REQ-508` · `Design: 05§5`
      원장에서 `executed·improved·improvement_rate·rolled_back·escalated·unverifiable·wasted_usd`를
      **유도**한다(저장 금지 — G8 계열). ★ **분모는 `executed`다** — 전체 건수로 나누면 게이트가
      잘 막을수록 성적이 나빠 보인다. 실행 0이면 비율은 `0`이 아니라 **정의되지 않는다**.
      `escalated`는 `rolled_back`의 **부정이 아니다**. **M-30·M-31·M-32 red 확인.**
      ⚠️ 루프가 쓰는 행의 `assumed`가 **항상 0**이라 `wasted_usd`는 비용 경로(T3·REQ-503)가
      붙기 전까지 실물 경로에서 늘 0이다.
- [x] [auto] **T5-3** 모델 호출도 원장에 · `Implements: REQ-603` · `Design: 06§5`
      모델 포트 호출 1회 = 원장 1행. 계량은 `MeteredModel._call` **한 곳**, 기록은 `finally`다.
      가드는 `ModelPort`의 **메서드 집합**과 `EntryKind` **전체**에 걸린다 — 늘면 자동으로 red다.
      ★ **공시 단가를 코드에 안 박았다**(실물 호출 전이라 — design 06§2): 단가표는 주입되고,
      표에 없는 모델은 0원이 아니라 **`none`+사유**다. **M-33·M-34·M-35·M-36 red 확인.**
      ⚠️ **T5-2가 못 박은 분모가 다시 흔들렸다**(M-34) — **정의의 입력 집합이 늘면 정의를
      다시 물어야 한다.**
- [ ] **T5-4** G5 (게이트 중 라이브 어댑터 0) 변이 확인 · `Implements: REQ-801` · **G5**

## T6 — 데모 (08-27~29)

- [ ] **T6-1** ★ **신호를 악화시키는 리비전** 준비 (장애 주입) · `Design: 11§1` 원칙 5
- [x] [auto] **T6-2** `make demo` 5단계 결정론 · `Implements: REQ-803` · `Design: 11§2`
      `warranty/demo.py`가 다섯 단계(provision·inject·remediate·manual·report)를 돌리고,
      **두 번 돌려 같은 결과**임을 테스트가 확인한다 — `run_demo()`와 `main()` 출력 둘 다에.
      ★ **결과를 박아 두지 않았다**: 찍힌 기준선·재측정값으로 판정을 **다시 계산해** 맞대 본다
      (M-40). 조치는 트래픽을 **실제로** 옮기고 롤백이 되돌린다(M-39).
      **M-37~M-41 red 확인.** ⚠️ `wasted_usd`가 **0으로 나오는 이유**를 출력 `caveats`에 적었다 —
      말 없는 0은 *"낭비가 없었다"*로 읽힌다.
- [x] [auto] **T6-3** 상수 한 모듈 · `Implements: REQ-804` · `Design: 11§5`
      대기·창 길이가 `warranty/tunables.py` 한 모듈의 명명 상수이고, 산재하면 가드가 red다.
      가드는 `ast`로 셋을 묻는다 — ① 모듈 **밖에** 그 모양의 상수가 없는가 ② 호출부에 숫자
      리터럴이 박혀 있지 않은가 ③ design 11§5의 **이름 집합**과 같은가.
      ★ **값은 안 묶었다**(촬영 때 조정한다 — 값까지 집행하면 **그런 가드는 결국 꺼진다**).
      **M-42~M-45 red 확인.** ⚠️ 기존 `test_req_206...`은 이 회귀를 **못 잡았다** —
      **이름을 쓰는지 물으려면 값이 아니라 구문을 봐야 한다**(M-44).

## T7 — *(선택)* 테넌트 신원 — **08-27 판단**

- [ ] **T7-1** 테넌트별 SA + impersonation · `Implements: REQ-701` · `Design: 07§2`
- [ ] **T7-2** WIF — 장기 키 없음 · `Implements: REQ-702` · `Design: 07§2`
- [ ] **T7-3** ★ **GCP가 403으로 거부하는 것**으로 검증 · `Design: 07§3`

## T8 — 제출 (08-30~31)

- [ ] **T8-1** 아키텍처 다이어그램 · `Implements: REQ-901`
- [ ] **T8-2** README — 재현 절차 · `Implements: REQ-901`
- [ ] **T8-3** ★ **4분 영어 영상** · `Implements: REQ-901` · `Design: 11§3`
- [ ] **T8-4** 신규 코드 확인 · `Implements: REQ-902` · `Design: 10§8`
- [ ] **T8-5** Devpost 제출 (**09-01 09:00 KST**)
- [ ] **T8-6** teardown 캘린더 등록 (**09-02**) · `Design: 10§6`

---

## T9 — 가드 하중 회수 *(변이 미확인 10건 · 전부 오프라인)*

> ⛔ **테스트가 있다는 것과 가드라는 것은 다르다**(PRINCIPLES #8). 아래 열은 `make trace`의
> **변이 칸이 비어 있다** — 지워 보고 red를 확인한 적이 없다. 오늘 두 번 그 차이가 실제로 났다:
> 예약이 M-18·M-27을 가렸고(`5a049f1`), `test_req_206`은 이름 대신 값만 물었다(M-44).
> ⚠️ **변이가 살아남으면 그것이 결과다.** 테스트를 고쳐 red를 만들고, 못 고치면 `[blocked]`로 남긴다.

- [x] [auto] **T9-1** 검증 3종 · `Implements: REQ-201, REQ-202, REQ-203`
      ⛔ **M-59는 살아남았고 M-57은 우연히 죽었다** — 판정 기준의 출처를 묻는 자리가 없었고,
      기준선의 시점을 묻는 자리도 없었다(죽인 건 REQ-405의 예약 테스트다).
      ⇒ `tests/test_verification_order.py` — design 02§2의 절차를 **파싱해** 순서와 인자 출처를 묻는다.
      기준선 측정 · **같은 신호를 같은 방법으로** 재측정 · 회복 판정 기준은 계약이 선언.
      `Done:` 셋 각각 변이 red 확인 + `mutations.md` 기록 + `requirements.md` → `VERIFIED`.
- [x] [auto] **T9-2** 롤백 2종 · `Implements: REQ-301, REQ-302`
      ⛔ **M-60·M-62가 살아남았다** — 계획이 *조치 전에* 묶이는지 묻는 자리도, *모델이 닫은*
      미회복이 롤백되는지 묻는 자리도 없었다. 물어지던 것은 REQ-301의 절반
      (*"다시 조회하지 않는가"* · `contracts.lookups == 1`)뿐이다.
      ⇒ `tests/test_rollback_plan_order.py` — 계획이 **조치 전에** 묶이고, 롤백이 **그 이름을**
      받고, 조치 뒤에 계약 저장소를 **다시 안 읽는지**를 AST로 묻는다.
      + 모델이 닫은 미회복도 롤백한다(반대편: 모델 경로라고 무조건 롤백하지도 않는다).
      `Done:` 셋 각각 변이 red 확인 + `mutations.md` 기록 + `requirements.md` → `VERIFIED`.
- [x] [auto] **T9-3** 계약 수명 2종 · `Implements: REQ-104, REQ-105`
      ⛔ **M-64가 살아남았다** — 계약 없음 전용 분기를 지워도 게이트가 같은 값(`MANUAL`)을 내서
      값으로는 안 보인다. 갈라지는 경우는 **예산이 먼저 막을 때** 하나뿐이고, 그때 원장은
      *"계약이 없었다"*를 말하지 않는다. ⛔ **M-63을 죽인 것은 데모 서사 테스트**였고
      (유닛 가드는 판정을 안 물었다), **REQ-105는 승인 경로에만** 물려 있었다 — 정상 입구인
      `remediate()`에서 죽은 계약을 태운 자리가 없었다.
      ⇒ `test_remediate_loop.py` 3건 — 계약 없음이 **원장에 검증 불가로** 남는가 ·
      예산이 막는 경우에도 **`manual_required`가 유지되는가** · 조치 경로에서 `retired`가
      **계약으로 안 실리는가**(회복하는 신호를 태워 공허 통과 방지).
      `Done:` 넷 각각 변이 red 확인 + `mutations.md` 기록 + `requirements.md` → `VERIFIED`.
- [x] [auto] **T9-4** 원장 완결성 · `Implements: REQ-507`
      ⛔ **M-68이 살아남았다** — 예약 실패 분기에 **도달하는 입력이 스위트에 없었다.**
      재진입으로는 그 순서가 안 만들어진다(안쪽 조치는 줄어든 여유를 다시 읽어 판정부터
      `DENY`다). 그때 원장은 게이트가 적은 `executed`로 남아 **리포트의 분모**에 실린다.
      ⛔ **M-67을 죽이던 아홉 건은 전부 승인 경로**였고(`approve()`가 행을 찾아야 해서),
      M-69의 하중은 **예산 정산 테스트**가 들고 있었다 — 둘 다 이 요구사항을 겨냥한
      자리가 아니다.
      ⇒ `test_remediate_loop.py` 3건 + `_RacingBudget` — 막힌 세 경우가 **원장에** 남는가
      (반환값이 아니라 `ledger.get`/`all_entries`에 묻는다) · 경합에서 진 예약이 `denied`로
      남는가 · 실패가 `failed`로 남는가(둘 다 회복하는 신호를 태워 공허 통과 방지).
      `Done:` 셋 각각 변이 red 확인 + `mutations.md` 기록 + `requirements.md` → `VERIFIED`.
- [x] [auto] **T9-5** 화해 2종 *(선택)* · `Implements: REQ-506, REQ-509`
      ⛔ **REQ-506의 기한 절반은 코드가 아니라 문서에만 있었다** — `unreconciled` 상태와
      `WR_RECONCILE_DEADLINE_DAYS` 설정은 있는데 **둘을 잇는 자리가 없어** 어떤 입력으로도
      그 상태에 도달하지 못했다. ⛔ **M-71이 살아남았다** — `measured`가 청구서에서 왔는지
      묻는 자리가 없어, 추정치를 실측 칸에 넣으면 `delta`가 0에 수렴해 *"추정이 정확했다"*로
      읽힌다. ⚠️ M-73의 하중은 이름이 REQ-505를 약속하는 테스트의 곁다리 단언이었다.
      ⇒ `InMemoryLedger.give_up_reconcile`(기한은 **저장소가** 묻는다 · 사유 필수 ·
      이미 화해된 행은 안 뒤집는다) + `test_domain_ledger.py` 6건.
      `Done:` 여덟 각각 변이 red 확인(M-70~M-77) + `mutations.md` 기록 + `requirements.md` → `VERIFIED`.

## T10 — 추적성의 빈 층 *(전부 오프라인)*

> ⛔ **T9-5가 드러낸 것**: `REQ-506`은 두 문장(멱등·기한)인데 기한 쪽은 코드에 없었고
> 상태 칸은 `IMPLEMENTED`였다. G6이 묻는 것은 *"테스트가 있는가"*지
> *"약속한 문장마다 코드가 있는가"*가 아니다.

- [x] [auto] **T10-1** ★ **문장 단위 커버리지** (G6⑤) — 규범 문단(EARS 표기~첫 빈 줄)을
      **구두점으로만** 자르고(종결 `다` 뒤 마침표 · 쉼표 붙은 `-고,`/`-며,`), 상태가 구현을
      주장하면 그 수만큼 **겨냥한 테스트 함수**를 요구한다. 세는 단위는 파일이 아니라 함수다 —
      파일로 세면 *"문장 셋에 겨냥 하나"*가 언제나 통과한다(M-80). 면제는 목록이 아니라
      **요구사항이 스스로 적은 구두점**이다(#4). ⚠️ 분리기는 **덜 세는 쪽으로** 틀린다.
      ⛔ 첫 실행이 잡은 것은 44개 중 **REQ-501 하나**였고 진짜였다 — 두 문장을 겨냥한 자리가
      이름이 둘 다를 약속하는 테스트 하나였고, 그 테스트는 `retry_of`를 안 묻고 있었다(M-82).
      `Done:` 변이 red 확인(M-78~M-82) + `mutations.md` 기록 + design 09§3.1에 ⑤ 명시.
- [x] [auto] **T10-2** 상태 칸이 증거와 어긋난 4건 — 넷은 **넷 다 다른 이유**였다.
      **REQ-206** 이미 덮여 있었다(M-42 다른 곳에 없는가 · M-44 리터럴 · M-50 지역 이름 우회)
      → `VERIFIED`. **REQ-305** 절반만 덮여 있었다 → M-83·M-84. **REQ-802** 디스크(M-12)만
      덮여 있고 시계·난수는 아니었다 → M-85·M-86. **REQ-801**은 `IMPLEMENTED`로 남긴다.
      ⛔ **REQ-305의 절반에는 도달하는 입력이 없었다** — 비가역 계약은 자동 경로에서
      게이트가 `APPROVE`로 먼저 멈춰서, `_rollback`의 *"계획이 없다"* 분기에 오는 입구는
      **승인뿐**이다. 그 경로를 태우자 **기존 가드 넷의 하중이 함께 늘었다**
      (M-13 13→14 · M-19 21→22 · M-26 10→11 · M-67 10→11).
      ⛔ *"더 조치하지 않는다"*는 **원장에 안 나타난다** — 롤백 실패 뒤 재시도를 넣어도
      `performed`도 `escalated`도 그대로고 늘어나는 것은 호출 횟수뿐이다(M-83).
      ⚠️ **REQ-801을 안 올린 이유**: 그 두 문장은 G5(T5-4)가 소유하고, 지금 G5를 만들면
      `adapters/`에 `fakes.py`뿐이라 **0개를 훑고 초록인 가드**가 된다. 실물 어댑터는 T2가
      잠근다. **M-86·M-85 red 확인 + M-83·M-84 red 확인 · 86종 전체 스윕 잔여 0.**
- [x] [auto] **T10-3** 설계↔코드 어긋남 2건을 **알려진 한계로 명시** — `mutations.md`에만
      적혀 있다: design 03§2의 절차에 코드에 없는 단계(원장 항목에 고정) · design 01§3의
      가역성 유도(타입만 보면 첫 배포가 REQ-102에 걸린다).
      ⚠️ **설계 변경은 범위 밖이다**(사람 판단) — 어긋남을 권위 문서가 말하게만 한다.
      `Done:` 둘 다 해당 설계 문서의 "알려진 한계"에 있고, 링크가 끊기지 않는다.

## T11 — 배포 축의 오프라인 절반 *(실행은 전부 `[manual]`)*

> ⛔ **T0-3이 열려도 지금은 거기서부터 다시 시작해야 한다** — `Dockerfile`도 배포 스크립트도 없다.
> 쓰는 것은 오프라인이고, **실행은 사람이 한다**. 루프의 권한 경계는 `gcloud`를 계속 막는다.
> ⚠️ **여기서 만드는 것은 전부 실물로 확인 안 된 것이다**(#3). 커밋이 그렇게 말해야 한다.

- [x] [auto] **T11-1** 배포 산출물 — `Dockerfile` + `scripts/deploy.sh`(**실행 안 했다**).
      값은 `config.py`가 소유하고(`SERVICE_NAME`·`MIN_INSTANCES`·`MAX_INSTANCES`·`IMAGE_REPO`),
      리전은 상수가 아니라 `Settings.region`이다 — 배포도 앱과 **같은 설정 로더**를 지난다.
      셸은 `tools/deploy_plan.py`가 렌더한 인자를 **받아서 실행만** 한다. **M-87~M-96 red 확인.**
      ⛔ **이 이미지는 HTTP를 서비스하지 않는다** — design 08§3의 엔드포인트는 REQ-602이고
      **T2-2가 소유한다.** `CMD`는 오프라인 데모를 돌린다. 이대로 올리면 포트 프로브에서 죽고,
      그것을 잡을 `deploy-check`(design 10§5)도 아직 없다(T2-4). Dockerfile 맨 위 상자가 그렇게 말한다.
      ⛔ **REQ-602·805를 승격하지 않았다**: 이 열은 *"산출물이 코드와 어긋나지 않는가"*를 태울 뿐
      *"Cloud Run에서 도는가"*를 태우지 않는다. `Verifies:`를 안 붙인 것도 같은 이유다 —
      붙이면 다음 사람이 **증명하지 않은 것을 근거로** 상태를 올린다.
      ⛔ **열 다 하중이 1이다** — 이 층에는 겹침이 없다. 검사 하나를 지우면 그 자리는 조용히
      사라지고 나머지 아홉은 초록이다. 배포는 게이트에 없으니 **두 번째로 알아챌 경로도 없다.**
      ⚠️ `chmod +x`가 무인 루프의 권한 밖이라 스크립트에 **실행 비트가 없다** —
      `make deploy`는 `bash`로 부른다(`mutate.sh`와 같은 방식).
- [x] [auto] **T11-2** REQ-805 가드 *(T11-1 선행)* · `Implements: REQ-805`
      "상시 과금 컴퓨트를 만들지 않는다" — 지금까지는 **훑을 대상이 0개라 공허 통과**였다.
      T11-1이 매니페스트를 만드니 비로소 집행 가능해졌다. `tests/test_no_always_billed_compute.py`
      넷: ① 표본으로 태우는 스캐너 ② 유휴 0을 **값으로** ③ 실행 표면의 GKE·VM·Cloud SQL
      ④ 설계 표의 같은 것. **M-97~M-99 red 확인.**
      ⛔ **일치 검사는 두 문서가 함께 틀리는 것을 못 잡는다**(M-97): 설계와 코드를 함께 1로
      바꾸면 T11-1의 열 개가 **전부 초록**이다. `1`이라는 값을 묻는 자리가 그때까지 없었다.
      ⛔ **상태를 안 올렸다**(TODO 그대로). 이 열은 *"산출물이 무엇을 선언하는가"*를 태우지
      *"실제로 0으로 수렴했는가"*를 못 태운다 — 후자는 T2-2의 증거다. ⚠️ 다만 `Verifies:`는
      붙였고(T11-1과 다른 판단: 여기는 REQ-805의 두 문장 자체를 태운다) 겨냥한 함수가
      **문장 수와 정확히 같아서**, 다음 사람이 올리면 G6⑤는 안 운다. 올리는 근거는 여기 없다.
      ⛔ **변이 번호의 천장은 M-99다** — `M-100`을 넣는 순간 신선도 가드가 `max(cases)`를
      문자열로 비교해 깨진다(`"M-100" < "M-99"`). 그래서 ①(표본·바닥)은 **안 태웠다.**
      다음에 변이를 더 넣는 사람이 그 비교를 숫자로 고치는 것부터 해야 한다.
- [x] [auto] **T11-3** 의존성 선언 정합성 — ⛔ **`google-adk`가 어디에도 선언돼 있지 않았다**
      (REQ-601은 대회 필수인데). `pyproject.description`도 구 정체성이었다.
      cloud extra에 선언(바닥 `>=2.7`은 design 06§2가 introspect한 2.7.1에서 온다) ·
      description 현행화 · `tests/test_dependency_declaration.py` 다섯:
      ① 표본으로 태우는 스캐너 ② 임포트마다 선언이 있는가 ③ 선언마다 매핑이 있는가
      ④ 필수 프레임워크가 선언에 **있는가**(값) ⑤ 클라우드 스택이 이미지에는 실리고
      게이트에는 **안 실리는가**. **M-100~M-105 red 확인.**
      ⛔ **④와 ⑤는 다른 질문이다**(M-102): `google-adk`를 `[project].dependencies`로 **옮기면**
      ④는 초록인데 게이트의 `.[dev]` 설치가 클라우드 스택을 끌고 와 REQ-801의 전제가 깨진다.
      ⇒ 선언의 **존재**와 선언의 **자리**를 따로 물어야 한다.
      ⛔ **REQ-601을 승격하지 않았다**(`TODO` 그대로) — 수용 기준은 **실물 호출**이다.
      `Verifies:`도 안 붙였다(T11-1과 같은 판단): 붙이면 다음 사람이 증명하지 않은 것을
      근거로 상태를 올린다.
      ⚠️ T11-2가 남긴 **M-99 천장을 이번에 걷었다** — `test_mutation_freshness.py`의
      `max(cases)`를 번호 비교로(`_number`), 그리고 **같은 계열의 두 번째 가정**인 M-49의
      `M-\d\d;`를 `M-\d+;`로. 천장은 하네스의 성질이 아니라 정규식 두 개였다.
      ⚠️ description은 **변이로 안 태웠다** — *"현행인가"*는 게이트가 판정할 질문이 아니다.
      판정 가능한 이웃(*"은퇴한 이름을 아직 말하는가"*)은 범위 밖이라 적어만 뒀다(#3).
- [x] [auto] **T11-4** `demo-target` 장애 주입 앱 *(T6-1의 오프라인 절반)* · `Design: 11§1`
      건강한 리비전(620ms) / **신호를 악화시키는 리비전**(900ms) + 테스트 7 · 변이 M-106~M-112.
      ⚠️ 지연을 `tunables`에 **안 뒀다** — 거기 사는 건 *재는 쪽*의 대기이고 이건 *재이는 쪽*의
      지연이다. 묶으면 타이머를 줄일 때 **장애 크기가 함께 바뀐다**.
      ⛔ 확인된 것은 "두 리비전이 서로 다른 이야기를 한다"뿐이다 — **Cloud Run이 그 이야기를
      하는지는 T2-3**(사람). `make check` 192 passed.
- [x] [auto] **T11-5** README 재현 절차 집행 · `Implements: REQ-901`
      README는 이 저장소에서 **아무 게이트도 안 지나는 유일한 문서였다.**
      `tests/test_readme_procedure.py` 다섯이 넷을 묻는다: ① 표본으로 태우는 스캐너
      ② 적어 둔 `make` 명령이 **실재하는 타깃인가** ③ 절차가 부트스트랩·게이트·**실행**을
      이름으로 갖는가(값) ④ 가리키는 저장소 경로가 실재하는가. **M-113~M-118 red 확인.**
      ⛔ **③이 첫 실행에서 red였다** — 재현 절차에 `make demo`가 **없었다.** 적힌 둘은
      *"이게 맞는가"*와 *"무엇을 약속했는가"*만 알려 준다. ②는 그때도 초록이었다(적힌 건 다
      살아 있었으니까) — **없는 것을 잡는 질문과 틀린 것을 잡는 질문은 다르다**(T11-2·T11-3
      계열의 네 번째 자리). ⇒ README에 한 줄을 넣었고 그 자리를 ③이 지킨다.
      ⚠️ 스캐너는 **코드 문맥만** 본다(펜스·인라인). 산문의 `make sure`를 타깃으로 읽으면
      영어 문장을 고칠 때마다 red고, **그런 가드는 꺼진다.** 대신 코드 밖 명령은 안 잡힌다(#3).
      ⛔ **REQ-901을 승격하지 않았고 `Verifies:`도 안 붙였다** — 나머지 넷(저장소 URL · 데모
      영상 · 다이어그램 · 실물 실행의 시각 증거)은 오프라인 게이트의 질문이 아니다.
      ⚠️ `make demo`의 *동작*은 이미 REQ-803이 subprocess로 태우고 있었다(M-114가 드러냈다).
      새로 생긴 것은 **README 쪽**이고, `make venv`·`make trace`는 여기가 처음 보는 자리다.
- [ ] [auto] **T11-6** 스윕 원본 로그의 **범위 집행** *(T11-5가 발견)* · `Implements: REQ-802`
      ⛔ `mutation-sweep-2026-08-22-192.log`는 **M-20에서 잘려 있다**(97줄·20 case). 그런데
      192 스윕 절은 그 파일을 *"112종 전부 red"*의 원본으로 **인용한다.** 신선도 가드는 절이
      무엇을 주장하는지는 보지만 **인용한 로그가 그 범위를 담는지는 안 본다** ⇒ 잘린 증거를
      커밋해도 초록이다. T11-2가 정한 *"원본을 커밋한다"* 규칙이 **한 판 만에 샜다.**
      `Done:` 최신 스윕 절이 인용한 로그가 ① 실재하고 ② 선언한 범위의 case를 **전부 담고**
      ③ ❌가 0인가 — 셋 다 변이 red 확인. ⚠️ 오래된 절은 안 건드린다(append-only).

## 가드 현황 (변이 red 확인한 것만 `[x]`)

| 가드 | 태스크 | 상태 |
|---|---|---|
| G1 실행 차단 | T4-9 | **[x]** M-18 |
| G2 `assumed` 불변 | T1-2 | **[x]** M-06 |
| G3 귀속↔검증가능성 | T1-1 | **[x]** M-07 |
| G4 모든 항목에 판정 | T4-10 | **[x]** M-19 |
| G5 게이트 오프라인 | T5-4 | [ ] |
| G6 ★ 추적성 | T0-2 · T0-5 · T10-1 | **[x]** M-01~M-05 · M-23~M-25 · M-78~M-81 |
| G7 1회=1행 | T1-3 | **[x]** M-08 |
| G8 ★ `improved` 유도 | T1-5 | **[x]** M-13 |
| G9 ★ 검증불가는 AUTO 아님 | T1-6 | **[x]** M-14 |

**게이트**: `make check` → **197 passed** (2026-08-23 로컬 macOS·py3.13)
⚠️ 숫자는 **여기 한 곳에만** 적는다. ⛔ 다만 `docs/evidence/mutations.md`의 최신 스윕 기준선은
**같은 숫자를 다시 적는다** — 그건 중복이 아니라 *"기록이 어느 스위트를 보고 적혔는가"*이고,
T0-8의 가드가 그 둘이 갈라지는 것을 집행한다.
