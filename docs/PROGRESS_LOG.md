# PROGRESS_LOG — warranty

> 최신 3–5개 증분. **최신이 위.** **≤120줄.** 넘치면 월별 아카이브로 민다.
> 밀려난 것 → [`archive/PROGRESS_LOG-2026-08.md`](archive/PROGRESS_LOG-2026-08.md) (T6-2 이전 전부).
> 권위: 요구사항=`specs/warranty/requirements.md` · 계획=`specs/warranty/tasks.md` ·
> 현재 상태=`docs/OVERVIEW.md` §10 · 검증 기록=`docs/evidence/`.

---

## 2026-08-30 — 실증하려다 이 시스템이 자기 논지에 네 번 걸렸다 (gate 442 → 447)

- **Status**: 제출물 일체 완성 + **push 완료**(`d0d012f`). 남은 것은 사람 손뿐 —
  Medium 발행 · Devpost 제출(**09-01 09:00 KST**) · teardown(09-02 · 서비스 여섯).
- **⛔ 이 세션의 발견**: 영상의 **성공 장면**을 찍으려는데 건강한 리비전으로 되돌려도 계속
  `not_recovered`가 나왔다. 파니 결함이 넷이었고 **넷 다 로그로는 초록이었다** —
  ① 재측정 창이 조치 **이전**을 물었다 ② 회복 기준이 **물리적으로 도달 불가**였다
  ③ 계약이 **옛 정책**을 들고 있었다 ④ 기다림을 늘리자 **답이 죽었다**.
  ⚠️ **로그로 못 찾았다.** 어긋나면 안 되는 두 숫자(`990`·`674`)를 노려보다 찾았다.
  ⇒ 원인·증거·수치 전부 [`evidence/verify-window-2026-08-30.log`](evidence/verify-window-2026-08-30.log).
- **⛔ 논지를 재구성했다** — *"클라우드 중립성"* 프레임이 공감대가 좁고 저장소를 **Flagger와
  같은 링**에 올려놨다. **코드 0줄 수정, 질문만 바꿨다**: *"에이전트 함대가 밤새 뭘 했는지
  아침에 어떻게 아는가."* 근거와 매핑은 D16 · `HACKATHON.md` §0.
- **⭐ 처음으로 `recovered`가 나왔다**(`990.04 → 674.17ms`), 반대편도 실물이 됐다
  (`674 → 990` → 롤백 → `signal_restored` → 트래픽 되읽기). **같은 조치, 두 판정.**
- **Changed**: 검증 상수 3종 + 배포 인자 `--timeout` · UI 전면 영어화 + 표 폭 ·
  가드 5개(`test_recovery_policy.py` 신설) · 변이 M-281~285 · 영상 3:47 ·
  갤러리 7장 + drawio + 로고 · Medium 아티클 · HACKATHON/OVERVIEW/DEVPOST 논지 교체.
- **Verified**: `make check` **447** · `mutate.sh all` **285/285 red · 조용히 무효 0**
  ([sweep](evidence/mutation-sweep-2026-08-30-447.log)) · 배포 `00012-487`(타임아웃 540s) ·
  실물 왕복 3종 · push 15커밋 · raw URL 200.
  촬영 기록 [`evidence/demo-video-2026-08-30.log`](evidence/demo-video-2026-08-30.log).
- **⚠️ 변이가 셋 낡았다**(M-275·276·284) — 번역과 상수 이동이 perl 대상을 옮겼다.
  하네스의 「조용히 무효」 검사가 잡았고 **재조준 후 전체를 다시 돌렸다.**
- **Blockers**: 없음.
- **Next**: T8-5 제출 → T8-8 URL 수명 → T8-6 teardown.
  ⚠️ `submission/SCRIPT.md`가 아직 **옛 논지의 대본**이다 — 산출물과 다른 이야기를 한다.

## 2026-08-29 — 세 번째 다리를 세우고, 논지가 도착할 화면을 만들었다 (gate 422 → 442)

- **Status**: 비용 축(③)이 실물이 됐고 **`GET /`에 원장 화면**이 생겼다. ⛔ 아직 배포 전이다.
- **⛔ 이 세션의 발견 — 논지의 세 다리 중 하나가 통째로 비어 있었다**: `requirements.md` §0은
  GCP 전용이 **검증·원자적 롤백·조치당 비용** 셋을 산다고 말한다. 프로덕션 원장을 읽어 보니
  셋째가 전부 0이었다. 파고 들어가니 셋이 더 나왔다:
  ① **`Method.RESOURCE_LABEL`이 테스트에만 있었다** — 코드가 `fl_entry` 라벨을 리소스에 박은
  적이 **한 번도 없다.** ② **`runtime.provision`은 원장 행조차 안 남겼다** — 돈을 쓰는
  리소스가 원장 밖에서 태어났다. ③ 단가표가 비어 있었다(`TokenPrices({})`).
- **⭐ 그런데 그 0은 버그가 아니었다**: `ModelCallMeter`는 단가를 못 찾으면 조용한 0을 안 만들고
  `Method.NONE` + 사유를 적어 왔다. **설계가 이미 옳았고 빠진 것은 숫자와 출처뿐이었다.**
- **⛔ 그리고 내 첫 처방이 틀렸다**: *"Flagger 아니냐"*에 **조치를 늘려서**(동시성) 답하려 했는데,
  그건 ①(검증) 축을 넓히는 일이고 **①이 정확히 Flagger가 이미 하는 축**이다. 진짜 답은 ③이다.
- **⛔ 조치의 compute 비용은 0이 맞다**: 트래픽 전환은 과금 리소스를 안 만들고, 동시성 변경이
  만드는 리비전의 트래픽은 **어차피 서빙됐을 것**이다. ⇒ *"이 조치가 $X 썼다"*를 만들면
  그건 지어낸 수다. **`wasted_usd`에 대한 기대를 낮추고 그 사실을 문서에 적었다.**
- **Changed**: `prices.py`(단가표 + 출처·티어·확인일·**유효기간**) · `EntryKind.PROVISION` ·
  `cost_labels`/`label_of`(순수) · `runtime.provision`이 원장 행과 라벨을 낸다 ·
  **`ui.py`**(순수 렌더러) + `GET /` 라우트 + `LiveDashboard`.
- **⭐ UI**: 배포된 서비스가 원장을 사람이 읽는 표로 낸다 — **읽기 전용, 버튼 없음.**
  `실행됨` 옆에 `나아짐`, 금액 옆에 **귀속**이 있다. `OVERVIEW` §11의 *"웹 대시보드"*를
  빼면서 왜 뺐는지 적었다: 범위 밖인 것은 **조작 표면**이고, *"화면이 아예 없다"*는 다른
  문제였다 — **읽는 사람에게 도착하지 않는 논지는 논지가 아니다.**
- **⚠️ 출처의 한계를 숨기지 않았다**: 우리는 Vertex로 부르는데 직접 읽힌 가격 페이지는
  Gemini Developer API 쪽이다. Cloud Run 단가는 **Cloud Billing Catalog API**에서 SKU id와
  함께 읽었다(`4856-B847-F1EB`·`02A2-9231-36A6`) — 웹 페이지보다 강한 출처다.
- **⛔ 변이가 내 테스트를 여섯 번 잡았다**: M-276(각주가 단언을 공짜로 통과시킴) ·
  M-277(이스케이프 경로가 둘인데 하나만 태움) · M-280(라우트에 테스트가 없었고, 붙인 뒤에도
  `except`가 같은 응답을 내 **행동적으로 동등**했다) · **M-234·M-243·M-250**(내가 고친 줄을
  가리키던 낡은 변이 셋 — 안 걸리는 변이는 red 확인을 공짜로 유지한다).
  ⇒ M-280은 *"합성이 안 됐다"*와 *"읽다가 죽었다"*를 갈라서 고쳤다. **둘은 다른 사실이다.**
- **Verified**: `make check` **442 passed** · 전체 스윕 **280종 전부 red**, `❌` 0,
  복구 280 · 잔여 0 (1400줄). ⚠️ 첫 실행은 278/280이었고 낡은 변이 둘을 고쳐 **다시 돌렸다.**
- **Changed(문서)**: `HACKATHON.md` §0 신설(목적↔심사 매핑) · §6의 REQ 번호 둘 정정
  (`Runtime(REQ-5xx)`→6xx · `비용 축(REQ-2xx/4xx)`→5xx — §4의 REQ-307과 같은 종류의 버그) ·
  `SCRIPT.md` 3:30 비트를 **Firestore 콘솔 → 우리 화면**으로 교체.
- **⭐ 실물 확인 완료** (리비전 `00007-mrq` · 이미지 `d51566c`):
  ① **화면**이 `GET /`에서 200 HTML(7,260B · 2.51s)을 낸다 — 배포된 에이전트가 그린 것이다.
  ② **라벨이 GCP 리소스에 박혔다**: `fl_entry=01m16hev85z5b7b4ykzc73tm95`이 원장 항목 id와
  **같다.** 응답의 주장이 아니라 `gcloud run services describe`로 **되읽은 값**이다.
  ③ **금액이 0을 벗어났다**: 모델 호출 행이 `token_meter · $0.00174675`.
  ⭐ 같은 화면에 **배포 전 행(`none`·0)과 배포 후 행(`token_meter`·0.0017)이 나란히** 있다 —
  그 대비가 무엇을 고쳤는지 한 줄로 말한다: 계량이 없어서가 아니라 **단가를 몰라서** 0이었다.
- **⛔ 0인 것들과 그게 맞는 이유도 로그에 적었다**: 갓 만든 서비스는 아직 안 썼고(추정 안 한다),
  트래픽 전환은 과금 리소스를 안 만들고, 그래서 `wasted_usd`는 0이 맞다.
- **teardown 범위가 다섯이 됐다**: `day1-cost-demo` 추가(T8-6 · OVERVIEW·tasks 반영).
- **문서 반영**: README 첫 표에 화면 링크 · DEVPOST "Try it out"의 **첫 줄**로 올림 ·
  DEVPOST §4의 40%/30% 자체평가를 실물에 맞춰 고침(약점 둘을 숨기지 않고 적음).
- **⭐ 그리고 두 번째 조치도 실물에서 완주했다**(T2-5): 부하를 켠 채 `concurrency:16` 한 줄로
  게이트 `AUTO` → 새 리비전 `demo-target-00003-67d`(동시성 16) → `674.17 → 988.60` →
  `not_recovered` → 롤백 → 배분 되읽기 `00001-swl = 100`. **롤백 코드는 한 줄도 안 늘었다.**
- **⛔ 그러면서 하나 알게 됐다 — 롤백은 트래픽이지 템플릿이 아니다.** 되돌린 뒤에도 서비스
  템플릿의 동시성은 16으로 남는다. 결함이 아니라 **원자성의 정의**다: 한 번의 호출로 즉시
  되돌릴 수 있는 것은 배분이고, 템플릿까지 되돌리면 **리비전을 또 만드는 일**이라 원자적이지
  않다. ⚠️ 대가는 있다 — 다음 템플릿 변경이 16을 물려받는다. 증거 로그 §11에 적었다.
- **화면이 세션 전체를 한 장으로 말한다**: `실행됨 1 · 나아짐 0 · 되돌림 1 · 헛쓴 비용 $0 ·
  그중 추정만 1`, 그리고 표의 마지막 조치 행이 `concurrency:16 · not_recovered · 롤백 true ·
  none · $0`이다.
- **⭐ 대본 전 비트를 프로덕션에서 리허설했다** — 실측 시간과 출력을 `SCRIPT.md` §3-b에 박았다:
  Day-1 13.8s · **핵심 141.6s** · 게이트 거부 5.6s · 리포트 4.3s · 화면 0.53s.
  ⛔ **핵심 비트가 대본 배정(100s)을 41초 넘는다** — `VERIFY_DELAY_S=45`가 두 번 들어간다.
  대기 구간을 컷하는 편집이 필요하고, **이제 그 근거가 추측이 아니라 실측이다.**
- **⛔ 리허설이 진짜 한계를 하나 잡았다**: 같은 조치를 두 번 돌렸는데 한 번은
  `674 → 989`, 한 번은 **`674 → 674`**였다 — 부하는 둘 다 켠 채였다. Monitoring 도착
  지연 때문에 120초 창이 조치 이전 표본에 지배당한다. ⭐ **판정은 둘 다 `not_recovered`로
  옳았다**(fail-closed). 그러나 *"효과가 없었다"*와 *"아직 못 읽었다"*를 이 창 안에서는
  못 가른다. ⇒ `OVERVIEW` §12 · DEVPOST 한계 · 대본에 **"나레이션을 값에 걸지 말 것"**으로 반영.
- **리허설 잔여물 정리**: `day1-take` 삭제(서비스 다섯 유지). demo-target 리비전은 넷이 됐다.
- **Next**: 부하를 켠 채 4분 영상(T8-3) → 제출(09-01) → teardown(09-02).
- **Evidence**: `docs/evidence/mutation-sweep-2026-08-29-442.log` ·
  `docs/evidence/live-cost-axis-2026-08-29.log`.
