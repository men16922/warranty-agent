# PROGRESS_LOG 아카이브 — 2026-08

> `docs/PROGRESS_LOG.md`에서 예산(120줄) 때문에 밀려난 증분이다. **최신이 위.**
> 살아 있는 로그는 [`PROGRESS_LOG.md`](../PROGRESS_LOG.md) — 여기는 **읽기 전용 역사**다.
> 권위: 요구사항=`specs/warranty/requirements.md` · 계획=`specs/warranty/tasks.md` ·
> 현재 상태=`docs/OVERVIEW.md` §10 · 검증 기록=`docs/evidence/`.

---

---

## 2026-08-29 — 첫 화면이 내세우던 Day-1 절반이 비어 있었다 · T3-1 (gate 394 → 399)

- **Status**: T3-1 `[x]` · REQ-101 `VERIFIED`. 실물 `provision`은 08-29까지
  `not_implemented`를 돌려주고 있었다 — Day-2만 실물이었다.
- **Changed**: `adapters/live_provision.py`(생성·되읽기) · `ContractStore.put`을 **포트에**
  넣고 Firestore에 구현 · `runtime.provision`이 생성→유도→기록을 **한 번에** 한다.
  계약은 어댑터가 안 만든다 — `derive_contract`가 생성 응답에서 유도한다(REQ-103).
- **이미지 출처가 하나다**: 새 서비스의 이미지를 설정으로 안 받고 *"지금 내가 무엇으로
  도는지"*를 Cloud Run에 물어서 쓴다. 그래서 만들어진 것은 `warranty-api`와 **같은 태그**
  (`dbb6f74`)로 떴다. 설정으로 받으면 값의 출처가 둘이 되고 한쪽만 낡는다.
- **Verified(실물)**: `day1-warranty-demo` 리비전 `00001-k9c` Ready·ContainerHealthy ·
  minScale 0 · 계약 `01m14xkweqd3bb0cf9ne4bwjkg`가 Firestore에 났고, **`inspect`가 같은
  실행에서 그것을 읽었다** — Day-1→Day-2 인계가 실물에서 성립했다.
- **⛔ `irreversible`은 결함이 아니다**: 갓 만든 서비스에는 돌아갈 리비전이 없다. 타입만
  보고 `reversible`을 쓰면 그 계약은 필요한 날 틀린다(design 01§3). 그리고 그 결과의 값은
  판정에서 난다 — 그 리소스는 자동 조치 대상이 아니다(REQ-402).
- **⛔ 만든 서비스는 아무도 못 부른다**: IAM 바인딩이 비어 403/401이다. 프로비저너는 초대
  권한을 주지 않는다 — 에이전트가 조용히 서비스를 전 세계에 여는 것은 기본값이 될 수 없다.
  대가는 분명하다: 그 리소스의 신호를 밖에서 부하로 못 채운다. 주장하지 않고 T3-5로 열었다.
- **G5가 자기 일을 했다**: `LiveContractStore.put`이 생성 경로를 부르면서 tripwire를 안
  지나 red가 났다. 가드가 시킨 대로 census가 아니라 **함수의 첫 줄**을 고쳤다(M-252).
- **Verified**: `make check` **399 passed** · 전체 스윕 **252종 전부 red**, `❌` 0,
  복구 252 · 잔여 0.
- **Next**: T8-3 영상(부하 켠 채) → 제출(09-01) → teardown(09-02, `day1-warranty-demo` 포함).
- **Evidence**: `docs/evidence/live-provision-2026-08-29.log`.

## 2026-08-29 — 참인데 말할 자리가 없던 요건 다섯을 회수했다 · T5-3 (gate 392 → 394)

- **Status**: T5-3 `[x]`. **TODO 7 → 3 · VERIFIED 36 → 41.** 남은 셋은 진짜 미구현이다.
- **Changed**: `tests/test_new_project.py`(REQ-902) 둘을 새로 썼다 — 선언이 저장소 밖 출처를
  끌어오는가 · 베끼어 심은 트리가 있는가. 편입의 흔한 모양이 **복사**라 따로 묻는다.
  REQ-602·901은 **새 테스트를 안 썼다** — 이미 검증하던 테스트가 선언만 안 하고 있었다.
  REQ-805·801은 겨냥·변이를 이미 갖고도 낮게 적혀 있었다.
- **⛔ 못 묻는 절반은 안 주장한다**: 실물에서 도는가(602) · 영상과 시각 증거(901) · 작성
  날짜(902). 셋 다 요구사항 본문에 *"게이트가 이것을 검증한다고 하지 않는다"*를 적었다.
  특히 902의 날짜는 `git log`로 물으면 shallow clone·tarball에서 **참인데 red**가 된다 —
  요구사항이 아니라 체크아웃 방식을 태우는 것이라 안 묻는다.
- **⛔ 하네스가 열한 번째로 스스로 틀렸다**: `backup`은 있는 파일만 다루고 `restore`는 복사만
  해서 *"금지된 것을 하나 만들어 본다"*류 변이를 표현할 수 없었다. `create()`를 넣었는데
  **잎**(`vendor/borrowed/__init__.py`)을 기록해 복구가 파일만 지우고 빈 `vendor/`를 남겼다.
  더 나쁜 것은 `residue`도 그 잎만 봐서 **"잔여 없음"이라고 말한 것**이다 — 복구 실패와 잔여
  보고가 서로 다른 것을 보고 있었다. **아직 없는 가장 위 조상**을 기록하게 고쳤다.
  ⇒ 오염된 첫 스윕은 버리고 전체를 처음부터 다시 돌렸다.
- **Verified**: `make check` **394 passed** · 전체 스윕 **248종 전부 red**, `❌` 0,
  복구 248 · 잔여 248/0 (1240줄 원본 로그).
- **Next**: T8-3 영상(부하 켠 채) → 제출(09-01) → teardown(09-02).
- **Evidence**: `docs/evidence/mutation-sweep-2026-08-29-394.log`.

## 2026-08-29 — 신규 코드 요건은 참이었고, 그것을 말할 자리가 없었다 · T8-4 (gate 392 유지)

- **Status**: T8-4 `[x]`(확인 완료). REQ-902의 상태 칸은 여전히 `TODO`다 — 아래가 이유다.
- **Verified**: 첫 커밋 `741cec5` 2026-08-19, 79개 전부 08-19~08-29 — 제출 기간 08-03~08-31
  안이다. `dependencies = []`로 게이트는 표준 라이브러리만 쓰고, `[cloud]`는 전부 공개 Google
  패키지다. `git+`·`file://`·사설 인덱스 0건, `vendor/`·`third_party/` 없음.
- **⛔ 같은 벽이 셋째**: REQ-602·901·902 셋 다 **사실은 참인데** 겨냥한 테스트가 0이라
  게이트가 `TODO`를 강제한다. 손으로 확인한 것을 상태 칸에 올리려면 집행하는 자리가 필요하다
  — 그게 이 저장소가 스스로에게 건 규칙이고(§9), 여기서 예외를 두면 규칙이 규칙이 아니게 된다.
  ⇒ T5-3을 셋을 함께 닫는 태스크로 넓혔다.
- **Next**: T5-3(겨냥 + 변이) 또는 T8-3 영상. 제출 09-01 · teardown 09-02.

## 2026-08-29 — 헤드라인이 측정하지 않은 수를 측정치처럼 말하고 있었다 · T8-2 (gate 392 유지)

- **Status**: T8-2 `[x]`. README를 심사자 기준으로 걸으며 결함 셋을 회수했다.
- **⛔ 가장 큰 것**: 첫 화면이 `executed 41 · improved 23 (56%) · rolled back 12`를
  **측정치처럼** 적고 있었다. 그 수는 어디서도 측정되지 않았다 — 영상 대본(design 11§3)의
  리포트 슬라이드 값이 README와 OVERVIEW§7로 번진 것이다. 원장의 조치는 **둘**이고
  `make demo`는 `executed 1 · improved 0`을 낸다. **심사자가 재현하면 첫 화면과 어긋난다.**
- **왜 세 번째인가**: OVERVIEW §10이 이미 같은 병으로 `120 passed`·`VERIFIED 18` 넷을 틀렸고
  (T0-6), 그때 내린 규칙이 *"세는 자리는 하나다"*였다. 그 규칙이 §7과 README에는 안 닿아
  있었다 — 규칙을 적은 곳과 어긴 곳이 **같은 파일**이었다.
- **Changed**: 세 자리에서 수를 지우고 모양만 남겼다. 11§3은 리포트 칸을 *"그 실행이 실제로
  낸 값"*으로 바꿨다 — 숫자를 크게 만들고 싶으면 원장을 채우는 실행이 먼저다.
- **또 둘**: README 상태가 *"Design complete, implementation in progress"*였다(실물이 도는데).
  실물 URL 표와 `/livez` 200 · 무토큰 401 · demo-target `/work` 200을 넣고 셋 다 재확인했다.
  `make demo`의 caveat은 *"REQ-601·602는 TODO"*라고 말했는데 REQ-601은 `VERIFIED`다 —
  상태 주장을 지우고 `requirements.md`를 가리키게 했다.
- **Verified**: `make check` **392 passed**. `test_demo.py`가 caveat에서 `REQ-601`을 요구하는데
  문자열은 남아 있어 초록이다 — 지운 것은 **상태 주장**이지 참조가 아니다.
- **Next**: T8-3 영상(부하 켠 채) → T5-3(REQ-602 겨냥) → 제출(09-01) → teardown(09-02).

## 2026-08-29 — 신호는 트래픽이 흐르는 동안에만 존재한다 · T8-1 (gate 392 유지)

- **Status**: T8-1 `[x]`. 08-28에 `null`이던 신호가 부하 아래에서 값을 냈다.
- **Changed**: 코드는 안 건드렸다 — 실물 관측만 했다. demo-target `/work`에 1140건
  (워커 5 · 200초 · 약 5.7 req/s)을 넣어 120초 창을 채우고, 수집 지연 동안 창이 비지 않게
  워커 3으로 부하를 유지한 채 프로덕션 `inspect`를 불렀다.
- **Verified**: p95 **674.17 ms** · 관측점 1 · 계약 `demo-target-warranty-v1` ·
  롤백 대상 `demo-target-00001-swl`. 조치는 실행하지 않았다.
- **대비가 요점이다**: 같은 질문에 08-28은 "점 0 · null", 08-29는 "674.17". 08-28의 답은
  고장이 아니라 계약대로였고(REQ-205), **영상에 담을 그림이 아니었을 뿐이다.**
  ⇒ 촬영은 부하를 켜 둔 채로 한다. 이건 scale-to-zero의 대가이고 REQ-805를 지키는 값이다.
- **⚠️ 확인하지 않은 것**: 674.171849767732가 08-28 기준선과 찍힌 자리까지 같다. 고정
  620ms + 히스토그램 버킷 보간이면 결정론적이 되는 것으로 설명되지만 **확인하지 않았다.**
  관찰만 적는다 — 재현성에는 유리하고, 원측정이 아닐 가능성은 열려 있다.
- **Next**: T8-3 영상(부하 켠 채) → T8-2 README 검수 → 제출(09-01) → teardown(09-02).
- **Evidence**: `docs/evidence/live-signal-load-2026-08-29.log`.

## 2026-08-28 — 공개 URL이 드디어 200을 냈다 · 커밋 → 배포 → 프로덕션 왕복 (gate 392)

- **Status**: T5-1의 배포 절반 완료. 직접 `/actions/*` 경로는 아직 501이다.
- **Changed**: 미커밋 29건을 `dbb6f74` 하나로 묶어 커밋했다 — 배포 태그가 커밋 SHA라
  **커밋이 곧 배포 블로커**였다. 빌드 `e5fb704c` SUCCESS, 리비전 `00003-z9m` 트래픽 100%.
- **Verified**: `/livez` 200 공개 · 무헤더 401 · 틀린 토큰 401 · **유효 토큰 200**(이전 배포는 501).
  프로덕션 `inspect` 한 번이 Firestore 원장에 model_call 두 행(`01m1404chq…`·`01m1404cnt…`)을
  남겼다 — REQ-603이 로컬이 아니라 배포된 리비전에서 성립한다.
- **⛔ 경계**: 신호 점 0 · 값 `null`. demo-target에 트래픽이 없어 p95 창에 표본이 없다.
  에이전트는 *"건강하다"*가 아니라 **"지금은 읽을 수 없다"**를 답했다 — 계약대로다(REQ-205).
  ⇒ 영상은 **부하가 먼저다**(T8-1을 새로 열었다).
- **⛔ 상태 칸이 실물보다 뒤에 있다**: REQ-602(대회 필수 · Cloud Run에서 돈다)는 실제로 도는데
  `TODO`다. `Verifies: REQ-602`를 단 테스트가 0이라 게이트가 그렇게 강제한다 — 게이트가 틀린 게
  아니라 **겨냥한 자리가 없다.** 조용히 올리지 않고 T5-3으로 열어 뒀다.
- **Next**: T8-1 부하 → T8-3 영상 → T8-2 README 검수 → 제출(09-01) → teardown(09-02).
- **Evidence**: `docs/evidence/live-agent-chat-2026-08-28.log` · `docs/evidence/deploy-2026-08-28.log`.

## 2026-08-28 — ADK가 실물 조치·검증·원자적 롤백을 끝까지 돌렸다 (gate 372 → 392)

- **Status**: T2-1·T2-4·T5-2·T6-1 `[x]`; REQ-601·603·604 `VERIFIED`. 공개 리비전 배포는 아직이다.
- **Changed**: 실행자·Firestore 예산·시계/ULID·Vertex 전송·합성 지점·ADK 단발 세션을 만들고,
  인증된 `/agent:chat` JSON을 실물 콜백에 연결했다. `(default)` Firestore Native DB와 계약도 생성했다.
- **Verified**: `make check` **392 passed** · M-01~M-246 **246종 전부 red**, 복구 392·잔여 0.
  실물 ADK 응답은 `AUTO`, p95 `674.17 → 988.60`, `not_recovered`, 건강 리비전 100% 롤백.
- **원장/예산**: `01m13fpgc8e091es3ekpqx48f4` · 잔액 `$0.49` · 미정산 0.
- **Boundary**: `signal_restored=false` — 120초 p95 창에 장애 표본이 남아 신호 복구는 증명 못 했다.
- **모델 계량**: 실물 `inspect`의 ADK 모델 응답 둘이 Firestore `model_call` 두 행이 됐다.
- **Blockers**: 현재 Cloud Run 리비전은 이전 SHA다. 커밋 없이 배포하면 이미지 태그가 거짓이 된다.
- **Next**: 명시적 커밋·재배포 → bearer `/agent:chat` 프로덕션 왕복 → 영상.
- **Evidence**: `docs/evidence/live-adk-remediate-2026-08-28.log`.

## 2026-08-27 — 논지 한 덩어리를 응답 모양으로 고정했다 · T5-1 렌더러 (gate 356 → 372)

- **Status**: T5-1 `[~]`. design 08§3.1의 응답을 내는 `wire.py`가 생겼다. 경로는 아직 501이다.
- **Changed**: `remediate_response()` — `rule`·`rationale`·`verified_traffic`이 **필수 칸**이고,
  `executed`·`improved`·`rolled_back`이 셋으로 따로 나간다. 돈은 문자열, 빈 창은 `null`,
  실측은 추정 **옆칸**이다. 테스트 16 · 변이 M-224~M-232.
- **왜 이 셋인가**: 4분 안에 논지가 전달되려면 화면에 보여야 한다. 로그에만 있으면 없는
  것과 같다 — `performed: true`만 남기고 배분을 지우면 그것은 측정이 아니라 주장이다.
- **Verified**: `make check` **372 passed** · 전체 **232종 red**, `❌` 0, 복구·잔여 232/0
  (1160줄 원본 로그).
- **Blockers**: `POST /actions/{action_id}:remediate`가 501이라 이 덩어리는 아직 아무에게도
  안 보인다. 경로를 열려면 실물 `ActionExecutor`·`BudgetStore`와 합성 지점이 먼저다.
- **Next**: **T2-4 잔여** — 실행자·예산·합성 지점 → ADK 도구 넷 → `/agent:chat` 실물 왕복.

---

## 2026-08-27 — 계약과 원장이 Firestore로 나갔다 오는 길 · T14-1 (gate 330 → 356)

- **Status**: T14-1 `[x]`. `ContractStore`·`Ledger`의 실물 절반이 생겼다. 배선은 아직 아니다.
- **Changed**: `live_store.py`(문서 매핑·질의·트랜잭션 전이) · 원장 전이 넷을 `domain/entry.py`의
  순수 함수로 분리하고 인메모리 저장소가 그것을 **위임**하게 했다 · design 08§2.1 · 테스트 26 ·
  `google-api-core` 선언(`AlreadyExists`를 이름으로 잡는다 — 1회=1행을 Firestore가 집행한다).
- **왜 분리했나**: 저장소가 둘이 되는 순간 불변식이 두 벌이 된다. 두 벌이면 한쪽만 고쳐지는
  날이 오고, 그날 원장은 **저장소에 따라 다른 것을 허용한다**. 어댑터에 `replace(` 가 없다는
  것을 소스로 집행한다.
- **⛔ 가드가 먼저 틀렸다**: M-222(Firestore 생성 입구의 tripwire 제거)가 **안 죽었다.**
  G5 census가 함수를 **단순 이름**으로 담고 있어서 `LiveContractStore._db`를 지워도
  `LiveLedger._db`가 그 자리를 덮고 있었다 — 같은 이름의 메서드가 서로를 가린 것이다.
  census를 클래스까지 붙인 이름으로 고친 뒤 M-222는 red가 됐다. 이 blind spot은
  live 어댑터가 **둘 이상 같은 이름의 메서드를 가진 첫 모듈**에서만 보였다.
- **⛔ 리팩터가 변이 여덟을 껐다**: 전이를 모듈 함수로 옮기자 M-06·M-70·M-71·M-73~M-77의
  패턴이 들여쓰기와 `{entry_id}`→`{current.entry_id}`에서 어긋나 **적용조차 안 됐다.**
  하네스가 *"변이가 파일을 바꾸지 못했다"*로 잡았다 — 그 줄이 없었으면 여덟은 조용히
  죽은 변이가 됐다. 전부 다시 겨눠 red 회복.
- **Verified**: `make check` **356 passed** · M-214~M-223 red · 전체 **223종 red**, `❌` 0,
  복구·잔여 223/0(1115줄 원본 로그).
- **Blockers**: 실물 Firestore 왕복은 미수행(컬렉션도 아직 없다). 실물 `ActionExecutor`·
  `BudgetStore`와 이들을 조립하는 합성 지점이 없어 `/agent:chat`은 여전히 501이다.
- **Next**: **T2-4 잔여** — 실행자·예산·합성 지점을 만들어 ADK 도구 넷을 실물 어댑터에 붙인다.

---

## 2026-08-27 — 공개 URL은 200, 과금 경로는 401 — 실물이 계약대로 답했다 · D15 (gate 330 유지)

- **Status**: T2-4의 D15 실물 `[x]`. 로컬에서만 참이던 인증 경계가 Cloud Run에서 같은 값을 냈다.
- **Changed**: `secretmanager` API 활성 · 비밀 `warranty-agent-auth` v1(64B) · 런타임 SA에
  `secretAccessor` · 이미지 `a7c660d` 재배포 → 리비전 `warranty-api-00002-c6q` 100% 트래픽.
  ⚠️ 배포 직전에 발견: 문서는 gate 330을 말하는데 HEAD는 296이었다. 증분 4개가 미커밋이라
  이미지 태그(=커밋)가 가리키는 곳에 D15 코드가 없었다 — 먼저 커밋하고 배포했다.
- **Verified**: `/livez` **200** 공개 · 무헤더/틀린 토큰/틀린 스킴 **전부 401** · 유효 토큰만
  **501**(어댑터 없음 사유 포함) · 선언 안 된 경로 404. 구형·신형 URL 둘 다 같은 서비스.
  증거 `docs/evidence/d15-auth-matrix-2026-08-27.log` · `deploy-2026-08-27.log`.
- **Blockers**: `503`(비밀 미주입) 분기는 실물에서 안 만들었다 — 만들려면 살아 있는 리비전의
  비밀 바인딩을 떼야 해서, 로컬 계약·변이 검증(M-206~M-213)으로만 남는다.
- **Next**: **T2-4 잔여** — `RunControl`·`SignalSource`·Firestore를 ADK 도구 경로에 배선해
  `/agent:chat`의 501을 실물 왕복으로 바꾸고 원장까지 잇는다.

---

## 2026-08-27 — 공개 URL과 무인 과금 권한을 갈랐다 · D15 (gate 311 → 330)

- **Status**: D15 앱 인증 경계의 로컬 구현·검증 완료. 실물 배포는 아직 이전 리비전이다.
- **Changed**: bearer 검증, 503/동일 401/유효→501 서버 경계, 공개 invoker+Secret Manager 배포 렌더링, 설계·테스트.
- **Verified**: `make check` **330 passed** · M-206~M-213 red · 전체 213종 red, `❌` 0, 복구·잔여 213/0(1065줄).
- **Blockers**: 비밀 생성·`secretAccessor` IAM·재배포·실물 HTTP 확인, ADK/Firestore 도구 배선은 미수행.
- **Next**: D15를 재배포해 `/livez` 공개와 503/401/501 행렬을 실증한 뒤 T2-4 실물 왕복을 잇는다.

---

## 2026-08-27 — 시작 문맥 예산을 다시 회복했다 · T13-6/T12-9 (gate 311 유지)

- **Status**: T13-6/T12-9 `[x]`. 완료 서사를 현재 계획에서 걷어내고 열린 판단만 남겼다.
- **Changed**: `PROGRESS_LOG` 1012→55줄 · open `tasks.md` 638→103줄 · `OVERVIEW` 240→239줄.
  옛 로그는 8월 아카이브에 최신순으로 병합했고, 정리 전 태스크 638줄은 읽기 전용 스냅샷으로 보존했다.
- **Completed**: `COMPLETED_SUMMARY`에 T13-1·2·4와 이번 정리를 M7 한 줄로 압축했다.
- **Verified**: 상대 링크 해석 · `git diff --check` · `make check` **311 passed**.
- **Blockers**: 공개 `/agent:chat`은 D15 앱 인증 선행. `/livez` 외 7경로는 정책상 501.
- **Next**: **T2-4** — D15 앱 인증을 설계·배선한 뒤 에이전트 실물 왕복을 원장까지 통과시킨다.

## 2026-08-27 — 기존 Mermaid를 단일 출처의 제출 다이어그램으로 회수 · T13-4 (gate 307 → 311)

- **Status**: T13-4/T8-1 `[x]`. 새 그림 없이 `OVERVIEW.md` §4를 독립 제출 표면으로 회수했다.
- **Changed**: README `Architecture diagram` 직접 앵커 · 링크 fragment 해석 · 테스트 4.
- **Verified**: 파일·§4 앵커·Mermaid 렌더 블록·단일 출처 · M-202~M-205 red.
  전체 **205종 전부 red**, `❌` 0, 복구 205, 잔여 없음 205(1025줄 원본 로그).
  최종 `make check` **311 passed**.
- **Blockers**: REQ-901의 영상·저장소 URL·실물 실행 시각 증거는 사람 판정이라 상태를 안 올렸다.
- **Next**: **T13-6 `/tidy-docs`**로 log≤120 · open plan≤200을 회복한 뒤 T2-4 실물 왕복 배선.

---

## 2026-08-27 — p95를 측정할 구간별 부하를 각본 값으로 고정 · T13-2 (gate 304 → 307)

- **Status**: T13-2 `[x]`. baseline/action/rollback의 부하 계획을 **40/40/40**으로 고정했다.
- **Changed**: design 11§1.1 · `demo.py` 출력 · 설계/코드/출력/0회·빈 계획을 묶는 테스트 3.
- **Verified**: `make demo`가 계획을 출력 · `make check` **307 passed** · M-198~M-201 red.
  전체 **201종 전부 red**, `❌` 0, 복구 201, 잔여 없음 201(1005줄 원본 로그).
- **Blockers**: 실제 부하 전송·실물 호출·배포는 미수행. 부하 계획 소비는 T2-4에 남아 있다.
- **Next**: **T13-4** 기존 Mermaid를 독립 산출물로 회수 → T2-4 실물 왕복 배선.
  T13-6 `/tidy-docs`도 남아 있다 — 이 파일은 여전히 120줄 한도를 크게 넘는다.

---

## 2026-08-27 — Cloud Monitoring 신호 어댑터의 오프라인 절반 완료 · T13-1 (gate 296 → 304)

- **Status**: T13-1 `[x]`. 실물 증거의 필터·p95 aligner/reducer·요청 모양을 어댑터로 옮겼다.
- **Changed**: `live_signal.py`(순수 요청/응답 + 지연 SDK + G5) · Monitoring 의존성/매핑 · 테스트 8.
- **Verified**: 실제 SDK 2.31 요청 타입과 proto-plus 응답 파싱 · `make check` **304 passed**.
  M-188~M-197 red · 전체 **197종 전부 red**, 복구 197, 잔여 0, `❌` 0(985줄 원본 로그).
- **Blockers**: 실물 API 재호출·배포·에이전트 도구 배선은 미수행. 공개 `/agent:chat`은 인증 선행.
- **Next**: **T13-2** 구간별 부하 → T13-4 독립 다이어그램 → T2-4 실물 에이전트 왕복 배선.

---

---

## 2026-08-23 (4) — 게이트가 못 보는 축 하나를 이름으로 적는다 · T12-7 (gate 292 유지)

- **Status**: 무인 루프. T4-12(파괴적 조치 강제 승인)가 `[ ]`였는데 **실제로는 끝나 있었다** —
  `test_req_406_destructive_actions_are_never_auto`(`Verifies: REQ-406`) · M-17 red 확인 ·
  REQ-406 `VERIFIED`. 체크박스만 스테일이었다. 게이트 **292 유지**(테스트 증감 없음).
- **⛔ 이 항목은 가드를 만들지 않는다 — 그게 요점이다.** G6 ②는 *"요구사항을 만드는 태스크가
  `tasks.md`에 있는가"*까지고 **그 태스크의 체크박스는 안 본다**(`test_g6_every_requirement_has_a_task`).
  그래서 이 어긋남은 **게이트가 292 초록인 내내** 살아 있었고, 사람이 읽어서만 잡힌다.
- **⚠️ 집행으로 안 올린 이유**: 체크박스는 요구사항 상태와 **다른 축**이다 — 한 태스크가
  요구사항 여럿을, 한 요구사항이 태스크 여럿을 가진다. 맞대려면 **세는 자리가 하나 더** 필요하고,
  그건 태스크가 명시적으로 범위 밖이라 적었다. ⇒ **고치지 않고 이름으로 적었다**(09§3.1).
  ⛔ 반대 방향(**안 끝난 태스크가 `[x]`**)도 같은 이유로 안 보인다 — 이쪽이 더 위험하다.
- **Changed**: `tasks.md`(T4-12 `[ ]`→`[x]` + 근거 · T12-7 `[x]` · **T13-5 `[x]`** — T12-7과
  같은 항목이라 따로 열지 않았다, T12-8/T11-6과 같은 처리) ·
  `design/09-quality-gate.md`(§3.1에 「알려진 한계」 문단 신설).
- **Verified**: `make check` → **292 passed** (exit 0) · 변경 전 기준선도 292/exit 0 —
  **문서·체크박스만 움직였고 스위트는 안 움직였다.** 새 변이 없음(집행하는 코드를 안 넣었다).
- **Blockers**: 없음.
- **Next**: T12-10(=T13-3, `-m "not live"` 배제 집행 — **REQ-601 승격이 여기 막혀 있다**) →
  T12-9/T13-6(`/tidy-docs` — 이 파일이 **한도 120의 다섯 배**다) → T13-1(`SignalSource` 어댑터).

---

## 2026-08-23 (3) — 모델 id 사본을 집행한다 · T12-6 (gate 287 → 292)

- **Status**: 무인 루프. `gemini-3.7-flash`가 **일곱 파일**에 사는데 맞대는 자리가
  하나도 없었다 — `tests/test_model_id_declarations.py` 다섯이 그것을 집행한다.
  게이트 287 → **292** · 변이 175 → **183종**.
- **⛔ 태스크가 적은 "네 곳"이 틀렸다 — 일곱이다**(`.env.example` · design 06 둘 ·
  테스트 픽스처 다섯). T0-5가 *"손으로 센 5곳이 아니라 6곳이었다 — 손 목록은 가드가
  아니다"*라고 적어 둔 자리가 그대로 재발했다. ⇒ 세는 단위를 **목록이 아니라 스캔 뿌리**로 뒀다.
- **묻는 것 넷**: ① 스캐너를 표본으로(산문의 `Gemini 3.x`·표시 이름은 안 잡고, 갱신 기록의
  양쪽은 따로 읽는다) + 바닥 ② 선언들이 서로 같은가 ③ 갱신 기록(`옛 id → 새 id`)의
  **오른쪽이 지금 이름인가** ④ 그 이름이 **대회 요건의 바닥(Gemini 3.5 이상)을 넘는가**.
- **⭐ ④가 이 판의 요점이다** — **일곱 곳을 함께 낮추면 ②는 초록이다**(M-180). 그때 게이트
  어디에도 *"3.5 이상이어야 한다"*고 말하는 자리가 없었고, 그건 **대회 필수 요건 위반**이
  심사까지 안 보인다는 뜻이다. M-97(`min-instances`)이 먼저 태운 갈라짐의 **여섯 번째** 자리다.
- **⛔⛔ 이 판이 실제로 가르친 것은 다른 것이다 — 게이트가 초록인 채로 틀렸다.**
  앞선 회수 판이 `[recovered]` 커밋을 냈고 로컬 `make check`는 **292 초록**이었다.
  러너의 **external re-gate가 그 커밋에서 red**를 냈고(`WARN PHANTOM-SUCCESS`) 자동으로
  되돌렸다(`3ac165f`). ⇒ **T11-2의 증거 가드는 워킹트리가 아니라 `HEAD`를 읽는다**
  (`_head_text` = `git show HEAD:…`). 그래서 새 스윕 절을 **커밋하기 전에는 그 절을 아무도
  안 본다** — 커밋하는 순간 처음 하중이 걸린다. **커밋 전 초록은 이 가드에 대해 무의미하다.**
- **⛔ 그 red의 내용**: 인용한 원본 로그가 **M-01~M-21에서 잘려** 있었다
  (`make: *** [mutate] Terminated: 15`). 스윕이 ~10분인데 무인 루프의 명령 시간 한도에 걸려
  **SIGTERM**을 맞았고, **잘린 로그가 증거로 커밋됐다.** 그 다음 판도 같은 자리에서 죽었다
  (M-11까지). ⇒ **두 판 연속 같은 함정**이고, 가드는 두 번 다 그것을 이름으로 짚었다.
- **⇒ 고친 방법**: `all` 한 번 대신 **case별 호출 183번을 3분할**(61+61+61 · 각 ~235s)로
  돌려 이어 붙였다. 판정 함수는 같다(`one`) — case마다 기준선·복구·잔여 대조를 다시 한다.
  로그 첫 줄과 `mutations.md` 292 절이 **그렇게 돌렸다고 적는다**(증거의 출처를 숨기지 않는다).
- **⚠️ 선언한 줄 수가 틀렸다** — 292 절이 *"915줄"*이라 적었는데 실제는 **916줄**이었다.
  스윕보다 절을 **먼저** 적는 순서가 강제돼 있어서(신선도 가드) 그 숫자는 **예측**이었고,
  예측은 스윕이 끝난 뒤 **세어서 덮는다**. 그 자리가 이번에 실제로 어긋났다.
- **Changed**: `tests/test_model_id_declarations.py`(신규 · 5건) · `scripts/mutate.sh`
  (M-176~M-183 + `all`·사용법) · `docs/evidence/mutations.md`(T12-6 절 + 292 스윕 절 ·
  줄 수 정정 · 분할 실행 명시) · `mutation-sweep-2026-08-23-292.log`(신규 · 916줄) ·
  `tasks.md`(T12-6 `[x]` · 게이트 숫자 268 → 292 — **한 곳에만 적는 그 줄이 스테일이었다**).
- **Verified**: `make check` → **292 passed** (exit 0) · 새 변이 여덟 각각 red 확인 ·
  전체 스윕 **183종 전부 red** · 열린 case 0 · `❌` 0 · 복구 후 292 · 잔여 0 ·
  원본 `mutation-sweep-2026-08-23-292.log` (916줄 · 183 case).
- **⛔ 안 태운 자리**: 산문의 **표시 이름**(*"Gemini 3.7 Flash"*)은 안 본다 — 산문을 스캔하면
  영어 문장을 고칠 때마다 red고 그런 가드는 꺼진다(T11-5). 그리고 이 가드는 **자기 자신을
  안 훑는다**(표본이 일부러 틀린 id를 담는다) — 구멍 하나를 적어 두고 남긴다.
- **⚠️ 무인 루프에 남는 교훈 둘**: ① **커밋 후 재게이트가 없으면 이 종류는 안 잡힌다** —
  러너의 `external re-gate`가 이번에 사람 대신 잡았다. ② **10분 넘는 증거 생성은 한 명령으로
  돌리면 안 된다** — 잘린 산출물이 완전한 것과 **같은 얼굴**을 한다.
- **Next**: T12-7(T4-12 체크박스 정정) → T12-9/T13-6(`/tidy-docs` — 이 파일이 한도를 크게
  넘었다) → T13-1(`SignalSource` 실물 어댑터). ⚠️ 제출 자격을 만드는 것은 여전히 **사람 쪽**이다.

---

## 2026-08-23 (3) — 루프 4회의 결산: 잘린 증거를 가드가 잡았다 (gate 296)

- **Status**: 무인 루프가 오늘 **4번 돌아 8커밋**. ⛔ **네 번 다 `MAX_ITER` 근처도 못 가고
  `no-progress 2`로 자살했다.** 일이 없어서가 아니라 **스윕(187종·약 19분)이 iteration 안에서
  잘려 커밋을 못 내기 때문**이다. 게이트 292 → **296** · 변이 183 → **187종**.
- **Changed**: ① `2b7d22b` T12-6(모델 id 사본 일곱 집행 — 낮에 3.5→3.7 갱신하며 만든 부채 회수)
  ② `83ee57a` T12-7(게이트가 292 초록인 내내 틀려 있던 T4-12 체크박스)
  ③ `e48fa01` **T12-10 회수** — 라이브 테스트가 게이트에서 안 도는 것을 **집행**한다.
  ④ `216c7bd` 크레딧 사실관계 정정. ⑤ 예산 경계 둘 신설(자비 지출용 · 크레딧 소진 감시용).
- **⭐ PHANTOM-SUCCESS 1건 — 그리고 가드가 이겼다**: `6bb023d`가 자기 게이트는 통과했는데
  **외부 재게이트에서 RED**였다. 실체는 **커밋된 스윕 로그가 21/183에서 잘린 것**
  (`make: *** [mutate] Terminated: 15`). 루프 자신이 만든 `test_sweep_log_integrity.py`가
  *"183종을 덮는다고 적었는데 로그에 162종이 없다"*로 잡았고 하네스가 자동 revert했다.
  ⇒ **증거-사슬 가드가 자기를 만든 루프의 잘린 증거를 잡았다.**
- **⛔ 같은 잘림이 오늘만 네 번**(T11-4·T12-3·T12-4·T12-10). 매번 사람이 스윕을 완주시켜
  회수했다. **원인은 하나이고 아직 안 고쳤다** — 스윕이 iteration 안에 있다.
- **⛔ 문서가 자기 자신에서 틀렸다**: `HACKATHON.md`가 *"크레딧 신청 완료됨"*이라 적어
  다음 사람이 *"별도로 받았다"*로 읽게 돼 있었다. 콘솔 확인 결과 **해커톤 전용 크레딧은 없고**
  대회가 말한 $150은 **가입 기본 Free Trial**이다(잔액 ₩332,713). *"신청했다"*와 *"받았다"*는
  다른 값인데 한 칸에 적혀 구별이 사라졌다 — 이 저장소가 계속 잡아 온 어긋남이다.
- **⛔ T12-10이 연 문**: `live` 마커 설명은 *"게이트에 포함되지 않는다"*고 **말만** 했고
  `addopts`에 배제가 없었다. 무해했던 이유는 라이브 테스트가 하나도 없어서일 뿐이다.
  ⚠️ **T2-1이 정확히 여기 막혀 REQ-601을 승격 못 했다** — 이제 열렸다.
- **Verified**: `make check` → **296 passed** (exit 0) · 전체 스윕 **187종 전부 red** · ❌ 0 ·
  복구 후 296 · 잔여 0 · 원본 `mutation-sweep-2026-08-23-296.log` (935줄 · 187 case).
  검수 기록: `docs/test/history/0823-2320-overnight-review-checklist.md`.
- **Blockers**: ⛔ **`/livez` 외 7경로가 501** — 실물 어댑터가 `RunControl` 하나뿐이다.
  ⛔ **크레딧 만료 2026-09-06** — teardown 09-02가 날짜 박힌 일이 됐다(T8-6 미완).
  ⚠️ 서비스가 공개다(D15) — `/agent:chat` 실물 연결 전에 앱 레벨 인증이 선행.
- **Next**: **T13-1 `SignalSource` 어댑터**(API introspect 완료 · `live-signal-2026-08-23.log`가 권위)
  → 두 어댑터를 에이전트 도구에 → REQ-601·602 승격 → **T8 다이어그램·4분 영상**(0%).
  ⛔ 루프를 다시 띄우기 전에 **스윕을 iteration 밖으로 빼라** — 안 고치면 다섯 번째가 난다.
  ⚠️ 이 파일이 **942줄**(한도 120) — `/tidy-docs` 필요.

---

## 2026-08-23 (2) — ⭐ 실물이 됐다: 배포 · 왕복 · Monitoring 증언 (gate 197 → 287)

- **Status**: **시뮬레이터에서 실물이 됐다.** Cloud Run에 둘 배포 · 트래픽 전환/롤백 실증 ·
  **Cloud Monitoring이 조치 실패와 롤백 회복을 직접 증언.** 게이트 197 → **287** · 변이 118 → **175종**.
- **실물 URL**: `https://warranty-api-povpqj6m5a-uc.a.run.app`(공개) ·
  `https://demo-target-450305106907.us-central1.run.app`(리비전 2개).
- **⭐ 신호 증언** (`docs/evidence/live-signal-2026-08-23.log`):
  `12:55 p95 674.2ms(기준선) → 12:56 988.6ms(조치 후 +46.6%) → 12:57 674.2ms(롤백 후)`.
  **각본이 아니다** — 신호를 낸 것은 Google이고 우리는 읽기만 했다.
- **⛔ 실물이 가르친 것 여섯 — 전부 오프라인 게이트가 원리상 못 보는 종류**:
  ① **Vertex location ≠ Cloud Run 리전** — Gemini 3.x는 `global`에만(us-central1은 404).
  설계대로 갔으면 배포 성공 후 **첫 모델 호출에서 404**였다(M-157·158).
  ② **빌드만 다른 프로젝트에서 돌았다** — `builds submit`에 `--project`가 없었다. 사본이
  파일이 아니라 **`gcloud config`(저장소 밖)**였다(M-165·166).
  ③ **`/healthz`를 Cloud Run이 삼킨다** — 로컬 200, 실물은 **컨테이너 도달 0**(M-167~169).
  ④ 그 경로였다면 **두 리비전 다 프로브에 못 답해** 트래픽 전환 자체가 안 일어났다.
  ⑤ **p95는 트래픽이 있어야 의미가 있다** — 구간당 3회로는 세 구간이 **구별 안 됐다**.
  40회를 넣자 갈라졌다. ⇒ **데모 각본(design 11)에 부하가 필요하다**(아직 반영 안 됨).
  ⑥ Monitoring의 `aggregation`은 kwarg가 아니라 request 안 — 첫 시도가 거기서 죽었다.
- **Changed**: T2-2 배포 · T2-3 진입점(`demo_target_server.py`, 기본값 금지) + 리비전 2개 배포 ·
  T2-4 절반(왕복 증거) · `live_run.py`(RunControl 실물) · `vertex_env`(location 분리) ·
  T12-4·T12-5 루프 산출물 회수 · 권한 경계 수정.
- **⭐ 어제 루프가 만든 가드가 오늘 사람을 잡았다** — `live_run.py`에서 G5 tripwire를 빠뜨리자
  `test_live_adapter_guard`가 **이름으로 짚어** red를 냈다. *"게이트는 이것을 안 부른다"*가
  도크스트링 문장으로만 남지 않게 하려고 만든 가드다(T12-5).
- **⛔ 내 실수 하나**: `082a872`가 **게이트 red인 채로 커밋됐다**(`&&`가 아니라 `;`로 이어서
  종료 코드를 안 봤다). 내용은 포맷 한 곳, `d1f90b6`으로 복구. ⚠️ 무인 루프는 커밋 후
  게이트를 **외부에서 다시 돌린다**(`external re-gate`) — 대화형 세션엔 그 자리가 없다.
- **Verified**: `make check` → **287 passed** (exit 0) · 전체 스윕 **175종 전부 red** · ❌ 0 ·
  원본 `mutation-sweep-2026-08-23-287.log` (875줄 · 175 case).
- **Blockers**: ⛔ **`/livez` 외 7경로가 501이다** — 실물 어댑터가 `RunControl` 하나뿐이다.
  ⚠️ 크레딧이 이 결제 계정에 붙었는지 **여전히 미확인**. ⚠️ 서비스가 공개다 —
  `/agent:chat`을 물리기 전에 앱 레벨 인증이 선행.
- **Next**: `SignalSource` 실물 어댑터(API는 오늘 introspect 완료) → 두 어댑터를 에이전트
  도구에 물리기 → T8(다이어그램·4분 영상·Devpost, 마감 **09-01 09:00 KST**).
  ⚠️ 이 파일이 한도(120줄)를 크게 넘었다 — `/tidy-docs` 필요.

---

## 2026-08-23 — ⭐ T0-3 열림 · T2-1 실물 호출 · 루프 5커밋 (gate 197 → 262)

- **Status**: **나흘 막혀 있던 T0-3이 열렸다.** 그리고 **실물 모델 호출에 성공했다**(T2-1).
  게이트 197 → **262 passed** · 변이 118 → **158종**.
- **⛔ T0-3의 블로커는 콘솔이 아니었다** — `gcloud billing accounts list`의 **`--billing-project`
  플래그 하나**였다. 쿼터 프로젝트에 Cloud Billing API가 안 켜져 있어서 난 오류를
  *"콘솔에서만 된다"*로 읽었고, **그 오독이 나흘을 잠갔다.** 사람이 *"gcloud로 다 된다"*고
  말해 준 뒤 5분 만에 풀렸다. ⇒ 프로젝트 `warranty-hack` · 결제 연결 · API 7종 ·
  Artifact Registry `warranty`. ⚠️ **크레딧이 그 계정에 붙었는지는 미확인**(design 10§1의 경고).
- **⭐ T2-1 — 실물 호출**: ADK 2.7.1 + Gemini 3.7 Flash + Vertex, **도구 호출 포함**, 계약
  기준으로 `not recovered` 판정. 증거 `docs/evidence/adk-live-call-2026-08-23.log`.
  *"임포트가 된다"·"이름이 실재한다"·"호출이 된다"* 셋 중 마지막이 처음으로 참이 됐다.
- **⛔ 그리고 실물이 설계를 깼다 — 이쪽이 더 크다.** `WR_REGION` 한 값이 Cloud Run 리전과
  Vertex location **둘**이었다. `global`에는 3.7·3.5·2.5가 다 있고 `us-central1`에는
  **2.5만** 있다 ⇒ *"리전이 틀렸다"*가 아니라 **"모델마다 다르다"**. 설계대로 갔으면 배포는
  성공하고 **첫 모델 호출에서 404**였다 — 빌드도 게이트도 조용한 채로.
  ⇒ `WR_VERTEX_LOCATION` 분리 · **M-157·M-158 red 확인**.
  ⛔ **오프라인 게이트가 원리상 못 잡던 자리다.** 실물 없이는 알 수 없었다.
- **⛔ 무인 루프가 두 판 연속 커밋 0으로 멈췄다** — 일이 없어서가 아니라 **게이트를 못 돌려서**다.
  `Bash(make check)`가 **정확 일치**라 `make check > /tmp/gate.log 2>&1`이 거부됐다.
  `iter-1`이 27분·$7.27을 쓰고 산출물을 못 남겼다. 경계를 고친 뒤 **9회 돌아 커밋 5개**:
  T11-6(증거 가드) · **T12-1(포트를 안 열던 이미지)** · T12-2(모델 판정) · T12-3(ADK 배선) ·
  T12-4(배포 전 검사, 사람이 회수). ⇒ **첫 배포가 죽는 함정이 제거됐다.**
- **⛔ 사람이 한 판을 되돌렸다** — 루프의 첫 T11-6 기록이 *"124종 전부 red"*라 적으면서
  **118 case짜리 옛 로그를 인용**했다(스윕은 권한 거부로 한 번도 안 돌았다).
  빠진 검사를 채우자 **가드끼리 데드락**이 드러나 되돌렸고, 방향 둘을 적어 다시 심었다.
  **루프는 다음 판에 스스로 풀었다**(`d67372e`).
- **Verified**: `make check` → **262 passed** (exit 0) · 전체 스윕 **158종 전부 red** · ❌ 0 ·
  복구 후 262 · 잔여 0 · 원본 `mutation-sweep-2026-08-23-262.log` (790줄 · 158 case).
- **Blockers**: ⛔ **T2-2 배포 실행 하나** — 계획·preflight 다 초록이고 `--yes`만 남았다.
  자동 승인 분류기가 과금 동작을 막아 **사람이 친다**. ⚠️ 중단 기준이 **오늘**이다.
  ⚠️ REQ-601 미승격 — 라이브 테스트가 게이트에서 안 도는 것을 **집행하는 자리가 없다**
  (마커 설명은 그렇다고 말만 한다). G5/T12-5와 같은 자리다.
- **Next**: T2-2(사람) → T2-3(demo-target 2리비전) → T2-4(실물 왕복 증거) → T8(제출물).
  ⚠️ 이 파일이 한도(120줄)를 크게 넘었다 — `/tidy-docs` 필요.

---

## 2026-08-23 — T12-3: ADK 배선 — **부를 라이브러리 없이 조립을 태운다** (gate 249)

- **Status**: `[auto]` T12-3 완료 — 크리티컬 패스의 오프라인 절반 셋째. 새 테스트 8건 ·
  새 변이 8종(M-141~M-148) · 241→**249 passed**. ⛔ **제출 자격은 안 움직였다**(T12 절의 전제).
- **Changed**: ① `src/warranty/adapters/adk_agent.py` — `AgentSpec`·`build_spec`·`agent_kwargs`
  ·`runner_kwargs`·`build_agent`·`build_runner`. ② `tests/test_adk_agent.py` 여덟이 일곱을
  묻는다 — 설계 도구 표 대조 · 모델의 출처 · 배선 거부 · `session_service` · **최상위 임포트
  금지와 지연 임포트의 실재** · 지시문의 도구 누락. ③ `mutate.sh` M-141~M-148 + `all`·사용법 ·
  `mutations.md` 새 절 + 스윕 절. ④ `tasks.md` 게이트 숫자 241 → 249.
- **Verified**: `make check` → **249 passed** (exit 0) · mypy strict 51 files · lint 초록
  (2026-08-23 로컬 macOS·py3.13). **M-01~M-148 전체 스윕 148종 전부 red** · 복구 후 249 ·
  잔여 0 · **`❌` 0건** · 원본 `mutation-sweep-2026-08-23-249.log`(740줄·148 case·exit 0).
- **⛔ 수법이 요점이다: 라이브러리를 못 부르는데 조립을 태운다.** `google-adk`는 cloud extra라
  게이트 venv에 없다(REQ-801). 그래서 태우는 자리를 라이브러리 위가 아니라 **아래**로 내렸다 —
  생성자에 실릴 인자를 순수 함수가 **딕셔너리로 렌더**하고, 라이브러리를 부르는 두 함수만
  지연 임포트한다. `deploy_argv`가 `gcloud` 플래그에 대해 한 것과 같다(T11-1).
- **⚠️ 그 수법이 실제로 산 것은 M-145다.** probe 로그 ②가 적어 둔 사실 — `Runner`의
  `session_service`는 **기본값이 없는 키워드 인자**다. 빠뜨리면 `TypeError`이고 그 예외는
  임포트에도 빌드에도 안 나타난다. **첫 실물 호출에서만** 나고, 그때 보이는 것은
  *"에이전트가 안 뜬다"*뿐이다. 인자를 값으로 렌더했기 때문에 게이트가 그 자리를 물 수 있다.
- **⚠️ 공허 통과가 이번엔 두 얼굴이었다**(M-146·M-148). *"최상위에 `google` 임포트가 없다"*는
  **배선이 통째로 없어도 참**이다 — 지연 임포트를 지우면 게이트는 가장 조용한 초록을 낸다.
  그래서 반대 방향(*"어딘가에는 있어야 한다"*)도 함께 묻는다. M-146은 반대편이다: 최상위로
  올리면 수집 단계에서 **전부** 죽는다. 그 둘 사이의 좁은 자리만이 REQ-801을 만족한다.
- **⚠️ 안 한 것 넷**: ① **ADK를 한 번도 임포트하지 않았다** — 게이트에 그 패키지가 없는 것이
  REQ-801이고, *"그 인자를 그 라이브러리가 받는다"*의 근거는 probe 로그(별도 venv)이지 이
  게이트가 아니다. ② **REQ-601을 안 올렸다** — 수용 기준은 실물 호출이다(T2-1). `Verifies:`도
  안 붙였다. ③ 지시문 **문구의 품질**은 안 물었다(`[manual]`) — 게이트가 묻는 것은 **누락**뿐이다.
  ④ 모델 id **사본 대조**는 안 했다 — T12-6의 자리다. M-143이 잡는 것은 다른 문장이다
  (*"명세가 설정을 안 읽고 자기가 적은 값을 쓴다"*). ⚠️ 하중 분포 대조도 **안 했다**(권한 경계).
- **⛔ 두 번째 시도다 — 첫 판은 커밋됐고 되돌려졌다**(`7f37ef2` → `210af21`). 코드가 아니라
  **증거가 잘렸다.** 스윕은 M-51에서 끊긴 채 253줄로 남았는데 절은 *"148종 전부"*라고 적었고,
  그 어긋남이 바로 이 저장소가 T11-6에 만든 가드가 잡는 것이다(192 사고와 같은 꼴).
  ⚠️ **끊은 것은 스윕이 아니라 도구의 타임아웃**이다 — 한 case가 약 5.9초이고 148종은
  **14분**인데, 전경 명령의 한도가 300초라 정확히 M-51에서 잘렸다. ⇒ 이번엔 **백그라운드로
  돌리고 끝난 뒤 148 case·148 red·닫힘 148·`❌` 0을 세어 확인한 다음** 커밋한다.
- **⛔ 그런데 그 커밋의 게이트는 초록이었다 — 그게 이 사고의 본체다.** 범위 가드는 설계상
  **워킹트리가 아니라 `HEAD`를 읽는다**(자기참조를 피하는 유일한 수 · T12-8). 그래서 잘린
  로그를 **커밋하는 그 순간의** 게이트는 *직전* 증거(241·140 case)를 보고 초록을 내고,
  red는 **다음 게이트**에 난다. 가드 자신의 docstring이 그 한 커밋의 지연을 예고해 뒀다.
  ⚠️ 잡은 것은 이터레이션이 아니라 **러너의 외부 재게이트**다(`PHANTOM-SUCCESS` → 자동 revert).
  ⇒ 무인 루프에서 *"커밋 직전 초록"*은 증거의 온전함에 대해서는 **아무 말도 안 한다**.
- **Blockers**: ⛔ **T0-3**(크레딧 결제 계정 → 전용 프로젝트) 그대로 — 콘솔에서만 열린다.
- **Next**: T12-4(`deploy-check` — 서버 없는 이미지를 올리기 전에 잡는다).
  ⚠️ 이 파일이 **한도 120의 여섯 배에 가깝다** — T12-9(`/tidy-docs`)가 그 자리다.

---

## 2026-08-23 — T12-2: 판정 어댑터 — **모델이 판단하는 자리는 인터페이스뿐이었다** (gate 241)

- **Status**: `[auto]` T12-2 완료 — 크리티컬 패스의 오프라인 절반 둘째. 새 테스트 25건 ·
  새 변이 8종(M-133~M-140) · 216→**241 passed**. ⛔ **제출 자격은 안 움직였다**(T12 절의 전제).
- **Changed**: ① `src/warranty/adapters/model_judge.py` — `ALLOWED_VERDICTS`·`FALLBACK_VERDICT`
  ·`build_prompt`·`parse_reply`·`fallback_rationale`·`PromptedJudge`. 전송은 `Transport` 포트
  뒤이고 게이트는 그 자리를 실물로 안 채운다(REQ-801). ② `tests/test_model_judge.py` 스물다섯이
  넷을 묻는다 — 프롬프트 · 거절(못 읽을 답 아홉을 값으로) · 폴백 · **근거가 원장에 실리는가**
  (전송→`PromptedJudge`→`MeteredModel`→`Remediator` 조립). ③ `mutate.sh` M-133~M-140 +
  `all`·사용법 · `mutations.md` 새 절 + 스윕 절. ④ `tasks.md` 게이트 숫자 216 → 241.
- **Verified**: `make check` → **241 passed** (exit 0) · mypy strict 49 files · lint 초록
  (2026-08-23 로컬 macOS·py3.13). **M-01~M-140 전체 스윕 140종 전부 red** · 복구 후 241 ·
  잔여 0 · **`❌` 0건** · 원본 `mutation-sweep-2026-08-23-241.log`(700줄·140 case).
- **⛔ 없앤 것은 빈 자리 하나다.** 여기까지 `ModelPort` 구현은 대역뿐이었고 원장은 늘
  `decided_by: rule`이었다. 규칙이 못 닫는 판정을 모델이 닫고 그 문장이 근거로 남는 것이
  REQ-204이고, 이 판이 그 **오프라인 절반**(프롬프트·파싱·폴백)이다.
- **⛔ 설계의 중심은 폴백의 *방향*이다.** 파싱 실패를 `recovered`로 접으면 못 읽은 응답 하나가
  *"나아졌다"*가 되고 롤백 없이 남는다 — 조용한 성공 그 자체다(M-134). 보수적이라는 것은
  *"덜 주장한다"*가 아니라 **롤백을 연다**는 뜻이고, 그래서 `not_recovered`다.
  ⚠️ **전송 실패는 안 삼킨다** — 얼마 썼는지 모르는데 0을 지어내면 그게 PRINCIPLES #2가
  금지한 조용한 0이다. 예외는 올라가고 `MeteredModel`의 `finally`가 `FAILED` 행을 남긴다.
- **⚠️ fake로는 절대 안 잡히던 자리가 하나 있었다**(M-139). 모델은 JSON을 코드펜스로 감싸는
  쪽이 흔한데, 대역은 우리가 정해 준 문자열을 내므로 펜스를 안 씌운다 ⇒ 안 벗기면 **정상
  응답이 전부 폴백**이 되고, 루프는 계속 돌고 판정은 늘 그럴듯하며 `decided_by: model`은
  그때부터 거짓말이다. 그 실패는 게이트에서도 데모에서도 안 보인다.
- **⚠️ 안 한 것 셋**: ① **모델을 한 번도 안 불렀다** — 확인된 것은 *"우리가 이렇게 묻고 이렇게
  읽는다"*지 *"Gemini가 이렇게 답한다"*가 아니다(REQ-601·T2-1이 소유). ② **REQ-604를 안
  올렸다** — 근거가 실린 곳은 원장이고 수용 기준은 *API 응답*이다. 그 경로는 아직 `501`이다.
  ③ 프롬프트 문구의 **품질**은 안 물었다 — 실물 응답 없이는 판정이 안 되는 종류다(`[manual]`).
  ⚠️ 하중 분포 대조도 이번에 **안 했다**(T12-1과 같은 이유 — 권한 경계 · T0-6).
- **⚠️ 태스크 줄의 `Implements: REQ-303`은 REQ-204의 오기로 보인다**(REQ-303은 원자적 롤백).
  T12-7과 같은 계열의 정정이라 **안 고치고 적어만 뒀다** — 본문 정정은 별개 항목이다.
- **Blockers**: ⛔ **T0-3**(크레딧 결제 계정 → 전용 프로젝트) 그대로 — 콘솔에서만 열린다.
  ⚠️ 설계 어긋남 2건 사람 판단 대기.
- **Next**: T12-3(ADK 에이전트 배선 — 지연 임포트로 명세만 태운다).
  ⚠️ 이 파일이 **한도 120의 다섯 배가 넘는다** — T12-9(`/tidy-docs`)가 그 자리다.

---

## 2026-08-23 — T12-1: HTTP 서버 — **이미지가 포트를 한 번도 안 열고 있었다** (gate 216)

- **Status**: `[auto]` T12-1 완료 — 크리티컬 패스의 오프라인 절반 첫 항목. 새 테스트 13건 ·
  새 변이 11종(M-122~M-132) · 203→**216 passed**. ⛔ **제출 자격은 안 움직였다**(T12 절의 전제).
- **Changed**: ① `src/warranty/server.py` — stdlib `http.server`만. 순수 `resolve()`가 판정하고
  `WarrantyHandler`는 얇은 껍데기다. ② `config.py`에 `PORT_ENV_KEY`·`DEFAULT_PORT`·`load_port`
  — **포트의 유일한 출처**. ③ `Dockerfile`의 `CMD`가 데모 → **서버**. ④ `tests/test_http_server.py`
  열셋이 여덟을 묻는다. ⑤ `mutate.sh` M-122~M-132 + `all`·사용법 · `mutations.md` 새 절 + 스윕 절.
  ⑥ `tasks.md` 게이트 숫자 203 → 216.
- **Verified**: `make check` → **216 passed** (exit 0) · mypy strict 47 files · lint 초록
  (2026-08-23 로컬 macOS·py3.13). **M-01~M-132 전체 스윕 132종 전부 red** · 복구 후 216 ·
  잔여 0 · **`❌` 0건** · 원본 `mutation-sweep-2026-08-23-216.log`(660줄·132 case).
- **⛔ 없앤 것은 함정 하나다.** 직전까지 `CMD`가 돌린 것은 오프라인 데모이고, 데모는 출력을
  찍고 **정상 종료한다** — 컨테이너가 포트를 한 번도 안 열어 포트 프로브에서 죽는다.
  ⚠️ `test_deploy_artifacts` ⑥은 이 함정에 대해 **처음부터 초록이었다**: 그것이 묻는 것은
  *"`CMD`가 가리키는 모듈이 실재하는가"*이고 `warranty.demo`는 실재한다. 있는 모듈을
  가리키는 것과 **서버를 가리키는 것**은 다른 질문이고, 새 ⑧만 후자를 본다(M-126).
- **⛔ 선언된 경로 중 답하는 것은 헬스뿐이고 나머지는 `501`이다.** 게으름이 아니라 정책이다 —
  실물 어댑터가 없는데 fake를 물려 `200`을 내면 *"돈다"*와 *"가짜로 돈다"*가 구별되지 않고,
  design 08§5가 `WR_ADAPTERS` 기본값을 fake로 안 두는 이유가 그것이다. M-124가 그 자리다(6 failed).
- **⛔ 이 판이 M-90의 전제를 지웠고, 잡은 것은 게이트가 아니라 스윕이다.** M-90은 `CMD`를
  `warranty.server`로 돌려 *"없는 모듈"*을 태우던 변이인데, T12-1이 그 모듈을 만들었다.
  `make check`는 내내 초록이었고 첫 스윕이 *"변이가 파일을 바꾸지 못했다"*로 exit 1을 냈다.
  목적지를 `warranty.ghost`로 옮겨 다시 돌렸다. ⚠️ **첫 판의 로그는 안 남겼다**(판정이 아니다).
- **⚠️ 안 한 것 둘**: ① **소켓을 한 번도 안 열었다** — 권한 경계가 막고(D14), 열더라도 게이트가
  기계 상태에 묶인다. 핸들러는 `__new__` + 파일 객체 둘로 돌렸다 ⇒ 확인된 것은 *"옳은 바이트를
  낸다"*지 **"Cloud Run이 그 포트를 프로브한다"**가 아니다. ② 직전(203) 로그와의 **하중 분포
  대조를 안 했다** — 그 파이프라인도 권한 경계 밖이고, 121쌍을 손으로 세는 것은 T0-6이 금지한
  짓이다. 그래서 이 절은 *"하중 분포가 안 바뀌었다"*를 **말하지 않는다**.
- **Blockers**: ⛔ **T0-3**(크레딧 결제 계정 → 전용 프로젝트) 그대로 — 콘솔에서만 열린다.
  ⚠️ 설계 어긋남 2건 사람 판단 대기.
- **Next**: T12-2(모델 판정 어댑터 — *"어디가 에이전트냐"*에 답하는 자리).
  ⚠️ 이 파일이 **한도 120의 다섯 배**다 — T12-9(`/tidy-docs`)가 그 자리다.

---

## 2026-08-23 — T11-6: 스윕 원본 로그 — **주장은 봤고 증거는 안 봤다** (gate 203)

- **Status**: `[auto]` T11-6 완료 — **한 번 실패했던 항목이다**(T12-8이 같은 것). 새 테스트 6건 ·
  새 변이 3종 · 197→**203 passed**. 앞선 판이 되돌아간 이유는 버그가 아니라 **자기참조**였고,
  이번엔 그 자리를 설계로 피했다.
- **Changed**: ① `tests/test_sweep_log_integrity.py` 여섯 — ① 스캐너를 표본 셋(온전·잘림·`❌`)으로
  태운다 ② 최신 스윕 절이 원본을 인용하고 그 파일이 실재하는가 ③ 선언한 case를 **전부** 담는가
  ④ 열린 블록 0 · `❌` 0. ② `mutate.sh` M-119~M-121 + `all`·사용법(M-118→M-121).
  ③ `mutations.md` 새 절 + 121종 스윕 절. ④ 원본 `mutation-sweep-2026-08-23-203.log`.
- **Verified**: `make check` → **203 passed** (exit 0) · mypy strict 45 files · lint 초록
  (2026-08-23 로컬 macOS·py3.13). **M-01~M-121 전체 스윕 121종 전부 red** · 복구 후 203 ·
  잔여 0 · **`❌` 0건** · 원본 606줄·121 case. 직전(197) 원본이 온전해 **118종 전부를 맞댔고
  갈라진 자리는 0이다** — 잘린 로그로는 18종밖에 못 맞대던 자리다. M-37·M-85는 여섯 번째로 2와 6.
- **⛔ 설계가 전부다 — 가드는 워킹트리가 아니라 `HEAD`를 읽는다.** 로그는 스윕이 만들고 스윕은
  이 스위트를 변이당 **세 번** 지난다. 워킹트리를 읽으면 *"인용한 로그가 다 차 있는가"*는
  **로그가 만들어지는 동안 이미 참이어야 하고**, 수렴점이 없다(앞선 판이 여기서 되돌아갔다).
  `HEAD`는 스윕 내내 얼어 있다. ⇒ **수렴을 실제로 보였다**: 아직 존재하지 않는 `…-203.log`를
  인용하는 상태에서 게이트가 **초록이었고**, 그 초록 위에서 121종 스윕이 돌아 그 로그를 만들었다.
- **⚠️ 대가는 한 커밋의 지연이다** — 잘린 로그를 커밋하는 그 커밋은 초록이고 **다음 게이트가
  red**다. 없앨 수 있는 지연이 아니다(자기참조가 그것을 산다). 무인 루프에서 그 red는
  *깨끗한 워킹트리에서 나는 red*이고 그건 멈추라는 신호다 — 192 사고가 정확히 그 모양이었다.
- **⚠️ M-121에서 ④가 조용해진다** — case가 0개면 열린 블록도 0이다. ④의 초록이 *"온전하다"*인지
  *"아무것도 안 봤다"*인지를 가르는 것은 ④가 아니라 **표본**이다(M-105·M-118과 같은 규칙).
- **⛔ 안 태운 자리**: *"인용이 있어야 한다"*. 오래된 절 23개 중 원본을 인용하는 것은 **넷뿐**이라
  (T11-2 전엔 규칙이 없었다) 소급 집행이 안 된다 ⇒ 집행 대상은 **최신 절 하나**다.
  REQ-802는 이미 `VERIFIED`라 상태를 안 건드렸고 `Verifies:`도 안 붙였다 — 이 열이 태우는 것은
  결정론이 아니라 **증거의 사슬**이다.
- **Blockers**: ⛔ **T0-3**(크레딧 결제 계정 → 전용 프로젝트) 그대로 — 콘솔에서만 열린다.
  ⛔ **첫 배포는 포트 프로브에서 죽는다**(T12-1이 그 자리다). ⚠️ 설계 어긋남 2건 사람 판단 대기.
- **Next**: T12-1(HTTP 서버) — 크리티컬 패스의 오프라인 절반 중 첫 항목.
  ⚠️ 이 파일이 **637줄**(한도 120)이다 — T12-9(`/tidy-docs`)가 그 자리다.

---

## 2026-08-23 — T11-5: README — **절차에 게이트만 있고 실행이 없었다** (gate 197)

- **Status**: `[auto]` T11-5 완료 — 오프라인 백로그의 **마지막 항목**이었다. 새 테스트 5건 ·
  새 변이 6종 · 192→**197 passed**. ⚠️ 직전 세션의 체크포인트 문서 셋도 커밋 안 된 채였다 —
  회수해서 먼저 커밋했다(`c28ec39`).
- **Changed**: ① `tests/test_readme_procedure.py` 다섯 — ① 표본으로 태우는 스캐너 ② 적어 둔
  `make` 명령이 **실재하는 타깃인가** ③ 절차가 부트스트랩·게이트·**실행**을 이름으로 갖는가(값)
  ④ 가리키는 저장소 경로가 실재하는가. ② `README.md`에 `make demo` 한 줄.
  ③ `mutate.sh` M-113~M-118 + `all`·사용법 · `mutations.md` 새 절 + 스윕 절.
  ④ `tasks.md` 게이트 숫자 **185 → 197**(T11-4가 안 올려서 stale이었다) + T11-6 신설.
- **Verified**: `make check` → **197 passed** (exit 0) · mypy strict 44 files · lint 초록
  (2026-08-23 로컬 macOS·py3.13). **M-01~M-118 전체 스윕 118종 전부 red** · 복구 후 197 ·
  잔여 0 · **❌ 0건** · 원본 `docs/evidence/mutation-sweep-2026-08-23-197.log`(591줄·118 case).
- **⛔ ③이 첫 실행에서 red였다 — 재현 절차에 `make demo`가 없었다.** 적혀 있던
  `make venv && make check`와 `make trace`는 *"이게 맞는가"*와 *"무엇을 약속했는가"*만
  알려 준다. 해커톤 제출물에서 그건 재현 절차가 아니라 **검사 절차**다.
  ⚠️ ②는 그때도 **초록이었다** — 적힌 둘 다 살아 있었으니까. **없는 것을 잡는 질문과 틀린 것을
  잡는 질문은 다르다**(T11-2 *"일치가 아니라 값"* · T11-3 *"선언의 존재와 자리"*의 네 번째 자리).
- **⛔ 직전 스윕의 원본 로그가 잘려 있다** — `mutation-sweep-2026-08-22-192.log`는 **97줄·20
  case**(M-20에서 끊긴다)인데 192 절은 그것을 *"112종 전부 red"*의 원본으로 **인용한다.**
  무인 루프가 스윕 도중 STOP을 맞아 tee가 끊긴 모양이다. 주장이 거짓이라는 뜻은 아니다 —
  이번 118종 스윕이 그 범위를 **전부 다시 red로 확인했다.** 틀린 것은 **증거의 사슬**이고,
  ⛔ **그것을 묻는 가드가 없다**(신선도 가드는 절이 주장하는 것만 본다). ⇒ **T11-6**으로 심었다.
- **⚠️ M-114만 겹쳤다** — 예측 1인데 실측 2. `make demo`의 *동작*은 이미 REQ-803이
  subprocess로 태우고 있었다. 새로 생긴 것은 **README 쪽**이고 M-113·M-116은 하중 1이다.
  겹치는 18종(직전 원본이 담은 범위)은 **값이 전부 같다** · M-37·M-85는 다섯 번째로 2와 6.
- **⚠️ 스캐너는 코드 문맥만 본다** — 산문의 `make sure`를 타깃으로 읽으면 영어 문장을 고칠
  때마다 게이트가 red고, **그런 가드는 꺼진다.** 대신 코드 밖에 적힌 명령은 안 잡힌다(#3).
- **⛔ REQ-901을 승격하지 않았고 `Verifies:`도 안 붙였다** — 나머지 넷(저장소 URL · 데모 영상 ·
  다이어그램 · 실물 실행의 시각 증거)은 오프라인 게이트가 판정할 질문이 아니다.
- **Blockers**: ⛔ **T0-3**(크레딧 결제 계정 → 전용 프로젝트) 그대로 — 콘솔에서만 열린다.
  ⛔ **첫 배포는 포트 프로브에서 죽는다**(T2-2). ⚠️ 설계 어긋남 2건 사람 판단 대기.
- **Next**: ⛔ **오프라인 `[auto]` 백로그가 고갈됐다.** 남은 것은 T11-6(스윕 로그 범위 집행)
  하나이고, 그건 **오늘 생긴 것**이다. 판정 대상은 여전히 **T2-2**다.
  ⚠️ 이 파일이 **610줄**(한도 120)이다 — `/tidy-docs` 필요.

---

## 2026-08-23 — T11-4 회수 + 무인 루프 세션 종료 (gate 192)

- **Status**: 루프 `STOP`으로 우아하게 종료(배치 5, 4 iters). 워킹트리 clean · 게이트 **192 passed**.
  ⚠️ 4회차가 T11-4를 **다 만들고 커밋 전에** STOP을 맞아 초록 잔여물로 남아 있었다 — 회수했다.
- **Changed**: ① `4dfa2b8` T11-4 `demo_target.py`(건강 620ms / 악화 900ms) + 테스트 7 + 변이
  M-106~M-112. 지연을 `tunables`에 **안 뒀다** — 거기 사는 건 *재는 쪽*의 대기이고 이건
  *재이는 쪽*의 지연이라, 묶으면 타이머를 줄일 때 장애 크기가 함께 바뀐다.
  ② `b345efa` 재개 지점에 **배포 함정** 명시.
- **Verified**: `make check` → 192 passed (exit 0) · `bash scripts/mutate.sh M-106` red·복구·잔여 0.
  ⛔ 사람이 잡은 기록 오류: `mutate.sh`의 M-106 주석이 `(620 → 640)`인데 실제는
  `DEGRADED 900 → 640`이었다(620은 건강한 쪽). `mutations.md` 표는 맞았다 ⇒ 주석을 맞췄다.
- **Blockers**: ⛔ **T0-3**(크레딧 결제 계정 → 전용 프로젝트) — 콘솔에서만 열린다. REQ-601·602는
  `TODO` 그대로. ⛔ **첫 배포는 실패한다**: 이미지가 HTTP를 안 내보내 포트 프로브에서 죽는다(T2-2).
  ⚠️ 사람이 결정할 설계 어긋남 2건(01§6 덜 적음 · 03§7 더 적음)이 「알려진 한계」에 대기 중.
- **Next**: 남은 `[auto]`는 **T11-5**(README 재현 절차 집행) 하나. 그 외 오프라인 백로그는 고갈.
  ⚠️ 이 파일이 **580줄**(한도 120)이다 — `/tidy-docs` 필요.

---

## 2026-08-22 — T11-3: 의존성 선언 — **일치는 설치된다는 뜻이 아니다** (gate 185)

- **Status**: `[auto]` T11-3 완료. ⛔ **`google-adk`가 어디에도 선언돼 있지 않았다.**
  REQ-601은 그것을 대회 필수로 이름하고 design 06§2는 `google-adk 2.7.1`을 실제로 설치해
  introspect한 증거까지 남겼는데, `pyproject.toml`은 그 이름을 **한 번도 안 적었다.**
  배포 이미지는 `pip install ".[cloud]"` 한 줄로 의존성을 얻는다 — 선언에 없으면 **안 실린다.**
  새 테스트 5건 · 새 변이 6종 · 180→**185 passed**.
- **Changed**: ① `pyproject.toml` — cloud extra에 `google-adk>=2.7`(바닥은 introspect한
  2.7.1에서 온다) · `description`을 구 정체성(`fleet ledger`)에서 현행으로.
  ② `tests/test_dependency_declaration.py` 다섯 — ① 표본으로 태우는 스캐너 ② 임포트마다
  선언이 있는가 ③ 선언마다 매핑이 있는가 ④ 필수 프레임워크가 선언에 **있는가**(값)
  ⑤ 클라우드 스택이 이미지에는 실리고 게이트에는 **안 실리는가**.
  ③ `mutate.sh` M-100~M-105 + `all`·사용법 · `mutations.md` 새 절 + 스윕 절 + 원본 로그.
- **Verified**: `make check` → **185 passed** · mypy strict 41 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-105 전체 스윕 105종 전부 red** · 복구 후 185 ·
  잔여 0 · **가려진 가드 없음** · 원본 `docs/evidence/mutation-sweep-2026-08-22-185.log`.
- **⛔ 선언의 *존재*와 선언의 *자리*는 다른 질문이다** — M-102가 그 자리다. `google-adk`를
  `[project].dependencies`로 **옮기면** ④(선언돼 있는가)는 **초록이다**. 그런데 게이트가
  설치하는 `.[dev]`가 그 순간부터 클라우드 스택을 끌고 오고, *"게이트는 이것들 없이
  통과한다"*(REQ-801)는 fake 어댑터의 전제가 조용히 깨진다. ⑤만 그 자리를 본다.
  ⚠️ 그래서 ⑤는 extra 이름을 자기가 안 적고 `Dockerfile`·`Makefile`의 설치 줄에서 읽는다.
- **⚠️ 반대 방향(선언했으면 임포트해야 한다)은 안 물었다** — 지금 이 저장소에서 거짓이다.
  클라우드 스택 넷은 실물 어댑터가 없어서 아직 아무도 임포트하지 않고, 그건 결함이 아니라
  순서다. 대신 ③이 선언 쪽에서 같은 자리를 본다(M-104): 선언은 멀쩡한데 스캐너가 그 이름을
  모르면 **증상은 오늘 없고**, 실물 어댑터를 쓰는 첫 커밋에서 *"선언 안 된 임포트"*로 나온다.
- **⚠️ T11-2가 남긴 M-99 천장을 걷었다** — `test_mutation_freshness.py`의 `max(cases)`를
  번호 비교로 고쳤고(`"M-100" < "M-99"`였다), **같은 계열의 두 번째 가정**도 찾았다:
  M-49의 정규식이 `M-\d\d;`라 목록 끝이 `M-105`가 되면 **패턴이 안 맞아 조용히 무효**가 된다
  (가드에 대한 판정이 아니라 하네스가 자기 자신을 못 깨는 상태다). ⇒ 천장은 하네스의 성질이
  아니라 **정규식 두 개**였다.
- **⚠️ 처음으로 두 스윕의 원본 로그를 직접 맞댔다** — 180 쪽 결정이 처음 값을 내는 자리다.
  하중이 1이 아닌 자리를 나란히 놓으면 **겹치는 45자리가 전부 같은 값**이고 갈라진 곳이
  하나도 없다. 새 여섯도 기존 어느 자리와 안 겹친다(M-100만 셋이 함께 운다 — ③·④·⑤가
  같은 어긋남을 세 방향에서 본다). 169↔176의 미해결 두 자리(M-37·M-85)는 네 번째 관측에서도
  2와 6이다. ⛔ **여전히 증명은 아니다** — 169 스윕의 원본이 없다.
- **⛔ 상태를 안 올렸다**(REQ-601은 `TODO` 그대로) · `Verifies:`도 안 붙였다. REQ-601의 수용
  기준은 **실물 호출**이고 이 열은 *"선언이 있는가"*만 태운다 — 붙이면 다음 사람이
  증명하지 않은 것을 근거로 올린다(T11-1과 같은 판단).
- **⚠️ description은 변이로 안 태웠다** — *"현행인가"*는 오프라인 게이트가 판정할 수 있는
  질문이 아니다. 판정 가능한 이웃 하나는 있다: *"은퇴한 이름을 아직 말하는가"* — 이 저장소는
  그 rot을 실제로 겪었다(`fleet-ledger`→`warranty`에 다섯 곳이 안 따라왔다).
  ⛔ 지금도 `.venv`에 `fleet_ledger-0.1.0.dist-info`가 남아 있다. **범위 밖이라 적어만 둔다**(#3).
- **Blockers**: 없음(이 항목 한정). 레포 전체는 그대로다 — 전용 GCP 프로젝트(T0-3)가 T2를
  잠그고, **08-24 중단 기준**의 판정 대상은 오프라인 백로그가 아니라 T2-2다.
- **Next**: T11-4(`demo-target` 장애 주입 앱) → T11-5.
  ⚠️ 이 파일은 예산(120줄)을 크게 넘겼다 — `/tidy-docs`로 밀 것.

## 2026-08-22 — T11-2: REQ-805 — **일치가 아니라 값을 묻는다** (gate 180)

- **Status**: `[auto]` T11-2 완료. REQ-805는 지금까지 **훑을 대상이 0개라 공허 통과**였다
  (T11-1 전에는 배포 산출물이 없었다). 새 테스트 4건 · 새 변이 3종 · 176→**180 passed**.
- **Changed**: ① `tests/test_no_always_billed_compute.py` 넷 — ① 금지 목록의 바닥 + **표본으로
  정규식이 아직 잡는지 태운다** ② 유휴 인스턴스 수가 **0인가**(코드·렌더·설계 셋 다, 값으로)
  ③ 실행 표면(`Dockerfile`·`deploy.sh`·`Makefile`·렌더된 인자)이 GKE·VM·Cloud SQL을
  **만들지 않는가** ④ design 10§2 표가 그것들을 **선언하지도 않는가**.
  ② `mutate.sh` M-97~M-99 + `all`·사용법 · `mutations.md` 새 절 + 스윕 절.
- **Verified**: `make check` → **180 passed** · mypy strict 40 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-99 전체 스윕 99종 전부 red** · 복구 후 180 ·
  잔여 0 · **가려진 가드 없음**. ⇒ 이번부터 **스윕 원본 로그를 커밋한다** —
  `docs/evidence/mutation-sweep-2026-08-22-180.log`(지난 스윕은 원본이 없어서 갈라진 두
  숫자의 원인과 오기를 구별 못 했다).
- **⛔ 일치 검사는 두 문서가 함께 틀리는 것을 못 잡는다** — M-97이 그 자리다. 설계와 코드를
  **함께 1로** 바꾸면 T11-1의 열 개가 **전부 초록**이다(같으니까 ④가 통과하고, 렌더러는 그
  상수를 그대로 실으니 ②도 통과한다). 그때 게이트 어디에도 *"0이어야 한다"*고 말하는 자리가
  없었다. M-88이 red였던 이유도 *"1이다"*가 아니라 *"설계와 다르다"*였다.
- **⚠️ 이번에 겹침이 생겼다** — `M-88 1→2 · M-92 1→2`. T11-1이 *"이 층에는 겹침이 하나도
  없다"*고 적은 자리 둘이 새 ②와 같이 운다. 이제 `min-instances`는 **세 질문**이 본다:
  같은가 · 실리는가 · **0인가**. 나머지 열일곱 자리는 직전 스윕(176)과 **값이 같다.**
- **⚠️ 169↔176에서 못 밝힌 두 자리(M-37·M-85)는 이번에도 2와 6이다.** 세 번째 독립 관측이고
  *"169 기록이 옮겨 적히며 틀렸다"*는 가설과 계속 맞지만 **증명은 아니다** — 하중 분포는
  아직 게이트가 안 지킨다(T0-8의 구멍). 다만 이번 원본 로그는 남겼으니 다음엔 되짚을 수 있다.
- **⛔ 상태를 안 올렸다**(REQ-805는 `TODO` 그대로). 이 열이 태우는 것은 *"산출물이 무엇을
  선언하는가"*이지 *"실제로 0으로 수렴했는가"*가 아니다 — 후자는 실물 배포(T2-2)의 증거다.
  ⚠️ 다만 `Verifies:`는 붙였다. **T11-1과 다른 판단이고, 그 차이가 이유다**: T11-1의 열은
  *"산출물이 코드와 어긋나지 않는가"*(이웃한 성질)를 태웠고, 이 둘은 REQ-805가 약속한
  **두 문장 자체**를 태운다. ⛔ 그런데 겨냥한 함수가 **문장 수와 정확히 같아서**(2 = 2),
  다음 사람이 `IMPLEMENTED`로 올려도 G6⑤는 안 운다. **올릴 근거는 여기 없다 — 사람이 볼 것.**
- **⛔ 변이 번호의 천장은 M-99다.** `M-100`을 정의하는 순간 신선도 가드가 깨진다 —
  `test_mutation_freshness.py`가 `max(cases)`를 **문자열로** 비교해서 `"M-100" < "M-99"`다.
  그래서 새 파일의 ①(표본·바닥)은 **변이로 안 태웠다** — 목록을 비우거나 정규식 한 글자를
  깨면 ③은 0건을 잡고 초록이 되는데, 그 경로가 지금 비어 있다. **다음에 변이를 넣는 사람은
  그 비교를 숫자로 고치는 것부터 해야 한다.** 범위 밖이라 안 고치고 적어 뒀다(#3).
- **Blockers**: 없음(이 항목 한정). 레포 전체는 그대로다 — 전용 GCP 프로젝트(T0-3)가 T2를
  잠그고, **08-24 중단 기준**의 판정 대상은 오프라인 백로그가 아니라 T2-2다.
- **Next**: T11-3(`google-adk` 선언) → T11-4 → T11-5.
  ⚠️ 이 파일은 예산(120줄)을 크게 넘겼다 — `/tidy-docs`로 밀 것.

## 2026-08-22 — T11-1: 배포 산출물 — **아무도 안 읽는 사본**을 미리 막는다 (gate 176)

- **Status**: `[auto]` T11-1 완료. `Dockerfile`·`scripts/deploy.sh`가 생겼고 **둘 다 실행된 적이
  없다**(T0-3이 전용 프로젝트를 잠그고 있다). 새 테스트 7건 · 새 변이 10종 · 169→**176 passed**.
- **Changed**: ① `config.py`에 배포 값의 **유일한 출처**(`SERVICE_NAME`·`MIN_INSTANCES=0`·
  `MAX_INSTANCES`·`IMAGE_REPO`·`INFRA_LABELS`·`RUNTIME_ENV_KEYS` + `image_uri`·`deploy_argv`).
  ② `tools/deploy_plan.py` — 렌더러(배포는 안 한다). ③ `Dockerfile` + `scripts/deploy.sh` —
  **플래그를 하나도 직접 안 적고** 렌더된 인자를 받아서 실행만 한다. ④ `make deploy`(게이트 아님).
  ⑤ `tests/test_deploy_artifacts.py` 7건 · `mutate.sh` M-87~M-96 + `all`·사용법 · `mutations.md` 새 절.
- **Verified**: `make check` → **176 passed** · mypy strict 39 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-96 전체 스윕 96종 전부 red** · 복구 후 176 ·
  잔여 0 · **가려진 가드 없음**.
- **⛔ 이 이미지는 HTTP를 서비스하지 않는다 — 그리고 그렇게 적었다.** design 08§3의 엔드포인트는
  REQ-602이고 **T2-2가 소유한다.** `CMD`는 오프라인 데모를 돌린다 ⇒ 이대로 올리면 포트 프로브에서
  죽는다. 서버를 지금 만들 수도 있었지만 그건 **무인 루프가 검증할 수 없는 변경**이라 안 했다.
  ⇒ Dockerfile 맨 위 상자와 `deploy.sh` 머리말이 그것을 말한다. **REQ-602·805는 `TODO` 그대로다.**
- **⛔ 새 테스트에 `Verifies:`를 안 붙였다** — 이 열이 태우는 것은 *"산출물이 코드와 어긋나지
  않는가"*지 *"Cloud Run에서 도는가"*가 아니다. 붙이면 G6①이 이것을 REQ-602의 증거로 세고,
  다음 사람이 **증명하지 않은 것을 근거로** 상태를 올린다. (`Spec:`는 붙였다 — 그건 반대 방향이다.)
- **⛔ 열 다 하중이 `1`이다 — 이 층에는 겹침이 하나도 없다.** 도메인은 한 자리를 깨면 서넛이
  같이 운다(M-13 14 · M-19 32). 여기는 아니다: 검사 하나를 지우면 그 자리는 조용히 사라지고
  나머지 아홉은 초록이다. **배포는 게이트에 없으니 두 번째로 알아챌 경로도 없다.**
- **⛔ `min-instances`는 두 자리에서 죽는다**(M-88 상수 · M-92 렌더러). 둘은 **서로를 안 잡는다** —
  값이 맞는지와 값이 실리는지는 다른 질문이다. M-87이 세 번째다(셸이 뒤에 하나 더 붙이면
  `gcloud`는 마지막 값을 쓴다). ⇒ REQ-805의 오프라인 절반은 **세 자리**다.
- **⛔ 직전 스윕(169)과 갈라진 자리 둘의 원인을 못 밝혔다**: `M-37 4→2 · M-85 4→6`. 이번 변경은
  그 둘이 태우는 자리를 안 지나고, 각각 두 번씩 다시 돌려 **지금 값이 안정적인 것은 확인했다.**
  가장 그럴듯한 설명은 169 기록의 두 숫자가 **옮겨 적히며 틀렸다**는 것이지만(합이 8로 같고
  목록에서 이웃) 증명은 못 했다 — 지난 스윕의 원본 로그가 없다.
  ⇒ **T0-8이 열어 둔 구멍이다**: 신선도 가드는 기준선과 변이 집합만 집행하고 **하중 분포는
  사람이 옮겨 적는다.** 다음에 갈라져도 원인과 오기를 구별 못 한다. **사람 검토 필요.**
- **⚠️ 실행 비트가 없다**: `chmod +x`가 무인 루프의 권한 밖이라 `make deploy`는 `bash`로 부른다
  (`mutate.sh`와 같은 방식). 사람이 `chmod +x scripts/deploy.sh`를 해도 게이트는 안 바뀐다.
- **Blockers**: 없음(이 항목 한정). 레포 전체는 그대로다 — 전용 GCP 프로젝트(T0-3)가 T2를 잠그고,
  **08-24 중단 기준**의 판정 대상은 오프라인 백로그가 아니라 T2-2다.
- **Next**: T11-2(REQ-805 가드 — 이제 훑을 매니페스트가 생겼다) → T11-3(`google-adk` 선언) →
  T11-4 → T11-5. ⚠️ 이 파일은 예산(120줄)을 크게 넘겼다 — `/tidy-docs`로 밀 것.

## 2026-08-22 — T10-3: 어긋남을 **지우지 않고 권위 문서가 말하게 한다** (gate 169)

- **Status**: `[auto]` T10-3 완료 — **T10의 마지막 오프라인 항목이다.** 지금까지
  `mutations.md`(증거)에만 적혀 있던 설계↔코드 어긋남 2건을 **해당 설계 문서의
  「알려진 한계」**로 올렸다. 테스트도 상태 분포도 안 움직인다(169 그대로).
- **Changed**: ① design 01§6에 `reversibility` 어긋남 — §3 표는 *"리소스 타입에서 유도"*지만
  코드는 **타입 ∧ 돌아갈 자리**를 본다. ② design 03에 **§7「알려진 한계」신설**
  (*"원장 항목에 고정"* 단계 + 얼어붙은 계약이 M-60을 살린 이유) + §2 블록에 §7 포인터 한 줄.
  ③ `usecases/provision.py` 도크스트링의 *"설계 문서에 아직 안 적혀 있다"*를 01§6 포인터로.
- **Verified**: `make check` → **169 passed** · mypy strict 37 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). 새 절은 **§ 번호를 안 밀도록 끝에 붙였고**(01§6은 이미 있던
  절, 03은 §6 뒤에 §7 신설), design 01§3 표를 파싱하는 자리(M-55·`_design_section()`)는
  다음 `## `까지만 읽으므로 영향 없다 — 게이트가 그것을 확인했다.
- **⛔ 어긋남 둘은 서로 반대 방향이다**: 01은 **설계가 덜 적었고**(코드가 조건을 하나 더 본다 —
  타입만 보면 첫 배포가 REQ-102에 걸려 계약이 아예 안 선다), 03은 **설계가 더 적었다**
  (코드에 없는 단계). ⇒ *"설계를 코드에 맞춘다"*는 한 방향 규칙으로는 둘 다 못 고친다.
- **⛔ 03§2를 파싱 못 하는 이유가 이제 설계에 있다**: T9-1은 02§2를 파싱했지만 03§2는 안 했다.
  그 이유(파싱하면 이 어긋남 때문에 **가드가 처음부터 red**)가 증거 파일에만 있으면,
  나중에 *"왜 02만 파싱하지"*를 본 사람이 03도 파싱하려다 red를 만나고 **설계나 원장 스키마를
  건드리는** 쪽으로 간다. 그건 사람 판단이다 — 그래서 판단의 근거를 설계 옆에 뒀다.
- **⚠️ 이건 가드가 아니라 문장이다.** 어긋남이 커져도 `make check`는 안 운다.
  집행하려면 `LedgerEntry`에 조치 시점 리비전 칸을 열거나 설계를 고쳐야 하고, **둘 다 T10-3의
  범위 밖(설계 변경)이다.** 원장은 지금 *"되돌린 대상"*은 말하고 *"되돌릴 수 있었던 대상"*은
  말하지 않는다.
- **Blockers**: 없음(이 항목 한정). 레포 전체 블로커는 그대로다 — 전용 GCP 프로젝트가 T2를
  잠그고, **08-24 중단 기준**의 판정 대상은 오프라인 백로그가 아니라 T2-2다.
- **Next**: ⛔ **T10이 끝나면서 `tasks.md`에 남은 `[auto]`가 없다.** 다음은 T0-3(전용 GCP
  프로젝트 — 사람) → T2-1 → T2-2. ⚠️ 이 파일은 예산(120줄)을 크게 넘겼다 —
  `/tidy-docs`로 T6-3·T0-8·T0-7·T9-1~T9-3을 밀 것.

## 2026-08-22 — T10-2: 상태 칸과 증거를 맞대니 **넷이 넷 다 다른 이유**였다 (gate 169)

- **Status**: `[auto]` T10-2 완료. `IMPLEMENTED`로 남아 있던 넷 중 **셋을 `VERIFIED`로**
  올리고 하나는 **근거와 함께 그대로 뒀다**. `make trace` 분포 IMPLEMENTED 4→1 · VERIFIED 31→34.
- **Changed**: ① `tests/test_remediate_loop.py` REQ-305 2건 — 승인된 비가역 조치가 미회복으로
  닫히는 경우 · 롤백이 실패한 뒤 재시도가 없는가. ② `scripts/mutate.sh` M-83~M-86 + `all`·사용법.
  ③ `mutations.md` 새 절 + 전체 스윕. ④ `requirements.md` REQ-206·305·802 → `VERIFIED`.
- **Verified**: `make check` → **169 passed** · mypy strict 37 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-86 전체 스윕 86종 전부 red** · 복구 후 169 ·
  잔여 0 · **가려진 가드 없음**.
- **⛔ REQ-305의 절반에는 도달하는 입력이 아예 없었다**: 자동 경로에서 비가역 계약은 게이트가
  `APPROVE`로 먼저 멈춘다 — 그래서 기존 `test_req_305_...`가 태우던 것은 **게이트가 막는 경우**고,
  *"에스컬레이션한다"*는 절반은 안 물어졌다. `_rollback`의 *"계획이 없다"* 분기에 오는 입구는
  **승인(REQ-404)뿐**이다. ⇒ 그 경로를 세웠고, **M-84를 죽이는 것은 그 하나뿐이다.**
- **⛔ 그 경로를 태우자 기존 가드 넷의 하중이 함께 늘었다**(M-13 13→14 · M-19 21→22 ·
  M-26 10→11 · M-67 10→11). 승인된 비가역 미회복은 G8·G4·승인 집행·원장 완결성을 한 번에
  지나가는데 **아무도 그 구간을 안 지나가고 있었다.** 요구사항 하나의 이야기가 아니었다.
- **⛔ *"더 조치하지 않는다"*는 원장에 안 나타난다**(M-83): 롤백 실패 뒤 조치를 다시 실행해도
  `performed`도 `escalated`도 그대로다. 늘어나는 것은 실행기 호출 횟수뿐 — **실패한 자동화가
  계속 시도하는** 상태가 값으로는 안 보인다. ⇒ 값이 아니라 **호출 흔적**에 묻는다(G1 계열).
- **⚠️ REQ-802의 증거는 데모 서사에 얹혀 있다**(M-85·M-86): 살아 있는 시계도 난수 id도 한 번
  돌려서는 안 보이고, 그 하중을 드는 자리는 *"두 번 돌려 같은가"* 하나다. 데모 시나리오가
  바뀌면 조용히 사라진다(M-63과 같은 계열).
- **Blockers**: 없음(이 항목 한정). ⛔ **REQ-801은 승격하지 않았다** — 그 두 문장은 G5(T5-4)가
  소유하고, 지금 G5를 만들면 `adapters/`에 `fakes.py`뿐이라 **0개를 훑고 초록인 가드**가 된다.
  실물 어댑터는 T2가 잠근다. 레포 전체 블로커도 그대로다(전용 GCP 프로젝트 · 08-24 중단 기준).
- **Next**: T10-3(설계↔코드 어긋남 2건을 알려진 한계로 명시) — T10의 마지막 `[auto]`.
  ⚠️ 이 파일은 예산(120줄)을 크게 넘겼다 — `/tidy-docs`로 T6-3·T0-8·T0-7·T9-1~T9-3을 밀 것.

## 2026-08-22 — T10-1: 귀속을 요구사항이 아니라 **문장**에 물린다 — G6⑤ (gate 167)

- **Status**: `[auto]` T10-1 완료. G6에 다섯 번째 형제 검사가 붙었다 — 요구사항이 약속한
  **문장 수만큼** 겨냥한 테스트 함수를 요구한다. 상태 분포는 안 움직인다(새 REQ가 아니다).
- **Changed**: ① `tools/spec_trace.py` — `normative_paragraph`(EARS 표기~첫 빈 줄) ·
  `split_promises`(구두점만: 종결 `다` 뒤 마침표 · 쉼표 붙은 `-고,`/`-며,`) ·
  `scan_test_functions`(**`파일::함수`** 단위, `scan_test_refs`는 여기서 유도) ·
  `Requirement.promises` · `TraceRow.test_funcs` · `MIN_MULTI_PROMISE` 바닥 ·
  리포트에 `문장`/`겨냥` 칸. ② `tests/test_g6_traceability.py` 4건.
  ③ 변이 M-78~M-82 + `mutate.sh`의 `all`·사용법. ④ design 09§3.1에 ⑤를 적었다.
- **Verified**: `make check` → **167 passed** · mypy strict 37 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-82 전체 스윕 82종 전부 red** · 복구 후 167 ·
  잔여 0 · **가려진 가드 없음**. 하중 분포는 다시 세었다.
- **⛔ 규칙을 켠 첫 실행이 잡은 것은 44개 중 REQ-501 하나 — 그리고 진짜였다**: 두 문장
  (*"1회=1행"*, *"재시도는 새 id를 만들고 `retry_of`로 원래를 가리킨다"*)을 겨냥한 자리가
  이름이 **둘 다를 약속하는** 테스트 하나였고, 그 테스트의 단언은 `len(all_entries())==2`
  하나뿐이었다 — 즉 두 번째 문장은 **아무도 안 묻고 있었다**(M-82가 그 자리를 세웠다:
  저장이 `retry_of`를 흘려도 합쳐진 테스트는 초록이다). 포인터가 끊기면 원장은 같은 사건의
  두 시도를 무관한 조치 둘로 세고, 둘 다 회복률의 **분모**에 실린다. ⇒ 문장마다 하나씩 쪼갰다.
- **⛔ 세는 단위가 함수여야 한다**(M-80): 테스트는 요구사항별로 한 파일에 모이므로 파일로
  세면 *"문장 셋에 겨냥 하나"*가 **언제나 `1 ≥ 1`로 통과한다** — 규칙이 겨냥한 바로 그 모양이다.
- **⛔ M-78의 하중은 `1`이고 그건 일부러 세운 자리다**: 지금 상태가 전부 규칙을 충족해서
  `all_violations`를 읽는 테스트는 검사를 지워도 초록이다 — *"위반이 없다"*와 *"검사가 없다"*가
  같은 초록으로 보이는 자리(M-03·M-25·M-45와 같은 계열). ⇒ 위반하는 행을 값으로 만들어
  판정을 직접 묻는 테스트를 따로 세웠다.
- **⚠️ 분리기는 일부러 덜 센다**: 쉼표 없는 접속(REQ-305 *"에스컬레이션하고 더 조치하지
  않는다"*)은 한 문장으로 읽힌다. 미탐이고, **그렇게 고른 것**이다 — 더 세면 없는 약속에
  테스트를 요구하게 되고 사람이 고치는 방향은 테스트를 쪼개는 것이라 가드가 자기 하중을 없앤다.
  면제는 목록이 아니라 **요구사항이 스스로 적은 구두점**이 정한다(PRINCIPLES #4).
- **⚠️ 직전 스윕과 갈라진 자리는 M-05 하나(`3 → 4`)**: REQ-101을 언급만 하는 테스트로
  `IMPLEMENTED`로 올리면 ⑤도 함께 죽는다(겨냥 `0`). ⑤는 *"여럿 중 하나만"*뿐 아니라
  **`0`을 겨냥한 경우도** 잡는다 — ①이 못 보던 구간이 ⑤에 포함된다는 관측이다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **⚠️ 이번 판에 찾았지만 **안 고친 것** — `specs/warranty/README.md`의 설계 문서 목차가
  통째로 스테일이다**: 12줄 중 `design/01-domain-model.md`·`02-attribution.md`·
  `03-budget-gate.md`·`04-agent-runtime.md`·`05-reconciliation.md`·`06-interfaces.md`·
  `07-verification.md`·`08-deployment.md`·`09-demo.md`가 **전부 실재하지 않는 파일**이다
  (실제는 `01-operational-contract` … `11-demo`). `tools/spec_trace.py:38`의 주석도 같은
  죽은 경로를 가리킨다. **G6④는 못 본다** — ④가 읽는 것은 `Spec:` 도크스트링뿐이고,
  이것들은 산문 참조다. T10-1의 범위가 아니라 손대지 않았다. ⇒ `[auto]` 후보 한 건.
- **Next**: T10-2(상태 칸이 증거와 어긋난 4건) → T10-3. ⚠️ 이 파일은 예산(120줄)을 크게
  넘겼다 — `/tidy-docs`로 T6-3·T0-8·T0-7·T9-1~T9-3을 아카이브로 밀 것.

## 2026-08-22 — T9-5: 요구사항의 절반이 코드가 아니라 문서에 있었다 — 청구 화해 8종 (gate 162)

- **Status**: `[auto]` T9-5 완료 — **T9의 마지막 오프라인 항목**. **REQ-506·509 `IMPLEMENTED`
  → `VERIFIED`**(M-70~M-77 red 확인) — `make trace` 분포가 IMPLEMENTED 6→4 · VERIFIED 29→31.
- **Changed**: ① `InMemoryLedger.give_up_reconcile` 신설 + `LedgerEntry.unreconciled_reason` ·
  포트에도 같은 자리(`ports.LedgerWriter`). ② 변이 M-70(멱등 아님) · M-71(**실측의 근거를
  안 묻는다**) · M-72(배율을 0으로) · M-73(차액을 안 남긴다) · M-74(**기한을 안 본다**) ·
  M-75(사유를 안 적는다) · M-76(이미 화해된 행도 뒤집는다) · M-77(사유 없이도 받는다) —
  `mutate.sh`의 `all`·사용법까지. ③ `tests/test_domain_ledger.py` 6건.
- **Verified**: `make check` → **162 passed** · mypy strict 37 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-77 전체 스윕 77종 전부 red** · 복구 후 162 ·
  잔여 0 · **가려진 가드 없음**. 하중 분포는 **다시 세었다**.
- **⛔ 이번 판의 요점 — REQ-506의 절반은 잴 코드가 없었다**: 요구사항은 두 문장(멱등·기한)인데
  기한 쪽은 **아무 데도 구현되어 있지 않았다.** 있던 것은 이름 둘뿐이다 —
  `ReconcileState.UNRECONCILED`(어떤 코드도 이 값을 안 쓴다)와 설정 `WR_RECONCILE_DEADLINE_DAYS`
  (읽히기만 하고 아무도 안 본다). 상태 칸은 `IMPLEMENTED`였고 **G6은 못 본다** — G6이 묻는
  것은 *"테스트가 있는가"*지 *"약속한 문장마다 코드가 있는가"*가 아니다(PRINCIPLES #8의 다른 얼굴).
  ⇒ 둘을 잇는 자리를 만들고(기한 판단은 **저장소가** 한다 — 호출자에게 맡기면 *"기한 내"*가
  관례가 되고, 아직 도착할 수 있는 청구서를 두고 닫은 행의 비용은 영원히 추정으로 남는다),
  그 위에 변이 넷을 심었다.
- **⛔ M-71은 156건 전부 초록이었다**: `measured`의 근거가 청구서인지 묻는 자리가 없었다.
  그 거절이 죽으면 **추정치를 실측 칸에 넣을 수 있고**, 그때 `assumed`와 `measured`는 같은
  계산의 두 벌이라 `delta`가 0에 수렴한다 — 리포트는 *"추정이 정확했다"*로 읽히지만 실제로는
  **청구서를 한 번도 안 본 것**이다.
- **⚠️ M-73의 하중은 이름이 다른 것을 약속하는 테스트**가 들고 있었다 —
  `test_req_505_reconcile_does_not_touch_assumed`의 곁다리 단언 세 줄. T0-10이 실제로 한
  정리(이름이 약속한 것만 묻게 좁히기)가 그 자리에 닿으면 REQ-509의 산출물은 조용히 사라진다.
- **⚠️ 직전 스윕과 갈라진 자리가 하나도 없다** — T9 들어 처음이다. 새 가드가 화해 경로에만
  물렸고 그 경로를 지나가는 다른 테스트가 없기 때문이다(T9-1~T9-4에서는 매번 M-14·M-19가
  함께 움직였다). **갈라지지 않았다는 것도 관측이어야 해서 다시 셌다.**
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: ⛔ **T9의 오프라인 `[auto]`는 여기서 끝났다.** 남은 것은 실물 GCP를 요구한다
  (T0-3 → T2-1 → T2-2). ⚠️ 이 파일은 예산(120줄)을 **316줄로** 넘겼다 — `/tidy-docs`로
  T6-3·T0-8·T0-7·T9-1~T9-3을 아카이브로 밀 것.

## 2026-08-22 — T9-4: 도달하는 입력이 없던 분기 하나 — 원장 완결성 3종의 하중 (gate 156)

- **Status**: `[auto]` T9-4 완료. **REQ-507 `IMPLEMENTED` → `VERIFIED`**(M-67~M-69 red 확인)
  — `make trace` 분포가 IMPLEMENTED 7→6 · VERIFIED 28→29로 움직인다.
- **Changed**: ① 변이 M-67(막는 판정에 **원장 행을 안 만든다**) · M-68(**예약이 막은 것**을
  원장에 안 적는다) · M-69(실패한 조치를 **`executed`로 적는다**) — `mutate.sh`의 `all`·사용법까지.
  ② `tests/test_remediate_loop.py` 3건 + 예산 대역 `_RacingBudget` — 막힌 세 경우가 **원장에
  남는가**(반환값이 아니라 `ledger.get`/`all_entries`에 묻는다) · 경합에서 진 예약이 `denied`로
  남는가 · 실패가 `failed`로 남는가(둘 다 **회복하는 신호**를 태워 공허 통과 방지).
- **Verified**: `make check` → **156 passed** · mypy strict 37 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-69 전체 스윕 69종 전부 red** · 복구 후 156 ·
  잔여 0 · **가려진 가드 없음**. 하중 분포는 **다시 세었다**.
- **⛔ 이번 판의 요점 — M-68은 153건 전부 초록이었다. 그 분기에 **도달하는 입력이 없었다***:
  예약 실패는 게이트가 여유를 **읽은 뒤** 다른 조치가 가져갔을 때만 나는데, 재진입 테스트로는
  그 순서가 안 만들어진다(안쪽 조치는 줄어든 여유를 게이트가 다시 읽어 **판정부터 `DENY`**다).
  그때 원장에 남는 것은 게이트가 적어 둔 **`executed`**다 — 실행기는 안 불렸는데 원장이
  *"실행했다"*고 말하고, 그 행은 리포트의 **분모**에 실려 회복률을 희석한다.
- **⛔ M-67을 죽이던 아홉 건은 전부 승인 경로였다**: 그것들이 죽은 이유는 *"막힌 조치가 원장에
  남는가"*가 아니라 **`approve()`가 그 행을 찾아야 하기 때문**이다. 즉 하중을 받던 상태는
  `awaiting_approval` 하나고, REQ-507이 이름을 부르는 **`denied`·`manual_required`는 아무도
  안 묻고 있었다.** M-69의 하중도 마찬가지로 **예산 정산 테스트**(REQ-405)가 들고 있었다.
- **⚠️ 스윕에서 두 자리가 갈라졌다** — **M-14 10→11**(G9)와 **M-19 20→21 failed**(G4).
  새 테스트가 각각 *"검증 불가"* 경우와 `_decision(entry)`를 지나간다. 이번에도 다시 셌다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: `[auto]` T9-5(화해 2종 *(선택)* · REQ-506·509) — T9의 마지막 오프라인 항목이다.
  ⚠️ 이 파일은 예산(120줄)을 더 넘겼다 — `/tidy-docs`로 T6-3·T0-8·T0-7을 아카이브로 밀 것.

## 2026-08-22 — T9-3: 하중을 들고 있던 것이 데모와 승인 경로였다 — 계약 수명 2종 (gate 153)

- **Status**: `[auto]` T9-3 완료. **REQ-104·105 `IMPLEMENTED` → `VERIFIED`**(M-63~M-66
  red 확인) — `make trace` 분포가 IMPLEMENTED 9→7 · VERIFIED 26→28로 움직인다.
- **Changed**: ① 변이 M-63(계약 **없음**을 검증 가능으로 친다) · M-64(계약 없음 **전용 분기
  삭제**) · M-65(저장소가 **`retired`도 돌려준다**) · M-66(**종료가 상태를 안 바꾼다**) —
  `mutate.sh`의 `all`·사용법까지 함께. ② `tests/test_remediate_loop.py` 3건 — 계약 없음이
  원장에 **검증 불가**로 남는가 · **예산이 막는 경우에도** `manual_required`가 유지되는가 ·
  조치 경로에서 `retired`가 계약으로 안 실리는가(회복하는 신호를 태워 공허 통과 방지).
- **Verified**: `make check` → **153 passed** · mypy strict 37 files · lint 초록
  (2026-08-22 로컬 macOS·py3.13). **M-01~M-66 전체 스윕 66종 전부 red** · 복구 후 153 ·
  잔여 0 · **가려진 가드 없음**. 하중 분포는 **다시 세었다**.
- **⛔ 이번 판의 요점 — 하중을 든 자리가 요구사항을 겨냥한 자리가 아니었다**: M-63을 죽인
  것은 유닛 가드가 아니라 **데모 서사 테스트**(`test_req_803_...`) 하나였고, REQ-105를
  물던 자리는 **승인 뒤 재판정 경로 하나**뿐이었다 — Day-2의 정상 입구인 `remediate()`에서
  죽은 계약을 태운 적이 없다. 둘 다 *"지금은 red"*지만, 데모 시나리오가 바뀌거나 승인
  경로가 정리되면 **그 하중은 조용히 사라진다.**
- **⛔ M-64는 150건 전부 초록이었다**: 계약이 없으면 게이트도 `MANUAL`을 내므로 전용 분기가
  덮으려던 값과 **똑같다.** 갈라지는 경우는 **예산이 먼저 막을 때** 하나뿐이고, 그때 원장은
  `denied`라고만 적어 *"계약이 없었다"*가 사라진다 — 예산을 채우면 자동으로 도는 조치처럼
  읽히지만 실제로는 **잴 신호조차 없는 리소스**다.
- **⚠️ 스윕에서 두 자리가 갈라졌다** — **M-14 9→10**(G9)와 **M-19 18→20 failed**(G4).
  새 테스트가 각각 *"검증 불가는 AUTO가 아니다"*와 `_decision(entry)`를 함께 지나간다.
  옮겨 적었으면 못 봤을 값이다(이번에도 다시 셌다).
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: `[auto]` T9-4(원장 완결성 · REQ-507). ⚠️ 이 파일은 예산(120줄)을 더 넘겼다 —
  `/tidy-docs`로 T6-3·T0-8·T0-7을 아카이브로 밀 것.

## 2026-08-21 — T9-2: 같은 요구사항의 두 변이가 갈렸다 — 롤백 2종의 하중 (gate 150)

- **Status**: `[auto]` T9-2 완료. **REQ-301·302 `IMPLEMENTED` → `VERIFIED`**(M-60~M-62
  red 확인) — `make trace` 분포가 IMPLEMENTED 11→9 · VERIFIED 24→26으로 움직인다.
- **Changed**: ① 변이 M-60(계획을 **조치 뒤에** 읽는다) · M-61(계획을 **저장소에서 다시**
  읽는다) · M-62(**모델이 닫은** 미회복은 롤백 안 한다) — `mutate.sh`의 `all`·사용법까지 함께.
  ② `tests/test_rollback_plan_order.py` 4건 — 계획이 조치 **전에** 묶이는가 · 롤백이 **그
  이름을** 받는가 · 조치 뒤에 계약 저장소를 **다시 안 읽는가**를 AST로 묻는다(+공허 통과 방지).
  ③ `test_remediate_loop.py` 2건 — 모델이 닫은 미회복도 롤백한다 · 모델 경로라고 **무조건
  롤백하지도** 않는다.
- **Verified**: `make check` → **150 passed** · mypy strict 37 files · lint 초록
  (2026-08-21 로컬 macOS·py3.13). **M-01~M-62 전체 스윕 62종 전부 red** · 복구 후 150 ·
  잔여 0 · **가려진 가드 없음**. 하중 분포는 **다시 세었다**.
- **⛔ 이번 판의 요점 — 한 요구사항이 두 문장이었다**: REQ-301의 변이 둘이 갈렸다.
  저장소를 다시 읽는 M-61은 red였지만(`contracts.lookups == 1`), **계획을 조치 뒤로 내리는
  M-60은 144건 전부 초록**이었다 — 계약은 이미 얼어붙은 값이라 계획도 판정도 전환 대상도
  안 바뀐다. 재조회는 **오늘의 위반**이고 묶이는 시점은 **내일의 위반을 막는 자리**다:
  계획이 이미 조치 뒤에 묶여 있으면, 나중에 누가 계약을 조치 뒤에 다시 읽어도 **그 자리는
  이미 조치 뒤라 아무것도 어긋나 보이지 않는다.**
- **⛔ M-62도 살아남았다**: REQ-302는 *"검증이 `not_recovered`일 때"*라고만 적는데, 롤백을
  묻는 자리는 **규칙이 닫은 경우 하나뿐**이었다. 모델이 불리는 경우는 정의상 tolerance
  안쪽 — **판단이 가장 어려운 구간에서** 롤백이 조용히 사라지고 원장은 똑같아 보인다.
- **⚠️ 발견 — 설계를 파싱하지 *않은* 이유를 남겼다**: design 03§2의 절차 블록에는 코드에
  없는 단계가 있다(*"원장 항목에 고정"* — 코드는 지역 이름에 묶는다). T9-1처럼 파싱하면
  가드가 **처음부터 red**이고, 초록으로 만들려면 설계나 원장 스키마를 건드려야 한다(범위 밖).
  ⇒ 자리를 AST로 묻고 어긋남은 `mutations.md`에 남겼다.
- **⚠️ M-13이 12→13으로 늘었다** — 새 REQ-302 테스트가 `entry.improved`를 함께 묻는다.
  옮겨 적었으면 못 봤을 값이다(직전 판의 교훈대로 이번에도 다시 셌다).
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: `[auto]` T9-3(계약 수명 2종 · REQ-104·105). ⚠️ 이 파일은 예산(120줄)을 더 넘겼다 —
  `/tidy-docs`로 T6-3·T0-8·T0-7을 아카이브로 밀 것.

## 2026-08-21 — T9-1: 하나는 살아남았고 하나는 우연히 죽었다 — 검증 3종의 하중 (gate 144)

- **Status**: `[auto]` T9-1 완료. **REQ-201·202·203 `IMPLEMENTED` → `VERIFIED`**(M-57~M-59
  red 확인) — `make trace` 분포가 IMPLEMENTED 14→11 · VERIFIED 21→24로 움직인다.
- **Changed**: ① 변이 M-57(기준선을 **조치 뒤에** 잰다) · M-58(재측정이 **자기 질의를 새로
  만든다**) · M-59(판정 기준을 **조치가 들고 있다**) — 셋 다 `mutate.sh`의 `all`·사용법까지
  함께. ② `tests/test_verification_order.py` 3건 — design 02§2의 절차 블록을 **파싱해서**
  순서(`read→execute→wait→read→judge`)와 **각 단계의 인자 표현식**을 요구한다.
- **Verified**: `make check` → **144 passed** · mypy strict 36 files · lint 초록
  (2026-08-21 로컬 macOS·py3.13). **M-01~M-59 전체 스윕 59종 전부 red** · 복구 후 144 ·
  잔여 0 · **가려진 가드 없음**. 하중 분포는 **다시 세었다**(옮겨 적지 않았다).
- **⛔ 이번 판의 요점 — 가드를 넣기 전에 먼저 쟀다**: **M-59는 141건 전부 초록으로
  살아남았다.** 판정 기준을 계약이 아니라 조치가 들고 있어도, 값이 같으면 회복·미회복·애매·
  빈 창 네 경우가 전부 그대로 통과한다. **M-57은 red였지만 죽인 것은 REQ-201의 가드가 아니라
  REQ-405의 예약 해제 테스트였다** — *"기준선은 조치 전에 잰다"*를 묻는 자리는 한 곳도 없었다.
  우연한 하중은 다음 리팩터에 사라지고, 사라진 것은 조용하다.
- **⚠️ 발견**: 새 파서가 처음에 **다섯 단계 중 넷만 읽었다** — 공백을 `\s*`로 둬서 인자 없는
  단계(`② execute`) 뒤에서 **줄바꿈을 삼켰고** `③ wait`가 조용히 빠졌다. 바닥
  (`MIN_DESIGN_STEPS`)이 잡았다. 없었으면 *"순서가 맞다"*가 **네 단계에 대한 문장**이었다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: `[auto]` T9-2(롤백 2종 · REQ-301·302). ⚠️ 이 파일은 예산(120줄)을 더 넘겼다 —
  `/tidy-docs`로 T6-3·T0-8·T0-7을 아카이브로 밀 것.

## 2026-08-21 — T3-2: 계약을 **응답에서** 유도한다 — 값이 아니라 출처를 묻는다 (gate 141)

- **Status**: `[auto]` T3-2 완료. **REQ-103 `TODO` → `VERIFIED`**(M-53~M-56 red 확인) —
  `make trace` 분포가 TODO 10→9 · VERIFIED 20→21로 움직인다. ⛔ **남은 `[auto]`는 없다.**
- **Changed**: ① `usecases/provision.py` — `ProvisionResponse`(생성 API가 실제로 만든 것) 하나에서
  계약을 유도하는 `derive_contract`. 아는 리소스 타입은 표(`KNOWN_KINDS`)에 있고 **모르는 타입은
  기본값이 아니라 거부**다. ② 데모가 그 경로를 탄다 — 손으로 조립하던 `OperationalContract(...)`를
  지우고 `DEMO_PROVISION_RESPONSE`에서 유도한다. `demo_contract(resource, at)` →
  `demo_contract(at)`로 좁혔다(**리소스도 계약에서 온다**). ③ `tests/test_provision_contract.py`
  10건. ④ 변이 M-53~M-56.
- **Verified**: `make check` → **141 passed** · mypy strict 35 files · lint 초록
  (2026-08-21 로컬 macOS·py3.13). **M-01~M-56 전체 스윕 56종 전부 red** · 복구 후 141 ·
  잔여 0 · **가려진 가드 없음**.
- **★ 이번 판의 요점**: **계약이 유도되는지는 값의 질문이 아니다.** 네 자리에 맞는 값이 들어
  있는가는 손으로 적어도 통과한다 — 그래서 셋을 구문으로 묻는다: 유도되는 자리가
  `derive_contract`의 **인자로 열려 있지 않은가**(`inspect.signature`) · 프로덕션 코드에서 계약을
  **조립하는 자리가 하나뿐인가**(`ast`) · 응답이 바뀌면 그 자리가 **전부 따라 바뀌는가**.
  금지 목록은 design 01§3의 표를 **파싱해서** 읽는다(T0-10의 규칙). **M-54는 값을 하나도 안
  바꾸는 변이다** — 인자 하나를 다시 열 뿐인데, 그 순간부터 계약은 리소스와 함께 늙지 않는다.
- **⛔ 발견**: **설계가 못 적은 자리가 하나 나왔다.** design 01§3은 가역성을 *"리소스 타입에서
  유도(Cloud Run 서비스 = 가역)"*로 적는데, 타입만 보면 **돌아갈 리비전이 없는 첫 배포**가
  REQ-102에 걸려(가역인데 롤백 계획 없음) 계약이 **아예 성립하지 않는다.** ⇒ 코드는 타입을
  **필요조건**으로 쓰고 목적지 유무를 함께 본다. 설계를 고치는 것은 범위 밖이라 어긋남을 남겼다.
- **⚠️ 발견**: 하중 분포를 적다가 **또 옮겨 적었다** — 초안에 *"직전 스윕과 같다"*라고 썼는데
  실제 M-19는 17이 아니라 **18**이었다(새 데모 테스트가 G4 집합에 들어갔다). 직전 스윕이
  `16 → 17`로 썩은 것을 **바로 윗 절에 적어 두고도** 그랬다. 게이트가 집행하는 것은 기준선뿐이다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: ⛔ **오프라인 `[auto]` 백로그가 비었다.** 남은 것은 전부 실물 GCP를 요구한다
  (T0-3 → T2-1 → T2-2). ⚠️ 스텁 초록 위험을 안고 있는 자리: 실물 Cloud Run 생성 응답이
  `ProvisionResponse` 모양으로 오는지는 **T3-1에서만 확인된다** — 데모 `caveats`가 그것을 말한다.
  ⚠️ 이 파일은 예산(120줄)을 더 넘겼다 — `/tidy-docs`로 T6-3·T0-8·T0-7을 아카이브로 밀 것.

## 2026-08-21 — T0-10: 이름이 약속한 것과 단언이 한 것이 달랐다 — 세 자리 (gate 130)

- **Status**: `[auto]` T0-10 완료. REQ 상태 변화 **없다**(테스트를 구문 층으로 올린 것이라
  요구사항을 안 건드린다 — `make trace` 분포 그대로). 남은 `[auto]`는 **T3-2 하나뿐이다.**
- **Changed**: 전수 점검에서 나온 세 자리를 승격했다. ① `test_tunables.py` — 검사 ④ 추가:
  대기·창 인자가 **`warranty.tunables`에서 들여온 이름**인지 `ast`로 묻는다(`_sink_args`로
  ②와 자리 목록을 공유). ② `test_report.py` — `DESIGN_COLUMNS` 손 사본을 지우고 design
  05§5의 ```jsonc 블록을 **파싱해서** 읽는다. ③ `test_demo.py` — `DESIGN_STEPS` 손 사본을
  지우고 design 11§2의 번호 매긴 줄을 읽는다. + `test_req_206_...used_once`를 **이름 정정**
  (그 단언은 값만 묻는다). + 변이 M-50·M-51·M-52.
- **Verified**: `make check` → **130 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-50·M-51·M-52 red 확인**(1건씩) · **M-01~M-52 전체 스윕 52종 전부 red** · 복구 후 130 ·
  잔여 0 · **가려진 가드 없음**. 새 가드가 **M-44를 1→2, M-45를 3→4로** 늘렸다(의도한 겹침).
- **⛔ 발견(이번 판에서 가장 큰 것)**: **세 자리 다 이미 초록으로 통과하고 있었다.** 셋 다
  이름이 *"설계가 이름 붙인 것을 지킨다"*고 말하는 자리였는데(`..._is_a_named_constant_used_once`
  · `..._every_column_the_design_names` · `..._the_ones_the_design_names`), 실제로 물은 것은
  **값의 일치**거나 **이 파일에 베껴 둔 사본과의 일치**였다. ⇒ M-44가 보여 준 것은 한 자리의
  실수가 아니라 **계열**이다: 테스트 이름은 약속이고, 약속과 단언은 따로 썩는다.
- **⚠️ 발견**: **리터럴을 막으면 이름으로 우회한다.** M-50(`delay_s = 45`를 함수 안에)은
  최상위 상수도 리터럴도 아니라 기존 가드 ①②를 **둘 다 통과한다.** 물어야 할 것은
  *"숫자가 아닌가"*가 아니라 **"어디서 온 이름인가"**였다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: **T3-2**(계약 유도)가 마지막 `[auto]`다. 그 뒤 오프라인 백로그는 **없다.**
  ⚠️ 남긴 것: M-52는 이름의 **정확한 일치**를 요구하지 않는다 — design 11§2는 ③④를
  `remediate #1/#2`로 부르고 코드는 `remediate`/`manual`로 부른다. 맞추려면 설계나 코드의
  이름을 고쳐야 하고 그건 이 태스크의 범위가 아니었다. 집행하는 것은 개수·순서·겹침없음과
  *"n번째 이름이 n번째 줄에 나오는가"*다. ⚠️ 이 파일은 여전히 예산(120줄)을 **넘겼다** —
  `/tidy-docs`로 T6-3·T0-8을 아카이브로 밀 것.

## 2026-08-21 — T0-9: 억제를 **읽는 사람**부터 만들었다 — 62개 중 61개는 아무도 안 읽고 있었다 (gate 129)

- **Status**: `[auto]` T0-9 완료. REQ 상태 변화 **없다**(테스트 타이핑이라 요구사항을 안 건드린다 —
  `make trace` 분포 그대로). 남은 `[auto]`는 T0-10·T3-2.
- **Changed**: ① `pyproject.toml` — mypy `files`에 **`tests` 추가**(22→33 파일). ② 픽스처가
  만들던 억제를 픽스처에서 없앴다: `_decide`·`_run`이 `object`가 아니라 `Decision`·`LedgerEntry`를
  반환하고(쓰이지도 않던 `**over` 통로는 제거), `_MISSING = object()`는 **단일 멤버 enum** 센티널로,
  `_build(**kwargs)` 루프는 `TypedDict`로, `LedgerEntry(**base)` 넷은 **생성자 + `replace`**로.
  ③ `decision`·`verification`·`rollback`·`approval`을 좁혀 읽는 헬퍼 넷과 fake 기록 표면
  (`_budgets`·`_clock`·`_contracts`)을 더했다. ④ `test_config`의 손수 monkeypatch를
  `pytest.MonkeyPatch` + **원래 시그니처를 받는** 스파이로. **`src/`는 0줄 건드렸다.**
- **Verified**: `make check` → **129 passed** · mypy strict **33 files 초록** · lint 초록
  (2026-08-21 로컬 macOS·py3.13). 통과 수는 작업 전과 **동일**하다. 억제 **62→1**.
  ⚠️ 새 하중은 **손으로** red 확인했다 — `tests/test_report.py`에 군더더기 `# type: ignore` 하나를
  심으니 `unused-ignore`로 red, 빼니 초록. **이 변이는 하네스에 없다**(아래).
- **⛔ 발견(이번 판에서 가장 큰 것)**: **62개 중 61개는 집행되지 않는 주석이었다.** mypy가
  `files = ["src", "tools"]`라 테스트를 아예 안 봤고, 그래서 strict의 `warn_unused_ignores`가
  안 돌았다 — 필요 없어진 억제가 쌓여도 아무도 몰랐다(실제로 5개는 이미 죽어 있었다).
  ⇒ **억제를 세기 전에 억제를 읽는 사람을 먼저 만들어야 했다.** 안 그러면 62를 0으로 만드는
  가장 쉬운 방법이 *"전부 지우기"*이고 그것도 초록이다. T0-7·T0-8과 같은 계열의 네 번째 사례다.
- **⚠️ 발견**: 억제가 **다른 결함을 덮고 있었다.** `attr-defined`로 눌러 둔 자리의 진짜 오류는
  `union-attr`이었다 — `entry.decision`은 `Decision | None`이고 **테스트는 그걸 모른 채** 쓰고
  있었다. 그리고 `cfg.load_env_file = lambda *a, **k: calls.append(1) or {}`는 `append`가
  `None`을 반환하므로 **`or {}`가 항상 타는 죽은 표현**이었다(mypy `func-returns-value`).
  둘 다 억제를 지우자마자 나왔다. ★ 억제는 자기가 이름 붙인 것만 덮지 않는다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다.
- **Next**: **T0-10**(값-대-구문 착각 전수 점검) · T3-2(계약 유도).
  ⛔ **남긴 빚 둘.** ① 위의 red 확인이 `scripts/mutate.sh`에 **없다** — 넣으려면 T0-8의 신선도
  가드 때문에 전체 스윕을 다시 돌려야 해서 이번 범위 밖으로 뒀다. 즉 *"tests가 mypy에 들어와
  있다"*를 지키는 **하네스 변이는 아직 없다.** ② 남은 억제 1개(`test_domain_ledger`의
  `[index]`)는 **일부러** 타입이 금지한 짓을 하는 자리라 사유를 달아 남겼다. 그리고
  재정의 인자(`**over: Any`)는 여전히 검사 밖이다 — 줄인 것은 억제지 `Any`가 아니다.
  ⚠️ 이 파일이 예산(120줄)을 **넘겼다** — 다음에 `/tidy-docs`로 T6-3을 아카이브로 민다.

## 2026-08-21 — T0-8: 기록이 아직 참인지를 게이트가 묻는다 — 신선도는 참임의 **대리**다 (gate 129)

- **Status**: `[auto]` T0-8 완료. REQ 상태 변화 **없다**(이 가드는 특정 요구사항이 아니라
  **증거 기계 자신**을 지킨다 — G6④·M-23~M-25와 같은 계열). 남은 `[auto]`는 T0-9·T0-10·T3-2.
- **Changed**: ① `tests/test_mutation_freshness.py` — 맨 **마지막**「전체 스윕」절만 현재를
  주장하는 것으로 보고 넷을 묻는다: 공허 통과 방지 · **선언한 기준선 = 지금 스위트 크기** ·
  기록이 덮는 변이 집합 = 하네스가 돌릴 수 있는 집합(`case`·`all` 목록·**사용법 문자열**까지) ·
  정의된 변이마다 `red 확인` 행. ② `make mutate`(`M=M-47`로 한 건) — **게이트 아님**.
  ③ 변이 M-47·M-48·M-49. 오래된 절은 안 건드렸다 — 그것들은 그때 참이었던 역사다.
- **Verified**: `make check` → **129 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-47·M-48·M-49 red 확인**(1·3·1건) · **M-01~M-49 전체 스윕 49종 전부 red** · 복구 후 129 ·
  잔여 0 · **가려진 가드 없음**. 새 가드가 **M-05의 하중을 늘렸다**(2→3 — 테스트를 덧붙이는
  변이라 스위트가 130이 되고 신선도가 red를 낸다. 의도한 부작용이다).
- **⛔ 발견(이번 판에서 가장 큰 것)**: **하중 분포를 손으로 옮겨 적던 자리가 이미 썩어 있었다.**
  기록이 `M-19 16건`이라고 말해 온 값은 기준선 **120**일 때 센 것이고, 지금 세면
  `17 failed + 10 errors`다. 124·125 스윕은 분포를 **다시 세지 않고** *"그대로"*라고만 적었다 —
  언제 갈라졌는지 못 되짚는다. ⇒ T0-7이 잡은 병이 **한 층 위에 그대로** 있었다.
  이 가드는 *기준선*만 집행한다. 분포는 여전히 사람이 옮겨 적는 숫자다.
- **⚠️ 발견**: 스위트 크기는 **pytest에게 물었다**(`--collect-only`). `def test_`를 세는 두 번째
  계산기를 만들면 파라미터화에서 갈라지고, 그러면 **가드는 자기가 만든 숫자만 지킨다**(M-44의
  다른 얼굴). `-q`를 또 주면 `-qq`가 되어 요약 줄이 사라진다 — 하네스의 첫 결함이라 안 줬다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고
  **08-24 중단 기준**의 판정 대상이다. ⚠️ `make mutate`는 권한 경계에 **없다** — 무인 루프는
  `bash scripts/mutate.sh`(이미 allow)로 돌렸고 경계는 안 건드렸다.
  ⚠️ 이 파일이 예산(120줄)에 닿았다 — 다음에 `/tidy-docs`로 T6-3을 아카이브로 민다.
- **Next**: **T0-9**(`type: ignore` 정리) · T0-10(값-대-구문 착각 전수 점검) · T3-2(계약 유도).
  ⛔ 이 가드는 *"M-01~M-49가 지금 전부 red다"*를 증명하지 않는다 — *"그 주장이 적힌 뒤로
  스위트와 하네스가 안 바뀌었다"*만 증명한다. 바뀌면 red고, 그때 답은 숫자를 고치는 것이
  아니라 `make mutate`를 다시 돌리는 것이다.

## 2026-08-21 — T0-7: 문서 예산을 회수했다 — 넘친 것보다 **세어 적은 숫자**가 문제였다 (gate 125)

- **Status**: `[auto]` T0-7 완료. 문서만 만졌다 — 코드·테스트 **0줄**. REQ 상태 변화 없다.
  두 번째 `[auto]` 배치(T0-8·T0-9·T0-10·T3-2)는 그대로 남아 있다.
- **Changed**: ① `PROGRESS_LOG` **281→73줄** — T5-3 이전 일곱 증분을
  `docs/archive/PROGRESS_LOG-2026-08.md`(217줄)로 밀고 머리말에 포인터를 달았다.
  ② `OVERVIEW` **245→240** — §10 상태 상자가 *세어 적던* 숫자를 **가리키기**로 바꿨다.
  ③ `tasks.md` **211→190** — 완료 항목의 서사를 로그/아카이브에 넘기고, 각 항목에는
  *무엇을 했는가* + **다음 사람이 몰라서 다칠 한 줄**만 남겼다(머리말에 그 규칙을 적었다).
- **Verified**: `make check` → **125 passed** (2026-08-21 로컬 macOS·py3.13) — **작업 전과 동일**.
  변이 기록은 안 늘렸다(문서 문자열이라 밀 가드가 없다 — 있다고 적으면 G6가 막는 거짓 주장이다).
  링크는 레포 전체 상대 링크 **14개를 열거해 전부 실재 확인**했다. `tasks.md`의 REQ 참조
  집합은 압축 전후 **44종 그대로**다 — 하나라도 빠지면 G6가 "태스크 없는 요구사항"으로 red다.
- **⛔ 발견(이번 판에서 가장 큰 것)**: **줄 수 초과는 증상이고 병은 중복 계수였다.**
  `OVERVIEW` §10은 `120 passed` · `VERIFIED 18` · `M-01~M-32` · `커밋 6개`를 말하고 있었고
  **넷 다 틀렸다**(실제 125 · 20 · M-45 · 14+). 그 상자 **바로 아래에** *"여기에 숫자를 다시
  세어 적지 않는다"*가 적혀 있었다 — **경고문과 위반이 같은 절에 공존했다.** 그리고
  `PRINCIPLES.md` #10은 *"게이트 통과 수는 `tasks.md` 한 곳에만"*이라고 이미 못 박아 뒀다.
  ⇒ **산문으로 쓴 금지는 금지가 아니다.** T0-6이 "기계가 안 읽는 자리는 재발한다"고 적었는데,
  재발한 자리가 바로 **그 교훈을 적어 둔 문단 옆**이었다.
- **⚠️ 발견**: `OVERVIEW` 맨 위 `▶ NEXT SESSION`이 *"`[auto]` 백로그가 **말랐다**"*고 말하는데
  권위인 `tasks.md`에는 **미완 `[auto]`가 다섯** 있었다(`6d7a00e`가 심었다). 계획 문서를
  갱신할 때 **그것을 요약한 문서는 같이 안 움직인다** — 요약이 곧 복제본이기 때문이다.
  ⇒ 그 자리도 세는 대신 `tasks.md`를 **가리키게** 바꿨다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를
  잠그고 **08-24 중단 기준**의 판정 대상이다. T6-2가 남긴 요청도 그대로다(사람이 `make demo`를
  한 번 눈으로 돌려 보고 권한 경계의 deny 항목을 옮길지 판단할 것).
- **Next**: 남은 오프라인 `[auto]` — **T0-8**(변이 기록 신선도 가드) · T0-9(`type: ignore` 정리) ·
  T0-10(값-대-구문 착각 전수 점검) · T3-2(계약 유도).
  ⚠️ **T0-8이 다음 순서다** — 이번 판이 "선언한 숫자가 조용히 썩는다"의 세 번째 사례고,
  T0-8이 잡으려는 것이 정확히 그 계열이다. ⛔ 다만 **문서 예산·중복 계수 자체는 여전히
  아무도 안 잡는다**(T0-8의 범위는 `mutations.md` 기준선이다). 다시 넘치면 또 사람 눈이다.

## 2026-08-21 — T6-3: 상수를 모았다 — 그리고 기존 테스트가 못 잡던 자리가 드러났다 (gate 124)

- **Status**: `[auto]` T6-3 완료. REQ-804 `TODO`→**`VERIFIED`**. 오프라인 `[auto]` 백로그는
  **이것으로 마른다** — 남은 것은 전부 실물 GCP나 사람 판단을 기다린다.
- **Changed**: `warranty/tunables.py` 신설 — 대기·창·예산·워밍이 **한 모듈**에만 있다
  (design 11§5). `remediate.py`는 정의 대신 **가리키고**, `demo.py`의 `DEMO_BUDGET_USD`도
  거기서 온다. `tests/test_tunables.py`가 `ast`로 셋을 묻는다: ① 그 모듈 **밖에** 대기·창
  모양의 모듈 상수가 없는가 ② 호출부(`sleep(...)`·`window_s=`)에 숫자가 박혀 있지 않은가
  ③ design 11§5가 선언한 **이름 집합**과 모듈이 정의한 집합이 같은가.
- **Verified**: `make check` → **124 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-42·M-43·M-44·M-45 red 확인** · **M-01~M-45 전체 스윕 45종 전부 red** · 복구 후 124 · 잔여 0.
  ⚠️ **가려진 가드 없음** — 하중 분포도 그대로다(M-13 12 · M-19 16 · M-14 9 · M-26 10).
  새 가드는 소스를 **텍스트로** 읽는 층이라 조치 경로 안쪽 변이를 흡수할 자리가 없다.
- **⛔ 발견(이번 판에서 가장 큰 것)**: **이미 있던 테스트가 이 회귀를 못 잡고 있었다.**
  `test_req_206_..._used_once`는 `set(clock.slept) == {VERIFY_DELAY_S}`를 묻는다 — 이름을
  쓰라고 요구하는 것처럼 보이지만 실제로 요구한 것은 **값의 일치**다. 호출부에 `45`를 박아도
  잔 시간은 여전히 45라서 **초록이다**(M-44). 갈라지는 것은 재촬영 때 tunables를 줄이는
  순간이고, 그때는 이미 늦다. ⇒ **이름을 쓰는지 물으려면 값이 아니라 구문을 봐야 한다.**
  테스트 이름에 `named_constant`가 들어 있다고 그것을 물은 것이 아니다.
- **⚠️ 발견**: 값은 **일부러 안 묶었다.** design 11§2가 *"타이머를 영상 길이에 맞춘다"*고
  말한다 — 값까지 집행하면 조정할 때마다 red고 **그런 가드는 결국 꺼진다.** 집행 가능한 것은
  *"어떤 손잡이가 있는가"*이지 *"손잡이가 몇 도인가"*가 아니다. 그래서 ③은 이름 집합만 묻는다.
- **⚠️ `WARMUP_REQUESTS`는 읽는 코드가 없다** — 워밍은 T8-3(촬영) 절차이지 코드 경로가 아니다.
  없애지 않고 남긴 이유는 design 11§5가 그 이름을 선언하기 때문이고, 어긋나면 M-43이 red다.
  **값을 두는 것과 읽는 곳이 생기는 것은 다른 일이다** — 후자는 아직 안 일어났다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를
  잠그고 **08-24 중단 기준**의 판정 대상이다. T6-2가 남긴 요청도 그대로다: 사람이
  `make demo`를 한 번 눈으로 돌려 보고, 권한 경계의 그 deny 항목을 옮길지 판단할 것.
- **Next**: ⛔ **오프라인 `[auto]`가 없다.** 다음 무인 반복은 소비할 것이 없어 DONE(drained)으로
  끝난다. 사람이 할 일은 **T0-3**(콘솔에서 결제 계정 확인 + 전용 프로젝트) 하나다.
  ⚠️ `PROGRESS_LOG`가 예산(120줄)을 크게 넘었다 — `/tidy-docs`.
## 2026-08-21 — T6-2: 데모는 결과를 박아도 멀쩡히 돈다 — 그래서 출처를 물었다 (gate 120)

- **Status**: `[auto]` T6-2 완료. REQ-803 `TODO`→**`VERIFIED`**. 전부 오프라인·fake 위다.
- **Changed**: `warranty/demo.py` — design 11§2의 다섯 단계(provision·inject·remediate·
  manual·report)를 한 프로세스에서 돌린다. 시계·id·예산·신호가 전부 주입이고(REQ-802),
  ★ **판정·검증·롤백은 실물 코드가 계산한다** — 이 파일이 각본으로 갖는 것은 **신호값 셋**뿐이다.
  조치(`ShiftTrafficExecutor`)는 트래픽을 **실제로** 나쁜 리비전으로 옮기고 롤백이 되돌린다.
  `tests/test_demo.py` 12건이 게이트 안에서 판정한다 — 사람이 출력을 보는 게 아니다(11§1 원칙 1).
- **Verified**: `make check` → **120 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-37·M-38·M-39·M-40·M-41 red 확인** · **M-01~M-41 전체 스윕 41종 전부 red** · 복구 후 120 · 잔여 0.
  ⚠️ **가려진 가드 없음** — 오히려 하중이 늘었다(M-13 12건 · M-19 16건 · M-14 9건 · M-26 10건).
  데모가 루프 **전체를** 한 번 더 돌리기 때문이다. **데모는 가드가 아니지만 가드에 하중을 준다.**
- **⛔ 발견(이번 판에서 가장 큰 것)**: **`verdict`를 박는 변이는 안 죽는다** — 박을 값이 어차피
  맞는 값이라서다. 데모에서 실제로 위험한 방향은 반대다: **숫자를 판정에 맞춰 손보는 것**(M-40).
  재측정값을 손보면 서사는 더 그럴듯해지고 다섯 단계는 여전히 멀쩡히 찍힌다.
  ⇒ 데모에 물어야 할 것은 *"결과가 맞는가"*가 아니라 **"결과가 어디서 왔는가"**다. 그래서
  가드는 판정을 확인하지 않고 **찍힌 숫자로 판정을 다시 계산해서** 맞대 본다.
- **⚠️ 발견**: *"두 번 돌려 같은가"*는 **같은 프로세스 안에서만** 묻는 질문이다. 삽입 순서에
  기댄 렌더는 그 안에서 늘 같은 답을 낸다 — 갈라지는 것은 다음 프로세스다(M-38). 정렬을
  결과로 묻지 않고 **직접** 물어야 했다.
- **⚠️ `wasted_usd`는 0으로 나온다** (T5-2 로그가 예고한 그것 — 비용 경로 REQ-503 미착수).
  손으로 채우지 않았고, **왜 0인지를 출력의 `caveats`에 적었다.** 말 없는 0은 *"낭비가 없었다"*로
  읽힌다. M-41이 그 문장을 지킨다.
- **Blockers**: ⛔ **권한 경계가 `make demo`·`*warranty.demo*`를 deny로 잡는다**
  (`scripts/overnight/overnight-settings.json`) — **오프라인 타깃인데 라이브로 분류돼 있다.**
  경계를 우회하지 않았다. 대신 진입점을 `main()` 직접 호출로 태웠고, 그 대가로 **`__main__`
  두 줄만 게이트 밖에 남았다**. ⇒ 사람이 ① `make demo`를 한 번 눈으로 돌려 보고
  ② 그 deny 항목을 allow로 옮길지 판단할 것(오프라인이므로 옮겨도 안전 조건은 안 깨진다).
  ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를 잠그고 **08-24 중단 기준**의 판정 대상이다.
  ⚠️ **뒤에 그 판단이 났다**(`2c0ee09`): 사람이 `make demo`를 돌렸더니 `__main__` 두 줄이 아니라
  호출 경로 전체가 죽어 있었고(M-46), 경계는 `make demo` allow로 고쳐졌다.
- **Next**: 오프라인 `[auto]` 남은 것은 **T6-3 하나**(상수 한 모듈, REQ-804). 그걸 끝내면
  오프라인 백로그는 마른다 — 그 뒤는 전부 실물 GCP를 기다린다.

## 2026-08-21 — T5-3: 모델 호출도 원장 1행 — 그리고 분모가 다시 흔들렸다 (gate 108)

- **Status**: `[auto]` T5-3 완료. REQ-603 `TODO`→**`VERIFIED`**. 전부 오프라인·fake 포트 위다.
- **Changed**: `ports.ModelPort`(모델 호출이 지나는 **단 하나의 문**, 응답이 `TokenUsage`를
  **함께** 낸다) · `domain/tokens.py`(`TokenUsage`·`Rate`·`TokenPrices`·`ModelReply`) ·
  `usecases/meter.py`의 `MeteredModel` — `ModelPort`를 감싸 `ModelJudge` 자리에 꽂히고,
  **계량은 `_call` 한 곳에만** 있다(design 06§5: 호출부마다 시키면 언젠가 한 곳이 빠진다).
  기록은 `finally`다 — 실패한 호출의 토큰도 이미 나갔을 수 있다(예산 정산과 같은 이유).
  ★ **단가를 코드에 안 박았다.** 이 레포는 실물 모델을 호출해 본 적이 없어서(design 06§2)
  공시 단가를 상수로 박으면 **확인 못 한 숫자가 사실처럼 굳는다**(#1). 단가표는 주입되고,
  표에 없는 모델은 `token_meter`+0원이 아니라 **`none`+사유**로 적는다.
  `LedgerEntry.kind`(`action`/`model_call`)를 더했다 — 장식이 아니라 **리포트가 읽는다**.
  모델 행에 `decision`이 붙으면 생성에서 거부한다(붙으면 G4가 그 거짓말을 초록으로 확인해 준다).
- **Verified**: `make check` → **108 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-33·M-34·M-35·M-36 red 확인** · **M-01~M-36 전체 스윕 36종 전부 red** · 복구 후 108 · 잔여 0.
  ⚠️ **가려진 가드 없음** — 계량은 조치 경로 *바깥*의 층이라 안쪽 변이를 흡수할 자리가 없다.
  M-18·M-27을 흡수했던 예약(REQ-405)과 다른 점이 그것이다: 흡수는 **같은 흐름에 방어를
  겹칠 때** 생긴다.
- **⛔ 발견(이번 판에서 가장 큰 것)**: **T5-2가 못 박은 분모가 다시 흔들렸다.** 회복률의
  분모를 `executed`로 고정했는데(M-30), **원장에 새 종류의 행이 들어오자 그 `executed`의
  의미가 바뀌었다.** 모델 호출 행은 원리상 절대 `improved`가 안 되므로 조치로 세면
  **모델을 부를수록 헤드라인이 나빠진다** — 리포트를 봐서는 안 보인다(M-34).
  ⇒ **정의를 한 번 고정했다고 끝이 아니다. 정의의 입력 집합이 늘면 정의를 다시 물어야 한다.**
- **⚠️ 발견(#9)**: 가드를 `ModelPort`의 메서드 집합(`get_protocol_members`)과 `EntryKind`
  전체에 걸었다 — 포트에 메서드가 늘거나 원장 종류가 늘면 **가드가 자동으로 red다.**
  산문이 *"모든 모델 호출은 포트를 지난다"*라고 말해도 가드가 하나만 태우면 나머지는
  안 물어진 채다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를
  잠그고 **08-24 중단 기준**의 판정 대상이다.
- **Next**: 오프라인 `[auto]` 남은 것은 **둘뿐** — T6-2(`make demo` 결정론, REQ-803) ·
  T6-3(상수 한 모듈, REQ-804). ⚠️ T6-2는 `wasted_usd`가 실물 경로에서 늘 0인 문제
  (T5-2 로그)를 먼저 읽고 들어갈 것. ⚠️ `PROGRESS_LOG`가 예산(120줄)을 넘었다 — `/tidy-docs`.

## 2026-08-21 — T5-2: 헤드라인 숫자를 유도로 냈다 — 깨질 수 있는 건 코드가 아니라 정의다 (gate 96)

- **Status**: `[auto]` T5-2 완료. REQ-508 `TODO`→**`VERIFIED`**. 전부 오프라인·fake 위다.
- **Changed**: `domain/report.py` — `daily_report(entries, day=, agent_id=)`가 원장에서
  `executed·improved·improvement_rate·rolled_back·escalated·unverifiable·manual_required·
  model_decided·wasted_usd`를 **유도한다.** 하나도 저장하지 않는다(G8과 같은 계열).
  `LedgerEntry.escalated`를 더했다 — **`rolled_back`의 부정이 아니다.** 부정으로 세면
  롤백이 필요 없었던(회복된) 조치가 전부 에스컬레이션으로 잡힌다.
  ★ **분모는 `executed`다.** 전체 건수로 나누면 거부·수동 필요가 회복률을 희석해서
  **게이트가 잘 막을수록 성적이 나빠 보이고**, 그건 게이트를 끄라는 압력이 된다.
  실행 0이면 비율은 `0`이 아니라 **`None`이다** — 0은 *"해 봤는데 못 고쳤다"*로 읽히고
  그건 *"할 게 없었다"*와 다른 문장이다(REQ-509 배율과 같은 이유).
  `wasted_usd`는 청구서가 온 건은 실측으로 세되 `assumed`는 **안 덮는다**(I-1) — 읽을 때
  고를 뿐이다. 아직 추정인 건수를 `wasted_assumed_only`로 함께 낸다.
  하루의 경계는 **UTC**로 못 박았고, 시간대 없는 시각은 **거부한다**(REQ-802).
- **Verified**: `make check` → **96 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-30·M-31·M-32 red 확인** · **M-01~M-32 전체 스윕 32종 전부 red** · 복구 후 96 passed · 잔여 0.
  ⚠️ **가려진 가드 없음** — 리포트는 원장을 *읽기만* 하는 층이라 흡수할 자리가 없다.
  오히려 M-13(G8)의 죽은 건수가 늘었다.
- **Verified(정의는 조용히 틀린다)**: M-30(`improved`를 `executed`로 센다)은 **버그처럼 안
  생겼다** — 코드는 돌고 숫자는 더 좋다. §2의 *"다들 `executed`만 세고 성공이라 부른다"*가
  그대로 코드가 된 형태다. M-31·M-32는 **한 건씩만** 죽였다: 회복된 항목과 실패한 항목을
  **한 리포트에 섞어 태우지 않으면 둘 다 초록이다**(M-27·M-14 계열).
- **⚠️ 발견**: 루프가 쓰는 원장 행의 `assumed`는 지금 **항상 0**이다
  (`remediate.py`가 `Method.NONE`·0으로 쓴다). ⇒ 실물 루프만 돌리면 `wasted_usd`는 늘 0이고,
  이 칸은 비용 경로(T3/REQ-503)가 붙기 전까지 **손으로 태운 행에서만 하중을 받는다.**
  숫자 자체는 원장에서 유도되므로 T5-2의 판정에는 영향 없지만, **데모(T6-2)에서 이 칸이
  0으로 나오면 그건 리포트 버그가 아니라 비용 경로가 아직 없는 것이다.**
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를
  잠그고 **08-24 중단 기준**의 판정 대상이다.
- **Next**: 오프라인 `[auto]` 남은 것 — T5-3(모델 호출 원장, REQ-603) · T6-2(T5-2 선행이었으니
  이제 열렸다) · T6-3(상수 한 모듈). ⚠️ T6-2는 `wasted_usd` 0 문제를 먼저 읽고 들어갈 것.

## 2026-08-21 — T4-11b: 예약이 기존 가드 둘을 가렸다 — 방어를 겹치면 증거가 사라진다 (gate 83)

- **Status**: `[auto]` T4-11b 완료. REQ-405 `TODO`→**`VERIFIED`**. 전부 오프라인·fake 위다.
- **Changed**: `BudgetStore.commit`을 **`reserve`→`settle`로 갈랐다**(REQ-405, design 04§3).
  `commit`은 실행 **뒤에** 한 번 부르는 것이라 게이트가 `headroom`을 읽은 시점과 돈이 나가는
  시점 사이가 비어 있었고, 그 창의 두 번째 조치는 **같은 여유를 다시 봤다.** 이제 예약은
  `_execute_and_verify` **한 자리**에서만 잡힌다 — AUTO와 승인이 같은 이음매를 타므로
  승인 경로가 예약을 건너뛸 자리가 없다(T4-11a와 같은 이유). 정산은 `finally`에 있다:
  ⚠️ 안 풀린 예약은 여유를 **조용히 잠그고**, 잠긴 예산은 "예산 없음"과 구분이 안 된다.
  실패한 조치는 **0으로 정산**한다 — API가 실패했으면 쓴 게 없다. `unsettled()`를 지표로 냈다.
  ⚠️ 여기서 "실제 비용"은 **공시 단가**다. 청구서 값은 REQ-506 화해로 따로 오고 예약을
  다시 열지 않는다 — requirements.md의 REQ-405에 그렇게 적어 뒀다.
- **Verified**: `make check` → **83 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-28·M-29 red 확인** · **M-01~M-29 전체 스윕 29종 전부 red** · 복구 후 83 passed · 잔여 0.
- **Verified(재진입이 아니면 안 물린다)**: M-28(예약이 여유를 안 붙잡는다)을 죽인 것은
  **재진입 테스트 하나뿐이다.** 순차로 부르면 `commit`도 같은 결과를 내니까 — 초과는
  판정과 지출 **사이의 창**에서만 생기고, 게이트에는 스레드가 없다(REQ-802).
  ⇒ 그 창을 값으로 태우는 유일한 방법이 `ReentrantExecutor`였다.
- **⛔ 발견(이번 판에서 가장 큰 것)**: **예약을 넣자마자 M-18(G1)과 M-27(승인 재판정)이
  초록으로 돌아섰다.** 코드가 틀려서가 아니다 — 그 둘의 기존 증거가 전부 *예산이 마른*
  케이스였고, 이제 판정 집행을 지워도 **예약이 실행기보다 먼저 막는다.**
  ⇒ **바깥 방어층이 안쪽 층의 변이를 흡수한다.** 안쪽 가드는 그대로인데 증거만 사라지고,
  그 사라짐은 조용하다. 막는 판정을 **여유가 남은 채로** 태우는 케이스 둘(REQ-403 `MANUAL` ·
  REQ-404 대기 중 신호 소실)을 더해 하중을 되돌렸다(둘 다 red 재확인).
  ⚠️ **전체 스윕이 아니었으면 못 봤다.** 새 변이 둘만 보고 넘어갔으면 M-18·M-27은
  기록상 `red 확인`인 채로 실제로는 죽어 있었을 것이다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를
  잠그고 **08-24 중단 기준**의 판정 대상이다.
- **Next**: 오프라인 `[auto]` 남은 것 — T5-2(회복률 리포트, REQ-508) · T5-3(모델 호출 원장) ·
  T6-2(T5-2 선행) · T6-3(상수 한 모듈). ⚠️ T5-2가 다음 순서다.
  그리고 **새 방어층을 넣을 때마다 전체 스윕을 돌 것** — 이번에 그게 가드 둘을 살렸다.

## 2026-08-21 — T4-11a: `APPROVE`는 종착역이었다 — 승인이 들어오는 문을 냈다 (gate 75)

- **Status**: `[auto]` T4-11a 완료. REQ-404 `TODO`→**`VERIFIED`**. 전부 오프라인·fake 위다.
- **Changed**: `Remediator.approve(entry_id, resource, approver)`를 냈다. ★ **승인 시 게이트를
  다시 평가한다**(REQ-404 후반) — 대기하는 동안 예산이 마르거나 계약이 `retired`가 될 수 있다.
  재판정에 쓰는 것은 원래 판정의 `projected_usd`·`destructive`(**같은 조치여야 하니까**)와
  **지금 다시 읽은** 계약·신호·예산이다. 원장에 `Approval(approver, approved_at, reevaluated)`을
  더했고 **원래 `decision`을 덮지 않는다** — 덮으면 "무엇에 동의했는가"가 사라진다.
  `InMemoryLedger.approve`는 `awaiting_approval`인 항목에만 **한 번만** 붙인다(사후 승인 금지).
  ⚠️ AUTO와 승인된 조치가 **같은 경로**(`_execute_and_verify`)를 타게 뽑았다 — 승인 경로를
  따로 배선하면 그 경로만 검증·롤백을 빠뜨리고, 그 누락은 조용하다.
  포트는 `Ledger = LedgerWriter + LedgerReader`로 갈랐다(승인은 **기록을 읽어서** 시작한다).
- **Verified**: `make check` → **75 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-26·M-27 red 확인** · **M-01~M-27 전체 스윕 27종 전부 red** · 복구 후 75 passed · 잔여 0.
- **Verified(죽은 건수의 차이가 위험도다)**: M-26(승인 없이 실행)은 **7건**을 죽였고
  M-27(승인이 게이트를 면제)은 **1건**뿐이다. 재판정을 빼도 *승인이 실제로 붙은 경로 하나*만
  어긋나고 나머지는 전부 초록이다 — **M-14와 같은 계열의 조용한 회귀**다. 승인 뒤 예산이
  마른 케이스를 값으로 태워 두지 않았다면 M-27은 초록이었을 것이다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를
  잠그고 **08-24 중단 기준**의 판정 대상이다.
- **Next**: 오프라인 `[auto]` 남은 것 — T4-11b(예산 예약/정산, REQ-405) · T5-2(회복률 리포트) ·
  T5-3(모델 호출 원장) · T6-2(T5-2 선행) · T6-3(상수 한 모듈).
  ⚠️ T4-11b는 지금 `budgets.commit`이 **실행 후 한 번**이라 동시 초과를 못 막는다 —
  `reserve`→`settle`로 갈라야 하고, 그 예약은 `approve`의 재판정과 같은 자리를 만진다.

## 2026-08-21 — T0-6: 기계가 안 읽는 문자열이 썩어 있었다 — 사람만 속는 자리 넷 (gate 68)

- **Status**: `[auto]` T0-6 완료. 문자열 정리뿐이고 동작은 안 건드렸다. 클라우드는 여전히 막혀 있다(T0-3).
- **Changed**: 넷을 현행으로 맞췄다. ① `mutate.sh` 사용법이 `<M-01|M-02|M-03|M-04|all>`이었다 —
  실제 변이는 **M-25까지**다(`all` 목록은 이미 맞았고 사용법 줄만 안 따라왔다) ⇒ `<M-01..M-25|all>`.
  ② `mutate.sh` 인라인 REQ 주석 넷이 **구 번호**였다 — M-06 `REQ-204`→`REQ-505` ·
  M-07 `REQ-203`→`REQ-504` · M-08 `REQ-201`→`REQ-501` · M-09 `REQ-202`→`REQ-503`
  (권위는 `docs/evidence/mutations.md`의 REQ 칸, 그리고 tasks.md의 T1-1~T1-3와 일치한다).
  ③ `tools/spec_trace.py`의 리포트 헤더가 아직 `fleet-ledger`였다 ⇒ `warranty`.
  ④ `mutations.md` 머리말의 절차 인용이 `specs/fleet-ledger/design/07-verification.md`였다 ⇒
  현행 `specs/warranty/design/09-quality-gate.md` §4(문서 번호까지 바뀌었다 — 07은 지금
  `07-tenant-identity.md`라 경로만 갈아끼우면 **다른 문서를 가리켰을 것**이다).
- **Verified**: `make check` → **68 passed** (2026-08-21 로컬 macOS·py3.13). 변이 기록은
  안 늘렸다 — 주석·사용법 문자열이라 밀 가드가 없다(있다고 적으면 그게 G6가 막는 거짓 주장이다).
- **Verified(왜 가드가 이걸 못 잡았나)**: 넷 다 **기계가 안 읽는 자리**다. `scan_mutation_refs`는
  `mutations.md`의 표만 읽고 `mutate.sh` 주석은 안 읽는다. G6④는 파이썬 도크스트링의 `Spec:`만
  본다 — 셸 주석·마크다운 산문·출력 헤더는 사거리 밖이다. ⚠️ **그래서 이건 재발한다.**
  다음에 이름을 바꾸면 같은 자리가 또 썩는다. 자동화는 T0-6의 범위가 아니라서 안 했다.
- **남긴 것(의도)**: `PROGRESS_LOG`·`DECISIONS`·`spec_trace.py:193` 도크스트링·
  `tests/test_g6_traceability.py`·`mutations.md`의 T0-5 회고에 남은 `fleet-ledger`는
  **이름 변경을 서술하는 역사**다. 고치면 문장이 거짓이 된다.
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체는 그대로 — **전용 GCP 프로젝트**가 T2를
  잠그고 **08-24 중단 기준**의 판정 대상이다.
- **Next**: 오프라인 `[auto]` 남은 것은 T4-11a(승인 집행) · T4-11b(예산 예약) · T5-2(회복률
  리포트) · T5-3(모델 호출 원장) · T6-2(T5-2 선행) · T6-3(상수 한 모듈).
  ⚠️ 그 여섯을 다 해도 **중단 기준의 판정 대상(T2-2)은 안 움직인다.**

## 2026-08-21 — T0-5: 참조를 **양방향으로** 물었다 — 코드→spec 방향이 썩고 있었다 (gate 68)

- **Status**: `[auto]` T0-5 완료. 오프라인 하네스 작업이고 클라우드는 여전히 막혀 있다(T0-3).
- **Changed**: G6에 **④ spec 참조 정합성**을 붙였다(`tools/spec_trace.py`의
  `scan_spec_refs`·`unresolved_spec_path`·`spec_reference_violations`). 코드 도크스트링의
  `Spec:` 블록에서 설계 경로와 인용 REQ를 AST로 읽어 **실재하는지** 묻는다.
  `REQ-201~206` 같은 **범위 표기를 펼친다**(안 펼치면 끝 번호가 검사에서 빠진다).
  판정은 `all_violations` 한 곳 → `make trace`와 테스트가 안 갈라진다.
  dangling 6곳을 고쳤다(`specs/fleet-ledger/...` 5 + `remediate.py` 줄임 표기 1).
  `entry.py`가 인용하던 **REQ-207은 존재하지 않는 요구사항**이었다.
- **Verified**: `make check` → **68 passed** (2026-08-21 로컬 macOS·py3.13).
  **M-23·M-24·M-25 전부 red 확인** · 복구 후 68 passed · 잔여 0 (`docs/evidence/mutations.md`).
  각각 없는 설계 문서 · 정의 없는 REQ · 스캐너 공허 통과를 민다.
- **Verified(가드가 태스크보다 정확했다)**: T0-5는 dangling을 **5곳**으로 적었는데 기계는 **6곳**을
  셌다. `remediate.py`가 `02-verification.md · 03-atomic-rollback.md`처럼 **직전 경로의 형제**를
  줄여 적고 있었다. 해석 규칙을 형제 추론으로 만들면 조용히 틀리므로 **저장소 루트 기준
  전체 경로만** 허용하고 줄임 표기 자체를 위반으로 센다. ⚠️**손으로 센 목록은 가드가 아니다.**
- **Blockers**: 없음(이 항목 한정). ⛔ 레포 전체의 블로커는 그대로 — **전용 GCP 프로젝트**가
  T2를 잠그고 있고 **08-24 중단 기준**의 판정 대상이다.
- **Next**: T0-6(스테일 문자열 — `mutate.sh` 사용법 `M-01~M-04`, `spec_trace.py` 리포트 헤더
  `fleet-ledger`, `mutations.md` 머리말의 `specs/fleet-ledger/design/07-verification.md`).
  ⚠️ 마지막 것은 **마크다운 산문**이라 G6④(파이썬 도크스트링)가 안 잡는다 — T0-6이 가져가야 한다.

## 2026-08-19 — 레포 생성부터 루프 배선까지: spec을 집행 가능하게 만들고, GCP 올인으로 재정의했다 (gate 65)

- **Status**: 저장소를 새로 만들었다(커밋 6). Google All Things Agentic Hackathon 제출물.
  **코드는 아직 클라우드에 안 올라갔다.** 논지의 루프는 fake 위에서 전부 배선·검증됐다.
- **Changed — ① SDD를 집행 가능하게**: `tools/spec_trace.py` + 가드 **G6**가
  요구사항의 **상태 주장을 현실에 맞댄다**(`IMPLEMENTED`면 테스트, `VERIFIED`면 red가 확인된
  변이 기록). 형제 검사 셋(테스트·태스크·설계 귀속)을 함께 묻는다. `scripts/mutate.sh`가
  변이·실행·복구를 한 스크립트에서 한다.
- **Changed — ② GCP 올인 재정의**(`fleet-ledger` → `warranty`): 주제를 비용 장부에서
  **자동화**로 옮겼다. 논지 = *"클라우드 중립성을 포기하면 에이전트가 무엇을 할 수 있게 되는가."*
  Day-1이 **운영 계약**(신호·회복기준·롤백계획·가역성)을 산출하고, Day-2가 조치 후
  **같은 신호를 다시 재고** 안 나아졌으면 **Cloud Run 트래픽 전환으로 되돌린다**.
  판정 게이트 축이 셋(가역성 × **검증 가능성** × 예산) — ⛔**검증할 수 없는 조치는 자동으로 하지 않는다.**
  REQ 36 → **44**, 설계 11편 재작성. 비용은 주제가 아니라 리포트의 한 열로 종속(D9).
- **Changed — ③ 도메인 + 루프**: 계약·검증·판정 게이트·원장 + 포트 9종 + fake 어댑터 +
  `Remediator`. `improved`를 **저장하지 않고 검증 결과에서 유도**한다(G8).
  **남은 것은 어댑터뿐이다** — 실물 Cloud Monitoring·Cloud Run·Vertex.
- **Verified**: `make check` → **65 passed** (2026-08-19 로컬 macOS·py3.13, ruff+mypy+pytest+trace).
  REQ 44종 — VERIFIED 14 · IMPLEMENTED 14 · TODO 16.
  **변이 M-01~M-22 전부 red 확인** · 복구 후 초록 · 백업 대조 잔여 0 (`docs/evidence/mutations.md`).
  **ADK 실물 확인**: `google-adk 2.7.1`을 별도 venv에 설치해 introspect —
  `tools`가 평범한 `Callable`을 받고, `Runner`는 `session_service`가 **필수**
  (`docs/evidence/adk-api-probe-2026-08-19.log`). ⛔**모델 호출은 아직 안 했다.**
- **Verified(가드 자신의 실패 다섯)**: ⚠️추적성 스캐너가 문자열 검색이라 **산문 언급이
  커버리지로 계산**됐다(REQ-802 오탐) → AST로. ⚠️변이 하네스가 출력 문자열로 판정해 **red를
  초록으로 읽었고**, `git status`로 잔여를 물어 무관한 변경을 오인했고, **stale `.pyc`**에
  속았고, 이름을 바꾼 뒤 **없는 파일에** 물었고, 포맷 변경으로 **조용히 무효인 변이**를 냈다
  → 종료 코드·백업 대조·바이트코드 끄기·대상 부재/무변경을 **판정이 아니라 오류**로.
  ⚠️**M-20은 가드가 없어서가 아니라 픽스처가 약해서 초록이었다**(원칙 #8).
- **Blockers**: ⛔**전용 GCP 프로젝트가 없다.** 활성 gcloud 계정이 `yeongsigchoe7@gmail.com`이고
  **Cloud Billing API 미활성**이라 크레딧이 어느 결제 계정에 붙었는지 **읽을 수 없다**(콘솔 확인 필요).
  이것이 T2(Cloud Run 배포) 전체를 잠근다. BQ 결제 내보내기는 **선택**으로 내려 크리티컬 패스에서 빠졌다.
- **Next**: ⛔**T2-2 (Cloud Run 배포)가 08-24 중단 기준의 판정 대상**이다.
  프로젝트가 열리면 T0-3 → T2 → T3. 그전까지 오프라인으로 가능한 것은
  **REQ-508(회복률 리포트) · REQ-404/405(승인·예약)** 셋뿐이다.
