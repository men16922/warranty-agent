# evidence

라이브 검증의 기록. **게이트에 없는 것**(실물 배포·실물 모델 호출·BQ 질의)은 여기 남긴다.

**규칙**
- 파일명에 날짜. 내용에 **명령과 출력 그대로**.
- ⚠️ **"한때 됐다"와 "지금 재현된다"는 다르다.** 재현 안 되면 재현 안 된다고 적는다.
- ⚠️ 게이트 통과 수를 적을 땐 **날짜와 잰 기계**를 함께 적는다.
