# 변이 기록

> **지워 보고 red가 나오지 않은 가드는 가드가 아니다.**
> 요구사항 상태를 `VERIFIED`로 올리려면 여기에 **red 확인** 행이 있어야 하고,
> `tools/spec_trace.py`가 그것을 읽어 집행한다(가드 G6).

**절차** (`specs/warranty/design/09-quality-gate.md` §4) — 전부 `scripts/mutate.sh` 안에서:
① 기준선 → ② 변이 → ③ 전체 스위트가 red인가·**몇 건이 죽었나** → ④ 복구 →
⑤ **초록 복귀 확인** → ⑥ **백업 대조로** 잔여 확인.

⚠️ **문법을 깬 변이는 red로 세지 않는다** — 컴파일 실패는 검증이 아니다.
⚠️ **전체 스위트에 묻는다** — 한 파일에만 물으면 "가드 없음"으로 잘못 읽는다.

---

## 실행 기록 — 2026-08-19 · 로컬 macOS · py3.13 · 기준선 **22 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-01 | — | `requirements.md`의 `상태:` 한 줄 삭제 | 7 errors | **red 확인** |
| M-02 | — | REQ 하나를 `IMPLEMENTED`로 올리고 테스트는 안 만듦 | 2 failed | **red 확인** |
| M-03 | — | 파서를 깨 요구사항 0개를 읽게 함 (공허 통과 방지) | 3 failed | **red 확인** |
| M-04 | — | 정의 없는 `REQ-999`를 `tasks.md`가 가리키게 함 | 2 failed | **red 확인** |
| M-05 | — | **산문 언급만** 있는 테스트로 커버리지가 되는지 | 2 failed | **red 확인** |
| M-06 | REQ-505 | 화해가 `assumed`를 덮게 함 (G2) | 1 failed | **red 확인** |
| M-07 | REQ-504 | `method↔verifiability` 매핑을 깸 (G3) | 2 failed | **red 확인** |
| M-08 | REQ-501 | 같은 id로 덮어쓰기를 허용 (G7) | 1 failed | **red 확인** |
| M-09 | REQ-503 | 수량·단가 키 일치 검사 제거 | 1 failed | **red 확인** |

전부 복구 후 **22 passed**로 복귀, 백업 대조 결과 **잔여 없음**.

## 설정 계층 — 2026-08-19 · 기준선 **29 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-10 | REQ-801 | ADK 프로젝트 불일치 검사 제거 | 1 failed | **red 확인** |
| M-11 | REQ-801 | 어댑터 기본값을 `fake`로 변경 | 1 failed | **red 확인** |
| M-12 | REQ-802 | `environ`을 줘도 `.env`를 함께 읽게 되돌림 | 6 failed | **red 확인** |

⚠️ **M-12의 red는 이 기계에 `.env`가 있고 그 값이 픽스처와 다르기 때문**이다.
`.env`가 없는 기계(CI·새 클론)에서는 **초록일 수 있다** — 즉 이 변이는 하중을 항상
주지 않는다. 그래서 **회귀 자체는 코드 구조로 막았다**(`environ`을 주면 디스크를
아예 안 읽는다). 어디에 물었는지를 적어 두지 않으면 다음 사람이 이 초록을 믿는다.

---

## ⚠️ 하네스 자신이 다섯 번 틀렸다 (2026-08-19)

레퍼런스가 경고한 그대로, **변이 하네스가 먼저 결함을 냈다.** 기록해 둔다 —
이 기록이 없으면 다음 사람이 같은 순서로 밟는다.

1. **판정을 출력 문자열로 했다.** 변이는 실제로 red를 냈는데 `tail -1`이 요약 줄이 아니라
   short summary의 마지막 줄을 집어 **"초록"으로 읽었다.** ⇒ **종료 코드로 판정한다.**
   (곁가지로: `addopts`의 `-q`와 하네스의 `-q`가 겹쳐 `-qq`가 되면서 요약 줄 자체가
   사라져 있었다 — "몇 건이 죽었나"를 못 적고 있었다.)
2. **잔여 확인을 `git status`로 했다.** 커밋 안 된 다른 작업이 **변이 잔여로 오인**됐다.
   ⇒ **백업과 파일 단위로 대조한다.**
3. **stale `.pyc`에 속았다.** 소스는 복구됐는데 `__pycache__`가 남아 테스트가 옛 코드를 보고
   red를 유지했고, 그것이 **"복구 실패"로 읽혔다.** 다음 변이는 기준선이 red라 중단됐다.
   ⇒ `PYTHONDONTWRITEBYTECODE=1` + 복구 시 `__pycache__` 제거.

4. **변이 대상 파일이 없는데 조용히 넘어갔다.** 프로젝트 이름을 바꾸고 이 스크립트의 경로를
   안 고쳤더니, 변이가 **적용되지 않은 채** "가드가 하중을 안 받는다"로 보고됐다.
   ⇒ `backup()`이 파일 부재를 **판정이 아니라 오류**로 낸다.
5. **파일은 있는데 패턴이 안 맞아 변이가 조용히 무효였다.** 설정 변수 접두사를 바꾼 뒤
   패턴을 안 고쳤을 때. 존재 검사로는 못 잡는 별개의 실패 모드다.
   ⇒ 변이 후 **파일이 실제로 바뀌었는지 확인**하고, 안 바뀌었으면 판정을 내지 않는다.

**교훈**: ⑤(초록 복귀)와 ⑥(잔여 대조)이 있었기 때문에 다섯이 전부 드러났다.
**변이가 red를 냈는지만 보는 하네스였다면 다섯 다 통과했을 것이다.**
그리고 ④⑤는 **초록을 "가드 없음"으로 잘못 읽게 만드는** 종류라 가장 위험하다 —
없는 가드를 만들러 가거나, 있는 가드를 지우게 된다.


## 재확인 — 2026-08-19 (GCP 올인 재정의 후) · 기준선 **30 passed**

M-01~M-12 **전부 red 재확인**, 복구 후 30 passed, 잔여 0.
⚠️ 이 재확인 과정에서 하네스 결함 ④⑤가 드러났다(위) — **재정의 전에는 초록이었다.**


## 도메인 — 2026-08-19 (계약·검증·판정 게이트) · 기준선 **51 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-13 | REQ-502 | ★ G8 — `improved`를 검증 없이 참으로 (조용한 성공) | 3 failed | **red 확인** |
| M-14 | REQ-402 | ★ G9 — **검증 가능성 축 제거** (정책 무력화) | 4 failed | **red 확인** |
| M-15 | REQ-102 | 가역인데 롤백 계획 없는 계약을 허용 | 1 failed | **red 확인** |
| M-16 | REQ-205 | 빈 창을 회복으로 판정 (거짓 판정) | 1 failed | **red 확인** |
| M-17 | REQ-406 | 파괴적 조치의 강제 승인 제거 | 1 failed | **red 확인** |

전부 복구 후 **51 passed**, 잔여 0.

⚠️ **M-14가 이 프로젝트에서 가장 중요한 변이다.** 검증 가능성 축을 빼면
*"되돌릴 수 있지만 확인은 못 하는"* 조치가 `AUTO`가 된다 — 그리고 **다른 테스트는 전부
통과한다.** 정책은 별도 가드로 지키지 않으면 조용히 사라진다.


## 루프 배선 — 2026-08-19 (조치→검증→롤백, 전부 fake) · 기준선 **64 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-18 | REQ-403 | ★ G1 — 막는 판정인데도 실행기를 부른다 | 1 failed | **red 확인** |
| M-19 | REQ-401 | ★ G4 — 판정 없는 항목을 만든다 | 4 failed | **red 확인** |
| M-20 | REQ-303 | 트래픽을 다시 안 읽고 **성공했다고 가정** | 1 failed | **red 확인** (아래 ⚠️) |
| M-21 | REQ-304 | 롤백 후 재측정을 건너뛴다 | 1 failed | **red 확인** |
| M-22 | REQ-204 | 명확한 경우에도 모델을 부른다 | 1 failed | **red 확인** |

### ⚠️ M-20은 처음에 **초록이었다** — 픽스처가 가드를 무력화하고 있었다

`FakeRun`이 전환 후 **항상 `{previous: 100}`**을 돌려줬다. 그래서 *"다시 읽는다"*를
*"성공했다고 가정한다"*로 바꿔도 **결과가 같았고**, 가드는 하중을 못 받았다.

이건 원칙 #8이 정확히 경고한 것이다 — **기본값과 같은 값을 고른 픽스처는 가드가 아니다.**

⇒ `FakeRun(honors_shift=False)`를 추가했다: **API는 성공했는데 배분이 안 옮겨진** 경우.
그 경우 `performed=False`이고 `rolled_back`도 False다. 그 테스트를 넣고 나서야 M-20이 red가 됐다.

⚠️ **변이가 초록으로 나왔을 때 "가드가 없다"가 아니라 "픽스처가 약하다"일 수 있다.**
둘을 구분하지 않으면 없는 가드를 만들러 가게 된다.


### ⚠️ M-21은 패턴이 포맷에 흔들렸다

`ruff format`이 여러 줄 표현식을 접자 M-21의 perl 패턴이 안 맞았고, 변이가 **적용되지 않은 채**
초록이 됐다. **무변경 탐지가 그걸 잡았다** — 없었으면 "REQ-304 가드가 없다"로 읽고
있는 가드를 다시 만들러 갔을 것이다.

⇒ **변이 패턴은 포맷에 안 흔들리는 조각을 고른다.** 여러 줄 표현식을 통째로 잡지 않는다.

## 전체 스윕 — 2026-08-19 · 기준선 **65 passed**

M-01~M-22 **22종 전부 red 확인** · 복구 후 65 passed · 잔여 0.

## spec 참조 정합성 (G6④) — 2026-08-21 · 기준선 **68 passed**

T0-5. `Spec:` 도크스트링은 **코드 → 설계** 방향의 참조인데, 그때까지 아무도 안 물었다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-23 | — | `Spec:`가 없는 설계 문서(`design/99-nonexistent.md`)를 가리킨다 | 2 failed | **red 확인** |
| M-24 | — | `Spec:`가 정의 없는 `REQ-999`를 인용한다 | 2 failed | **red 확인** |
| M-25 | — | 스캐너가 `Spec:`를 하나도 못 읽게 한다 (공허 통과 방지) | 2 failed | **red 확인** |

셋 다 복구 후 **68 passed** 복귀 · 백업 대조 잔여 0.
REQ 칸이 `—`인 이유는 M-01~M-05와 같다 — 이 가드는 **spec 기계 자신**을 지키지
특정 요구사항을 구현하지 않는다. 여기에 REQ를 적으면 그 REQ가 변이 증거를 **공짜로** 얻는다.

### ⚠️ 가드를 켜자 태스크가 적어 둔 것보다 한 곳이 더 나왔다

`tasks.md` T0-5는 dangling을 **5곳**으로 적었다(`specs/fleet-ledger/...` 다섯). 실제로는
**6곳**이었다 — `remediate.py`가 `02-verification.md · 03-atomic-rollback.md · 04-decision-gate.md`
처럼 **직전 경로의 형제**를 줄여 적고 있었고, 뒤의 둘은 어떤 규칙으로도 안 풀렸다.

⇒ 해석 규칙을 "직전 경로의 형제"로 만들면 그 규칙이 조용히 틀린다. **경로는 저장소 루트 기준
전체 경로로만 적는다**(`SPEC_PATH_PREFIX`)로 못 박고, 줄임 표기 자체를 위반으로 센다.

⚠️ **손으로 센 목록은 가드가 아니다.** 세어 둔 5와 기계가 센 6의 차이가 가드를 만드는 이유다.

## 승인 집행 (REQ-404) — 2026-08-21 · 기준선 **75 passed**

T4-11a. `APPROVE`는 원래 **아무것도 안 하는 판정**이었다 — 실행기를 안 부르는 것까지는
맞았는데, 그 다음이 없었다. 승인이 들어오는 문이 없으니 `awaiting_approval`은 종착역이었다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-26 | REQ-404 | 승인 대기 항목을 **승인 없이** 실행한다 (승인이 장식이 된다) | 7 failed | **red 확인** |
| M-27 | REQ-404 | ★ 승인이 게이트를 **면제**하게 한다 (재판정 결과를 안 본다) | 1 failed | **red 확인** |

둘 다 복구 후 **75 passed** 복귀 · 백업 대조 잔여 0.

### ⚠️ 둘의 죽은 건수 차이가 그대로 위험도다

**M-26은 7건을 죽였다** — `APPROVE`로 멈추는 경로가 여럿이라(검증 불가 · 비가역 · 파괴적)
이미 여러 테스트가 그 자리를 밟고 있었다. 반면 **M-27은 1건뿐이다.** 재판정을 안 해도
*승인이 실제로 붙은 경로 하나*만 어긋나고 **나머지는 전부 초록이다.**

⇒ 이건 M-14와 같은 계열이다. **승인을 게이트 면제로 만드는 회귀는 거의 아무것도 깨지 않는다.**
승인 뒤에 예산이 마른 케이스를 값으로 태워 두지 않았다면 M-27은 초록이었을 것이고,
그러면 "승인 = 우회로"가 조용히 들어와 있었을 것이다.

## 전체 스윕 — 2026-08-21 · 기준선 **75 passed**

M-01~M-27 **27종 전부 red 확인** · 복구 후 75 passed · 잔여 0.
⚠️ `remediate.py`가 이번에 갈라졌는데(`_execute_and_verify` 추출) **M-19~M-22의 패턴은
그대로 물렸다** — 메서드 본문의 들여쓰기가 안 바뀌는 조각을 고른 덕이다(M-21의 교훈).

## 예산 예약 (REQ-405) — 2026-08-21 · 기준선 **83 passed**

T4-11b. `commit`은 **실행 뒤에** 한 번 부르는 것이었다. 게이트가 `headroom`을 읽은 시점과
돈이 나가는 시점 사이가 비어 있었고, 그 창에 들어온 두 번째 조치는 **같은 여유를 다시 봤다.**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-28 | REQ-405 | 예약이 여유를 **안 붙잡는다** (예약이 `commit`의 다른 이름이 된다) | 1 failed | **red 확인** |
| M-29 | REQ-405 | 정산을 건너뛴다 (예약이 안 풀려 예산이 **조용히 잠긴다**) | 4 failed | **red 확인** |

둘 다 복구 후 **83 passed** 복귀 · 백업 대조 잔여 0.

### ⚠️ M-28을 죽인 것은 재진입 테스트 **하나뿐이다**

예약이 여유를 안 붙잡아도 *순차* 테스트는 전부 초록이다 — 순차라면 `commit`도 같은 결과를
낸다. 초과는 **판정과 지출 사이의 창**에서만 생기고, 게이트에는 스레드가 없다(REQ-802).
⇒ 그 창을 값으로 태우는 유일한 방법이 **재진입**(`ReentrantExecutor`)이었다.
재진입 케이스를 안 썼으면 M-28은 초록이고, 예약은 이름만 예약이었을 것이다.
M-27·M-14와 같은 계열이다.

### ⛔ 예약이 **기존 가드 둘을 가렸다** — 이번 판의 가장 큰 발견

REQ-405를 넣자마자 **M-18(G1: 막는 판정인데 실행기를 부른다)과 M-27(승인이 게이트를 면제)이
초록으로 돌아섰다.** 코드가 틀려서가 아니라, 그 둘의 기존 증거가 전부 **예산이 마른 케이스**로
쓰여 있었기 때문이다 — 판정 집행을 지워도 이제는 **예약이 실행기보다 먼저 막는다.**

```
   기존 증거:  DENY(여유 없음)      → 판정 집행을 지워도 → 예약이 막는다 → 초록
   되돌린 증거: MANUAL(여유 남음)   → 판정 집행을 지우면 → 실행된다     → red
```

⇒ 방어를 겹쳐 놓으면 **바깥 층이 안쪽 층의 변이를 흡수한다.** 안쪽 가드는 그대로인데
증거만 사라지고, 그 사라짐은 조용하다. 그래서 막는 판정을 **여유가 남은 채로** 태우는
케이스 둘(`REQ-403` MANUAL · `REQ-404` 대기 중 신호 소실)을 더해 하중을 되돌렸다.

⚠️ **전체 스윕이 아니었으면 못 봤다.** 새 변이 둘만 확인하고 넘어갔으면 M-18·M-27은
기록상 `red 확인`인 채로 실제로는 죽어 있었다.

## 전체 스윕 — 2026-08-21 · 기준선 **83 passed**

M-01~M-29 **29종 전부 red 확인** · 복구 후 83 passed · 잔여 0.

## 회복률 리포트 (REQ-508) — 2026-08-21 · 기준선 **96 passed**

T5-2. 헤드라인 숫자를 원장에서 **유도한다**. 여기서 깨질 수 있는 것은 코드가 아니라
**숫자의 정의**다 — 셋 다 리포트를 봐서는 안 보이고, **전부 성적이 좋아 보이는 방향으로만**
틀린다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-30 | REQ-508 | 회복률의 분자를 `executed`로 센다 (★ 헤드라인이 늘 100%가 된다) | 3 failed | **red 확인** |
| M-31 | REQ-508 | 낭비 비용이 회복 여부를 안 본다 ("쓴 돈 전부"가 되어 아무것도 안 말한다) | 1 failed | **red 확인** |
| M-32 | REQ-305 | 에스컬레이션을 `rolled_back`의 **부정**으로 센다 | 1 failed | **red 확인** |

셋 다 복구 후 **96 passed** 복귀 · 백업 대조 잔여 0.

### ⚠️ M-30이 이 프로젝트의 논지를 그대로 태운다

`improved`를 `executed`로 세는 변이는 **버그처럼 안 생겼다.** 코드는 돌고, 리포트는
나오고, 숫자는 더 좋다. 그게 §2에서 말한 *"다들 `executed`만 세고 그것을 성공이라
부른다"*의 코드판이다. 이 변이가 red가 아니면 리포트는 다른 도구와 같은 것을 낸다.

### ⚠️ M-31·M-32는 **한 건씩만** 죽였다 — M-27·M-14와 같은 계열

낭비 비용도 에스컬레이션도, 그 정의를 태운 케이스가 **각각 하나뿐**이다.
`wasted`를 `executed` 전부로 바꿔도 *모두 회복 실패인* 픽스처에서는 값이 같고,
`escalated`를 `rolled_back`의 부정으로 바꿔도 *롤백이 붙은* 항목만 보면 결과가 같다.
⇒ **회복된 항목과 실패한 항목을 한 리포트에 섞어 태우지 않으면 둘 다 초록이다.**

### ⛔ M-32는 REQ-305를 `VERIFIED`로 만들지 않는다

M-32가 죽인 것은 `LedgerEntry.escalated`의 **정의**뿐이다. REQ-305의 나머지 절반
(*"에스컬레이션하고 **더 조치하지 않는다**"*)은 이 변이가 안 건드린다.
⇒ REQ-305는 `IMPLEMENTED`로 둔다. 변이 기록이 생겼다고 상태를 올리면 **그 기록이
증명하지 않는 것까지 주장하게 된다** — G6이 막는 것과 정확히 반대 방향의 거짓이다.

## 전체 스윕 — 2026-08-21 · 기준선 **96 passed**

M-01~M-32 **32종 전부 red 확인** · 복구 후 96 passed · 잔여 0.
⚠️ **가려진 가드 없음** — 리포트는 원장을 *읽기만* 하는 층이라 안쪽 변이를 흡수할 자리가
없다. 오히려 M-13(G8 `improved` 유도)의 죽은 건수가 늘었다(리포트 테스트가 하중을 더했다).

## 모델 호출 계량 (REQ-603) — 2026-08-21 · 기준선 **108 passed**

T5-3. **호출 1건 = 원장 1행.** 여기서 깨질 수 있는 것은 "기록하는가"만이 아니다 —
기록한 행이 **무엇인 척하는가**가 같이 깨지고, 그쪽이 훨씬 조용하다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-33 | REQ-603 | 모델 호출이 원장에 안 남는다 (호출은 있었는데 지출이 없다) | 8 failed | **red 확인** |
| M-34 | REQ-508 | 리포트가 모델 호출 행을 **조치로 센다** (★ 모델을 쓸수록 회복률이 나빠진다) | 1 failed | **red 확인** |
| M-35 | REQ-504 | 단가를 모르는 호출을 `token_meter` + 0원으로 적는다 | 1 failed | **red 확인** |
| M-36 | REQ-603 | 호출이 **예외로 끝나면** 행을 안 남긴다 | 1 failed | **red 확인** |

넷 다 복구 후 **108 passed** 복귀 · 백업 대조 잔여 0.

### ⚠️ M-34가 이번의 조용한 실패다 — 분모는 다시 흔들린다

T5-2에서 분모를 `executed`로 못 박았는데(M-30), **원장에 새 종류의 행이 들어오자 그
`executed`의 의미가 다시 흔들렸다.** 모델 호출 행은 원리상 절대 `improved`가 되지 않으므로,
조치로 세면 **모델을 부를수록 헤드라인이 나빠진다.** 리포트를 봐서는 안 보인다 —
숫자는 나오고, 코드는 돌고, 그냥 성적이 낮을 뿐이다.
⇒ 정의를 한 번 고정했다고 끝이 아니다. **정의의 입력 집합이 늘면 정의를 다시 물어야 한다.**

### ⚠️ M-35는 "모른다"를 "공짜"로 바꾼다

`token_meter`라고 적고 0을 넣은 행은 화해기에게 *"계량됐고 0원이었다"*로 읽힌다.
그건 *"단가를 모른다"*와 다른 문장이고, 둘을 같은 값으로 적으면 그 구분은 **영원히**
사라진다 (docs/PRINCIPLES.md #2). 그래서 단가 미상은 `none` + 사유다.

### ⚠️ M-36 — 실패한 호출의 토큰은 어디로 가는가

성공 경로에만 기록하면 **실패했지만 이미 청구된** 호출이 원장에 없다.
없는 지출은 리포트에서 0으로 보이고, 0은 "안 썼다"로 읽힌다. 예산 정산을 `finally`에
둔 것과 정확히 같은 이유다 (M-29 계열).

### ⛔ M-33이 REQ-601을 `VERIFIED`로 만들지 않는다

넷 다 **fake 모델 포트 위**에서 죽었다. 이 변이들이 증명하는 것은 *"우리 원장이 모델
호출을 빠짐없이 센다"*이지 *"그 인터페이스가 존재한다"*가 아니다 (docs/PRINCIPLES.md #3).
REQ-601·602는 그대로 `TODO`다.

## 전체 스윕 — 2026-08-21 · 기준선 **108 passed**

M-01~M-36 **36종 전부 red 확인** · 복구 후 108 passed · 잔여 0.
⚠️ **가려진 가드 없음.** 계량은 조치 경로 **바깥**에 있는 층이라 안쪽 변이를 흡수할 자리가
없다 — M-18·M-27을 흡수했던 예산 예약(REQ-405)과 다른 점이 그것이다. 흡수는 **같은 흐름에
방어를 겹칠 때** 생긴다.

## `make demo` 결정론 (REQ-803) — 2026-08-21 · 기준선 **120 passed**

T6-2. 데모에서 깨질 수 있는 것은 "도는가"가 아니다. **매번 같은가**, 그리고 **화면의 숫자가
계산된 것인가**다. 둘 다 출력을 봐서는 안 보인다 — 데모는 어느 쪽이든 멀쩡하게 돈다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-37 | REQ-803 | 데모가 **살아 있는 시계**를 쓴다 (재현이 아니라 실행이 된다) | 2 failed | **red 확인** |
| M-38 | REQ-803 | 렌더가 **사전 순서**에 기댄다 (프로세스마다 다른 문자열) | 1 failed | **red 확인** |
| M-39 | REQ-303 | 데모의 조치가 트래픽을 **안 옮긴다** (롤백의 배분 재확인이 공허해진다) | 1 failed | **red 확인** |
| M-40 | REQ-803 | 화면의 재측정값을 **보기 좋게 손본다** (판정과 숫자가 어긋난다) | 1 failed | **red 확인** |
| M-41 | REQ-803 | 데모가 **증명하지 않는 것**을 말하지 않는다 | 2 failed | **red 확인** |

다섯 다 복구 후 **120 passed** 복귀 · 백업 대조 잔여 0.

### ⚠️ M-38 — 한 프로세스에서 두 번 돌려 보는 것으로는 안 잡힌다

"두 번 돌려 같은가"는 **같은 프로세스 안에서만** 묻는 질문이다. 삽입 순서에 기댄 렌더는
그 안에서 늘 같은 답을 낸다 — 갈라지는 것은 **다음 프로세스**다. 그래서 정렬을 결과로 묻지
않고 **직접** 묻는다. ⛔ 원래 형태는 `python -m warranty.demo`를 두 번 띄워 stdout을 비교하는
것이었고 그쪽이 더 강하다. 무인 루프의 권한 경계가 그 호출을 막아서(`overnight-settings.json`이
`make demo`·`*warranty.demo*`를 deny로 잡는다 — 오프라인 타깃인데 라이브로 분류돼 있다)
진입점은 `main()` 직접 호출로 태웠다. **경계를 우회하지 않았고, 그 대가로 `__main__` 두 줄이
게이트 밖에 남았다.**

### ⚠️ M-40이 이번의 조용한 실패다 — 데모는 결과를 박아도 돈다

`verdict`를 박는 변이는 **안 죽는다.** 박아 넣을 값이 어차피 맞는 값이기 때문이다. 실제로
위험한 것은 그 반대 방향이다 — **숫자를 판정에 맞춰 손보는 것.** 재측정값을 120ms로 바꾸면
서사는 더 그럴듯해지고("나아졌어야 했는데"), 출력은 여전히 다섯 단계를 멀쩡히 찍는다.
그래서 가드는 판정을 확인하지 않고 **찍힌 숫자로 판정을 다시 계산해서** 맞대 본다.
⇒ 데모에서 물어야 할 것은 *"결과가 맞는가"*가 아니라 **"결과가 어디서 왔는가"**다.

### ⚠️ M-39 — 픽스처가 완벽하면 롤백의 증명이 사라진다

롤백은 트래픽을 옮기고 **다시 읽어** 100%인지 확인한다(REQ-303). 그런데 조치가 아무것도
안 옮기면 트래픽은 **처음부터 롤백 목적지에 있다** — 안 옮겨도 100%고, 재확인은 초록이다.
그 초록은 롤백에 대한 판정이 아니다 (docs/PRINCIPLES.md #8). 그래서 데모의 실행기는
트래픽을 진짜로 나쁜 리비전으로 옮긴다.

### ⛔ M-37~M-41은 REQ-601·602를 `VERIFIED`로 만들지 않는다

다섯 다 **fake 어댑터 위**에서 죽었다. 증명된 것은 *"이 서사가 매번 같다"*이지
*"실물에서 이 서사가 일어난다"*가 아니다 (docs/PRINCIPLES.md #3).
데모 출력의 `caveats`가 그것을 **화면에도** 적는다 — M-41이 지키는 것이 그것이다.

## 전체 스윕 — 2026-08-21 · 기준선 **120 passed**

M-01~M-41 **41종 전부 red 확인** · 복구 후 120 passed · 잔여 0.
⚠️ **가려진 가드 없음.** 오히려 하중이 늘었다 — 데모가 루프 **전체를** 한 번 더 돌리므로
M-13(G8 `improved` 유도) 12건 · M-19(G4 판정 없는 항목) 16건 · M-14(G9) 9건 ·
M-26(REQ-404) 10건으로 죽는 테스트가 늘었다. **데모는 가드가 아니지만 가드에 하중을 준다.**

## 대기·창 상수 한 모듈 (REQ-804) — 2026-08-21 · 기준선 **124 passed**

T6-3. *"상수는 한 곳에 둔다"*는 **관례로는 안 지켜진다.** 깨지는 순서는 늘 같다 — 재촬영
전에 타이머를 줄이고, 호출부 하나에 박아 둔 숫자를 못 본다. 그 자리는 예전 값으로 계속 돌고
**게이트는 초록이다.** 그래서 물어야 할 것은 *"한 곳에 있는가"*가 아니라
**"다른 곳에 없는가"**다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-42 | REQ-804 | 대기 상수를 **두 번째 자리**에 다시 정의한다 | 1 failed | **red 확인** |
| M-43 | REQ-804 | 설계가 선언한 손잡이(`WARMUP_REQUESTS`)를 코드에서 뺀다 | 1 failed | **red 확인** |
| M-44 | REQ-206 | 호출부에 대기 초를 **박는다** (`clock.sleep(45)`) | 1 failed | **red 확인** |
| M-45 | REQ-804 | 스캔 경로를 틀리게 한다 (0개를 훑고 초록) | 3 failed | **red 확인** |

넷 다 복구 후 **124 passed** 복귀 · 백업 대조 잔여 0.

### ⚠️ M-44가 이번의 조용한 실패다 — 기존 테스트는 이것을 **못 잡는다**

`test_req_206_verification_delay_is_a_named_constant_used_once`가 이미 있었다. 그런데 그것이
묻는 것은 `set(clock.slept) == {VERIFY_DELAY_S}`다 — **호출부에 45를 박아도 잔 시간은 여전히
45라서 초록이다.** 이름을 쓰라고 요구한 것처럼 보이지만 실제로 요구한 것은 **값의 일치**였고,
값은 아직 안 바뀌었으니 통과한다. 재촬영 때 tunables를 20으로 줄이는 순간에야 갈라진다.
⇒ **이름을 쓰는지 물으려면 값이 아니라 구문을 봐야 한다.** 그래서 가드는 실행 결과가 아니라
`ast`로 호출부를 읽는다. 두 자리 중 **한 자리만** 박은 것이 M-44다 — 실제로 놓치는 모양이 그것이다.

### ⚠️ M-43 — 설계가 선언한 손잡이와 코드가 갈라진다

`WARMUP_REQUESTS`는 **아직 읽는 코드가 없다**(워밍은 T8-3 촬영 절차이지 코드 경로가 아니다).
읽는 데가 없는 값은 조용히 사라지기 쉽고, 사라진 줄 모른 채 촬영 당일에 *"그 손잡이 어디 있죠"*가
된다. 그래서 가드는 design 11§5의 `python` 코드 블록이 **선언한 이름 집합**과 모듈이 정의한 집합을
맞댄다(G6④와 같은 계열 — 설계→코드 방향).
⚠️ **값은 안 묶었다.** design 11§2가 *"타이머를 영상 길이에 맞춘다"*라고 말하기 때문이다.
값까지 집행하면 조정할 때마다 red고, **그런 가드는 결국 꺼진다.** 집행할 수 있는 것은
*"어떤 손잡이가 있는가"*이지 *"손잡이가 몇 도인가"*가 아니다.

### ⚠️ M-45 — 셋이 죽는 것이 정상이다

경로 하나를 틀리면 공허 통과 방지 한 건만 죽는 게 아니라 **그 경로에서 유도되는 검사 전부**가
죽는다(산재 검사의 하한, 설계↔코드 집합). 한 건만 죽었다면 오히려 나머지가 경로에 안 걸려
있다는 뜻이다.

### ⛔ M-42~M-45는 `[auto]`가 검증할 수 있는 마지막 종류다

넷 다 **정적·오프라인**이다. 파일을 읽고 `ast`로 묻는다 — 실물 클라우드도, 사람 눈도 필요
없다. 남은 REQ 대부분이 이것과 반대편에 있다(REQ-601·602는 실물 호출이 있어야 판정된다).

## 전체 스윕 — 2026-08-21 · 기준선 **124 passed**

M-01~M-45 **45종 전부 red 확인** · 복구 후 124 passed · 잔여 0.
⚠️ **가려진 가드 없음** — 하중 분포도 그대로다(M-13 12건 · M-19 16건 · M-14 9건 · M-26 10건).
새 가드는 소스를 **텍스트로** 읽는 층이라 조치 경로 안쪽 변이를 흡수할 자리가 없다.
흡수는 **같은 흐름에 방어를 겹칠 때** 생긴다(예약이 M-18·M-27을 가렸던 것처럼).

---

## 진입점 (REQ-803) — 2026-08-21 · 로컬 macOS · py3.13 · 기준선 **125 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-46 | REQ-803 | Makefile의 `demo` 레시피에서 `PYTHONPATH=src`를 뺀다 | 1 failed | **red 확인** |

⛔ **이 자리는 실제로 깨져 있었다.** `demo.main()`을 직접 부르는 테스트 셋(결정론·출처·caveats)이
전부 초록인데 `make demo`는 `ModuleNotFoundError`로 죽어 있었다 — pytest는
`pythonpath = ["src", "."]`를 주입하지만 `python -m`은 아무도 안 주입한다.
**진입점을 그 진입점으로 태우지 않으면 그 진입점은 검증되지 않는다.**

⚠️ 가드가 `PYTHONPATH`를 스스로 세우면 안 된다. 세우면 Makefile이 그것을 빠뜨려도 초록이다 —
물어야 할 것은 **선언된 명령**이지 테스트가 고쳐 놓은 환경이 아니다. 그래서 `make demo`를 그대로 태운다.

⚠️ 무인 루프가 이 자리를 못 봤다. 권한 경계가 `make demo`를 deny로 잡고 있었고,
루프는 **우회하지 않고 구멍을 적어 두었다**(`0a3387c`: "`__main__` 두 줄만 게이트 밖에 남았다 —
사람이 한 번 눈으로 돌려 볼 것"). 사람이 그 한 번을 돌렸더니 두 줄이 아니라 호출 경로 전체였다.
⇒ 경계를 고쳤다(`make demo` allow — 오프라인·결정론이고 게이트가 그것을 집행한다).

## 전체 스윕 — 2026-08-21 · 기준선 **125 passed**

M-01~M-46 **46종 전부 red 확인** · 복구 후 125 passed · 잔여 0 · **가려진 가드 없음**.

---

## 기록 신선도 (T0-8) — 2026-08-21 · 로컬 macOS · py3.13 · 기준선 **129 passed**

이 문서의 맨 아래 「전체 스윕」 절은 **현재에 대한 주장**이다 — *"지금 이 스위트에서 M-01~M-NN이
전부 red다."* 나머지 절은 그때 참이었던 역사지만 그 한 절만은 **오늘 참이어야 한다.**
그리고 그 문장은 **스위트가 바뀌는 순간 조용히 거짓이 된다.**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-47 | — | 최신 스윕의 기준선을 어긋나게 한다 (기록이 **옛 스위트**를 보고 적힌 상태) | 1 failed | **red 확인** |
| M-48 | — | 가드가 스윕 절을 하나도 못 찾게 한다 (0개를 읽고 초록) | 3 failed | **red 확인** |
| M-49 | — | `all` 목록에서 마지막 변이를 뺀다 (스윕이 **조용히 덜 돈다**) | 1 failed | **red 확인** |

셋 다 복구 후 **129 passed** 복귀 · 백업 대조 잔여 0.
REQ 칸이 `—`인 이유는 M-01~M-05·M-23~M-25와 같다 — 이 가드는 **증거 기계 자신**을 지키지
특정 요구사항을 구현하지 않는다. 여기에 REQ를 적으면 그 REQ가 변이 증거를 **공짜로** 얻는다.

### ⛔ 이 가드가 잡으려는 것은 이미 한 번 일어났다

예산 예약(REQ-405)을 넣자 **M-18·M-27이 초록으로 돌아섰다**(`5a049f1`). 바깥 방어층이
안쪽 층의 변이를 흡수했고, 그 순간부터 이 문서의 *"전부 red 확인"*은 **거짓**이었다.
게이트는 아무 말도 안 했다 — 사람이 전체 스윕을 다시 돌려 보고서야 알았다.
⇒ 게이트가 오프라인에서 물을 수 있는 것은 *"변이가 지금도 red인가"*가 아니다(스윕은
변이당 스위트를 세 번 돌린다). 물을 수 있는 것은 **"기록이 본 세계와 지금 세계가 같은가"**다.
그래서 red일 때 고칠 것은 **숫자가 아니라 스윕**이다.

### ⚠️ M-48이 셋을 죽이는 것이 정상이다 — M-45와 같은 계열

표제 하나를 틀리면 공허 통과 방지 한 건만 죽는 게 아니라 **그 절에서 유도되는 검사 전부**가
죽는다(기준선 대조 · 변이 집합 대조). 한 건만 죽었다면 나머지가 그 경로에 안 걸려 있다는 뜻이다.

### ⚠️ 스위트 크기는 pytest에게 묻는다 — 두 번째 계산기를 만들지 않았다

`def test_`를 세는 계산기를 따로 만들면 파라미터화(지금 두 곳)·클래스에서 갈라지고, 그러면
**가드는 자기가 만든 숫자만 지킨다.** 기록이 적는 숫자는 pytest가 센 숫자이므로 묻는 쪽도
pytest여야 한다(`--collect-only`). ⚠️ 그때 `-q`를 또 주면 `addopts`의 `-q`와 겹쳐 `-qq`가 되고
**요약 줄 자체가 사라진다** — 하네스 자신의 첫 번째 결함이 정확히 그것이었다(위 §「다섯 번 틀렸다」).

### ⛔ 이 가드가 증명하지 않는 것

*"M-01~M-49가 지금 전부 red다"*를 증명하지 않는다. 증명하는 것은 *"그 주장이 적힌 뒤로
스위트와 하네스가 안 바뀌었다"*뿐이다 (docs/PRINCIPLES.md #3). 바뀌면 red가 되고, 그때
답은 `make mutate`를 다시 돌리는 것이다. **신선도는 참임의 대리지 참임이 아니다.**

## 전체 스윕 — 2026-08-21 · 기준선 **129 passed**

M-01~M-49 **49종 전부 red 확인** · 복구 후 129 passed · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 새 가드가 M-05에 하중을 더했다 (2 → 3)

M-05는 테스트 파일에 한 건을 **덧붙이는** 변이다 — 그래서 스위트가 130이 되고,
신선도 가드가 *"기록은 129를 보고 적혔다"*로 red를 낸다. 의도한 부작용이다:
**스위트 크기를 바꾸는 변이는 이제 신선도에도 걸린다.**

### ⛔ 하중 분포를 손으로 옮겨 적던 자리가 이미 썩어 있었다

이전 절들은 분포를 *"M-13 12 · M-19 16 · M-14 9 · M-26 10"*으로 적어 왔다. 이번에 실제로
센 값은 **M-13 12 · M-14 9 · M-26 10**은 그대로인데 **M-19는 `17 failed + 10 errors`**다.
그 `16`은 기준선 **120**일 때 센 숫자이고, 124·125 스윕은 분포를 다시 세지 않고
*"그대로"*라고만 적었다. 언제 갈라졌는지는 이 로그로 못 되짚는다.
⇒ **T0-8이 고친 병이 한 층 위에 그대로 있다** — 분포는 여전히 사람이 옮겨 적는 숫자다.
이 가드는 *기준선*만 집행한다. 분포는 집행 대상이 아니다(그러려면 스윕을 게이트에 넣어야 하고,
그건 오프라인 게이트가 감당할 수 있는 것이 아니다). **여기 적힌 분포는 주장이 아니라 관측이다.**

---

## 값-대-구문 착각 전수 점검 (T0-10) — 2026-08-21 · 로컬 macOS · py3.13 · 기준선 **130 passed**

M-44가 남긴 질문을 스위트 전체에 되물었다: **이름을 요구하는 것처럼 보이는데 실제로는 값만
묻는 단언이 또 어디 있는가.** 이 착각은 두 얼굴로 나온다 — ① 코드가 *이름을 쓰는지* 물어야
하는데 *값이 맞는지* 묻는 것, ② 설계가 *이름 붙인 집합*과 맞대야 하는데 **이 파일에 베껴 둔
사본**과 맞대는 것. 둘 다 지금은 초록이고, **갈라지는 순간에만** 틀린다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-50 | REQ-206 | 대기 초를 **함수 안 지역 이름**에 담아 쓴다 (`delay_s = 45`) | 1 failed | **red 확인** |
| M-51 | REQ-508 | 설계가 리포트 칸의 **이름을 바꾼다** (`model_decided` → `…_count`) | 1 failed | **red 확인** |
| M-52 | REQ-803 | 설계에서 데모 단계 **하나를 없앤다** (④ MANUAL) | 1 failed | **red 확인** |

셋 다 복구 후 **130 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ 세 자리 다 **이미 초록으로 통과하고 있었다**

셋은 T0-10 전에는 **아무 테스트도 안 죽였다.** 그런데 셋 다 이름이 *"설계가 이름 붙인 것"*을
지킨다고 말하는 자리였다 — `..._is_a_named_constant_used_once` ·
`..._every_column_the_design_names` · `..._the_ones_the_design_names`.
**이름이 약속한 것과 단언이 한 것이 달랐다.**

### ⚠️ M-50 — 리터럴을 막으면 이름으로 우회한다

M-44는 `sleep(45)`를 박는 변이였고 가드 ②(리터럴)가 그것을 잡는다. M-50은 같은 45를
`delay_s`라는 **지역 이름**에 담는다. 최상위 상수가 아니라 ①에 안 걸리고, 리터럴이 아니라
②에도 안 걸린다. ⇒ 물어야 할 것은 *"숫자가 아닌가"*가 아니라 **"어디서 온 이름인가"**다.
그래서 ④는 인자가 `warranty.tunables`에서 **들여온 이름**인지를 `ast`로 묻는다.

### ⚠️ M-51·M-52 — 손으로 베낀 목록은 설계→코드 방향을 안 본다

`DESIGN_COLUMNS`·`DESIGN_STEPS`는 주석에 *"design 05§5가 이름 붙인 칸"*·*"design 11§2가
이름 붙인 다섯 단계"*라고 적혀 있었지만 **설계를 안 읽었다.** 설계가 칸 이름을 바꾸거나
단계를 빼도 사본은 안 따라오고 게이트는 초록이다. T0-5(G6④)가 *"손 목록은 가드가 아니다"*로
같은 것을 배운 자리다. ⇒ 둘 다 이제 설계 문서의 블록을 **파싱해서** 읽는다.

### ⚠️ M-52가 이름의 **정확한 일치**를 요구하지 않는 이유

design 11§2는 ③을 `remediate #1`, ④를 `remediate #2 … ★ MANUAL`로 부르고 코드는
`remediate`·`manual`로 부른다. **설계의 이름은 서사의 이름이고 코드의 이름은 식별자다.**
정확한 일치를 요구하면 둘 중 하나를 상대에 맞춰 고쳐야 하고, 그건 이 태스크의 범위가 아니라
설계 변경이다. 그래서 집행하는 것은 **개수·순서·겹치지 않음**과 *"n번째 이름이 n번째 줄에
나오는가"*다. ⚠️ 이 완화는 공짜가 아니다 — ④를 `remediate`로 **改名**하면 여전히 ④ 줄에
포함되므로, 그 경로는 이름 겹침(distinct) 검사가 대신 막는다.

### ⛔ 남긴 것 — 승격하지 않은 자리

`test_req_402_every_cell_of_the_matrix_is_reached_by_value`·`d.rule == "reversible but not
verifiable"`처럼 **값을 묻는다고 이름에 적어 둔** 단언은 그대로 뒀다. 그것들은 값이 답인
질문이고, 착각은 *"이름이 약속한 것 ≠ 단언이 한 것"*일 때만 성립한다.

## 전체 스윕 — 2026-08-21 · 기준선 **130 passed**

M-01~M-52 **52종 전부 red 확인** · 복구 후 130 passed · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 새 가드가 M-44·M-45의 하중을 늘렸다 (1→2 · 3→4)

이번에 실제로 센 분포: **M-13 12 · M-14 9 · M-19 `17 failed + 10 errors` · M-26 10**
(직전 스윕과 같다) · **M-05 3** · **M-42 1** · **M-44 2**(1→2) · **M-45 4**(3→4).
④는 리터럴도 *"tunables에서 온 이름이 아닌 것"*으로 잡으므로 M-44에 겹쳐 걸리고,
스캔 경로가 틀리면(M-45) ④도 함께 죽는다 — **M-45가 셋이 아니라 넷을 죽이는 것이 정상이다.**
⚠️ 이 분포는 주장이 아니라 관측이다(직전 절과 같은 규칙). 게이트는 기준선만 집행한다.

---

## 계약은 선언이 아니라 산출이다 (T3-2 · REQ-103) — 2026-08-21 · 로컬 macOS · py3.13 · 기준선 **141 passed**

계약의 네 자리에 **맞는 값이 들어 있는가**는 손으로 적어도 통과한다. 물어야 할 것은
**어디서 온 값인가**다(T0-10과 같은 계열). 그래서 이 넷은 값이 아니라 **출처**를 깬다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-53 | REQ-103 | `resource_filter`를 응답의 이름이 아니라 **박은 문자열**로 | 2 failed | **red 확인** |
| M-54 | REQ-103 | 유도되는 자리를 **인자로 다시 연다**(`previous_revision=`) | 1 failed | **red 확인** |
| M-55 | REQ-103 | 설계(01§3)의 유도 표에서 **한 행을 지운다** | 1 failed | **red 확인** |
| M-56 | REQ-103 | 가역성을 **리소스 타입만 보고** 정한다(돌아갈 자리 없이 가역) | 1 failed | **red 확인** |

### ⛔ M-54는 **값을 하나도 안 바꾼다**

`previous_revision=`을 인자로 열어도 유도 경로는 그대로 돌고 계약에 실리는 값도 그대로다.
값만 묻는 테스트는 전부 초록이다 — 그런데 그 순간부터 호출부가 **손으로 적은 리비전**을
계약에 실을 수 있고, 그 계약은 리소스와 함께 늙지 않는다. ⇒ *"유도된다"*를 지키려면
**시그니처를 구문으로** 물어야 한다(`inspect.signature`).

### ⚠️ M-55 — 금지 집합은 설계에서 온다

인자 금지 목록을 테스트에 베껴 두면 설계가 유도 자리를 늘려도 사본은 안 는다(M-51·M-52와
같은 얼굴). 그래서 목록을 design 01§3의 표에서 **파싱한다.** 표가 **줄어드는** 쪽은 파싱만으로
안 잡혀서 — 행이 빠지면 금지 집합도 조용히 준다 — 바닥(`MIN_DERIVED_FIELDS`)이 그 경로를 막는다.
⚠️ 바닥은 공허 통과 방지지 완전성이 아니다: 행이 **늘** 때 그 자리가 실제로 유도되는지는
여전히 사람이 본다.

## 전체 스윕 — 2026-08-21 · 기준선 **141 passed**

M-01~M-56 **56종 전부 red 확인** · 복구 후 141 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-13 12 · M-14 9 · M-19 `18 failed + 10 errors` · M-26 10 · M-05 3 ·
M-42 1 · M-44 2 · M-45 4** · 새 넷은 **M-53 2 · M-54 1 · M-55 1 · M-56 1**.

### ⛔ 이 절을 처음 쓸 때 **또 옮겨 적었다** (M-19 17 → 18)

이 문단의 초안은 *"직전 스윕과 같다"*라고 적혀 있었다. 스윕을 돌려 보니 **M-19는 18이었다** —
새로 넣은 데모 계약 테스트가 G4가 죽이는 집합에 한 건 더 들어갔기 때문이다. 직전 스윕이
`16 → 17`에서 같은 방식으로 썩은 것을 **바로 위 절에 적어 두고도** 그랬다.
⇒ 게이트는 기준선만 집행한다. **분포는 여전히 사람이 옮겨 적는 숫자이고, 그래서 또 틀린다.**

---

## 검증 3종의 하중 회수 (T9-1 · REQ-201·202·203) — 2026-08-21 · 로컬 macOS · py3.13 · 기준선 **141 passed**

세 요구사항 다 `IMPLEMENTED`였고 테스트도 있었는데 **변이 칸이 비어 있었다**(PRINCIPLES #8).
셋 다 *"어디서 온 값인가"*를 깬다 — ①은 기준선의 **시점**, ②는 재측정의 **질의**,
③은 판정의 **기준**. 셋 중 **하나는 살아남았고 하나는 우연히 죽었다.**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-57 | REQ-201 | 기준선을 **조치 뒤에** 잰다 | 3 failed | **red 확인** |
| M-58 | REQ-202 | 재측정이 **자기 질의를 새로 만든다**(같은 값, 다른 스펙) | 3 failed | **red 확인** |
| M-59 | REQ-203 | 판정 기준을 **조치가 들고 있다**(계약과 같은 값) | 1 failed | **red 확인** |

⚠️ 위 숫자는 **가드를 넣은 뒤**의 값이다. 넣기 전에 먼저 재 봤고, 그 결과가 이 절의 요점이다.

### ⛔ M-59는 141건 전부 초록으로 **살아남았다**

`classify(baseline, after, contract.recovery_criterion)`의 셋째 인자를 모듈 상수로 바꿨다 —
**계약의 기준과 값이 같은** `Criterion(DECREASE, 0.5, RELATIVE, 0.1)`이다. 값이 같으니
판정도 같고, 회복·미회복·애매·빈 창 네 경우가 전부 그대로 통과한다. 그런데 그 순간부터
**계약이 회복 기준을 바꿔도 판정은 안 바뀐다** — 조치가 자기 성공 기준을 들고 있는 상태이고,
REQ-203이 금지하는 것이 정확히 그것이다(*"조치가 자기 기준을 정하면 모든 조치가 성공한다"*).
⇒ 값을 묻는 테스트로는 절대 안 잡힌다. **인자의 출처를 구문으로** 물어야 한다(M-54와 같은 계열).

### ⛔ M-57은 red였지만 **REQ-201의 가드가 죽인 게 아니었다**

가드를 넣기 전 M-57은 `1 failed`였고, 그 하나는
`test_req_405_the_reservation_is_released_even_when_the_loop_raises`였다 — **예산 예약**
테스트다. 기준선을 실행 뒤로 옮기면 예외 경로의 읽기 횟수가 달라져 우연히 걸린 것이지,
*"기준선은 조치 전에 잰다"*를 묻는 자리는 **한 곳도 없었다.** 우연한 하중은 다음 리팩터에
사라지고, 사라진 것은 조용하다 — 예약이 M-18·M-27을 가렸을 때와 방향만 반대인 같은 일이다.

### ⚠️ 절차는 베끼지 않고 design 02§2를 **파싱한다**

새 가드(`tests/test_verification_order.py`)는 순서와 인자 출처를 손으로 적어 두지 않는다.
설계의 절차 블록(`① baseline = read(contract.health_signal)` …)을 읽어 **설계가 선언한
순서와 표현식**을 그대로 요구한다. 손으로 적힌 것은 *"설계의 `read` ↔ 코드의
`self.signals.read`"* 대응 하나뿐이고, 설계가 모르는 단계를 말하면 파서가 **소리 내어**
실패한다(T0-10·M-51·M-52가 남긴 규칙).

### ⚠️ 그 파서가 처음에 **다섯 중 넷만 읽었다**

단계 사이 공백을 `\s*`로 뒀더니 인자 없는 단계(`② execute`) 뒤에서 **줄바꿈을 삼켜**
다음 줄머리가 `^`를 못 만났고, `③ wait`가 조용히 빠졌다. 바닥(`MIN_DESIGN_STEPS`)이 그걸
잡았다 — 없었으면 *"순서가 맞다"*는 초록이 **네 단계에 대한 문장**이었을 것이다.
⇒ 공허 통과 방지의 바닥은 장식이 아니다(M-03·M-25·M-45와 같은 자리).

## 전체 스윕 — 2026-08-21 · 기준선 **144 passed**

M-01~M-59 **59종 전부 red 확인** · 복구 후 144 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-13 12 · M-14 9 · M-19 `18 failed + 10 errors` · M-26 10 · M-05 3 ·
M-42 1 · M-44 2 · M-45 4 · M-53 2 · M-54 1 · M-55 1 · M-56 1** — 직전 스윕과 **같다**
(이번엔 옮겨 적지 않고 다시 셌다). 새 셋은 **M-57 3 · M-58 3 · M-59 1**이고,
새 가드가 늘린 것은 그 셋뿐이다: M-57 `1 → 3` · M-58 `2 → 3` · M-59 **`0 → 1`**.

---

## 롤백 2종의 하중 회수 (T9-2 · REQ-301·302) — 2026-08-21 · 로컬 macOS · py3.13 · 기준선 **144 passed**

둘 다 `IMPLEMENTED`였고 테스트도 있었는데 **변이 칸이 비어 있었다**(PRINCIPLES #8).
REQ-301은 하나로 안 끝나서 **둘로 쪼개 물었다** — 계획이 *언제* 묶이는가와 *어디서* 오는가는
같은 문장이 아니고, **하나는 하중을 받고 있었고 하나는 아니었다.**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-60 | REQ-301 | 롤백 계획을 **조치 뒤에** 읽는다 (값은 그대로) | 1 failed | **red 확인** |
| M-61 | REQ-301 | 롤백 계획을 **계약 저장소에서 다시 읽는다** | 4 failed | **red 확인** |
| M-62 | REQ-302 | **모델이 닫은** 미회복은 롤백하지 않는다 | 1 failed | **red 확인** |

⚠️ 위 숫자는 **가드를 넣은 뒤**의 값이다. 넣기 전에 먼저 재 봤고, 그 결과가 이 절의 요점이다.

### ⛔ M-60은 144건 전부 초록으로 **살아남았다** — M-61은 죽었다

같은 요구사항의 두 변이가 갈렸다. **계약은 루프 시작에 한 번 읽은 얼어붙은 값이다** —
그래서 `plan = contract.rollback_plan`을 조치 **뒤로** 내려도 계획도, 판정도, 트래픽 전환
대상도, 원장도 **하나도 안 바뀐다**(M-60). 반면 저장소를 다시 읽으면 조회가 1→2로 늘고,
`test_req_301_...`의 `contracts.lookups == 1`이 그것을 잡는다(M-61 · `1 failed`).

⇒ 지금까지 물어진 것은 *"다시 조회하지 않는가"*뿐이었다. *"조치 전에 고정되는가"*는
**한 곳도 없었다.** 그 둘은 시간축이 다르다: 재조회는 **오늘의 위반**이고, 묶이는 시점은
**내일의 위반을 막는 자리**다 — 계획이 이미 조치 뒤에 묶여 있으면, 나중에 누가 `contract`를
조치 뒤에 다시 읽도록 고쳐도 **그 자리는 이미 조치 뒤라 아무것도 어긋나 보이지 않는다.**

### ⛔ M-62도 살아남았다 — 롤백이 사라지는 구간이 **애매한 구간과 겹친다**

REQ-302는 *"검증이 `not_recovered`일 때"*라고만 적는다. **누가 판정했는지는 안 적는다.**
그런데 롤백 테스트가 태우던 것은 **규칙이 닫은 경우 하나뿐**이었다 — 모델이 닫은 미회복을
롤백에서 빼도 144건 전부 초록이다. 모델이 불리는 경우는 정의상 tolerance 안쪽,
즉 **판단이 가장 어려운 구간**이다. 하필 거기서 롤백이 조용히 사라지고, 원장에는
`executed=true`만 남아 **되돌린 경우와 똑같아 보인다.**

⚠️ 반대편도 함께 막았다: *"모델 경로면 무조건 롤백"*도 초록이 되면 안 된다 —
그건 **나아진 조치를 되돌리는** 오류다. 롤백을 여는 것은 경로가 아니라 **판정**이다.

### ⚠️ 이번엔 설계를 파싱하지 **않았다** — 그리고 그 이유를 남긴다

T9-1은 design 02§2의 절차 블록을 파싱했다. 03§2도 같은 모양의 블록을 갖고 있지만
거기엔 **코드에 없는 단계가 있다**: *"원장 항목에 고정"* — 코드는 지역 이름에 묶는다.
파싱하면 그 어긋남 때문에 이 가드는 **처음부터 red**이고, 그걸 초록으로 만들려면 설계나
원장 스키마를 건드려야 한다(이 항목의 범위 밖이다). ⇒ 자리를 AST로 묻고,
**어긋남은 지우지 않고 여기 적어 둔다.**

## 전체 스윕 — 2026-08-21 · 기준선 **150 passed**

M-01~M-62 **62종 전부 red 확인** · 복구 후 150 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-13 13 · M-14 9 · M-19 `18 failed + 10 errors` · M-26 10 · M-05 3 ·
M-42 1 · M-44 2 · M-45 4 · M-53 2 · M-54 1 · M-55 1 · M-56 1 · M-57 3 · M-58 3 · M-59 1**.
새 셋은 **M-60 1 · M-61 4 · M-62 1**이다.

⚠️ **M-13이 12에서 13으로 늘었다** — 옮겨 적었으면 못 봤을 값이다. 새 REQ-302 테스트 둘이
`entry.improved`를 함께 묻기 때문이고(검증 없이 `improved`가 참이 되는 변이가 거기서도
걸린다), 의도한 겹침이다. 나머지 열둘은 직전 스윕과 같다 — **이번에도 다시 셌다.**

새 가드가 늘린 것: M-60 **`0 → 1`** · M-61 `1 → 4` · M-62 **`0 → 1`**.
⇒ 62종 중 **둘이 살아 있었다**(M-60·M-62). 그리고 M-61이 받던 하중은 처음부터
*"다시 조회하지 않는가"* 하나뿐이었다.

## 계약 수명 2종의 하중 회수 (T9-3 · REQ-104·105) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **150 passed**

둘 다 `IMPLEMENTED`였고 테스트도 있었는데 **변이 칸이 비어 있었다**(PRINCIPLES #8).
둘 다 **둘로 쪼개 물었다** — *"계약이 없다"*와 *"계약이 죽었다"*는 각각 **어디서 집행되는가**가
서로 다르고, 이번에도 **한쪽만 하중을 받고 있었다.**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-63 | REQ-104 | 계약 **없음**을 검증 가능으로 친다 | 2 failed | **red 확인** |
| M-64 | REQ-104 | 계약 없음을 **게이트 결과에 맡긴다**(전용 분기 삭제) | 1 failed | **red 확인** |
| M-65 | REQ-105 | 저장소가 **`retired` 계약도 돌려준다** | 2 failed | **red 확인** |
| M-66 | REQ-105 | 종료해도 **상태가 `active`로 남는다** | 3 failed | **red 확인** |

⚠️ 위 숫자는 **가드를 넣은 뒤**의 값이다. 넣기 전에 먼저 재 봤고, 그 결과가 이 절의 요점이다.

### ⛔ M-64는 150건 전부 초록으로 **살아남았다** — 두 경로가 같은 값을 낸다

계약이 없으면 `verifiable=False` · `reversibility=IRREVERSIBLE`이고, 게이트는 그 조합에
`MANUAL`을 낸다. `_status_for(MANUAL)`은 `manual_required`다 — **전용 분기가 덮어쓰려던 값과
똑같다.** 그래서 `status = Status.MANUAL_REQUIRED if contract is None else ...`를 통째로
지워도 아무 테스트도 안 움직인다.

두 경로가 갈라지는 경우는 **하나뿐이다: 예산이 먼저 막을 때.** 돈은 게이트의 첫 칸이라
`projected > headroom`이면 판정은 `DENY`이고, 분기가 없으면 원장은 `denied`라고만 적는다.
그러면 *"계약이 없었다"*는 사실은 **어디에도 안 남는다** — 예산을 채우면 자동으로 도는
조치처럼 읽히지만, 실제로는 **잴 신호조차 없는 리소스**다.
⇒ 그 한 경우를 값으로 태웠다(`headroom=0.10` · `projected=5.00`).

### ⛔ M-63은 red였지만 **REQ-104의 가드가 죽인 게 아니었다**

가드를 넣기 전 M-63은 `1 failed`였고, 그 하나는
`test_demo.py::test_req_803_an_uncontracted_resource_is_manual_and_the_executor_is_not_called`
— **데모 서사 테스트**다. 유닛 쪽 `test_req_104_...`는 상태·`contract_id`·호출 수만 묻고
**판정을 안 묻는다**. 그런데 M-63이 바꾸는 것은 **판정과 그 사유뿐**이다: `status`는 위의
전용 분기가 따로 덮으니 그대로 `manual_required`이고, 실행기도 여전히 안 불린다.

⇒ *"계약 없음이 곧 검증 불가"*라는 문장의 하중을 **데모 시나리오 하나가 들고 있었다.**
데모는 서사를 바꾸려고 손대는 자리다 — 바뀌는 순간 이 하중은 조용히 사라진다.

### ⛔ REQ-105는 **승인 경로에만** 물려 있었다 — 정상 입구엔 자리가 없었다

M-65(저장소가 죽은 계약도 돌려준다)를 죽인 것도, M-66(종료가 상태를 안 바꾼다)이 죽인 둘 중
하나도 전부 `test_req_404_..._a_retired_contract_is_manual` **하나**였다. 즉 지금까지
*"죽은 계약으로는 자동 조치를 하지 않는다"*가 물어진 곳은 **승인 뒤 재판정 경로**뿐이고,
Day-2의 **정상 입구인 `remediate()`에서는 한 번도 태워진 적이 없다.**

⚠️ 그 자리가 조용한 이유는 계약을 거르는 주체가 **저장소**이기 때문이다 — `active_for`가
`retired`를 걸러 `None`을 주면, `remediate()`는 *"계약이 없다"*와 *"계약이 죽었다"*를
구분하지 않는다(구분할 필요도 없다). 그래서 거르기를 지워도 **유스케이스 쪽 테스트는
하나도 안 움직이고**, 죽은 계약이 살아 있는 계약으로 원장에 실린다.
⇒ `remediate()`에 죽은 계약을 **회복하는 신호와 함께** 태웠다 — 계약이 살아 있었다면
`AUTO`로 실행되고 `recovered`로 닫혔을 입력이라, 공허하게 통과할 수 없다.

## 원장 완결성 3종의 하중 회수 (T9-4 · REQ-507) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **156 passed**

REQ-507은 **세 경우를 한 문장으로** 말한다(거부·실패·`manual_required`). 테스트는 셋이나
달려 있었는데 **변이 칸은 비어 있었다**(PRINCIPLES #8). 셋으로 쪼개 물었더니 셋 다
**다른 이유로** 약했다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-67 | REQ-507 | 막는 판정에 **원장 행을 안 만든다** | 10 failed | **red 확인** |
| M-68 | REQ-507 | **예약이 막은 것**을 원장에 안 적는다 | 1 failed | **red 확인** |
| M-69 | REQ-507 | 실패한 조치를 **`executed`로 적는다** | 2 failed | **red 확인** |

⚠️ 위 숫자는 **가드를 넣은 뒤**의 값이다. 넣기 전에 먼저 재 봤고(153건 스위트에서
M-67 `9` · M-68 **`0`** · M-69 `1`), 그 셋의 차이가 이 절의 요점이다.

### ⛔ M-68은 153건 전부 초록으로 **살아남았다** — 그 분기는 한 번도 안 태워졌다

`_execute_and_verify`의 첫 칸은 *"예약이 실패하면 `denied`로 닫는다"*인데, **그 분기에
도달하는 입력이 스위트에 없었다.** 예약 실패는 게이트가 여유를 **읽은 뒤** 다른 조치가
그 여유를 가져갔을 때만 나는데, 재진입 테스트(REQ-405)로는 그 순서가 안 만들어진다 —
안쪽 조치는 **이미 줄어든 여유를** 게이트가 다시 읽으므로 **판정부터 `DENY`**이고,
예약까지 가지 못한다. 그래서 그 자리를 통째로 지워도(행을 안 고치고 그대로 반환) 아무
테스트도 안 움직인다.

그때 원장에 남는 것은 게이트가 적어 둔 **`executed`**다. 실행기는 안 불렸는데 원장은
*"실행했다"*고 말하고, 그 행은 리포트의 **분모**에 실려 회복률을 희석한다 — 그리고
`improved`는 원리상 절대 참이 안 되므로 **경합이 잦을수록 헤드라인이 나빠진다.**
⇒ 게이트가 여유를 읽는 **순간에** 다른 조치가 가져가는 예산 대역(`_RacingBudget`)으로
그 창을 값으로 태웠다. 판정이 `AUTO`인 것을 함께 단언한다 — 게이트에서 먼저 막히면
이 테스트는 예약 분기를 안 묻는 것이므로.

### ⛔ M-67을 죽인 아홉 건은 **전부 승인 경로였다** — 막힌 행을 묻는 자리가 아니었다

가드를 넣기 전 M-67은 `9 failed`였고, 여덟은 `test_req_404_*`, 하나는 데모 리포트
테스트다. 그것들이 죽는 이유는 *"막힌 조치가 원장에 남는가"*가 아니라 **`approve()`가
그 행을 찾아야 하기 때문**이다 — 즉 하중을 받고 있던 상태는 `awaiting_approval` 하나고,
REQ-507이 이름을 부르는 **`denied`와 `manual_required`는 아무도 안 묻고 있었다.**
반환값은 어느 쪽이든 똑같은 `LedgerEntry`고 상태 문자열도 그대로라, 그 어긋남은
**반환만 보는 자리에서 안 보인다.** ⇒ 세 경우를 **원장에 직접** 물었다
(`ledger.all_entries()` · `ledger.get()`).

### ⚠️ M-69의 하중은 **예산 정산 테스트**가 들고 있었다

`1 failed`의 그 하나는 `test_req_405_a_failed_action_settles_to_zero_and_gives_the_headroom_back`
이다. 실패를 `executed`로 적어도 그 테스트가 죽는 이유는 상태가 아니라 **정산액**이고,
정산을 묻는 자리가 정리되면 *"실패도 원장에 남는다"*는 조용히 안 물어지게 된다.
⇒ 회복하는 신호를 태운 채(막히지 않았다면 `improved`로 닫혔을 입력) 원장의 상태를
직접 묻는 자리를 따로 세웠다.

## 전체 스윕 — 2026-08-22 · 기준선 **153 passed**

M-01~M-66 **66종 전부 red 확인** · 복구 후 153 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-13 13 · M-14 10 · M-19 `20 failed + 10 errors` · M-26 10 · M-05 3 ·
M-42 1 · M-44 2 · M-45 4 · M-53 2 · M-54 1 · M-55 1 · M-56 1 · M-57 3 · M-58 3 · M-59 1 ·
M-60 1 · M-61 4 · M-62 1**. 새 넷은 **M-63 2 · M-64 1 · M-65 2 · M-66 3**이다.

⚠️ **직전 스윕과 두 자리가 갈라졌다** — 옮겨 적었으면 못 봤을 값이다:
**M-14 `9 → 10`**(G9 — 새 REQ-104 테스트가 *"검증 불가는 AUTO가 아니다"*를 함께 묻는다)와
**M-19 `18 → 20 failed`**(G4 — 새 테스트 셋 중 둘이 `_decision(entry)`를 지나간다).
둘 다 의도한 겹침이다. 나머지 열여섯은 직전과 같다 — **이번에도 다시 셌다.**

새 가드가 늘린 것: M-63 `1 → 2` · M-64 **`0 → 1`** · M-65 `1 → 2` · M-66 `2 → 3`.
⇒ 66종 중 **하나가 살아 있었다**(M-64). 그리고 M-63·M-65가 받던 하중은 **각각 한 자리뿐**,
그것도 이 요구사항을 겨냥하지 않은 자리였다(데모 서사 · 승인 경로).

## 전체 스윕 — 2026-08-22 · 기준선 **156 passed**

M-01~M-69 **69종 전부 red 확인** · 복구 후 156 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-13 13 · M-14 11 · M-19 `21 failed + 10 errors` · M-26 10 · M-05 3 ·
M-42 1 · M-44 2 · M-45 4 · M-53 2 · M-54 1 · M-55 1 · M-56 1 · M-57 3 · M-58 3 · M-59 1 ·
M-60 1 · M-61 4 · M-62 1 · M-63 2 · M-64 1 · M-65 2 · M-66 3**.
새 셋은 **M-67 10 · M-68 1 · M-69 2**다.

⚠️ **직전 스윕과 두 자리가 갈라졌다** — 옮겨 적었으면 못 봤을 값이다:
**M-14 `10 → 11`**(G9 — 막힌 행을 묻는 새 테스트가 *"검증 불가는 AUTO가 아니다"*의
경우를 함께 태운다)와 **M-19 `20 → 21 failed`**(G4 — 경합 테스트가 `_decision(entry)`를
지나간다). 둘 다 의도한 겹침이다. 나머지 스무 자리는 직전과 같다 — **이번에도 다시 셌다.**

새 가드가 늘린 것: M-67 `9 → 10` · M-68 **`0 → 1`** · M-69 `1 → 2`.
⇒ 69종 중 **하나가 살아 있었다**(M-68 — 도달하는 입력 자체가 없던 분기). 그리고 M-67·M-69가
받던 하중은 **둘 다 이 요구사항을 겨냥하지 않은 자리**였다(승인 경로 · 예산 정산).

## 청구 화해 8종의 하중 회수 (T9-5 · REQ-506, REQ-509) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **162 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-70 | REQ-506 | 화해가 **멱등이 아니다** | 1 failed | **red 확인** |
| M-71 | REQ-506 | **실측의 근거를 안 묻는다** | 1 failed | **red 확인** |
| M-72 | REQ-509 | 추정이 0인데 **배율을 0으로 낸다** | 1 failed | **red 확인** |
| M-73 | REQ-509 | 화해가 **차액을 안 남긴다** | 2 failed | **red 확인** |
| M-74 | REQ-506 | **기한을 안 본다** | 1 failed | **red 확인** |
| M-75 | REQ-506 | 포기하면서 **사유를 안 적는다** | 1 failed | **red 확인** |
| M-76 | REQ-506 | **이미 화해된 행도 뒤집는다** | 1 failed | **red 확인** |
| M-77 | REQ-506 | **사유 없이도 포기를 받는다** | 1 failed | **red 확인** |

⚠️ 위 숫자는 **가드를 넣은 뒤**의 값이다. 먼저 156건 스위트에서 재 봤고
(M-70 `1` · M-71 **`0`** · M-72 `1` · M-73 `1`), 그 차이가 이 절의 요점이다.
M-74~M-77은 **잴 코드가 없어서** 재지 못했다 — 아래가 그 이야기다.

### ⛔ REQ-506의 절반은 코드가 아니라 **문서에만** 있었다 — `unreconciled`에 도달하는 길이 없었다

요구사항은 두 문장이다: *"재실행해도 결과가 같으며"*(멱등)와 *"기한 내 못 맞추면
`unreconciled`로 사유와 함께 표시한다"*(기한). 앞의 절반은 `reconcile()`에 있었다.
**뒤의 절반은 아무 데도 없었다.** 있던 것은 이름 둘뿐이다 —
`ReconcileState.UNRECONCILED`(어떤 코드도 이 값을 쓰지 않는다)와
설정 `WR_RECONCILE_DEADLINE_DAYS`(읽히기만 하고 아무도 안 본다).
⇒ 그 둘을 잇는 자리를 만들었다(`InMemoryLedger.give_up_reconcile`). **변이를 심을 코드가
없으면 그 요구사항은 `VERIFIED`가 될 수 없다** — 상태 칸은 `IMPLEMENTED`라고 말하고
있었고, G6은 *"테스트가 있는가"*만 물으므로 이 어긋남을 못 본다(PRINCIPLES #8의 다른 얼굴).

기한 판단은 **저장소가** 한다. 호출자에게 맡기면 *"기한 내"*는 약속이 아니라 관례가 되고,
아직 도착할 수 있는 청구서를 두고 닫아 버린 행의 비용은 **영원히 추정으로 남는다**(M-74).
그리고 이미 화해된 행은 안 뒤집는다 — 청구서가 말한 값이 기한보다 세다(M-76).

### ⛔ M-71은 156건 전부 초록으로 **살아남았다** — `measured`가 청구서에서 왔는지 아무도 안 물었다

`reconcile()`의 첫 칸은 *"근거가 `billing_export`가 아니면 거절한다"*인데, 그 거절을 통째로
지워도 아무 테스트도 안 움직였다. 그 자리가 죽으면 **추정치를 `measured` 칸에 넣을 수 있고**,
그때 `assumed`와 `measured`는 같은 계산의 두 벌이 된다 — `delta`는 0에 수렴하고, 리포트는
*"추정이 정확했다"*로 읽힌다. 실제로는 **청구서를 한 번도 안 본 것**이다.
⇒ 거절을 직접 묻고, 거절이 **아무것도 안 남기는** 거절인지(`measured is None` ·
`reconcile_state`가 `pending` 그대로) 함께 단언한다.

### ⚠️ M-73의 하중은 **이름이 다른 것을 약속하는 테스트**가 들고 있었다

`1 failed`의 그 하나는 `test_req_505_reconcile_does_not_touch_assumed`다. 이름이 약속하는
것은 `assumed` 불변(REQ-505)이고, *"차액과 배율을 남긴다"*(REQ-509)는 그 테스트의 **곁다리
단언 세 줄**이었다. 그 테스트가 자기 이름이 약속한 것만 묻도록 좁혀지는 순간 — T0-10이
실제로 세 자리에서 한 정리다 — REQ-509의 산출물은 **아무도 안 묻게 된다.**
⇒ 파생값을 겨냥한 자리를 따로 세웠다(`1 → 2`).

### ⚠️ M-70·M-72는 겨냥한 자리가 처음부터 하나씩 있었다 (`1 → 1`)

T9에서 처음이다. 멱등(M-70)과 *"배율이 정의되지 않는다"*(M-72)는 각각
`test_req_506_reconcile_is_idempotent`·`test_req_509_delta_ratio_is_undefined_when_assumed_is_zero`가
**그 요구사항의 이름으로** 물고 있었다. 하중이 하나뿐이라는 것은 얇다는 뜻이지만,
**빌려 온 하중은 아니다** — T9-1~T9-4에서 문제였던 것은 개수가 아니라 출처였다.

## 전체 스윕 — 2026-08-22 · 기준선 **162 passed**

M-01~M-77 **77종 전부 red 확인** · 복구 후 162 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-13 13 · M-14 11 · M-19 `21 failed + 10 errors` · M-26 10 · M-05 3 ·
M-42 1 · M-44 2 · M-45 4 · M-53 2 · M-54 1 · M-55 1 · M-56 1 · M-57 3 · M-58 3 · M-59 1 ·
M-60 1 · M-61 4 · M-62 1 · M-63 2 · M-64 1 · M-65 2 · M-66 3 · M-67 10 · M-68 1 · M-69 2**.
새 여덟은 **M-70 1 · M-71 1 · M-72 1 · M-73 2 · M-74 1 · M-75 1 · M-76 1 · M-77 1**이다.

⚠️ **직전 스윕과 갈라진 자리가 하나도 없다** — T9 들어 처음이다. 새 가드가 화해 경로
(`reconcile`·`give_up_reconcile`)에만 물렸고, 그 경로를 지나가는 다른 테스트가 없기 때문이다.
조치 루프를 건드린 T9-1~T9-4에서는 매번 M-14(G9)와 M-19(G4)가 함께 움직였다.
**그래도 다시 셌다** — 갈라지지 않았다는 것도 관측이어야 한다.

새 가드가 늘린 것: M-71 **`0 → 1`** · M-73 `1 → 2`. M-70·M-72는 `1 → 1`이다.
⇒ 77종 중 **하나가 살아 있었고**(M-71), 요구사항의 **절반은 잴 코드조차 없었다**(M-74~M-77).

## 문장 단위 커버리지 (T10-1 · G6⑤ · REQ-501) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **167 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-78 | — | G6⑤ 문장 단위 검사를 통째로 삭제 | 1 failed | **red 확인** |
| M-79 | — | 분리기가 **쉼표 붙은 대등 접속**에서 안 자르게 함 | 1 failed | **red 확인** |
| M-80 | — | 겨냥을 **파일 단위로** 세게 함 (함수 단위 → 파일 단위) | 2 failed | **red 확인** |
| M-81 | — | 분리기가 **늘 한 문장**을 내게 함 (공허 통과 방지) | 3 failed | **red 확인** |
| M-82 | REQ-501 | 저장이 `retry_of`를 흘리게 함 | 1 failed | **red 확인** |

⛔ **M-78의 하중은 `1`이다 — 그리고 그 하나는 일부러 세운 자리다.**
현재 상태가 전부 규칙을 충족하므로 `all_violations`를 읽는 테스트는 검사를 지워도 **초록**이다.
*"지금 위반이 없다"*와 *"검사가 없다"*가 같은 초록으로 보이는 자리 — 이 저장소가 세 번 속은
그 모양이다(M-03·M-25·M-45). ⇒ 위반하는 행을 값으로 만들어 판정을 직접 묻는 테스트를
따로 세웠고(`test_g6_half_covered_requirement_is_caught`), M-78을 죽이는 것은 그 하나다.

### ⛔ 이 층이 없으면 귀속은 **요구사항 단위로만** 지켜진다

G6①이 묻는 것은 *"테스트가 있는가"*다. 요구사항 하나가 여러 문장을 약속하면, 그중 한 문장만
겨냥해도 그 질문의 답은 **참**이다. REQ-506이 정확히 그 모양이었고(T9-5), 게이트는 초록이었다.
⇒ 요구사항이 **스스로 약속한 문장 수**를 세고(`split_promises`), 상태가 구현을 주장하면
그 수만큼 **겨냥한 테스트 함수**를 요구한다.

**세는 단위가 함수여야 한다**(M-80). 파일로 세면 한 파일에 몰린 열 건이 1로 읽히는 게 아니라
그 반대 — 요구사항별로 테스트가 한 파일에 모여 있으므로 *"문장 셋에 겨냥 하나"*가
**언제나 `1 ≥ 1`로 통과한다.** 그게 이 규칙이 겨냥한 바로 그 모양이다.

**분리기는 구두점에만 기댄다**(M-79). 문장 끝은 종결 `다` 뒤의 마침표, 대등 접속은
**쉼표가 붙은 `-고,`·`-며,`**뿐이다. 쉼표 없는 접속(`에스컬레이션하고 더 조치하지 않는다`)은
일부러 **안 센다** — 이 분리기는 덜 세는 쪽으로 틀린다. 더 세면 없는 약속에 테스트를 요구하게
되고, 그때 사람이 고치는 방향은 **테스트를 쪼개는 것**이라 가드가 자기 하중을 스스로 없앤다.

**바닥이 없으면 가장 조용하게 죽는다**(M-81). 분리기가 전부 한 문장으로 읽으면 판정은
전부 `1 ≤ N`이라 항상 초록이다 — 검사는 남아 있는데 아무것도 안 묻는다.
⇒ `MIN_MULTI_PROMISE` = 문장 둘 이상을 약속하는 요구사항이 최소 8개는 읽혀야 한다.

### ⛔ 규칙을 켠 첫 실행이 잡은 것은 REQ-501 하나였다 — 그리고 그건 진짜였다

44개 중 **딱 하나**가 걸렸다. REQ-501은 두 문장을 약속한다 — *"조치 1회 = 원장 1행"*과
*"재시도는 새 id를 만들고 `retry_of`로 원래를 가리킨다"*. 겨냥한 자리는
`test_req_501_one_action_is_one_row_and_retry_does_not_overwrite` **하나**였고,
**이름이 둘 다를 약속하고 있었다**(M-73이 보여 준 모양의 재발).

그리고 그 테스트는 두 번째 문장을 **실제로 안 묻고 있었다** — 단언은
`len(ledger.all_entries()) == 2`, 즉 **행이 둘이 됐는가**뿐이다. 저장이 `retry_of`를 흘려도
초록이다(M-82). 포인터가 끊기면 원장은 같은 사건의 두 시도를 **무관한 조치 둘로** 세고,
그 둘은 회복률의 **분모에 각각** 실린다. ⇒ 문장마다 하나씩 둘로 쪼갰고, 두 번째는
`stored.retry_of == ENTRY_ID`를 직접 묻는다.

⚠️ **44개 중 하나만 걸렸다는 것은 이 규칙이 느슨하다는 뜻이기도 하다.** 분리기가 덜 세는
쪽으로 틀리도록 설계됐기 때문이다 — REQ-305(*"에스컬레이션하고 더 조치하지 않는다"*)는
쉼표가 없어 한 문장으로 읽힌다. 그건 오탐이 아니라 **미탐이고, 그렇게 고른 것**이다.

## 전체 스윕 — 2026-08-22 · 기준선 **167 passed**

M-01~M-82 **82종 전부 red 확인** · 복구 후 167 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-01 `10 errors` · M-13 13 · M-14 11 · M-19 `21 failed + 10 errors` ·
M-26 10 · M-67 10 · M-33 8 · M-12 7 · M-03 5 · M-22 5 · M-05 4 · M-29 4 · M-30 4 ·
M-37 4 · M-45 4 · M-61 4**. 나머지는 1~3이다.
새 다섯은 **M-78 1 · M-79 1 · M-80 2 · M-81 3 · M-82 1**이다.

### ⛔ 직전 스윕(162)과 갈라진 자리는 **M-05 하나** — `3 → 4`다

M-05는 REQ-101을 `IMPLEMENTED`로 올리고 **언급만 하는** 테스트를 붙인다. 여태 죽던 셋에
**G6⑤가 합류했다** — REQ-101은 한 문장을 약속하는데 겨냥한 테스트가 `0`이기 때문이다.
즉 ⑤는 *"여러 문장 중 하나만 겨냥한"* 경우뿐 아니라 **`0`을 겨냥한 경우도** 잡는다.
①과 겹치는 자리가 있다는 뜻이고, 그건 중복이 아니라 **①이 못 보던 구간이 ⑤에 포함된다**는
관측이다.

⚠️ 그 외 25자리(M-13·M-14·M-19·M-26·M-42·M-44·M-45·M-53~M-69·M-70~M-77)는
직전 스윕과 **같은 값**이다. 새 규칙이 추적성 경로(`tools/spec_trace.py`)에만 물렸고
조치 루프를 안 지나가기 때문이다. **갈라지지 않았다는 것도 관측이어야 해서 다시 셌다.**

## 상태 칸과 증거를 맞댄다 (T10-2 · REQ-305·802) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **169 passed**

T10-2. 물은 것은 *"이 요구사항에 변이 기록이 있는가"*가 아니라
**"그 기록이 이 요구사항이 약속한 문장을 전부 덮는가"**다. 넷(REQ-206·305·801·802) 중
**둘은 절반만 덮여 있었고**, 하나는 이미 덮여 있었고, 하나는 아직 태울 대상이 없다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-83 | REQ-305 | 롤백이 실패하면 **조치를 다시 해 본다** (재시도) | 4 failed | **red 확인** |
| M-84 | REQ-305 | 되돌릴 계획이 없는데 **롤백을 성공으로 적는다** | 1 failed | **red 확인** |
| M-85 | REQ-802 | 항목의 시각을 **살아 있는 시계**에서 읽는다 | 4 failed | **red 확인** |
| M-86 | REQ-802 | 항목 id를 **난수**에서 만든다 (`uuid4`) | 2 failed | **red 확인** |

넷 다 복구 후 **169 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ REQ-305의 절반은 **도달하는 입력이 없었다**

`_rollback`의 *"계획이 없다"* 분기는 스위트 어디에서도 값이 되지 않았다. 자동 경로에서
비가역 계약은 게이트가 `APPROVE`로 먼저 멈추기 때문이다 — 그래서 기존
`test_req_305_irreversible_failure_escalates_instead_of_retrying`이 실제로 태우는 것은
**게이트가 막는 경우**이고, *"에스컬레이션한다"*는 절반은 안 물어진다. 그 분기에 도달하는
유일한 입구는 **승인**(REQ-404)이다. ⇒ 승인된 비가역 조치가 미회복으로 닫히는 경우를
세웠고, M-84가 그 자리에서만 죽는다.

### ⛔ *"더 조치하지 않는다"*는 **값으로는 안 보인다** (M-83)

롤백 실패 뒤에 조치를 다시 실행해도 원장은 **한 글자도 안 바뀐다** — `rollback.performed`는
여전히 False고 `escalated`도 True 그대로다. 늘어나는 것은 실행기 호출 횟수뿐이고,
그게 정확히 *"실패한 자동화가 계속 시도하는"* 상태다. REQ-303의 기존 케이스
(배분이 안 옮겨진 경우)는 `performed is False`까지만 물었다.
⇒ 값이 아니라 **호출 흔적**에 묻는다: 전환 1회 · 조치 1회 (G1과 같은 계열).

### ⚠️ REQ-802 — 결정론은 **한 번 돌려서는 안 보인다** (M-85·M-86)

살아 있는 시계도 난수 id도 예외를 안 내고 값도 그럴듯하다. 갈라지는 것은 **같은 입력의
두 실행**뿐이라, 한 번만 돌리는 테스트는 전부 초록이다. 이 하중을 들고 있는 자리는
`test_req_803_the_narrative_is_identical_when_run_twice` 하나다 — 즉 **REQ-802의 증거는
데모 서사에 얹혀 있다.** 데모 시나리오가 바뀌면 그 하중은 조용히 사라진다(M-63과 같은 계열).

⚠️ M-12(디스크)는 이 기계에 `.env`가 있어서 red였다 — 그 한계는 위에 적힌 그대로다.
⇒ REQ-802의 세 축(시계·난수·디스크) 중 **디스크만 기계 의존이고, 나머지 둘은 아니다.**

### ⛔ REQ-801은 승격하지 않는다 — 아직 **태울 대상이 없다**

REQ-801이 약속하는 두 문장은 *"네트워크 없이 과금 API 없이 완료된다"*와
*"모델·Firestore·BigQuery·Monitoring·Cloud Run이 전부 fake로 대체된다"*이다.
M-10·M-11이 태우는 것은 **설정 층**(기본값이 fake가 아닌가 · ADK 프로젝트가 어긋나는가)이지
게이트가 실제로 오프라인인가가 아니다. 그것은 G5이고 **T5-4가 소유한다.**

⛔ 그리고 지금 G5를 만들면 **0개를 훑고 초록인 가드**가 된다 — `src/warranty/adapters/`에는
`fakes.py`밖에 없어서 *"라이브 어댑터가 게이트에 안 들어온다"*가 값으로 태워지지 않는다
(M-03·M-25·M-45가 세 번 보여 준 모양). 실물 어댑터는 T2가 잠그고 있다.
⇒ **REQ-801은 `IMPLEMENTED`로 둔다.** 변이 기록이 있다고 상태를 올리면 그 기록이
증명하지 않는 것까지 주장하게 된다 — M-32와 REQ-305에 적어 둔 것과 같은 이유다.

### ⚠️ REQ-206은 **이미 덮여 있었다** — 승격은 새 증거가 아니라 판독이다

REQ-206은 한 문장(*"`VERIFY_WINDOW`·`VERIFY_DELAY`는 한 모듈의 명명 상수다"*)이고,
그 문장의 세 조각에 각각 변이가 물려 있다: **다른 곳에 없는가**(M-42) ·
**호출부에 리터럴이 없는가**(M-44) · **지역 이름으로 우회하지 않는가**(M-50).
공허 통과 방지까지 있다(M-45). ⇒ `IMPLEMENTED`로 남아 있던 것은 증거의 부족이 아니라
**아무도 기록과 상태 칸을 맞대 보지 않았기 때문**이다.

## 전체 스윕 — 2026-08-22 · 기준선 **169 passed**

M-01~M-86 **86종 전부 red 확인** · 복구 후 169 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-01 `10 errors` · M-19 `22 failed + 10 errors` · M-13 14 · M-14 11 ·
M-26 11 · M-67 11 · M-33 8 · M-12 7 · M-03 5 · M-22 5 · M-05 4 · M-29 4 · M-30 4 ·
M-37 4 · M-45 4 · M-61 4 · M-83 4 · M-85 4**. 나머지는 1~3이다.
새 넷은 **M-83 4 · M-84 1 · M-85 4 · M-86 2**다.

### ⛔ 직전 스윕(167)과 갈라진 자리는 **넷이고, 전부 늘었다**

`M-13 13→14 · M-19 21→22 · M-26 10→11 · M-67 10→11`. 넷 다 **새 테스트가 더한 하중**이고,
넷 다 이번에 처음 태운 경로 하나에서 나왔다 — **승인된 비가역 조치가 미회복으로 닫히는 경우**.
그 경로는 G8(`improved` 유도) · G4(모든 항목에 판정) · 승인 집행 · 원장 완결성을 한 번에
지나간다. ⇒ *"REQ-305의 절반에 도달하는 입력이 없었다"*는 말은 그 요구사항만의 이야기가
아니었다. **네 가드가 그 구간을 안 지나가고 있었고, 그건 아무도 안 물으면 안 보인다.**

⚠️ 직전 스윕이 이름을 적어 둔 나머지 열둘(M-01·M-14·M-33·M-12·M-03·M-22·M-05·M-29·
M-30·M-37·M-45·M-61)은 **같은 값**이다. 갈라지지 않았다는 것도 관측이어야 해서 다시 셌다.

⚠️ **M-84의 하중은 `1`이다** — 새로 세운 그 테스트 하나뿐이다. 그 자리를 안 세웠으면
*"계획이 없으면 에스컬레이션한다"*는 문장은 86종 중 **아무 변이도 안 태우는 문장**이었다.

## 배포 산출물 — **아무도 안 읽는 사본** (T11-1 · REQ-602·805·801) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **176 passed**

T11-1. `Dockerfile`과 `scripts/deploy.sh`가 생겼다. **둘 다 실행된 적이 없고**, 앞으로도
게이트는 그것을 안 돌린다(과금한다 · design 10§5). ⇒ 배포 산출물은 이 저장소에서
**가장 오래 틀린 채로 있을 수 있는 파일**이다. 물은 것은 *"배포가 되는가"*가 아니라
**"값이 한 곳에서 오는가"**다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-87 | REQ-602 | 배포 스크립트가 config가 소유한 값을 **다시 적는다** | 1 failed | **red 확인** |
| M-88 | REQ-805 | `MIN_INSTANCES`를 1로 (설계는 0이라고 적었다) | 1 failed | **red 확인** |
| M-89 | REQ-602 | 이미지의 파이썬이 `requires-python`과 어긋난다 | 1 failed | **red 확인** |
| M-90 | REQ-602 | `CMD`가 **없는 모듈**을 가리킨다 (M-46 계열) | 1 failed | **red 확인** |
| M-91 | REQ-602 | `make deploy`가 스크립트를 안 거치고 **직접** 배포한다 | 1 failed | **red 확인** |
| M-92 | REQ-805 | 렌더러가 `--min-instances`를 **아예 안 낸다** | 1 failed | **red 확인** |
| M-93 | REQ-602 | 산출물 목록을 **비운다** (0개를 훑고 초록) | 1 failed | **red 확인** |
| M-94 | REQ-602 | **빈 태그**를 그냥 받는다 (`latest`의 다른 얼굴) | 1 failed | **red 확인** |
| M-95 | REQ-602 | 설정 견본의 리전이 **설계와 어긋난다** | 1 failed | **red 확인** |
| M-96 | REQ-801 | 게이트가 **배포를 선행으로 건다** (`make check`가 과금한다) | 1 failed | **red 확인** |

열 다 복구 후 **176 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ 열 다 하중이 **1이다** — 이 층에는 겹침이 하나도 없다

96종 중 하중이 1인 변이는 흔하지만, **한 태스크가 낸 열이 전부 1인 것은 처음이다.**
뜻은 하나다: 이 열 개의 검사는 서로를 안 받쳐 준다. 테스트 함수 하나를 지우면 그 검사는
**조용히 사라지고** 나머지 아홉은 초록이다. 도메인 층은 안 그렇다 — 거기서는 한 자리를
깨면 서너 곳이 같이 운다(M-13 14 · M-19 32 · M-26 11).

⚠️ 이건 결함이 아니라 **이 층의 모양**이다. 배포 산출물에는 도메인이 없다. 서비스명이
틀렸을 때 그것을 알아채는 두 번째 경로는 **배포뿐**이고, 배포는 게이트에 없다.
⇒ 그래서 이 파일의 검사는 **지우기 쉽고, 지워도 안 아프다.** 다음 사람이 알아야 할 것이 그것이다.

### ⛔ `min-instances`는 **두 자리에서 죽을 수 있다** (M-88 · M-92)

M-88은 상수를 1로 바꾸고, M-92는 렌더러가 그 플래그를 **아예 안 내게** 한다. 둘은 서로를
안 잡는다: M-88에서 렌더러는 그 값을 그대로 실으므로 *"렌더러가 낸다"*는 검사는 **초록**이고,
M-92에서 상수는 여전히 0이므로 *"설계와 같다"*는 검사도 **초록**이다.
⇒ 값이 맞는지와 값이 실리는지는 **다른 질문**이고, 하나만 물으면 REQ-805는 절반만 지켜진다.
⚠️ M-87은 세 번째 자리다 — 둘 다 옳은데 셸이 뒤에 하나 더 붙이면 `gcloud`는 **마지막 값**을 쓴다.

### ⚠️ M-95는 **코드를 한 글자도 안 바꾼다**

리전은 상수가 아니라 `WR_REGION`이다. 그래서 `.env.example`의 리전이 설계와 어긋나도
어떤 프로덕션 코드도 안 바뀌고, 어떤 도메인 테스트도 안 운다 — **처음 채우는 사람만**
다른 리전에 배포한다. 그리고 Vertex의 모델 가용성은 리전마다 다르다(design 10§1).
⇒ 견본 파일은 코드가 아니지만 **설정의 일부**다. 그 어긋남을 물을 자리가 없으면 없는 것과 같다.

## 전체 스윕 — 2026-08-22 · 기준선 **176 passed**

M-01~M-96 **96종 전부 red 확인** · 복구 후 176 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-19 `22 failed + 10 errors` · M-13 14 · M-14 11 · M-26 11 · M-67 11 ·
M-01 `10 errors` · M-33 8 · M-12 7 · M-85 6 · M-03 5 · M-22 5 · M-05 4 · M-29 4 · M-30 4 ·
M-45 4 · M-61 4 · M-83 4**. 나머지는 1~3이다.
새 열은 **M-87~M-96 전부 1**이다 (위 절).

### ⛔ 직전 스윕(169)과 갈라진 자리는 **둘이고, 서로 반대 방향이다** — 그리고 원인을 못 밝혔다

`M-37 4→2 · M-85 4→6`. 나머지 열여섯은 **같은 값**이다.

⚠️ **이번 변경으로는 설명이 안 된다.** T11-1이 건드린 것은 `config.py`(추가만)·`Makefile`·
`Dockerfile`·`deploy.sh`·새 테스트 파일이고, M-37(데모의 살아 있는 시계)과 M-85(항목 시각의
살아 있는 시계)가 태우는 자리는 그 어느 것도 안 지난다. 둘을 각각 **두 번씩 다시 돌려**
2와 6이 안 흔들리는 것은 확인했다 — **지금 값은 안정적이다.**

⛔ 가장 그럴듯한 설명은 **169 스윕의 그 두 숫자가 옮겨 적히면서 틀렸다**는 것이다.
합이 8로 같고 둘이 목록에서 이웃해 있다. 이 저장소는 정확히 그 방식으로 이미 네 번 틀렸다
(T0-6·T0-7 — 세어 적은 숫자가 넷 다 틀렸던 자리). **하지만 증명은 못 했다.**
지난 스윕의 원본 로그가 없어서 `2`였는지 `4`였는지 되짚을 방법이 없다.

⛔ **여기가 T0-8이 열어 둔 구멍이다**: 신선도 가드는 *기준선*(`N passed`)과 *변이 집합*은
집행하지만 **하중 분포는 안 집행한다** — 그 숫자는 여전히 사람이 옮겨 적는다. ⇒ 이 표의
숫자들은 게이트가 지키지 않는다. 다음에 갈라졌을 때도 **원인과 오기를 구별할 수 없다.**

## 상시 과금 컴퓨트 — **일치가 아니라 값을 묻는다** (T11-2 · REQ-805) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **180 passed**

T11-2. REQ-805는 지금까지 **훑을 대상이 0개라 공허 통과**였다. T11-1이 매니페스트를 만들었으니
이제 집행할 수 있다. 물은 것은 *"값이 한 곳에서 오는가"*(T11-1)가 아니라
**"그 값이 0인가 · 상시 과금 리소스를 만들지 않는가"**다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-97 | REQ-805 | 설계와 코드를 **함께** 1로 (일치 검사는 초록이다) | 1 failed | **red 확인** |
| M-98 | REQ-805 | 배포 스크립트가 **GKE 클러스터를 만든다** | 1 failed | **red 확인** |
| M-99 | REQ-805 | **설계 표**가 GKE를 구성 요소로 선언한다 | 1 failed | **red 확인** |

셋 다 복구 후 **180 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-97 — **기존 열 열 개가 전부 초록인 변이다**

T11-1은 `min-instances`를 두 자리에서 물었다: *"설계와 코드가 같은가"*(④)와
*"렌더러가 낸다"*(②). **M-97은 둘 다 만족시킨다** — 설계도 1, 코드도 1이니 같고,
렌더러는 그 상수를 그대로 실으니 낸다. 그때 `1`이라는 값은 게이트 어디에서도 안 걸린다.
⇒ 일치 검사는 **두 문서가 함께 틀리는 것**을 못 잡는다. 값을 묻는 자리가 따로 있어야 한다.

⚠️ M-88(코드만 1)과 다르다. M-88이 red인 이유는 *"설계와 다르다"*이지 *"1이다"*가 아니었다.
그 차이는 M-97에서만 보인다.

### ⚠️ M-98과 M-99는 **서로를 안 잡는다** — 그리고 순서는 늘 M-99가 먼저다

M-98은 스크립트가 만드는 것을, M-99는 우리가 만들기로 **선언한** 것을 본다.
사람이 먼저 고치는 자리는 설계이고, 그 상태에서 실행 표면 스캔은 **초록**이다.
⇒ 그 초록은 *"안 만든다"*가 아니라 *"스크립트는 아직 안 따라왔다"*이고, 그동안 사람은
콘솔에서 만든다. 손으로 만든 리소스는 계약이 없어서 자동 조치의 대상도 아니다(OVERVIEW §12).

### ⛔ 안 태운 자리가 하나 있다 — ①의 표본·바닥, 그리고 **왜 못 태웠는가**

새 파일의 ①(금지 목록의 바닥 + 정규식이 아직 잡는지 태우는 표본 넷)에는 변이가 없다.
목록을 비우거나 정규식 한 글자를 깨면 ③은 **0건을 잡고 초록**이 되는데, 그 경로를 태울
변이를 **못 넣었다**: `M-100`을 정의하는 순간 신선도 가드가 깨진다 —
`test_mutation_freshness.py`가 `max(cases)`를 **문자열로** 비교해서
`"M-100" < "M-99"`이고, 사용법 문자열과 안 맞는다고 red를 낸다.
⇒ **변이 번호의 천장은 M-99다.** 다음에 변이를 더 넣으려는 사람은 그 비교를 숫자로
고치는 것부터 해야 한다. 이건 이 태스크의 범위가 아니라서 **안 고치고 적어 둔다**(#3).

## 전체 스윕 — 2026-08-22 · 기준선 **180 passed**

M-01~M-99 **99종 전부 red 확인** · 복구 후 180 · 잔여 0 · **가려진 가드 없음**.

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-19 `22 failed + 10 errors` · M-13 14 · M-14 11 · M-26 11 · M-67 11 ·
M-01 `10 errors` · M-33 8 · M-12 7 · M-85 6 · M-03 5 · M-22 5 · M-05 4 · M-29 4 · M-30 4 ·
M-45 4 · M-61 4 · M-83 4**. 나머지는 1~3이다. 새 셋(M-97·M-98·M-99)은 **전부 1**이다.

### ⚠️ 직전 스윕(176)과 갈라진 자리는 **둘뿐이고, 둘 다 이번 변경이 설명한다**

`M-88 1→2 · M-92 1→2`. 이름을 적어 둔 나머지 열일곱은 **전부 같은 값**이다.

⛔ 둘은 REQ-805를 태우던 T11-1의 변이다. 새 가드의 ②(유휴 0을 **값으로**)가 같은 자리에서
같이 운다 — M-88은 상수를 1로 만들고, M-92는 렌더에서 그 플래그를 지운다. **둘 다 ②가 본다.**
⇒ T11-1이 적어 둔 *"이 층에는 겹침이 하나도 없다"*는 **이번에 두 자리에서 깨졌다.**
겹침은 결함이 아니라 층이 하나 늘었다는 뜻이다: 이제 `min-instances`는 **세 질문**이 본다 —
같은가(T11-1 ④) · 실리는가(T11-1 ②) · **0인가**(T11-2 ②).

### ⚠️ 169↔176에서 못 밝혔던 두 자리는 **이번에도 안 흔들렸다**

직전 기록이 원인을 못 밝힌 채 남겨 둔 `M-37 4→2 · M-85 4→6`은 이번에도 **2와 6**이다.
세 번째 독립 관측이고, *"169 기록의 두 숫자가 옮겨 적히며 틀렸다"*는 가설과 계속 맞는다.
⛔ **그래도 증명은 아니다.** 지난 스윕의 원본 로그는 여전히 없고, 하중 분포는 아직
게이트가 안 지킨다(T0-8의 구멍). ⇒ **이번 스윕부터 원본 로그를 커밋한다** —
[`mutation-sweep-2026-08-22-180.log`](mutation-sweep-2026-08-22-180.log) (99종 전부의 실제 출력).
다음에 갈라지면 **이번 것과는 되짚을 수 있다.** 위 숫자들은 그 로그에서 옮겨 적은 것이고,
옮겨 적는 그 행위가 지난번에 틀린 자리라서 **원본을 옆에 둔다.**

## 의존성 선언 — **일치는 설치된다는 뜻이 아니다** (T11-3 · REQ-601·801) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **185 passed**

T11-3. `google-adk`가 **어디에도 선언돼 있지 않았다.** REQ-601은 그것을 대회 필수로 이름하고
design 06§2는 `google-adk 2.7.1`을 실제로 설치해 introspect한 증거까지 남겼는데,
`pyproject.toml`은 그 이름을 한 번도 적지 않았다. 배포 이미지는 `pip install ".[cloud]"`
한 줄로 의존성을 얻는다 — **선언에 없으면 이미지에 안 실린다.**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-100 | REQ-601 | 선언에서 `google-adk`를 지운다 (**T11-3 이전의 상태 그 자체**) | 3 failed | **red 확인** |
| M-101 | REQ-601 | 이미지가 **extra 없이** 설치한다 (`.[cloud]` → `.`) | 1 failed | **red 확인** |
| M-102 | REQ-801 | `google-adk`를 **`[project].dependencies`로 옮긴다** (선언은 그대로) | 1 failed | **red 확인** |
| M-103 | REQ-801 | 소스가 **선언 없는** 서드파티를 임포트한다 (설치는 돼 있다) | 1 failed | **red 확인** |
| M-104 | REQ-801 | 새 배포판을 선언하고 스캐너에게는 안 가르친다 | 1 failed | **red 확인** |
| M-105 | REQ-801 | 공허 통과 — stdlib 판정을 깨서 **아무것도 안 잡게** 한다 | 1 failed | **red 확인** |

여섯 다 복구 후 **185 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-102 — **선언은 멀쩡한데 전제가 깨지는 자리**

`google-adk`를 cloud extra에서 `[project].dependencies`로 옮기면 *"선언돼 있는가"*(④)는
**초록이다.** 선언은 선언이니까. 그런데 게이트가 설치하는 `.[dev]`가 그 순간부터 클라우드
스택을 끌고 오고, *"게이트는 이것들 **없이** 통과한다"*(REQ-801)는 fake 어댑터의 전제가
조용히 깨진다. ⇒ **선언의 존재와 선언의 자리는 다른 질문이다.** ⑤만 뒤쪽을 본다.

⚠️ 그래서 ⑤는 extra 이름을 **자기가 안 적는다** — `Dockerfile`과 `Makefile`의 설치 줄에서
읽는다. 적으면 그 줄이 바뀌어도 옛 이름을 지키는 두 번째 사본이 된다(M-101이 그 자리다).

### ⚠️ M-103과 M-104는 **같은 어긋남의 양쪽 끝이다**

M-103은 *"임포트했는데 선언이 없다"*, M-104는 *"선언했는데 스캐너가 모른다"*.
⛔ 후자는 **오늘은 아무 증상이 없다** — 클라우드 스택 넷은 실물 어댑터가 없어서 아직 아무도
임포트하지 않고, 그래서 ②는 그 배포판에 대해 아무 말도 안 한다. 증상은 실물 어댑터를 쓰는
첫 커밋에서 나오고, 그때 잡히는 것은 **선언이 멀쩡한 임포트**다. 그 시차가 ③을 따로 둔 이유다.

### ⚠️ `M-100`을 정의할 수 있게 만든 것은 한 줄이다

T11-2는 이 자리의 변이를 **못 넣고 적어 두고 끝냈다** — `test_mutation_freshness.py`가
`max(cases)`를 **문자열로** 비교해서 `"M-100" < "M-99"`였다. 이번에 그 비교를 번호로 고쳤다
(`_number`). ⛔ 같은 계열의 두 자리 가정이 **하나 더 있었다**: M-49의 정규식이 `M-\d\d;`라서
목록의 마지막이 `M-105`가 되는 순간 **패턴이 안 맞아 조용히 무효**가 된다. 그 결과는 가드에
대한 판정이 아니라 하네스가 자기 자신을 못 깬 것이다 — `\d+`로 고쳤다.
⇒ **천장은 하네스의 성질이 아니라 두 개의 정규식이었다.**

### ⛔ 안 태운 자리 — `pyproject.description`

T11-3은 description도 현행화했다(구 정체성 `fleet ledger`가 남아 있었다). 그 줄에는 변이가
**없다** — *"현행인가"*는 오프라인 게이트가 판정할 수 있는 질문이 아니다(사람 판단).
⚠️ 다만 판정 가능한 이웃이 하나 있다: *"은퇴한 이름을 아직 말하는가"*. 이 저장소는 그 rot을
실제로 겪었다(`fleet-ledger` → `warranty` 이름 변경에 다섯 곳이 안 따라왔다 — spec_trace.py의
상자). **범위 밖이라 안 넣고 적어 둔다**(#3).

## 전체 스윕 — 2026-08-22 · 기준선 **185 passed**

M-01~M-105 **105종 전부 red 확인** · 복구 후 185 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-22-185.log`](mutation-sweep-2026-08-22-185.log).

### ⚠️ 이번 스윕의 하중 분포 — 관측이지 주장이 아니다

실제로 센 값: **M-19 `22 failed + 10 errors` · M-13 14 · M-14 11 · M-26 11 · M-67 11 ·
M-01 `10 errors` · M-33 8 · M-12 7 · M-85 6 · M-03 5 · M-22 5 · M-05 4 · M-29 4 · M-30 4 ·
M-45 4 · M-61 4 · M-83 4**. 나머지는 1~3이다. 새 여섯 중 **M-100만 3**이고 다섯은 1이다.

### ⛔ 처음으로 **두 로그를 직접 맞댔다** — 옮겨 적은 숫자가 아니라

직전 스윕(180)부터 원본 로그를 커밋하기로 했고, 이번이 그 결정이 처음 값을 내는 자리다.
두 로그에서 *"하중이 1이 아닌 변이"*를 뽑아 나란히 놓으면 **겹치는 45자리가 전부 같은 값**이고,
185 쪽에 하나(M-100 3)가 더 있을 뿐이다. ⇒ **이번엔 갈라진 자리가 하나도 없다.**

⚠️ 169↔176에서 원인을 못 밝힌 채 남겨 둔 `M-37 · M-85`는 이번에도 **2와 6**이다(네 번째 관측).
⛔ 여전히 증명은 아니다 — *"169 기록의 두 숫자가 옮겨 적히며 틀렸다"*는 가설은 169 스윕의
원본이 없는 한 확인할 수 없다. 다만 **이제는 다음 번에 갈라지면 되짚을 수 있다.**

### ⚠️ 새 여섯이 **기존 어느 자리와도 안 겹친다**

T11-2가 `min-instances`에서 겪은 겹침(M-88·M-92 `1→2`)이 이번엔 없다. 기존 104자리 중
값이 움직인 것은 **하나도 없다.** ⇒ 의존성 선언은 이 저장소가 아직 아무도 안 보던 층이었고,
새 다섯 개의 질문은 **각자 혼자 서 있다.** 하나를 지우면 그 자리는 조용히 사라진다 —
M-100만 셋이 함께 우는데, 그건 *"선언에 없다"*가 세 방향(③ 매핑 · ④ 값 · ⑤ 이미지)에서
동시에 보이는 어긋남이기 때문이다.

## 장애 주입 앱 — **장애의 크기가 서사를 만든다** (T11-4 · REQ-803·304) — 2026-08-22 · 로컬 macOS · py3.13 · 기준선 **192 passed**

T11-4. 데모의 절정은 *"검증이 실패하고 롤백이 되돌린다"*인데, 그 실패를 정직하게 만들려면
**진짜로 신호를 악화시키는 리비전**이 있어야 한다(design 11§1 원칙 5). 그 리비전이 지금까지
어디에도 없었다 — 데모는 신호 셋을 손으로 적어 두고 있었고, `demo-target`은 **이름뿐**이었다.
`src/warranty/demo_target.py`가 두 리비전을 갖고, 데모의 각본이 그 앱에서 온다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-106 | REQ-304 | 장애를 **노이즈보다 작게** 줄인다 (900 → 640) | 1 failed | **red 확인** |
| M-107 | REQ-803 | 나쁜 리비전이 **헬스 프로브에서 죽는다** | 1 failed | **red 확인** |
| M-108 | REQ-803 | 지연을 **선언만 하고 안 쓴다** (`pause` 호출 삭제) | 1 failed | **red 확인** |
| M-109 | REQ-803 | 장애 주입이 신호를 **좋게** 만든다 (900 → 200) | 4 failed | **red 확인** |
| M-110 | REQ-803 | 데모가 기준선을 **다시 적는다** (값은 오늘 똑같다) | 1 failed | **red 확인** |
| M-111 | REQ-803 | 공허 통과 — 두 리비전이 **같은 이름**을 갖는다 | 1 failed | **red 확인** |
| M-112 | REQ-803 | 모르는 경로도 **작업으로 친다** (404 분기 삭제) | 1 failed | **red 확인** |

일곱 다 복구 후 **192 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-106 — **판정 기준으로는 안 보이는 자리**

계약의 회복 기준은 *"50% 개선"*이다. 그 기준은 **"안 나아졌다"와 "더 나빠졌다"를 구별하지
않는다** — 장애를 900에서 640으로 줄여도 판정은 여전히 `not_recovered`고, 검증 실패도
자동 롤백도 그대로 일어난다. 화면은 멀쩡하다.

⛔ 그런데 롤백 **후** 재측정은 다른 잣대를 쓴다 — 기준선의 ±15% 안이면 *"돌아왔다"*다
(`_within`). 640은 620의 3.2%다. ⇒ **되돌리지 않아도 돌아온 것으로 읽힌다.**
그 순간 REQ-304(*"롤백도 검증한다"*)는 늘 참인 문장이 되고, 그 초록은 조치가 아무것도
안 해도 난다. **장애의 크기를 묻는 자리는 하나뿐이고**, 그 검사는 허용 오차를 자기가 안
적는다 — `_within`을 그대로 부른다(사본을 두면 판정 쪽이 오차를 넓혀도 옛 숫자를 지킨다).

### ⛔ M-110 — 값 검사가 **전부 초록인** 자리

`BASELINE_MS = Decimal("620")`은 오늘 이 앱의 지연과 값이 같다. 그래서 *"각본이 앱과
맞는가"*를 값으로 물으면 **네 개의 단언이 전부 통과한다.** 갈라지는 것은 앱의 지연을
바꾸는 날이고, 그때 데모는 **존재하지 않는 리비전의 이야기**를 하며 게이트는 초록이다.
⇒ T11-1(`min-instances` 사본)·T11-3(`google-adk` 선언)과 **같은 계열의 세 번째 자리**다:
물어야 하는 것은 *"값이 같은가"*가 아니라 **"어디서 온 이름인가"**이고, 그건 구문으로만
물어진다(`test_tunables.py`의 `_unsourced_sinks`가 먼저 쓴 규칙이다).

### ⚠️ M-109만 시끄럽다 — 그리고 그게 정보다

새 일곱 중 여섯은 하중이 1이다. M-109(장애가 신호를 **좋게** 만든다)만 넷이 함께 운다 —
앱의 두 테스트와 데모 서사의 두 테스트다. ⇒ **장애의 방향**은 이 저장소가 여러 층에서
기대는 값이고, 크기와 달리 뒤집히면 어디서든 보인다. 조용한 것은 크기 쪽이다(M-106).

### ⛔ 안 태운 자리 — 실물 `pause`

이 앱에는 **벽시계를 만지는 코드가 없다.** 지연을 실제로 쓰는 구현은 주입받고, 그것을 주는
쪽은 배포 진입점(T2-3)이다 — 여기 두면 게이트가 그만큼 느려지고 결정론이 샌다(REQ-802).
⚠️ 그래서 *"배포된 프로세스가 진짜로 그만큼 느린가"*를 태우는 변이는 **없다.** 그 질문은
오프라인 게이트가 판정할 수 없다(#3). 여기서 확인된 것은 *"쓰겠다고 넘긴 값"*까지다.

## 전체 스윕 — 2026-08-22 · 기준선 **192 passed**

M-01~M-112 **112종 전부 red 확인** · 복구 후 192 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-22-192.log`](mutation-sweep-2026-08-22-192.log).

## README 재현 절차 — **적어 둔 명령이 실재하는가** (T11-5 · REQ-901) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **197 passed**

T11-5. README는 이 저장소에서 **아무 게이트도 안 지나는 유일한 문서였다.** REQ-901은 제출물에
*재현 가능한 실행 절차*를 요구하고, 그 절차가 사는 곳이 거기다. `tests/test_readme_procedure.py`
다섯이 넷을 묻는다: ① 스캐너를 표본으로 태운다 ② 적어 둔 `make` 명령이 실재하는 타깃인가
③ 절차가 부트스트랩·게이트·**실행**을 이름으로 갖는가 ④ 가리키는 저장소 경로가 실재하는가.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-113 | REQ-901 | README가 **없는 타깃**을 적는다 (`trace` → `traceability`) | 1 failed | **red 확인** |
| M-114 | REQ-901 | Makefile에서 **타깃이 사라진다** (`demo:` → `run-demo:`) | 2 failed | **red 확인** |
| M-115 | REQ-901 | 공허 통과 — 재현 절차 블록을 **통째로 지운다** | 2 failed | **red 확인** |
| M-116 | REQ-901 | 절차에서 **실행만** 빠진다 (`make demo` 한 줄) | 1 failed | **red 확인** |
| M-117 | REQ-901 | 진입점이 **없는 문서를 가리킨다** | 1 failed | **red 확인** |
| M-118 | REQ-901 | 공허 통과 — 스캐너가 **아무 명령도 못 뽑는다** | 3 failed | **red 확인** |

여섯 다 복구 후 **197 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ 이 열이 **첫 실행에서 잡은 것** — 절차에 실행이 없었다

③을 처음 돌렸을 때 red였다. README의 재현 절차는 `make venv && make check`와 `make trace`
둘뿐이었고, **`make demo`가 없었다.** 그 둘은 *"이게 맞는가"*와 *"무엇을 약속했는가"*를
알려 준다 — *"이게 무엇을 하는가"*는 어디에도 없었다. 해커톤 제출물에서 그것은 재현 절차가
아니라 검사 절차다. ⇒ README에 한 줄을 넣었고, 그 자리를 지키는 것이 ③이다.

⚠️ **이건 오타가 아니라 빈칸이었다.** ②(적어 둔 것이 실재하는가)는 그때도 **초록이었다** —
적힌 둘 다 살아 있었으니까. 없는 것을 잡는 질문과 틀린 것을 잡는 질문은 다르다
(T11-2의 *"일치가 아니라 값"*, T11-3의 *"선언의 존재와 자리"*와 같은 계열의 네 번째 자리다).

### ⛔ M-114 — **반대 방향에서도 같은 자리가 울어야 한다**

M-113은 README를 깨고 M-114는 Makefile을 깬다. 둘 다 ②가 잡지만 **고쳐야 하는 쪽이 다르다** —
전자는 README의 오타고, 후자는 *"타깃 이름을 바꾸고 README를 안 고쳤다"*이다. 후자가 실제로
났던 모양이고(죽은 `make demo`), 이름을 바꾼 사람은 자기가 README를 깼다는 걸 모른다.
⚠️ 그래서 ②의 실패 메시지는 **값의 출처가 Makefile 하나**라고 말한다 — 어느 쪽을 고칠지는
그 문장이 정한다.

⚠️ **M-114는 이 열에서 유일하게 겹치는 자리다** — 예측은 1이었는데 실측이 2였다.
`test_demo.py::test_req_803_declared_entrypoint_runs`가 함께 운다. 그 열은 `make demo`를
**subprocess로 실제로 돌린다**(REQ-803이 결정론을 요구하니까). ⇒ `demo` 타깃의 *존재와 동작*은
이미 지켜지고 있었다. **새로 생긴 것은 README 쪽이다** — M-113·M-116은 하중이 1이고, 그 자리를
보는 열은 이 파일이 처음이다. 나머지 넷도 전부 1~3이며 기존 어느 자리와도 안 겹친다.

### ⛔ M-118 — 스캐너는 **스캐너로 검사할 수 없다**

`MAKE_RE`를 한 글자 깨면 README에서 뽑히는 명령이 0개가 되고, ②는 *"죽은 명령이 없다"*로,
③은... 아니다, ③은 운다(필수 셋이 안 보이니까). 우는 것은 ①·②의 바닥과 ③ **셋**이다.
⇒ 바닥을 **기대값이 박힌 표본**으로 두는 것이 유일한 방법이다(M-105가 먼저 쓴 규칙).
⚠️ 표본은 실패해야 하는 것들을 **일부러 담는다** — 산문의 `make sure`, 주석 뒤의 `make ghost`,
외부 URL 셋이다. 그 셋이 안 걸러지면 나머지 검사는 판정이 아니다.

### ⚠️ 산문을 안 보는 것은 **정확성이 아니라 지속성**의 결정이다

`make`는 영어 동사다. 산문까지 훑으면 `make sure`·`make it work`가 전부 타깃이 되고, README의
영어 문장을 고칠 때마다 게이트가 red다. ⛔ **그런 가드는 꺼진다.** 그래서 스캐너는 펜스 블록과
인라인 코드만 본다 — 대신 **코드 문맥 밖에 적힌 명령은 안 잡힌다.** 적어 둔다(#3).

### ⛔ 안 태운 자리 — *"그 명령이 성공하는가"*

이 열이 태우는 것은 **이름이 실재하는가**까지다. `make check`가 자기를 부르면 게이트가
자기 자신을 재귀로 돌리고, `make venv`는 네트워크를 쓴다(REQ-801). ⇒ 죽은 *타깃*은 잡히고
죽은 *동작*은 안 잡힌다.

⚠️ **셋 중 하나는 예외다** — `make demo`의 동작은 REQ-803이 이미 subprocess로 태운다
(M-114가 그것을 드러냈다). 안 태워지는 것은 `venv`(네트워크)와 `check`(재귀) 둘이고,
그 둘은 **오프라인 게이트가 원리상 못 여는 자리**다. 실물 배포 절차 쪽은 T2-4가 태운다.
⚠️ **REQ-901의 나머지 넷**(저장소 URL · 데모 영상 · 아키텍처 다이어그램 · 실물 실행의 시각
증거)도 오프라인 게이트의 질문이 아니다 — 그래서 `Verifies:`를 안 붙였고 상태도 안 올렸다.

## 전체 스윕 — 2026-08-23 · 기준선 **197 passed**

M-01~M-118 **118종 전부 red 확인** · 복구 후 197 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-197.log`](mutation-sweep-2026-08-23-197.log) (591줄 · 118 case).

### ⛔ 직전 스윕의 원본 로그가 **잘려 있다** — 이번에 발견했다

`mutation-sweep-2026-08-22-192.log`는 **97줄 · 20 case**다(M-20에서 끊긴다). 그런데 192 스윕
절은 그 파일을 *"M-01~M-112 112종 전부 red 확인"*의 **원본으로 인용한다.** ⇒ 그 문장이
거짓이라는 뜻은 아니다 — 무인 루프가 스윕 도중 `STOP`을 맞아 tee가 끊긴 모양이고, 실제로
이번 118종 전체 스윕이 그 범위를 **전부 다시 red로 확인했다.** 틀린 것은 주장이 아니라
**증거의 사슬**이다.

⛔ **그리고 그것을 묻는 가드가 없다.** 신선도 가드는 최신 스윕 절이 *무엇을 주장하는지*는
보지만, 그 절이 **인용한 로그가 그 범위를 담는지**는 안 본다. ⇒ 잘린 로그를 커밋해도 게이트는
초록이다. T11-2가 *"이번부터 원본 로그를 커밋한다"*고 정한 규칙이 **한 판 만에 조용히 샜다.**
범위 밖이라 안 고치고 **T11-6으로 심었다**(docs/PRINCIPLES.md #3).

⚠️ **오래된 절은 안 고쳤다** — 192 절은 그때 참이라고 믿은 것을 적은 역사다(append-only).
지금 참인 문장은 이 절이 갖는다.

### ⚠️ 하중 분포 — 겹치는 자리 18종이 **전부 같은 값**

직전 원본이 M-20까지뿐이라 맞댈 수 있는 것은 M-01~M-19(공통 18종)뿐이었고, **갈라진 자리는
0이다.** 새 여섯은 M-114만 기존 자리와 겹친다(REQ-803의 진입점 열). 나머지 다섯은 하중 1~3에
독립이다. ⛔ 185↔192에서 못 밝힌 두 자리(M-37·M-85)는 이번에도 **2와 6**이다 — 다섯 번째
관측이고 여전히 증명이 아니다. 이번 로그는 온전하니 다음 판은 되짚을 수 있다.

## 스윕 원본 로그의 범위 집행 — **인용한 증거가 그 스윕을 담는가** (T11-6 · REQ-802) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **203 passed**

T11-6. 바로 위 절이 발견한 자리다 — 192 로그는 **97줄 · 20 case**인데 192 스윕 절은 그것을
*"112종 전부 red 확인"*의 원본으로 인용한다. 신선도 가드는 최신 절이 **무엇을 주장하는지**는
보지만 **인용한 로그가 그 주장을 담는지**는 안 본다. `tests/test_sweep_log_integrity.py`
여섯이 넷을 묻는다: ① 스캐너를 표본 셋으로 태운다 ② 인용이 있고 그 파일이 실재하는가
③ 선언한 case를 **전부** 담는가 ④ 열린 블록 0 · `❌` 0.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-119 | REQ-802 | 닫힘 표지가 **아무 데나 걸린다** (잘림이 안 보인다) | 1 failed | **red 확인** |
| M-120 | REQ-802 | 실패 표지를 **못 알아본다** (`❌` → `❌❌`) | 1 failed | **red 확인** |
| M-121 | REQ-802 | 공허 통과 — 스캐너가 **case를 하나도 못 뽑는다** | 5 failed | **red 확인** |

셋 다 복구 후 **203 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ 이 가드는 워킹트리가 아니라 `HEAD`를 읽는다 — **앞선 한 판이 여기서 되돌아갔다**

2026-08-23에 한 판이 이것을 만들었고 **되돌렸다.** 버그가 아니라 **자기참조**였다: 로그는
스윕이 만들고, 스윕은 변이 하나마다 이 스위트를 **세 번** 지난다. 워킹트리를 읽으면
*"인용한 로그가 다 차 있는가"*는 **그 로그가 만들어지는 동안 이미 참이어야 한다** —
tee가 첫 줄을 쓰는 순간부터 스윕이 끝날 때까지 121 case가 전부 red로 오염된다. 수렴점이 없다.

⇒ `git show HEAD:`로 **커밋된 증거**를 읽는다. `HEAD`는 스윕이 도는 내내 얼어 있으므로 답이
스윕 중이든 아니든 같다. **이번 판이 그 수렴을 실제로 보였다**: 새 절이 아직 존재하지 않는
`…-203.log`를 인용하는 상태에서 게이트는 **초록이었고**(HEAD는 197 절과 온전한 197 로그를
본다), 그 초록 위에서 121종 스윕이 돌아 로그를 만들었다.

⚠️ **대가는 한 커밋의 지연이다.** 잘린 로그를 커밋하는 그 커밋의 게이트는 초록이고,
**바로 다음 게이트가 red**다. 없앨 수 있는 지연이 아니다 — 위의 자기참조가 그것을 산다.
무인 루프에서 그 red는 *깨끗한 워킹트리에서 나는 red*이고, 그건 멈추라는 신호다(그리고
192 사고가 실제로 그 모양이었다 — 커밋은 됐고, 아무도 다음 판에 안 물었다).

### ⛔ M-121 — ④는 **조용해진다**, 그래서 표본이 바닥이다

`CASE_RE`를 한 글자 깨면 case가 0개가 되고, 우는 것은 **다섯**이다(표본 셋 · 바닥 · ③).
⛔ **④(열린 블록 0 · `❌` 0)는 초록이다** — 볼 블록이 없으면 열린 블록도 없다. 즉 ④의 초록은
*"증거가 온전하다"*가 아니라 **"아무것도 안 봤다"**일 수 있고, 그 둘을 가르는 것은 ④가 아니라
**기대값이 박힌 표본**이다(M-105·M-118이 먼저 쓴 규칙). 스캐너는 스캐너로 검사할 수 없다.

### ⚠️ 안 태운 자리 — *"인용이 있어야 한다"*

②는 *"인용했다면 그 파일이 실재하고 온전해야 한다"*까지다. **인용을 통째로 빼면** 그 자리는
red지만(`cited_log` 픽스처가 운다), 오래된 절 23개 중 원본을 인용하는 것은 **넷뿐이다** —
T11-2 전에는 규칙이 없었다. 그래서 *"모든 절이 인용해야 한다"*는 소급해서 집행할 수 없고,
집행하는 것은 **최신 절 하나**다(신선도 가드와 같은 경계). 적어 둔다(docs/PRINCIPLES.md #3).

## 전체 스윕 — 2026-08-23 · 기준선 **203 passed**

M-01~M-121 **121종 전부 red 확인** · 복구 후 203 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-203.log`](mutation-sweep-2026-08-23-203.log) (606줄 · 121 case).

### ⚠️ 하중 분포 — 겹치는 118종이 **전부 같은 값**

직전(197) 원본이 이번엔 온전하니 **118종 전부를 맞댈 수 있었다** — 잘린 로그로는 18종밖에
못 맞대던 자리다. **갈라진 자리는 0이다.** 새 셋(M-119·M-120·M-121)은 하중 1·1·5이고
기존 어느 자리와도 안 겹친다 — 이 로그를 읽는 열이 이 파일에 처음 생겼으니 당연한 값이다.
⛔ M-37·M-85는 이번에도 **2와 6**이다 — **여섯 번째 관측**이고 여전히 증명은 아니다.

## HTTP 서버 — **이미지가 포트를 여는가** (T12-1 · REQ-602) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **216 passed**

T12-1. 직전까지 이미지의 `CMD`는 **오프라인 데모**였다. 데모는 출력을 찍고 정상 종료한다 —
컨테이너가 포트를 한 번도 안 열어서 **포트 프로브에서 죽는다.** T11-1이 예고해 둔 함정이고,
그 실패는 빌드에서도 게이트에서도 안 보이며 **첫 배포에서만** 보인다.
`src/warranty/server.py`(stdlib만)와 `tests/test_http_server.py` 열셋이 여덟을 묻는다:
① 설계 표 파서가 실제로 읽는가 ② `ROUTES`가 design 08§3 표와 **같은 집합**인가
③ `/healthz`가 `200`인가 ④ 배선 안 된 경로가 `501`인가 ⑤ 미등록 `404` · 잘못된 메서드 `405`
⑥ 포트가 `config.py` 한 곳에서 오는가 ⑦ 실제 핸들러가 **바이트를 옳게 내는가**
⑧ `CMD`가 **서버**를 가리키는가.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-122 | REQ-602 | 헬스 경로가 `200`을 안 낸다 | 2 failed | **red 확인** |
| M-123 | REQ-602 | 라우터에서 경로 한 행이 사라진다 (코드→설계) | 1 failed | **red 확인** |
| M-124 | REQ-602 | ★ 배선 안 된 경로가 `200`을 낸다 | 6 failed | **red 확인** |
| M-125 | REQ-602 | `load_port`가 주입된 값을 무시한다 | 1 failed | **red 확인** |
| M-126 | REQ-602 | ★ `CMD`가 데모로 돌아간다 | 1 failed | **red 확인** |
| M-127 | REQ-602 | 설계 표에서 행 하나가 사라진다 (설계→코드) | 1 failed | **red 확인** |
| M-128 | REQ-602 | 공허 통과 — 설계 표 파서가 한 행도 못 뽑는다 | 2 failed | **red 확인** |
| M-129 | REQ-602 | 미등록 경로를 `405`로 낸다 | 1 failed | **red 확인** |
| M-130 | REQ-602 | 응답에 `Content-Length`가 안 실린다 | 1 failed | **red 확인** |
| M-131 | REQ-602 | 요청 본문을 안 읽는다 | 1 failed | **red 확인** |
| M-132 | REQ-602 | 산출물이 포트를 다시 적는다 | 1 failed | **red 확인** |

열하나 다 복구 후 **216 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-126 — **함정을 통과시킨 초록이 이미 있었다**

`test_deploy_artifacts` ⑥은 *"`CMD`가 가리키는 모듈이 실재하는가"*를 묻는다. `warranty.demo`는
**실재한다.** 그래서 그 가드는 이 함정에 대해 처음부터 초록이었고, 앞으로도 초록이다.
⇒ *있는 모듈을 가리키는 것*과 *서버를 가리키는 것*은 **다른 질문**이고, ⑧만 후자를 본다.
⚠️ ⑧은 기대값을 문자열로 안 적는다 — 서버 모듈이 `__name__`으로 자기 이름을 말한다.

### ⛔ M-124 — 가장 위험한 초록은 **`501`을 `200`으로 바꾸는 것**이다

실물 어댑터가 아직 없다(REQ-601·602는 TODO). 여기서 fake를 물려 `200`을 내면 이 서비스는
*"돌아간다"*와 *"가짜로 돌아간다"*를 **구별할 수 없게 된다** — design 08§5가 `WR_ADAPTERS`의
기본값을 `fake`로 두지 않는 이유가 정확히 그것이다. 그 금지를 서버 계층에서 우회하지 않는다.
⇒ 답할 수 없는 경로는 **시끄럽게** 답할 수 없다고 말한다(docs/PRINCIPLES.md #3).

### ⚠️ M-127·M-128 — 같은 열을 **다른 방향에서** 태운다

M-123은 코드에서 행을 지우고, M-127은 **설계에서** 지운다. 둘 다 ②가 잡지만 메시지가 다르고,
고칠 곳도 다르다. M-128(파서가 0행)은 ②도 함께 울리지만 **①이 우는 것이 요점이다** —
①이 없으면 *"설계가 지워졌다"*와 *"파서가 깨졌다"*가 같은 red로 보인다.

### ⛔ 이 판이 **M-90의 전제를 지웠다** — 스윕이 그것을 잡았다

M-90(T11-1)은 `CMD`를 `warranty.demo` → `warranty.server`로 바꿔 *"없는 모듈을 가리킨다"*를
태우던 변이다. 그때 `warranty.server`는 **없는 모듈이었다.** T12-1이 그것을 만들고 `CMD`를
거기로 옮기자 ① 패턴이 안 맞고 ② 맞았더라도 ⑥은 **초록**이다 — 겨냥하던 것은 이름이 아니라
**부재**였는데, 그 부재가 사라졌다.

⚠️ 게이트는 이 어긋남에 대해 **아무 말도 안 했다**(`make check`는 내내 초록이었다).
잡은 것은 스윕의 *"변이가 파일을 바꾸지 못했다 — 이 결과는 판정이 아니다"* 한 줄이다.
그 한 줄이 없었으면 M-90은 **조용히 무효인 변이**로 남아, 기록의 *"132종 전부 red"*는
131종에 대한 문장이 됐을 것이다(하네스 자신의 여덟 번째 결함과 같은 모양).
⇒ 목적지를 **실재하지 않는 이름**(`warranty.ghost`)으로 옮겼다. M-126과 겹치지 않는다:
저기는 *있는데 틀린* 모듈(⑧), 여기는 *없는* 모듈(⑥)이다.

### ⚠️ 안 태운 자리 — 자리표시자의 콜론

`PARAM_CHARS`를 `[^/]+`로 넓혀도 **red가 안 난다**: `/ledger/{entry_id}`와
`/ledger/{entry_id}:approve`는 **메서드가 달라서**, 콜론을 먹어도 `POST` 승인 요청은 여전히
승인 경로로 간다. 하중을 안 받는 자리에 변이를 만들면 그 변이는 *"가드가 있다"*를 거짓으로
말하게 된다 ⇒ 안 만들었다. ⑤의 셋째 줄은 **회귀 검사**지 집행이 아니다.

### ⛔ 이 절이 확인하지 **않은** 것

소켓을 한 번도 안 열었다 — 무인 루프의 권한 경계가 그것을 막고(D14), 막지 않더라도 포트를
여는 테스트는 게이트를 기계 상태에 묶는다. 핸들러는 `__new__`로 만들고 파일 객체 둘을 물려
돌렸다. ⇒ 여기서 확인된 것은 *"핸들러가 옳은 바이트를 낸다"*이지 **"Cloud Run이 그 포트를
프로브한다"**가 아니다. 후자는 T2-2에서만 확인된다(docs/PRINCIPLES.md #3).

## 전체 스윕 — 2026-08-23 · 기준선 **216 passed**

M-01~M-132 **132종 전부 red 확인** · 복구 후 216 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-216.log`](mutation-sweep-2026-08-23-216.log) (660줄 · 132 case).

### ⛔ 첫 스윕은 exit 1이었다 — **M-90이 무효였다**

한 번 더 돌린 이유가 그것이다. 첫 판은 M-90에서 *"변이가 파일을 바꾸지 못했다"*를 찍고
exit 1로 끝났다(위 절 참조). 목적지를 고친 뒤 두 번째 판이 **132종 전부 red · `❌` 0 · exit 0**이다.
⚠️ 첫 판의 로그는 **안 남긴다** — 판정이 아닌 결과를 증거로 커밋하면 그게 192 사고다.

### ⚠️ 하중 분포 — **이번엔 안 맞댔다**

직전(203) 로그와 겹치는 121종의 하중을 이번에는 **비교하지 않았다.** 두 로그를 맞대는 것은
스크립트 한 줄인데, 무인 루프의 권한 경계가 그 파이프라인을 막는다(D14). 손으로 121쌍을
세어 적는 것은 이 저장소가 T0-6에서 정확히 금지한 짓이다 ⇒ **안 세고 안 적는다.**
⛔ 그러므로 이 절은 *"가려진 가드가 없다"*를 **스윕 결과로만** 말한다 — 132종이 전부 red고
`❌`가 0이다. *"하중 분포가 안 바뀌었다"*는 **말하지 않는다**(docs/PRINCIPLES.md #3).
사람이 여는 자리다.

## 판정 어댑터 — **모델이 판단하고 근거가 남는가** (T12-2 · REQ-204) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **241 passed**

T12-2. 여기까지 이 저장소의 `ModelPort` 구현은 **대역 하나뿐**이었고(`ScriptedModel`),
원장은 늘 `decided_by: rule`이었다 — *"이거 어디가 에이전트냐"*에 답하는 자리가 비어 있었다.
`src/warranty/adapters/model_judge.py`가 그 오프라인 절반이다: **무엇을 물었나(프롬프트) ·
답을 어떻게 읽나(파싱) · 못 읽으면 무엇으로 적나(폴백).** 전송은 포트 뒤이고 게이트는
그 자리를 실물로 안 채운다(REQ-801). `tests/test_model_judge.py` 스물다섯이 넷을 묻는다:
① 프롬프트가 두 측정과 기준을 싣고 닫히지 않는 답을 선택지로 주지 않는가
② 못 읽을 답을 **거절하는가**(아홉 가지를 값으로 태운다)
③ 파싱 실패가 **롤백을 여는 쪽**으로 접히고 근거·사용량이 함께 남는가
④ 그 근거가 **원장 항목에** 실리는가(전송→`PromptedJudge`→`MeteredModel`→`Remediator` 조립).

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-133 | REQ-204 | 모델이 `ambiguous`로 되돌려줘도 통과한다 | 3 failed | **red 확인** |
| M-134 | REQ-204 | ★ 파싱 실패가 조용히 `recovered`가 된다 | 2 failed | **red 확인** |
| M-135 | REQ-204 | 폴백이 근거를 안 남긴다 | 3 failed | **red 확인** |
| M-136 | REQ-204 | 빈 근거를 통과시킨다 | 1 failed | **red 확인** |
| M-137 | REQ-204 | 프롬프트가 재측정 대신 기준선을 두 번 싣는다 | 1 failed | **red 확인** |
| M-138 | REQ-204 | 폴백이 원문을 안 싣는다 | 1 failed | **red 확인** |
| M-139 | REQ-204 | 코드펜스를 안 벗긴다 | 1 failed | **red 확인** |
| M-140 | REQ-603 | 판정 호출이 사용량을 0으로 지어낸다 | 2 failed | **red 확인** |

여덟 다 복구 후 **241 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-134 — 가장 위험한 초록은 **폴백의 방향**이다

파싱 실패를 `recovered`로 접으면 못 읽은 응답 하나가 *"나아졌다"*가 된다. 그 조치는
롤백 없이 남고(REQ-302), 원장에는 `improved=true`로 실리며, 리포트의 회복률만 오른다 —
**이 프로젝트가 겨냥하는 실패(조용한 성공) 그 자체다.** 그리고 하필 모델이 불리는 구간은
정의상 **판단이 어려운 구간**(tolerance 안쪽)이라, 그 조용함이 가장 두꺼운 자리에 앉는다.
⇒ 보수적이라는 것은 *"덜 주장한다"*가 아니라 **롤백을 연다**는 뜻이다.

### ⛔ M-139 — 정상 응답이 **전부** 폴백이 되는 실패는 초록으로 보인다

모델은 JSON을 코드펜스로 감싸 답하는 쪽이 흔하다. 안 벗기면 파싱은 **항상** 실패하고
어댑터는 **항상** 보수적으로 닫는다. 루프는 계속 돌고, 롤백은 늘 열리고, 판정은 늘
그럴듯하다 — 그런데 모델은 한 번도 판단한 적이 없다. 원장의 `decided_by: model`이
그때부터 거짓말이 된다. ⚠️ 이것이 이 절에서 **fake로는 절대 안 잡히던** 자리다:
대역은 우리가 정해 준 문자열을 내므로 펜스를 안 씌운다.

### ⚠️ M-135·M-136 — 같은 구멍의 **양쪽 끝**이다

`Verification`은 *"모델이 판정했는데 근거가 없다"*를 예외로 막는다. M-136(파서가 빈 근거를
통과시킨다)은 그 예외를 **조치 한 건을 날리는 사고**로 만들고, M-135(폴백이 근거를 안 남긴다)는
같은 예외를 **응답이 이상할 때마다** 만든다. 둘 다 red지만 고칠 곳이 다르다 —
전자는 파서, 후자는 폴백이다.

### ⛔ 이 절이 확인하지 **않은** 것

**모델을 한 번도 안 불렀다.** 전송은 대역이고, 확인된 것은 *"우리가 이런 프롬프트를 만들고
이런 응답을 이렇게 읽는다"*까지다 — *"Gemini가 이 프롬프트에 이렇게 답한다"*는 **미확인**이고
그것은 REQ-601(T2-1)이 소유한다. ⇒ `decided_by: model`인 행은 아직 이 저장소에서
**한 번도 실물 모델이 만든 적이 없다**(docs/PRINCIPLES.md #3).
⚠️ 프롬프트 문구의 **품질**도 안 물었다 — 그것은 실물 응답 없이는 판정이 안 되는 종류이고,
무인 루프가 손대면 안 되는 자리다(`[manual]`).

## 전체 스윕 — 2026-08-23 · 기준선 **241 passed**

M-01~M-140 **140종 전부 red 확인** · 복구 후 241 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-241.log`](mutation-sweep-2026-08-23-241.log) (700줄 · 140 case).

### ⚠️ 하중 분포 — **이번에도 안 맞댔다**

직전(216) 로그와 겹치는 132종의 하중을 이번에도 **비교하지 않았다.** 이유는 T12-1 절과 같다 —
두 로그를 맞대는 파이프라인이 무인 루프의 권한 경계 밖이고(D14), 132쌍을 손으로 세어 적는 것은
T0-6이 금지한 짓이다. ⛔ 그러므로 이 절은 *"가려진 가드가 없다"*를 **스윕 결과로만** 말한다 —
140종이 전부 red고 `❌`가 0이다. *"하중 분포가 안 바뀌었다"*는 **말하지 않는다**(PRINCIPLES #3).

## ADK 에이전트 배선 — **무엇을 조립하는가** (T12-3 · REQ-601) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **249 passed**

T12-3. `google-adk`는 게이트에 **안 깔린다**(cloud extra · REQ-801). 그러니 태울 자리는
라이브러리 위가 아니라 **아래**다: `src/warranty/adapters/adk_agent.py`가 생성자에 실릴 인자를
**값으로** 낸다(`agent_kwargs`·`runner_kwargs`), 라이브러리를 부르는 두 함수만 지연 임포트한다.
`tools/deploy_plan.py`가 `gcloud` 인자에 대해 한 것과 같은 수법이다(T11-1).
`tests/test_adk_agent.py` 여덟이 일곱을 묻는다: ① 설계 도구 표 파서가 실제로 읽는가
② 명세의 이름·도구가 design 06§3과 같은가 ③ 모델이 **설정에서** 오는가
④ 명세와 다른 함수를 붙이면 거부하는가 ⑤ `Runner`에 `session_service`가 실리는가
⑥ 모듈이 `google`을 **최상위에서** 임포트하지 않는가(그리고 지연 임포트가 실재하는가)
⑦ 지시가 선언한 도구를 전부 이름으로 말하는가.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-141 | REQ-601 | 명세의 도구가 설계와 다르다 | 1 failed | **red 확인** |
| M-142 | REQ-601 | 공허 통과 — 설계 도구 표 파서가 0개를 읽는다 | 2 failed | **red 확인** |
| M-143 | REQ-802 | ★ 모델 id가 코드에 박힌다 | 1 failed | **red 확인** |
| M-144 | REQ-601 | 명세와 다른 함수를 붙여도 통과한다 | 1 failed | **red 확인** |
| M-145 | REQ-601 | ★ `Runner`에 세션 서비스가 안 실린다 | 1 failed | **red 확인** |
| M-146 | REQ-801 | ★ 클라우드 스택을 최상위에서 임포트한다 | 9 failed | **red 확인** |
| M-147 | REQ-601 | 지시가 도구 하나를 이름으로 안 부른다 | 1 failed | **red 확인** |
| M-148 | REQ-801 | 공허 통과 — 지연 임포트가 아예 없어진다 | 1 failed | **red 확인** |

여덟 다 복구 후 **249 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-146·M-148 — **같은 검사의 양쪽 끝**이다

⑥이 묻는 것은 *"최상위에 `google` 임포트가 없다"*인데, 그 문장은 **배선이 통째로 없어도 참**이다.
M-148이 정확히 그 상태를 만든다: 지연 임포트를 지우면 최상위는 여전히 깨끗하고, 게이트는
가장 조용한 초록을 낸다 — 그런데 이 태스크가 만들려던 것은 사라졌다. 그래서 ⑥은 반대 방향도
함께 묻는다(*"어딘가에는 있어야 한다"*). M-146은 그 반대편이다: 최상위로 올리면 게이트는
**수집 단계에서 통째로** 죽는다. 한쪽은 너무 조용하고 한쪽은 너무 시끄러우며, 둘 사이의
좁은 자리만이 REQ-801을 만족한다.

### ⛔ M-145 — 게이트가 **볼 수 없는 실패**를 값으로 끌어온 자리다

probe 로그 ②가 적어 둔 사실: `Runner.__init__`의 `session_service`는 **기본값이 없다.**
안 넘기면 `TypeError`이고, 그 예외는 임포트에도 빌드에도 안 나타난다 — **첫 실물 호출**에서만
난다. 그때는 이미 이미지가 올라가 있고, 보이는 것은 *"에이전트가 안 뜬다"*뿐이다.
⇒ 생성자 인자를 **딕셔너리로 렌더**했기 때문에 게이트가 `google-adk` 없이도 그 자리를 물 수 있다.
이것이 이 절 전체의 수법이고, `deploy_argv`가 `--min-instances`에 대해 한 것과 같다.

### ⚠️ M-143 — 여기서 red인 것은 **사본이 아니라 우선순위**다

모델 id는 지금 `.env.example`·테스트 픽스처·design 06 넷에 산다. 그 사본들이 서로
어긋나는지 집행하는 자리는 **T12-6이고 여기가 아니다.** M-143이 잡는 것은 다른 문장이다:
*"명세가 설정을 안 읽고 자기가 적은 값을 쓴다."* 그러면 `WR_MODEL`을 바꿔 배포해도
에이전트는 옛 모델을 부르고, 그 어긋남은 로그가 아니라 **청구서**에서 보인다.

### ⛔ 이 절이 확인하지 **않은** 것

**ADK를 한 번도 임포트하지 않았다.** 게이트에 그 패키지가 없으니 당연하고, 그것이 REQ-801이다.
⇒ 확인된 것은 *"우리가 이런 인자로 조립한다"*까지고, *"그 인자를 그 라이브러리가 받는다"*는
**미확인**이다. 후자의 근거는 `docs/evidence/adk-api-probe-2026-08-19.log`(별도 venv에서
introspect한 기록)이지 이 스윕이 아니다. **실물 호출은 REQ-601(T2-1)이 소유한다.**
⚠️ 지시문 **문구의 품질**도 안 물었다 — 실물 응답 없이는 판정이 안 되는 종류다(`[manual]`).
⑦이 묻는 것은 취향이 아니라 **누락**이다.

## 전체 스윕 — 2026-08-23 · 기준선 **249 passed**

M-01~M-148 **148종 전부 red 확인** · 복구 후 249 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-249.log`](mutation-sweep-2026-08-23-249.log) (740줄 · 148 case).

### ⚠️ 하중 분포 — **이번에도 안 맞댔다**

직전(241) 로그와 겹치는 140종의 하중을 이번에도 **비교하지 않았다.** 이유는 T12-1·T12-2 절과
같다 — 두 로그를 맞대는 파이프라인이 무인 루프의 권한 경계 밖이고(D14), 140쌍을 손으로 세어
적는 것은 T0-6이 금지한 짓이다. ⛔ 그러므로 이 절은 *"가려진 가드가 없다"*를 **스윕 결과로만**
말한다 — 148종이 전부 red고 `❌`가 0이다. *"하중 분포가 안 바뀌었다"*는 **말하지 않는다**.

### ⛔ 첫 판의 로그는 **M-51에서 잘렸다** — 그 커밋은 되돌려졌다

이 절이 인용하는 로그는 **두 번째 판**이다. 첫 판(`7f37ef2`)은 같은 선언을 적고 **253줄 ·
51 case**짜리 로그를 인용했다 — `mutation-sweep-2026-08-22-192.log`와 **같은 꼴**이고,
T11-6의 범위 가드가 정확히 그것을 잡으라고 만들어진 것이다.

⚠️ **끊은 것은 스윕이 아니라 도구의 타임아웃**이다. 한 case가 약 5.9초라 148종은 **14분**인데
전경 명령의 한도가 300초다 — 51 × 5.9 ≈ 300. 스윕은 죽은 게 아니라 **잘린 채 tee만 남았다.**
⇒ 두 번째 판은 백그라운드로 돌리고, 끝난 뒤 **148 case · `✅ red` 148 · 닫힘 148 · `❌` 0 ·
exit 0**을 세어 확인한 다음 커밋했다.

⛔ **그 커밋의 게이트는 초록이었다.** 범위 가드는 설계상 워킹트리가 아니라 `HEAD`를 읽으므로
(자기참조를 피하는 유일한 수 · 이 파일이 아니라 `test_sweep_log_integrity.py`의 docstring이
권위다), 잘린 로그를 **커밋하는 그 순간**의 게이트는 *직전* 증거(241 · 140 case)를 본다.
red는 **다음** 게이트에 났고, 잡은 것은 이터레이션이 아니라 **러너의 외부 재게이트**다.
⚠️ 그러므로 *"커밋 직전 초록"*은 증거의 온전함에 대해 **아무 말도 안 한다** — 세는 자리는
스윕이 끝난 **뒤**다.

⚠️ **첫 판의 로그는 안 남긴다** — 판정이 아닌 결과를 증거로 커밋하는 것이 192 사고 그 자체다
(T12-1 절이 먼저 쓴 규칙).

## 배포 전 검사 — **올리기 전에 막는가** (T12-4 · REQ-602) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **260 passed**

T12-4. T11-1이 예고해 둔 자리다: *"이대로 올리면 포트 프로브에서 죽고, 그것을 잡을
`deploy-check`도 아직 없다."* T12-1이 서버를 만들어 그 함정을 없앴지만 **그때 생긴 것은 지금
이 순간의 초록이지 집행이 아니다.** `tools/deploy_preflight.py`가 `CMD`의 진입점을 AST로 읽어
*"포트를 `config`에서 읽고 · 소켓을 열고 · 안 끝나는가"*를 묻고, `scripts/deploy.sh`가
**빌드보다 먼저** 그것을 부른다. `tests/test_deploy_preflight.py` 열하나가 그 둘을 태운다.

⚠️ design 10§5의 `deploy-check`는 올린 **뒤**의 절반(`/healthz` + 실제 remediate 왕복)이고
실물 없이는 판정이 안 된다. 여기 있는 것은 **앞의 절반**이다 — §5를 그렇게 갈라 적었다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-149 | REQ-602 | ★ 진입점이 `serve_forever`를 안 부른다 | 1 failed | **red 확인** |
| M-150 | REQ-602 | 진입점이 포트를 박는다 | 1 failed | **red 확인** |
| M-151 | REQ-602 | 진입점의 `__main__` 가드가 사라진다 | 1 failed | **red 확인** |
| M-152 | REQ-602 | 공허 통과 — 호출 이름 수집기가 늘 둘 다 있다고 말한다 | 3 failed | **red 확인** |
| M-153 | REQ-602 | ★ 셸이 검사를 빌드 **뒤에** 부른다 | 1 failed | **red 확인** |
| M-154 | REQ-602 | 셸이 검사를 아예 안 부른다 | 1 failed | **red 확인** |
| M-155 | REQ-602 | 발견이 있어도 종료 코드 0 | 1 failed | **red 확인** |
| M-156 | REQ-602 | 이미지 대조가 아무것도 안 본다 | 1 failed | **red 확인** |

여덟 다 복구 후 **260 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-149 — `test_http_server` ⑧이 **못 보는** 자리다

⑧이 묻는 것은 *"`CMD`의 모듈 이름이 서버 모듈의 이름과 같은가"*다. M-149는 이름을 한 글자도
안 건드린다 — 지우는 것은 `httpd.serve_forever()` 한 줄이고, 그러면 `main()`은 소켓을 열자마자
**정상 종료한다.** 종료 코드는 0이고 빌드도 게이트도 아무 말을 안 하며, 보이는 것은
*"리비전이 트래픽을 못 받는다"*뿐이다. ⇒ 이름을 묻는 것과 **하는 일**을 묻는 것은 다른 질문이고,
T12-1이 산 것은 전자였다.

### ⛔ M-153·M-154 — **검사가 있다**와 **검사를 지난다**는 다른 문장이다

M-154는 셸에서 호출 한 줄을 지운다. 도구도 테스트도 그대로 초록이다 — 아무도 안 지날 뿐이다
(M-91이 `make deploy`에 대해 먼저 쓴 모양이다). M-153은 더 조용하다: 호출은 **남아 있고**
한 칸 아래로 내려갈 뿐이다. 그때 검사는 이미 빌드 비용을 쓰고 리비전을 만든 **뒤에** 운다.
⇒ 이 태스크의 done-criterion이 *"올리려 하면 red"*인 이유가 그것이다 — **순서가 본체다.**

### ⚠️ 이 절이 확인하지 **않은** 것

**한 번도 빌드하지 않았고 올리지도 않았다.** 확인된 것은 *"우리가 이 조건을 올리기 전에
묻는다"*까지고, *"그 조건이 Cloud Run의 프로브를 통과시킨다"*는 **미확인**이다(PRINCIPLES #3).
⛔ **REQ-602는 그대로 TODO다** — 수용 기준은 실물 서비스이고, `Verifies:`도 안 붙였다.
⚠️ 올린 **뒤**의 절반(`deploy-check` — `/healthz` + 실제 remediate 왕복)은 여전히 없다.
그것은 실물 서비스가 있어야 판정되는 종류라 무인 게이트가 못 만든다(`[manual]`).
⚠️ 하중 분포 대조도 **안 했다**(권한 경계 · 아래 스윕 절과 같은 이유).

## Vertex location — **실물이 두 값을 갈랐다** (T2-1 · REQ-601) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **262 passed**

T2-1이 실물 호출에 성공하면서(`docs/evidence/adk-live-call-2026-08-23.log`) 설계의 가정 하나가
깨졌다. `.env.example`과 design 10§1은 `WR_REGION` **한 값**을 *"Cloud Run·Vertex 리전"*으로
썼는데, **Gemini 3.x는 Vertex에서 `global`에만 있다** — `us-central1`은 404다(`gemini-2.5-flash`는
거기 있다. 그래서 *"리전이 잘못됐다"*가 아니라 **"모델마다 다르다"**이다).

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-157 | REQ-601 | Vertex location에 **Cloud Run 리전**을 싣는다 | 1 failed | **red 확인** |
| M-158 | REQ-601 | 어댑터가 location을 **자기가 적는다** (`"global"`) | 1 failed | **red 확인** |

### ⛔ 오프라인 게이트가 **원리상 못 잡는** 자리였다

이 어긋남은 임포트로도 타입으로도 안 보인다. 두 값 다 그럴듯한 리전 문자열이고, 빌드도
게이트도 조용하다. **배포 후 첫 모델 호출에서만** 보인다 — 그때는 이미 Cloud Run이 떠 있고,
로그에는 404 하나만 남는다. ⇒ 실물 없이 이 저장소가 **절대 알 수 없었던 종류의 결함**이고,
T2-1의 값은 REQ-601을 켠 것보다 **이것을 찾은 쪽이 크다**.

⚠️ **M-158은 오늘 값이 같아서 증상이 없다** — `vertex_env`가 `"global"`을 자기가 적어도
지금 배포는 멀쩡히 돈다. 갈라지는 것은 모델이 리전을 늘리는 날이고, 그때 아무도 그 줄을
못 찾는다. T11-1(배포 값의 사본)·T11-4(데모 기준선)와 **같은 계열의 세 번째 자리**다:
물어야 하는 것은 *"값이 같은가"*가 아니라 **"어느 칸에서 왔는가"**다.

## 전체 스윕 — 2026-08-23 · 기준선 **262 passed**

M-01~M-158 **158종 전부 red 확인** · 복구 후 262 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-262.log`](mutation-sweep-2026-08-23-262.log) (     780줄 · 156 case).

### ⚠️ 하중 분포 — **이번에도 안 맞댔다**

직전(249) 로그와 겹치는 148종의 하중을 이번에도 **비교하지 않았다.** 이유는 앞선 절들과
같다 — 두 로그를 맞대는 파이프라인이 무인 루프의 권한 경계 밖이고(D14), 148쌍을 손으로 세어
적는 것은 T0-6이 금지한 짓이다. ⛔ 그러므로 이 절은 *"가려진 가드가 없다"*를 **스윕 결과로만**
말한다 — 156종이 전부 red고 `❌`가 0이다. *"하중 분포가 안 바뀌었다"*는 **말하지 않는다**.

### ⚠️ 스윕은 **백그라운드로** 돌렸다 — T12-3이 산 규칙이다

한 case가 약 6초라 156종은 **16분**인데 전경 명령의 한도가 300초다. T12-3의 첫 판이 정확히
거기서 잘렸고(M-51에서 끊긴 253줄) 그 커밋은 되돌려졌다. ⇒ 끝난 뒤 **156 case · `✅ red` 156 ·
닫힘 156 · `❌` 0 · exit 0**을 세어 확인한 다음 커밋한다. 세는 자리는 스윕이 끝난 **뒤**다.

## G5 — 게이트 중 **라이브 어댑터 생성 0** (T12-5 · T5-4 · REQ-801) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **268 passed**

**마지막 미착수 가드였다.** T10-3이 미룬 이유는 정직했다 — 그때 `adapters/`에는 `fakes.py`뿐이라
*"라이브 어댑터를 안 만든다"*를 집행해 봐야 **0개를 훑고 초록인 가드**였다. T12-3이
`adk_agent.build_agent`·`build_runner`를 만들면서 훑을 대상이 생겼고, 그 두 함수는 도크스트링에
*"게이트는 이것을 안 부른다"*고 **적어만 두었다.** 여기가 그 문장을 집행으로 바꾸는 자리다.

`src/warranty/adapters/live_guard.py`가 관측 지점이고(design 09§3의 *"어댑터 팩토리 카운터"*),
`tests/conftest.py` 한 줄이 게이트 전체에 그것을 건다. `tests/test_live_adapter_guard.py` 여섯이
census(정적) · tripwire(런타임) · 흔적(`sys.modules`) 셋을 태운다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-159 | REQ-801 | ★ 라이브 클라이언트 주입 — `build_agent`가 tripwire를 안 지난다 | 1 failed | **red 확인** |
| M-160 | REQ-801 | `_load_adk`가 tripwire를 안 지난다 | 1 failed | **red 확인** |
| M-161 | REQ-801 | 게이트가 tripwire를 **안 건다**(`conftest.py`) | 2 failed | **red 확인** |
| M-162 | REQ-801 | tripwire의 **극성이 뒤집힌다** — 걸렸을 때 통과시킨다 | 1 failed | **red 확인** |
| M-163 | REQ-801 | 공허 통과 — census가 지연 임포트를 못 본다 | 2 failed | **red 확인** |
| M-164 | REQ-801 | ★ census가 **전이 폐포를 안 따라간다** | 2 failed | **red 확인** |

여섯 다 복구 후 **268 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-164 — T12-5가 **미리 적어 둔 함정**의 정확한 얼굴이다

태스크가 경고한 문장은 이것이었다: *"'임포트가 없다'가 아니라 '생성이 없다'를 물어야 한다 —
지연 임포트가 들어오면 전자는 조용히 참이 된다."* M-164가 그 자리를 태운다. `build_agent`는
클라우드 이름을 **한 글자도 안 적는다** — 적는 것은 `_load_adk` 하나다. 그래서 임포트하는
함수만 census에 담으면 *"라이브 어댑터를 안 만든다"*는 **부르는 자리를 한 번도 안 본 채** 참이 된다.
⇒ 씨앗은 지연 임포트지만 판정 대상은 **전이 폐포**다.

### ⛔ M-159·M-161 — 같은 초록의 두 얼굴

M-159는 지나야 할 함수가 tripwire를 **안 지나게** 하고, M-161은 tripwire를 **안 걸리게** 한다.
둘 다 코드는 전부 제자리에 있고 게이트는 초록이며, 갈라지는 것은 *"만들었을 때 무슨 일이
일어나는가"*뿐이다 — 아무 일도 안 일어난다. M-91(`make deploy`가 아무도 안 지나는 자리)·
M-154(셸이 검사를 안 부른다)와 같은 계열의 세 번째 자리다: **검사가 있다 ≠ 검사를 지난다.**

### ⚠️ 이 절이 확인하지 **않은** 것

- ⛔ **라이브 어댑터를 한 번도 안 만들어 봤다.** 확인된 것은 *"게이트가 그 자리를 지나면
  터진다"*까지고, *"실물 프로세스에서는 안 터진다"*는 **미확인**이다 — 기본값이 *안 걸림*이라
  원리상 참이지만, 그 문장을 태운 실행은 없다(PRINCIPLES #3). T2-2가 소유한다.
- ⛔ **REQ-801을 안 올렸다.** 두 번째 문장(*"모델·Firestore·BigQuery·Monitoring·Cloud Run은
  전부 fake로 대체된다"*)은 지금 **대체할 실물이 없어서** 참이다. 그건 집행이 아니라 **부재**다.
  `Verifies:`는 붙였지만 상태 승격은 실물 어댑터가 생긴 뒤의 일이다.
- ⚠️ **임포트 시점의 생성은 tripwire가 아니라 순서가 막는다.** `conftest.py`는 모듈 임포트
  시점에 걸리므로 테스트 모듈보다 먼저지만, `conftest.py` **자신보다 먼저** 도는 것은 못 본다.
- ⚠️ census의 `CLOUD_ROOTS`는 `google`·`vertexai` 둘이다. **여기 없는 SDK는 못 본다** —
  목록이 아니라 경계로 만들 방법을 아직 못 찾았다(PRINCIPLES #4의 예외).
- ⚠️ 하중 분포 대조는 **안 했다**(권한 경계 · 아래 스윕 절과 같은 이유).

## 배포 — **빌드 명령만 다른 프로젝트에서 돌았다** (T2-2 · REQ-602) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **270 passed**

⛔ **이건 설계 리뷰가 아니라 실물이 가르친 것이다.** 첫 배포(`bash scripts/deploy.sh --yes`)가
죽었다. `deploy.sh`는 `gcloud run deploy`에는 렌더된 `--project=warranty-hack`을 넘기면서
`gcloud builds submit`에는 **안 넘겼다.** 그래서 빌드만 `gcloud config`의 옛 프로젝트에서
돌았고, 그 프로젝트의 빌드 서비스 계정은 우리 Artifact Registry에 쓸 권한이 없다:

    Created [.../projects/claude-study-501117/.../builds/fe8417e7-...]
    denied: artifactregistry.repositories.uploadArtifacts denied
            on projects/warranty-hack/.../repositories/warranty

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-165 | REQ-602 | 빌드 인자에서 **`--project`를 뺀다** | 1 failed | **red 확인** |
| M-166 | REQ-602 | 셸이 빌드 플래그를 **자기가 적는다** | 2 failed | **red 확인** |

### ⛔ **사본이 파일이 아니라 CLI의 기본값이었다**

T11-1이 세운 규칙은 *"셸은 렌더된 인자를 받아서 실행만 한다"*였고, 게이트가 그것을
`run deploy`에 대해 집행하고 있었다. 그런데 **`builds submit` 한 줄이 그 규칙 밖에 있었다.**
⚠️ **규칙이 있는데 한 줄만 빠져 있는 것**이 가장 안 보이는 모양이다 — 규칙의 존재가
그 줄도 지켜진다는 인상을 준다.

⛔ 그리고 이 사본은 **파일이 아니었다.** `gcloud config`의 주변 설정이 출처였고,
명시하지 않으면 그쪽이 이긴다. 이 저장소가 잡아 온 사본은 전부 저장소 안의 문자열이었는데,
이번 것은 **저장소 밖에 있었다.** 오프라인 게이트가 그것을 볼 방법은 *"우리가 명시했는가"*뿐이다.

### ⚠️ 빌드는 **성공했다** — 그래서 안 보였다

로그 300줄이 초록이고(이미지가 만들어졌고 `google-adk`도 깔렸다) 실패는 맨 아래 한 줄이다.
*"이미지가 잘못됐다"*로 읽기 딱 좋은 모양이고, 실제로 첫눈에는 그렇게 보였다.
⇒ 실패한 것은 **push**지 build가 아니다. 그 구별이 진단의 전부였다.

## 플랫폼 예약 경로 — **로컬에서 200이던 것이 실물에서 도달조차 안 했다** (T2-2 · REQ-602) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **273 passed**

⛔ **실물 배포가 가르친 것이다.** 로컬 스모크에서 `/healthz`는 200이었다
(`docs/evidence/server-local-smoke-2026-08-23.log`). Cloud Run에 올리자 404였고, 그 404는
**우리 것이 아니었다**:

    GET /healthz   -> 404  <!DOCTYPE html>…            ← Google 프론트엔드
    GET /healthz2  -> 404  {"error":"unknown_route"}   ← 우리 코드

한 글자 차이고, 앞의 것은 **컨테이너 로그에 한 줄도 안 찍힌다.** 우리 프로세스에 도달조차
안 한다. ⇒ `HEALTH_PATH`를 `/livez`로 옮겼다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-167 | REQ-602 | 헬스 경로를 **플랫폼 예약어로** 되돌린다 | 4 failed | **red 확인** |
| M-168 | REQ-602 | 공허 통과 — 예약 경로 목록을 **비운다** | 1 failed | **red 확인** |
| M-169 | REQ-602 | `demo-target`이 헬스 경로를 **자기가 적는다** | 1 failed | **red 확인** |

### ⛔ 오프라인 게이트가 **원리상** 못 보는 종류다

이건 우리 코드의 성질이 아니라 **플랫폼의 성질**이다. 임포트로도 타입으로도 로컬 소켓으로도
안 보인다 — 실물에 올려야 보인다(docs/PRINCIPLES.md #3). 그래서 가드가 하는 일은
*"알아내는 것"*이 아니라 **관찰한 것을 다시 잃지 않는 것**이다. 목록을 늘리는 사람은
증거 파일 경로를 함께 적어야 한다.

### ⚠️ 지금은 무해했다 — 그래서 더 위험했다

`min-instances=0`이라 시작 프로브가 TCP였고 리비전은 정상적으로 떴다. 즉 **증상이 없었다.**
갈라지는 것은 헬스 경로를 *쓰는* 날(`deploy-check` · T2-4)이고, 그때 증상은
*"헬스가 404다"*가 아니라 **"리비전이 트래픽을 못 받는다"**로 온다 — 훨씬 늦게, 훨씬 안 보이게.

### ⚠️ M-169 — 같은 지뢰를 두 번 밟는 자리

`demo-target`도 Cloud Run에 올라간다(T2-3). 거기서 경로를 따로 적으면 오늘은 값이 같아
증상이 없고, 한쪽만 고치는 날 **두 리비전이 프로브에 답을 못 한다.** 그러면 트래픽 전환이
안 일어나고 **데모의 절정이 통째로 사라진다.** ⇒ T11-1(배포 값)·T11-4(데모 기준선)·
T2-1(Vertex location)에 이은 **네 번째 "값이 아니라 출처" 자리**다.

## demo-target 배포 진입점 — **기본값을 두지 않는 것이 요점이다** (T2-3 · REQ-803·602) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **281 passed**

T11-4는 `demo_target`을 오프라인 모델로만 만들고 **벽시계를 안 만졌다**(결정론 · REQ-802).
지연을 실제로 쓰는 자리는 배포 진입점 하나이고, 그것이 없어서 두 리비전을 올릴 수 없었다.
`src/warranty/demo_target_server.py`가 그 자리다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-170 | REQ-803 | 진입점이 리비전의 **기본값을 갖는다** | 2 failed | **red 확인** |
| M-171 | REQ-803 | 공허 통과 — 두 역할이 **같은 리비전**을 가리킨다 | 2 failed | **red 확인** |
| M-172 | REQ-802 | 벽시계를 **진입점 밖으로** 내보낸다 | 1 failed | **red 확인** |

### ⛔ M-170 — **조용히 건강한 쪽으로 뜨는 것**이 최악이다

기본값을 두면 `WR_DEMO_REVISION`을 빠뜨린 배포가 **에러 없이 뜬다.** 장애 주입은 안 일어나고
화면은 멀쩡하다 — 데모는 조치→검증→롤백을 한 번도 못 보여 주는데 아무도 그것을 모른다.
그 침묵은 **데모 당일에** 깨진다. design 08§5가 *"설정은 fake를 기본값으로 두지 않는다"*고
정한 것과 같은 규칙이고, 여기서는 *"리비전을 기본값으로 두지 않는다"*이다.

⚠️ **리비전 이름도 거부한다** — `WR_DEMO_REVISION=demo-target-00008-slow`는 red다.
받으면 그 이름이 진입점에 **사본**으로 살고, 서비스명을 바꾸는 날 여기만 안 따라온다.
역할(`healthy`/`degraded`)로 받으면 이름은 `demo_target`이 서비스명에서 파생한 것 하나뿐이다.

### ⛔ M-172 — T11-4의 결정이 **문서에만 남는 것**을 막는다

T11-4는 *"이 모듈은 지연의 구현을 갖지 않고 주입받는다"*고 정했고, 그 이유는 게이트의
결정론이었다. 그런데 그 결정을 **집행하는 자리가 없었다** — 누가 `time.sleep`을 한 줄
넣으면 스위트가 그만큼 자게 되고, 아무도 안 운다. ④가 AST로 `src` 전체를 훑어 진입점
바깥의 `time.sleep`을 잡는다.

## Cloud Run Admin 어댑터 — **부분 전환은 롤백이 아니다** (T2-4 · REQ-301·302) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **287 passed**

T2-3이 실물 왕복의 물리적 절반을 증명했지만, 그 왕복을 한 것은 **사람의 `gcloud`**였다.
그것을 에이전트가 하려면 `RunControl` 포트의 실물 구현이 있어야 한다.
`src/warranty/adapters/live_run.py`가 그 자리다.

⛔ **게이트가 태울 수 있는 것은 요청의 모양까지다.** *"Cloud Run이 우리 말을 듣는가"*는
실물의 질문이다(#3). 그래서 순수 함수 셋(`service_path`·`traffic_spec`·`parse_traffic`)을
분리하고 그것을 태운다 — `deploy_plan`(T11-1)·`adk_agent`(T12-3)와 같은 수법이다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-173 | REQ-302 | 전환이 **부분적**이 된다 (100 → 50) | 1 failed | **red 확인** |
| M-174 | REQ-301 | 경로가 **설정의 리전**에서 온다 | 1 failed | **red 확인** |
| M-175 | REQ-302 | 되읽기가 **이름 없는 항목을 버린다** | 1 failed | **red 확인** |

### ⛔ M-173 — 이 저장소의 논지가 걸린 자리

OVERVIEW §2가 *"되돌림이 점진적이면 자동 롤백을 못 믿는다"*고 적고, 그것이 GCP 전용을
고른 이유다. 전환이 부분적이 되는 순간 **그 문장이 거짓이 된다** — 남은 비율이 다른
리비전에 남고, *"되돌렸다"*는 부분적으로만 참이다. 그 위에서 재측정한 신호는 두 리비전의
혼합이고, 그것으로는 아무것도 증명 못 한다.

### ⛔ M-174 — 실패가 "권한 없음"이 아니라 "다른 것이 되돌아갔다"로 온다

경로의 리전을 설정에서 가져오면 오늘은 값이 같아 증상이 없다. 리전이 둘이 되는 날
**엉뚱한 서비스의 트래픽을 옮기고**, 그 실패는 예외로 안 온다 — 조용히 성공한다.
⇒ T11-1(배포 값)·T11-4(데모 기준선)·T2-1(Vertex location)·T2-2(헬스 경로)에 이은
**다섯 번째 "값이 아니라 출처" 자리**다.

### ⭐ G5가 이 파일을 만드는 나를 잡았다

첫 판에서 `shift_all_traffic`·`read_traffic`이 tripwire를 안 지났고,
`test_live_adapter_guard.py`가 **이름으로 짚어** red를 냈다:

    tripwire를 안 지나는 생성 경로가 있다:
      live_run.py::read_traffic (생성 경로를 부른다: _services)
      live_run.py::shift_all_traffic (클라우드 SDK를 지연 임포트한다: google.cloud)

⇒ T12-5가 만든 가드가 **새 라이브 어댑터의 첫 커밋에서** 하중을 받았다. 그것이 그 가드를
만든 이유였다 — *"게이트는 이것을 안 부른다"*가 도크스트링의 문장으로만 남지 않게.

## 전체 스윕 — 2026-08-23 · 기준선 **287 passed**

M-01~M-175 **175종 전부 red 확인** · 복구 후 287 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-287.log`](mutation-sweep-2026-08-23-287.log) (818줄 · 164 case).

### ⛔ 이 절은 스윕보다 **먼저** 적혔다 — 그 순서가 강제된 것이다

신선도 가드 ①은 *"최신 스윕의 기준선 = 지금 스위트 크기"*를 요구한다. 새 테스트 여섯이
들어온 순간 기준선은 262에서 268로 갈라지고, **그 red가 스윕의 기준선 검사를 먼저 막는다**
(`⛔ 기준선이 이미 red다 — 변이 결과를 신뢰할 수 없다`). 그래서 순서는 하나뿐이다:
**선언 → 스윕 → 대조.** T12-8이 *"수렴하지 않는다"*고 적어 둔 자기참조의 다른 얼굴이고,
여기서는 커밋 전에 대조가 끝나므로 수렴한다.
⛔ **선언이 증거가 아니다.** 위 숫자는 스윕이 끝난 뒤 `── M-` 수 · `✅ red` 수 · `❌` 수를
세어 맞댄 값이고, 어긋났으면 고칠 것은 **선언이 아니라 코드**다.

### ⚠️ 하중 분포 — **이번에도 안 맞댔다**

직전(262) 로그와 겹치는 158종의 하중을 이번에도 **비교하지 않았다.** 이유는 앞선 절들과
같다 — 두 로그를 맞대는 파이프라인이 무인 루프의 권한 경계 밖이고(D14), 158쌍을 손으로 세어
적는 것은 T0-6이 금지한 짓이다. ⛔ 그러므로 이 절은 *"가려진 가드가 없다"*를 **스윕 결과로만**
말한다 — 164종이 전부 red고 `❌`가 0이다. *"하중 분포가 안 바뀌었다"*는 **말하지 않는다**.

### ⚠️ 직전 절의 괄호는 **로그를 안 세고 적혔다**

262 절은 원본을 *"(780줄 · 156 case)"*로 적었는데 그 파일은 **790줄 · 158 case**다. 260 절의
괄호가 그대로 따라온 자리다. ⛔ **고치지 않았다** — append-only 기록이고, 두 가드 중 어느
쪽도 그 괄호를 안 읽는다(범위는 `M-01~M-NN`에서, case는 로그에서 온다). ⇒ 그 괄호는
**기계가 안 읽는 문자열**이고, T0-6이 말한 대로 그런 것은 재발한다. 여기 적어 둔다.

## 모델 id 사본 — **선언들이 서로 어긋나지 않는가** (T12-6 · REQ-802) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **292 passed**

T12-6. `gemini-3.7-flash`가 일곱 파일에 살고 있었고 **맞대는 자리가 하나도 없었다.**
`tests/test_model_id_declarations.py` 다섯이 넷을 묻는다: ① 스캐너를 표본으로 태운다(산문의
`Gemini 3.x`·표시 이름은 안 잡고, 갱신 기록의 양쪽은 따로 읽는다) + 바닥 ② 선언들이 서로
같은가 ③ 갱신 기록(`옛 id → 새 id`)의 **오른쪽이 지금 이름인가** ④ 그 이름이 대회 요건의
바닥(Gemini 3.5 이상)을 **값으로** 넘는가.

⛔ **태스크가 적은 "네 곳"이 틀렸다 — 일곱이다**(`.env.example` · design 06 둘 · 테스트 픽스처
다섯). T0-5가 *"손으로 센 5곳이 아니라 6곳이었다 — 손 목록은 가드가 아니다"*라고 적어 둔
자리가 그대로 재발했다. 그래서 이 가드가 세는 단위는 **목록이 아니라 스캔 뿌리**다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-176 | REQ-802 | **값의 출처만** 바뀐다 (`.env.example`의 `WR_MODEL`) | 2 failed | **red 확인** |
| M-177 | REQ-802 | **설계만** 바뀐다 (design 06§3의 `model:` 줄) | 1 failed | **red 확인** |
| M-178 | REQ-802 | **테스트 픽스처 하나만** 바뀐다 (`test_config.py`) | 1 failed | **red 확인** |
| M-179 | REQ-802 | 갱신 기록의 **오른쪽이 낡는다** | 1 failed | **red 확인** |
| M-180 | REQ-802 | ★ **일곱 곳을 함께 낮춘다** (3.7 → 2.5 · 요건 바닥 아래) | 1 failed | **red 확인** |
| M-181 | REQ-802 | 공허 통과 — 스캐너가 **id를 하나도 못 뽑는다** | 2 failed | **red 확인** |
| M-182 | REQ-802 | 요건의 **바닥이 따로 썩는다** (`(3, 5)` → `(2, 0)`) | 1 failed | **red 확인** |
| M-183 | REQ-802 | 스캔 뿌리에서 **`tests/`가 빠진다** (사본 다섯이 검사 밖) | 1 failed | **red 확인** |

여덟 다 복구 후 **292 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-180 — 일치 검사는 **함께 틀리는 것을 못 잡는다**

일곱 곳을 전부 `gemini-2.5-flash`로 바꾸면 ②는 **초록이다.** 선언이 서로 어긋나지 않으니까.
그런데 그 값은 REQ-601(대회 필수)이 요구하는 *"Gemini 3.5 이상"* **아래**고, 그 사실을 말하는
자리는 게이트 어디에도 없었다. ⇒ ④는 일치가 아니라 **값**을 묻는다 — M-97이
`min-instances`에 대해 먼저 태운 갈라짐과 같은 계열이고, 이 저장소에서 **여섯 번째** 자리다.

⚠️ 그리고 그 바닥 상수(`MODEL_FLOOR`)도 **따로 썩는다**(M-182). 그래서 ④는 요구사항 원문이
적은 숫자를 정규식으로 읽어 상수와 맞댄다 — 두 출처가 갈라지면 red고, 그때 고칠 것은
둘 중 하나가 아니라 **대회 요건을 다시 읽는 것**이다.

### ⛔ M-183 — 범위가 줄어드는 것은 *"어긋난 게 없다"*와 같은 얼굴로 온다

스캔 뿌리에서 `tests/`를 빼면 사본 다섯이 검사 밖으로 나가고, 남은 둘(`.env.example` ·
design 06)이 서로 같아서 ②는 **초록이다.** 이 가드가 죽는 가장 값싼 길이 그것이고,
그것을 보는 것은 ②가 아니라 **파일 수의 바닥**이다.

### ⚠️ 안 태운 자리 — *표시 이름*

이 가드는 id 표기(`gemini-3.7-flash`)만 본다. 산문의 표시 이름(*"Gemini 3.7 Flash"* ·
OVERVIEW §8 · design 06§1 표)은 **안 본다** — 산문을 스캔하면 영어 문장을 고칠 때마다 게이트가
red고, **그런 가드는 꺼진다**(T11-5가 먼저 쓴 규칙). ⛔ 그래서 표시 이름이 따로 썩는 것은
못 잡는다. 판정 가능한 이웃이지만 범위 밖으로 뒀다(docs/PRINCIPLES.md #3).

### ⚠️ 이 가드는 **자기 자신을 안 훑는다**

표본이 *일부러 틀린* id(`gemini-2.5-flash`·`gemini-3.5-flash`)를 담기 때문이다. 대가는 구멍
하나다 — `test_model_id_declarations.py` 안에 어긋난 사본을 적으면 아무도 안 잡는다.
M-105·M-118이 먼저 만든 것과 같은 구멍이고, 같은 이유로 **적어 두는 것 말고는 수가 없다.**

## 전체 스윕 — 2026-08-23 · 기준선 **292 passed**

M-01~M-183 **183종 전부 red 확인** · 복구 후 292 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-292.log`](mutation-sweep-2026-08-23-292.log) (916줄 · 183 case).
⚠️ 이 로그는 `mutate.sh all` 한 번이 아니라 **case별 호출 183번을 3분할로** 돌려 이어 붙였다
(로그 첫 줄이 그렇게 적혀 있다). 무인 루프의 명령 시간 한도가 ~10분이고 `all` 한 번이 거기에
걸려 **두 판 연속 SIGTERM으로 잘렸기** 때문이다. 판정 내용은 같다 — case마다 기준선을 다시
찍고 복구·잔여 대조까지 하는 것은 `all`의 루프가 하는 일과 **같은 함수**(`one`)다.

### ⛔ 이 절도 스윕보다 **먼저** 적혔다 — 그 순서가 강제된 것이다

287 절이 적어 둔 것과 같은 자기참조다: 신선도 가드 ①이 *"최신 스윕의 기준선 = 지금 스위트
크기"*를 요구하므로, 새 테스트 다섯이 들어온 순간 기준선은 287에서 292로 갈라지고 **그 red가
스윕의 기준선 검사를 먼저 막는다**(`⛔ 기준선이 이미 red다`). 순서는 하나뿐이다:
**선언 → 스윕 → 대조.** ⛔ **선언이 증거가 아니다** — 위 숫자와 표의 `죽은 건수`는 스윕이
끝난 뒤 로그의 `── M-` 수 · `✅ red` 수 · `❌` 수를 세어 맞댄 값이고, 어긋났으면 고칠 것은
**선언이 아니라 코드**다.

### ⚠️ 하중 분포 — **이번에도 안 맞댔다**

직전(287) 로그와 겹치는 175종의 하중을 이번에도 비교하지 않았다. 이유는 앞선 절들과 같다 —
두 로그를 맞대는 파이프라인이 무인 루프의 권한 경계 밖이고(D14), 175쌍을 손으로 세어 적는
것은 T0-6이 금지한 짓이다. ⛔ 그러므로 이 절은 *"가려진 가드가 없다"*를 **스윕 결과로만**
말한다 — 183종이 전부 red고 `❌`가 0이다. *"하중 분포가 안 바뀌었다"*는 **말하지 않는다.**

## 라이브 배제 — **게이트가 과금 테스트를 안 도는가** (T12-10 · REQ-801) — 2026-08-23 · 로컬 macOS · py3.13 · 기준선 **296 passed**

T12-10. `pyproject.toml`의 `live` 마커 설명은 *"게이트에 포함되지 않는다"*고 **말한다.**
그런데 `addopts`에 `-m "not live"`가 **없었다** ⇒ 라이브 테스트를 하나라도 만드는 순간
`make check`가 그것을 돌리고 **과금한다.** 지금까지 무해했던 이유는 라이브 테스트가
**하나도 없어서**일 뿐이다 — **부재는 집행이 아니다.** (REQ-601 승격이 여기 막혀 있었다:
실물 호출은 증거가 있는데 그것을 재현하는 라이브 테스트를 **넣을 수가 없었다.**)

`tests/test_gate_excludes_live.py` 넷이 묻는다: ① 두 실행이 실제로 무언가를 돌렸는가(바닥)
② 게이트 설정으로 돌린 표본에서 라이브가 **안 돌았는가** ③ `make live-check`의 선택으로는
**반대로** 라이브만 도는가 ④ Makefile이 그 배제를 **덮어쓰지 않는가**.

⚠️ **문자열을 대조하지 않는다.** 표본 스위트(오프라인 하나 · `live` 하나)를 `tmp_path`에 쓰고
게이트와 **같은 설정**으로 실제로 돌린 뒤, 각 테스트가 남긴 **파일**로 판정한다. pytest의
요약 줄은 `-q`가 겹치면 모양이 바뀌고(하네스가 실제로 그것으로 한 번 죽었다) 그때 파서는
*"안 돌았다"*와 *"못 읽었다"*를 구별하지 못한다. **파일은 그 구별을 공짜로 준다.**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-184 | REQ-801 | ★ **본체** — `addopts`에서 `-m 'not live'`가 빠진다 | 1 failed | **red 확인** |
| M-185 | REQ-801 | ★ `make live-check`가 **자기 선택을 잃는다** (`-m live` 제거) | 1 failed | **red 확인** |
| M-186 | REQ-801 | `test:` 레시피가 **명령줄로 배제를 덮는다** | 1 failed | **red 확인** |
| M-187 | REQ-801 | 공허 통과 — 라이브 **표본이 흔적을 안 남긴다** | 2 failed | **red 확인** |

넷 다 복구 후 **296 passed** 복귀 · 백업 대조 잔여 0.

### ⛔ M-185 — **배제를 넣는 행위가 만드는 구멍이다**

`addopts`가 `-m "not live"`를 들면 `make live-check`의 `-m live`는 **두 번째 `-m`**이다.
*나중 것이 이긴다*는 것은 pytest의 **동작**이지 우리가 적은 문장이 아니다. 틀리면
`make live-check`는 라이브를 **0건 돌고 초록**이고, 그 초록은 *"라이브가 전부 통과했다"*와
**같은 얼굴**을 한다 — 이 저장소가 계속 잡아 온 **실행 성공 ≠ 증상 해소**의 게이트판이다.
⇒ ②만 넣으면 이 자리가 조용히 죽는다. ②와 ③은 **서로의 바닥**이라 둘 다 있어야 한다.

### ⛔ M-186 — ②의 표본은 이 변이를 **못 본다**

`addopts`는 멀쩡한 채로 `test:` 레시피에 `-m` 하나만 붙이면, ②가 돌리는 표본은 그 인자를
안 받으므로 **여전히 초록**이고 실제 `make check`만 라이브를 돈다. ③이 쓰는 바로 그 성질
(명령줄이 `addopts`를 이긴다)의 반대쪽 얼굴이고, 보는 자리는 ④ 하나다.

### ⚠️ 대가 — **스위트가 느려졌다**

②·③은 하위 프로세스 pytest를 **한 번씩** 띄운다(서로 다른 선택이라 한 번으로 못 줄인다).
스위트가 ~0.84s → ~1.0s가 되고 **스윕은 그만큼 길어진다.** 값으로 태우지 않으면 이 자리는
문자열 대조로 내려가고, 그건 M-185가 보여 준 구멍을 **원리상 못 본다.** ⇒ 비용을 택했다.

### ⚠️ 안 태운 자리 — **라이브 테스트 자체는 만들지 않는다**

이 열이 태우는 것은 **배제**까지다. 실제로 과금하는 테스트를 넣는 것은 무인 루프의 권한
경계 밖이고(D14), 그 테스트의 초록은 게이트가 판정할 수 있는 종류가 아니다.
⛔ 그러니 *"라이브 테스트가 실물에서 통과한다"*는 이 기록이 **하지 않는 말**이다.

## 전체 스윕 — 2026-08-23 · 기준선 **296 passed**

M-01~M-187 **187종 전부 red 확인** · 복구 후 296 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-23-296.log`](mutation-sweep-2026-08-23-296.log) (935줄 · 187 case).
⭐ **292·287과 달리 이번은 `mutate.sh all` 한 번이다** — 나누지 않았다. 앞의 두 판이 나눈
이유는 명령이 **전경 시간 한도**에 걸려 SIGTERM을 맞고 잘렸기 때문인데, 이번엔 스윕을
**백그라운드로** 돌려 그 한도 자체를 피했다(로그 첫 줄이 그렇게 적혀 있다).
⚠️ 그래서 이 로그는 **끊긴 적 없는 한 판**이고, 이어 붙인 자리가 없다.

### ⛔ 이 절도 스윕보다 **먼저** 적혔다 — 그 순서가 강제된 것이다

292·287 절이 적어 둔 것과 같은 자기참조다: 신선도 가드 ①이 *"최신 스윕의 기준선 = 지금 스위트
크기"*를 요구하므로, 새 테스트 넷이 들어온 순간 기준선은 292에서 296으로 갈라지고 **그 red가
스윕의 기준선 검사를 먼저 막는다**(`⛔ 기준선이 이미 red다`). 순서는 하나뿐이다:
**선언 → 스윕 → 대조.** ⛔ **선언이 증거가 아니다** — 위 숫자와 표의 `죽은 건수`, 그리고 이
절이 적은 **줄 수**는 스윕이 끝난 뒤 로그를 세어 덮은 값이다. 292 절이 정확히 그 자리에서
한 번 어긋났다(915라 적고 실제는 916이었다).

### ⚠️ 하중 분포 — **이번에도 안 맞댔다**

직전(292) 로그와 겹치는 183종의 하중을 이번에도 비교하지 않았다. 이유는 앞선 절들과 같다 —
두 로그를 맞대는 파이프라인이 무인 루프의 권한 경계 밖이고(D14), 183쌍을 손으로 세어 적는
것은 T0-6이 금지한 짓이다. ⛔ 그러므로 이 절은 *"가려진 가드가 없다"*를 **스윕 결과로만**
말한다 — 187종이 전부 red고 `❌`가 0이다. *"하중 분포가 안 바뀌었다"*는 **말하지 않는다.**

## Cloud Monitoring 신호 요청 — **같은 신호를 실제 요청 모양으로 읽는가** (T13-1 · REQ-201·202·205) — 2026-08-27 · 로컬 macOS · py3.13 · 기준선 **304 passed**

T13-1. `docs/evidence/live-signal-2026-08-23.log`에서 실물로 확인한 필터·p95 aligner·p95
reducer·요청 모양을 `adapters/live_signal.py`의 순수 함수 셋으로 분리했다. 실제 SDK는 첫
호출에서만 지연 임포트하고 G5 tripwire를 지난다. 응답은 순서에 기대지 않고 가장 최신 포인트를
고르며, 빈 창은 0이 아니라 `Measurement(None, 0)`으로 내려 `unverifiable`을 보존한다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-188 | REQ-201·202 | 필터가 계약의 서비스 이름 대신 `demo-target`을 고정한다 | 1 failed | **red 확인** |
| M-189 | REQ-201 | p95 aligner를 평균으로 바꾼다 | 1 failed | **red 확인** |
| M-190 | REQ-201 | p95 reducer를 평균으로 바꾼다 | 1 failed | **red 확인** |
| M-191 | REQ-201 | 실물의 60초 alignment를 120초로 바꾼다 | 1 failed | **red 확인** |
| M-192 | REQ-201·202 | `aggregation`을 요청 안에서 잃는다 | 1 failed | **red 확인** |
| M-193 | REQ-201·202 | 최신이 아니라 가장 오래된 포인트를 고른다 | 1 failed | **red 확인** |
| M-194 | REQ-205 | 빈 창을 0이라는 관측값 하나로 바꾼다 | 1 failed | **red 확인** |
| M-195 | REQ-202 | 계약의 `window_s` 대신 60초 사본을 쓴다 | 1 failed | **red 확인** |
| M-196 | REQ-801 | Monitoring 클라이언트 생성 입구의 tripwire를 지운다 | 1 failed | **red 확인** |
| M-197 | REQ-801 | 이미지에서 `google-cloud-monitoring` 선언을 지운다 | 3 failed | **red 확인** |

⛔ **이 표는 스윕보다 먼저 적혔다.** 신선도 가드가 새 기준선·변이 집합·`red 확인` 행을 모두
요구해, 선언 없이는 변이 하네스의 기준선 자체가 red다. 아래 전체 스윕이 끝난 뒤 원본 로그의
case 수·`✅ red`·`❌`·복구·잔여를 세어 이 표의 `죽은 건수`와 선언을 대조한다. 어긋나면
고칠 것은 선언이 아니라 코드다.

## 전체 스윕 — 2026-08-27 · 기준선 **304 passed**

M-01~M-197 **197종 전부 red 확인** · 복구 후 304 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-27-304.log`](mutation-sweep-2026-08-27-304.log)
(985줄 · 197 case · `✅ red` 197 · `❌` 0 · 복구 197 · 잔여 없음 197).
이번 스윕은 분할하지 않고 `mutate.sh all` 한 번으로 끝까지 실행했다.

⛔ **이 절도 스윕보다 먼저 적혔다.** 새 테스트 여덟과 변이 열이 들어온 순간 최신 기록의
296 기준선·M-187 범위가 모두 낡아 하네스가 기준선을 거부한다. 그래서 기존 절들이 기록한
순서대로 **선언 → 스윕 → 대조**한다. 이 문단은 실행 뒤 원본 로그와 맞지 않으면 확정되지 않는다.
✅ 위 숫자와 표의 죽은 건수를 원본 로그에서 대조했고 모두 일치했다.

## p95 구간 부하 — **측정할 트래픽이 각본에 있는가** (T13-2 · REQ-803) — 2026-08-27 · 로컬 macOS · py3.13 · 기준선 **307 passed**

T13-2. 2026-08-23 실물 실험은 구간당 3회 요청으로 세 구간을 구별하지 못했고, 40회에서
674/989/674ms로 갈라졌다. design 11§1.1과 `demo.py`가 `baseline`·`after_action`·
`after_rollback` 각각 40회의 부하 계획을 값으로 갖고, 오프라인 `make demo`는 그 계획을
출력한다. ⛔ 이 태스크는 요청을 보내지 않는다 — 실물 소비는 T2-4의 남은 절반이다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-198 | REQ-803 | ★ 조치 후 구간의 부하를 0회로 만든다 | 3 failed | **red 확인** |
| M-199 | REQ-803 | 설계의 부하 값만 40→41로 바꾼다 | 2 failed | **red 확인** |
| M-200 | REQ-803 | 데모 출력에서 부하 계획을 지운다 | 1 failed | **red 확인** |
| M-201 | REQ-803 | 공허 통과 — 설계와 코드를 함께 빈 계획으로 만든다 | 2 failed | **red 확인** |

⛔ 앞선 절과 같은 순서다. 새 테스트 셋과 변이 넷이 들어온 순간 신선도 가드가 하네스의
기준선을 막으므로 **선언 → 스윕 → 대조**한다. 이 표의 죽은 건수는 실행 뒤 원본과 맞춘다.

## 전체 스윕 — 2026-08-27 · 기준선 **307 passed**

M-01~M-201 **201종 전부 red 확인** · 복구 후 307 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-27-307.log`](mutation-sweep-2026-08-27-307.log) — **1005줄**,
case 201 · `✅ red` 201 · `❌` 0 · 복구 201 · 잔여 없음 201.

⛔ **이 절은 스윕보다 먼저 적혔다.** 실행 뒤 case 수·`✅ red`·`❌`·복구·잔여를 원본에서
대조하며, 하나라도 다르면 이 선언을 확정하지 않는다.
✅ 분할하지 않은 `mutate.sh all` 한 번의 원본 로그에서 다섯 수치를 대조했고 모두 일치했다.

## 아키텍처 제출 표면 — **그림의 원본이 하나인가** (T13-4 · REQ-901) — 2026-08-27 · 로컬 macOS · py3.13 · 기준선 **311 passed**

T13-4. 새 그림을 만들지 않았다. README의 `Architecture diagram` 항목은 이미 있던
`docs/OVERVIEW.md#4-아키텍처`를 직접 가리킨다. 링크의 파일·앵커·Mermaid 렌더 블록을 확인하고,
같은 Mermaid가 다른 Markdown 문서에 복제되면 red다. ⛔ REQ-901의 영상·저장소 URL·실물 실행
시각 증거는 여전히 사람 판정이므로 상태를 승격하지 않는다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---:|---|
| M-202 | REQ-901 | 제출 링크가 §4 대신 §5의 다른 Mermaid를 가리킨다 | 1 failed | **red 확인** |
| M-203 | REQ-901 | 권위 블록의 렌더 언어를 Mermaid에서 text로 바꾼다 | 2 failed | **red 확인** |
| M-204 | REQ-901 | 권위 아키텍처를 README에 그대로 복제한다 | 1 failed | **red 확인** |
| M-205 | REQ-901 | 공허 통과 — 사본 검사에서 README를 제외한다 | 1 failed | **red 확인** |

⛔ 새 테스트와 변이가 들어온 순간 신선도 가드가 기존 307/201 기록을 거부했다. 앞선 절과 같은
순서로 **선언 → 스윕 → 대조**하며, 이 표의 죽은 건수와 아래 전체 스윕 수치는 실행 뒤 확정한다.

## 전체 스윕 — 2026-08-27 · 기준선 **311 passed**

M-01~M-205 **205종 전부 red 확인** · 복구 후 311 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-27-311.log`](mutation-sweep-2026-08-27-311.log) — **1025줄**,
case 205 · `✅ red` 205 · `❌` 0 · 복구 205 · 잔여 없음 205.

✅ 분할하지 않은 `mutate.sh all` 한 번의 원본 로그에서 다섯 수치를 대조했고 모두 일치했다.

## D15 앱 인증 — **공개 URL이 무인 과금 권한으로 번지지 않는가** — 2026-08-27 · 로컬 macOS · py3.13 · 기준선 **330 passed**

Cloud Run invoker는 공개로 두되 `/agent:chat`은 Secret Manager가 주입한 32바이트 이상 bearer
token을 요구한다. 비밀 미설정은 503, 누락·형식 오류·불일치는 같은 401이며, 유효 토큰도 실물
에이전트가 아직 없으므로 501이다. 배포 렌더러는 `--allow-unauthenticated`와 `--set-secrets`를
한 경계로 낸다. 토큰 값은 argv·응답·로그에 싣지 않는다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---:|---|
| M-206 | D15 | 32바이트 미만 서버 비밀을 허용 | 2 failed | **red 확인** |
| M-207 | D15 | Authorization 헤더 누락을 인증 성공으로 판정 | 3 failed | **red 확인** |
| M-208 | D15 | `compare_digest`를 평범한 동등 비교로 교체 | 1 failed | **red 확인** |
| M-209 | D15 | 서버의 `/agent:chat` 인증 게이트 제거 | 3 failed | **red 확인** |
| M-210 | D15 | 잘못된 토큰을 통과시킴 | 2 failed | **red 확인** |
| M-211 | D15 | Cloud Run invoker를 비공개로 되돌림 | 1 failed | **red 확인** |
| M-212 | D15 | Secret Manager 바인딩 제거 | 1 failed | **red 확인** |
| M-213 | D15 | HTTP 핸들러가 Authorization 헤더를 전달하지 않음 | 1 failed | **red 확인** |

⛔ 이 표는 스윕보다 먼저 선언했다. 아래 원본 로그를 만든 뒤 각 죽은 건수와 전체 수치를
대조하기 전에는 완료 기록이 아니다.

## 전체 스윕 — 2026-08-27 · 기준선 **330 passed**

M-01~M-213 **213종 전부 red 확인** · 복구 후 330 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-27-330.log`](mutation-sweep-2026-08-27-330.log) — **1065줄**,
case 213 · `✅ red` 213 · `❌` 0 · 복구 213 · 잔여 없음 213.

✅ 분할하지 않은 `mutate.sh all` 한 번의 원본 로그에서 다섯 수치를 대조했고 모두 일치했다.

## T14-1 Firestore 문서 매핑 — **저장이 성공했다는 것과 값이 살아 있다는 것은 다르다** — 2026-08-27 · 로컬 macOS · py3.13 · 기준선 **356 passed**

계약과 원장이 Firestore로 나가고 돌아온다. 돈은 double로 왕복시키지 않고(문자열), 모르는
필드·빠진 필드·모르는 열거값은 조용히 넘기지 않으며, `retired`된 계약은 **질의에서** 걸러진다.
원장의 전이 규칙은 어댑터에 사본으로 생기지 않고 `warranty.domain.entry`의 순수 함수를 지난다.

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---:|---|
| M-214 | REQ-503 | 돈을 문자열이 아니라 double로 실음 | 8 failed | **red 확인** |
| M-215 | REQ-505 | 돈 자리에 온 수치를 그대로 복원 | 1 failed | **red 확인** |
| M-216 | REQ-501 | 문서에 없는 필드를 기본값으로 채움 | 1 failed | **red 확인** |
| M-217 | REQ-501 | 모르는 필드를 조용히 버림 | 1 failed | **red 확인** |
| M-218 | REQ-507 | 열거형에 없는 값을 문자열 그대로 복원 | 1 failed | **red 확인** |
| M-219 | REQ-105 | 계약 질의에서 `state` 조건 제거 | 1 failed | **red 확인** |
| M-220 | REQ-102 | 살아 있는 계약이 둘일 때 첫 번째를 고름 | 1 failed | **red 확인** |
| M-221 | REQ-201 | 수 자리에 온 `True`를 받아들임 | 1 failed | **red 확인** |
| M-222 | REQ-801 | Firestore 생성 입구의 G5 tripwire 제거 | 1 failed | **red 확인** |
| M-223 | REQ-501 | 어댑터가 도메인 전이를 안 지나고 필드를 직접 고침 | 2 failed | **red 확인** |

⛔ 이 표는 스윕보다 먼저 선언했다. 아래 원본 로그를 만든 뒤 각 죽은 건수와 전체 수치를
대조하기 전에는 완료 기록이 아니다.

## 전체 스윕 — 2026-08-27 · 기준선 **356 passed**

M-01~M-223 **223종 전부 red 확인** · 복구 후 356 · 잔여 0 · **가려진 가드 없음**.
원본: [`mutation-sweep-2026-08-27-356.log`](mutation-sweep-2026-08-27-356.log) — **1115줄**,
case 223 · `✅ red` 223 · `❌` 0 · 복구 223 · 잔여 없음 223.

✅ 분할하지 않은 `mutate.sh all` 한 번의 원본 로그에서 다섯 수치를 대조했고 모두 일치했다.

⛔ **리팩터가 변이 여덟을 조용히 껐다 (2026-08-27).** 원장 전이를 `InMemoryLedger`의
메서드에서 모듈 수준 순수 함수로 옮기자 M-06·M-70·M-71·M-73~M-77의 perl 패턴이 들여쓰기와
`{entry_id}`→`{current.entry_id}`에서 어긋났다. 첫 스윕이 그것을 *"변이가 파일을 바꾸지
못했다"*로 잡았다 — **이 줄이 없었으면 여덟 개는 초록인 채로 죽은 변이가 됐다.**
⇒ 도메인 코드를 옮기는 변경은 **하네스의 패턴도 함께 옮겨야 한다.** 여덟 전부 다시 겨눠
red를 회복한 뒤 위 스윕을 다시 돌렸다.

⛔ **G5 census가 같은 이름의 메서드를 서로 덮고 있었다 (2026-08-27).** M-222(Firestore
생성 입구의 tripwire 제거)가 처음엔 안 죽었다 — census가 함수를 **단순 이름**으로 담아
`LiveContractStore._db`를 지워도 `LiveLedger._db`가 그 자리를 채웠기 때문이다. 같은 이름의
메서드를 둘 이상 가진 첫 live 모듈에서만 보이는 blind spot이었다. census를 클래스까지 붙인
이름으로 고친 뒤 M-222는 red가 됐다.
