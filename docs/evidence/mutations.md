# 변이 기록

> **지워 보고 red가 나오지 않은 가드는 가드가 아니다.**
> 요구사항 상태를 `VERIFIED`로 올리려면 여기에 **red 확인** 행이 있어야 하고,
> `tools/spec_trace.py`가 그것을 읽어 집행한다(가드 G6).

**절차** (`specs/fleet-ledger/design/07-verification.md` §4) — 전부 `scripts/mutate.sh` 안에서:
① 기준선 → ② 변이 → ③ 전체 스위트가 red인가·**몇 건이 죽었나** → ④ 복구 →
⑤ **초록 복귀 확인** → ⑥ **백업 대조로** 잔여 확인.

⚠️ **문법을 깬 변이는 red로 세지 않는다** — 컴파일 실패는 검증이 아니다.
⚠️ **전체 스위트에 묻는다** — 한 파일에만 물으면 "가드 없음"으로 잘못 읽는다.

---

## 실행 기록 — 2026-08-19 · 로컬 macOS · py3.13 · 기준선 **22 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-01 | — | `requirements.md`의 `상태:` 한 줄 삭제 | 7 errors | **red 확인** |
| M-02 | — | REQ 하나를 `IMPLEMENTED`로 올리고 테스트는 안 만듦 | 2 failed | **red 확인** |
| M-03 | — | 파서를 깨 요구사항 0개를 읽게 함 (공허 통과 방지) | 3 failed | **red 확인** |
| M-04 | — | 정의 없는 `REQ-999`를 `tasks.md`가 가리키게 함 | 2 failed | **red 확인** |
| M-05 | — | **산문 언급만** 있는 테스트로 커버리지가 되는지 | 2 failed | **red 확인** |
| M-06 | REQ-505 | 화해가 `assumed`를 덮게 함 (G2) | 1 failed | **red 확인** |
| M-07 | REQ-504 | `method↔verifiability` 매핑을 깸 (G3) | 2 failed | **red 확인** |
| M-08 | REQ-501 | 같은 id로 덮어쓰기를 허용 (G7) | 1 failed | **red 확인** |
| M-09 | REQ-503 | 수량·단가 키 일치 검사 제거 | 1 failed | **red 확인** |

전부 복구 후 **22 passed**로 복귀, 백업 대조 결과 **잔여 없음**.

## 설정 계층 — 2026-08-19 · 기준선 **29 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-10 | REQ-801 | ADK 프로젝트 불일치 검사 제거 | 1 failed | **red 확인** |
| M-11 | REQ-801 | 어댑터 기본값을 `fake`로 변경 | 1 failed | **red 확인** |
| M-12 | REQ-802 | `environ`을 줘도 `.env`를 함께 읽게 되돌림 | 6 failed | **red 확인** |

⚠️ **M-12의 red는 이 기계에 `.env`가 있고 그 값이 픽스처와 다르기 때문**이다.
`.env`가 없는 기계(CI·새 클론)에서는 **초록일 수 있다** — 즉 이 변이는 하중을 항상
주지 않는다. 그래서 **회귀 자체는 코드 구조로 막았다**(`environ`을 주면 디스크를
아예 안 읽는다). 어디에 물었는지를 적어 두지 않으면 다음 사람이 이 초록을 믿는다.

---

## ⚠️ 하네스 자신이 다섯 번 틀렸다 (2026-08-19)

레퍼런스가 경고한 그대로, **변이 하네스가 먼저 결함을 냈다.** 기록해 둔다 —
이 기록이 없으면 다음 사람이 같은 순서로 밟는다.

1. **판정을 출력 문자열로 했다.** 변이는 실제로 red를 냈는데 `tail -1`이 요약 줄이 아니라
   short summary의 마지막 줄을 집어 **"초록"으로 읽었다.** ⇒ **종료 코드로 판정한다.**
   (곁가지로: `addopts`의 `-q`와 하네스의 `-q`가 겹쳐 `-qq`가 되면서 요약 줄 자체가
   사라져 있었다 — "몇 건이 죽었나"를 못 적고 있었다.)
2. **잔여 확인을 `git status`로 했다.** 커밋 안 된 다른 작업이 **변이 잔여로 오인**됐다.
   ⇒ **백업과 파일 단위로 대조한다.**
3. **stale `.pyc`에 속았다.** 소스는 복구됐는데 `__pycache__`가 남아 테스트가 옛 코드를 보고
   red를 유지했고, 그것이 **"복구 실패"로 읽혔다.** 다음 변이는 기준선이 red라 중단됐다.
   ⇒ `PYTHONDONTWRITEBYTECODE=1` + 복구 시 `__pycache__` 제거.

4. **변이 대상 파일이 없는데 조용히 넘어갔다.** 프로젝트 이름을 바꾸고 이 스크립트의 경로를
   안 고쳤더니, 변이가 **적용되지 않은 채** "가드가 하중을 안 받는다"로 보고됐다.
   ⇒ `backup()`이 파일 부재를 **판정이 아니라 오류**로 낸다.
5. **파일은 있는데 패턴이 안 맞아 변이가 조용히 무효였다.** 설정 변수 접두사를 바꾼 뒤
   패턴을 안 고쳤을 때. 존재 검사로는 못 잡는 별개의 실패 모드다.
   ⇒ 변이 후 **파일이 실제로 바뀌었는지 확인**하고, 안 바뀌었으면 판정을 내지 않는다.

**교훈**: ⑤(초록 복귀)와 ⑥(잔여 대조)이 있었기 때문에 다섯이 전부 드러났다.
**변이가 red를 냈는지만 보는 하네스였다면 다섯 다 통과했을 것이다.**
그리고 ④⑤는 **초록을 "가드 없음"으로 잘못 읽게 만드는** 종류라 가장 위험하다 —
없는 가드를 만들러 가거나, 있는 가드를 지우게 된다.


## 재확인 — 2026-08-19 (GCP 올인 재정의 후) · 기준선 **30 passed**

M-01~M-12 **전부 red 재확인**, 복구 후 30 passed, 잔여 0.
⚠️ 이 재확인 과정에서 하네스 결함 ④⑤가 드러났다(위) — **재정의 전에는 초록이었다.**


## 도메인 — 2026-08-19 (계약·검증·판정 게이트) · 기준선 **51 passed**

| 변이 | REQ | 무엇을 깼나 | 죽은 건수 | 결과 |
|---|---|---|---|---|
| M-13 | REQ-502 | ★ G8 — `improved`를 검증 없이 참으로 (조용한 성공) | 3 failed | **red 확인** |
| M-14 | REQ-402 | ★ G9 — **검증 가능성 축 제거** (정책 무력화) | 4 failed | **red 확인** |
| M-15 | REQ-102 | 가역인데 롤백 계획 없는 계약을 허용 | 1 failed | **red 확인** |
| M-16 | REQ-205 | 빈 창을 회복으로 판정 (거짓 판정) | 1 failed | **red 확인** |
| M-17 | REQ-406 | 파괴적 조치의 강제 승인 제거 | 1 failed | **red 확인** |

전부 복구 후 **51 passed**, 잔여 0.

⚠️ **M-14가 이 프로젝트에서 가장 중요한 변이다.** 검증 가능성 축을 빼면
*"되돌릴 수 있지만 확인은 못 하는"* 조치가 `AUTO`가 된다 — 그리고 **다른 테스트는 전부
통과한다.** 정책은 별도 가드로 지키지 않으면 조용히 사라진다.
